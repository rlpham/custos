import t from "@firebase/app";

import { __extends as n, __assign as i, __spreadArrays as e, __awaiter as r, __generator as s } from "tslib";

import { Logger as u, LogLevel as o } from "@firebase/logger";

import { Component as h } from "@firebase/component";

import { XhrIo as a, EventType as c, ErrorCode as f, createWebChannelTransport as l, WebChannel as d } from "@firebase/webchannel-wrapper";

import { isMobileCordova as v, isReactNative as w, isElectron as m, isIE as p, isUWP as g, isBrowserExtension as b } from "@firebase/util";

/**
 * @license
 * Copyright 2017 Google Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/** The semver (www.semver.org) version of the SDK. */ var y = t.SDK_VERSION, _ = /** @class */ function() {
    function t(t) {
        this.uid = t;
    }
    return t.prototype.t = function() {
        return null != this.uid;
    }, 
    /**
     * Returns a key representing this user, suitable for inclusion in a
     * dictionary.
     */
    t.prototype.i = function() {
        return this.t() ? "uid:" + this.uid : "anonymous-user";
    }, t.prototype.isEqual = function(t) {
        return t.uid === this.uid;
    }, 
    /** A user with a null UID. */
    t.UNAUTHENTICATED = new t(null), 
    // TODO(mikelehen): Look into getting a proper uid-equivalent for
    // non-FirebaseAuth providers.
    t.s = new t("google-credentials-uid"), t.u = new t("first-party-uid"), t;
}(), T = {
    // Causes are copied from:
    // https://github.com/grpc/grpc/blob/bceec94ea4fc5f0085d81235d8e1c06798dc341a/include/grpc%2B%2B/impl/codegen/status_code_enum.h
    /** Not an error; returned on success. */
    OK: "ok",
    /** The operation was cancelled (typically by the caller). */
    CANCELLED: "cancelled",
    /** Unknown error or an error from a different error domain. */
    UNKNOWN: "unknown",
    /**
     * Client specified an invalid argument. Note that this differs from
     * FAILED_PRECONDITION. INVALID_ARGUMENT indicates arguments that are
     * problematic regardless of the state of the system (e.g., a malformed file
     * name).
     */
    INVALID_ARGUMENT: "invalid-argument",
    /**
     * Deadline expired before operation could complete. For operations that
     * change the state of the system, this error may be returned even if the
     * operation has completed successfully. For example, a successful response
     * from a server could have been delayed long enough for the deadline to
     * expire.
     */
    DEADLINE_EXCEEDED: "deadline-exceeded",
    /** Some requested entity (e.g., file or directory) was not found. */
    NOT_FOUND: "not-found",
    /**
     * Some entity that we attempted to create (e.g., file or directory) already
     * exists.
     */
    ALREADY_EXISTS: "already-exists",
    /**
     * The caller does not have permission to execute the specified operation.
     * PERMISSION_DENIED must not be used for rejections caused by exhausting
     * some resource (use RESOURCE_EXHAUSTED instead for those errors).
     * PERMISSION_DENIED must not be used if the caller can not be identified
     * (use UNAUTHENTICATED instead for those errors).
     */
    PERMISSION_DENIED: "permission-denied",
    /**
     * The request does not have valid authentication credentials for the
     * operation.
     */
    UNAUTHENTICATED: "unauthenticated",
    /**
     * Some resource has been exhausted, perhaps a per-user quota, or perhaps the
     * entire file system is out of space.
     */
    RESOURCE_EXHAUSTED: "resource-exhausted",
    /**
     * Operation was rejected because the system is not in a state required for
     * the operation's execution. For example, directory to be deleted may be
     * non-empty, an rmdir operation is applied to a non-directory, etc.
     *
     * A litmus test that may help a service implementor in deciding
     * between FAILED_PRECONDITION, ABORTED, and UNAVAILABLE:
     *  (a) Use UNAVAILABLE if the client can retry just the failing call.
     *  (b) Use ABORTED if the client should retry at a higher-level
     *      (e.g., restarting a read-modify-write sequence).
     *  (c) Use FAILED_PRECONDITION if the client should not retry until
     *      the system state has been explicitly fixed. E.g., if an "rmdir"
     *      fails because the directory is non-empty, FAILED_PRECONDITION
     *      should be returned since the client should not retry unless
     *      they have first fixed up the directory by deleting files from it.
     *  (d) Use FAILED_PRECONDITION if the client performs conditional
     *      REST Get/Update/Delete on a resource and the resource on the
     *      server does not match the condition. E.g., conflicting
     *      read-modify-write on the same resource.
     */
    FAILED_PRECONDITION: "failed-precondition",
    /**
     * The operation was aborted, typically due to a concurrency issue like
     * sequencer check failures, transaction aborts, etc.
     *
     * See litmus test above for deciding between FAILED_PRECONDITION, ABORTED,
     * and UNAVAILABLE.
     */
    ABORTED: "aborted",
    /**
     * Operation was attempted past the valid range. E.g., seeking or reading
     * past end of file.
     *
     * Unlike INVALID_ARGUMENT, this error indicates a problem that may be fixed
     * if the system state changes. For example, a 32-bit file system will
     * generate INVALID_ARGUMENT if asked to read at an offset that is not in the
     * range [0,2^32-1], but it will generate OUT_OF_RANGE if asked to read from
     * an offset past the current file size.
     *
     * There is a fair bit of overlap between FAILED_PRECONDITION and
     * OUT_OF_RANGE. We recommend using OUT_OF_RANGE (the more specific error)
     * when it applies so that callers who are iterating through a space can
     * easily look for an OUT_OF_RANGE error to detect when they are done.
     */
    OUT_OF_RANGE: "out-of-range",
    /** Operation is not implemented or not supported/enabled in this service. */
    UNIMPLEMENTED: "unimplemented",
    /**
     * Internal errors. Means some invariants expected by underlying System has
     * been broken. If you see one of these errors, Something is very broken.
     */
    INTERNAL: "internal",
    /**
     * The service is currently unavailable. This is a most likely a transient
     * condition and may be corrected by retrying with a backoff.
     *
     * See litmus test above for deciding between FAILED_PRECONDITION, ABORTED,
     * and UNAVAILABLE.
     */
    UNAVAILABLE: "unavailable",
    /** Unrecoverable data loss or corruption. */
    DATA_LOSS: "data-loss"
}, E = /** @class */ function(t) {
    function i(n, i) {
        var e = t.call(this, i) || this;
        return e.code = n, e.message = i, e.name = "FirebaseError", 
        // HACK: We write a toString property directly because Error is not a real
        // class and so inheritance does not work correctly. We could alternatively
        // do the same "back-door inheritance" trick that FirebaseError does.
        e.toString = function() {
            return e.name + ": [code=" + e.code + "]: " + e.message;
        }, e;
    }
    return n(i, t), i;
}(Error), S = function(t, n) {
    this.user = n, this.type = "OAuth", this.o = {}, 
    // Set the headers using Object Literal notation to avoid minification
    this.o.Authorization = "Bearer " + t;
}, A = /** @class */ function() {
    function t() {
        /**
         * Stores the listener registered with setChangeListener()
         * This isn't actually necessary since the UID never changes, but we use this
         * to verify the listen contract is adhered to in tests.
         */
        this.h = null;
    }
    return t.prototype.getToken = function() {
        return Promise.resolve(null);
    }, t.prototype.l = function() {}, t.prototype.v = function(t) {
        Wn(!this.h, "Can only call setChangeListener() once."), this.h = t, 
        // Fire with initial user.
        t(_.UNAUTHENTICATED);
    }, t.prototype.m = function() {
        Wn(null !== this.h, "removeChangeListener() when no listener registered"), this.h = null;
    }, t;
}(), I = /** @class */ function() {
    function t(t) {
        var n = this;
        /**
         * The auth token listener registered with FirebaseApp, retained here so we
         * can unregister it.
         */        this.p = null, 
        /** Tracks the current User. */
        this.currentUser = _.UNAUTHENTICATED, this.g = !1, 
        /**
         * Counter used to detect if the token changed while a getToken request was
         * outstanding.
         */
        this._ = 0, 
        /** The listener registered with setChangeListener(). */
        this.h = null, this.forceRefresh = !1, this.p = function() {
            n._++, n.currentUser = n.T(), n.g = !0, n.h && n.h(n.currentUser);
        }, this._ = 0, this.auth = t.getImmediate({
            optional: !0
        }), this.auth ? this.auth.addAuthTokenListener(this.p) : (
        // if auth is not available, invoke tokenListener once with null token
        this.p(null), t.get().then((function(t) {
            n.auth = t, n.p && 
            // tokenListener can be removed by removeChangeListener()
            n.auth.addAuthTokenListener(n.p);
        }), (function() {
            /* this.authProvider.get() never rejects */})));
    }
    return t.prototype.getToken = function() {
        var t = this;
        Wn(null != this.p, "getToken cannot be called after listener removed.");
        // Take note of the current value of the tokenCounter so that this method
        // can fail (with an ABORTED error) if there is a token change while the
        // request is outstanding.
        var n = this._, i = this.forceRefresh;
        return this.forceRefresh = !1, this.auth ? this.auth.getToken(i).then((function(i) {
            // Cancel the request since the token changed while the request was
            // outstanding so the response is potentially for a previous user (which
            // user, we can't be sure).
            if (t._ !== n) throw new E(T.ABORTED, "getToken aborted due to token change.");
            return i ? (Wn("string" == typeof i.accessToken, "Invalid tokenData returned from getToken():" + i), 
            new S(i.accessToken, t.currentUser)) : null;
        })) : Promise.resolve(null);
    }, t.prototype.l = function() {
        this.forceRefresh = !0;
    }, t.prototype.v = function(t) {
        Wn(!this.h, "Can only call setChangeListener() once."), this.h = t, 
        // Fire the initial event
        this.g && t(this.currentUser);
    }, t.prototype.m = function() {
        Wn(null != this.p, "removeChangeListener() called twice"), Wn(null !== this.h, "removeChangeListener() called when no listener registered"), 
        this.auth && this.auth.removeAuthTokenListener(this.p), this.p = null, this.h = null;
    }, 
    // Auth.getUid() can return null even with a user logged in. It is because
    // getUid() is synchronous, but the auth code populating Uid is asynchronous.
    // This method should only be called in the AuthTokenListener callback
    // to guarantee to get the actual user.
    t.prototype.T = function() {
        var t = this.auth && this.auth.getUid();
        return Wn(null === t || "string" == typeof t, "Received invalid UID: " + t), new _(t);
    }, t;
}(), C = /** @class */ function() {
    function t(t, n) {
        this.S = t, this.A = n, this.type = "FirstParty", this.user = _.u;
    }
    return Object.defineProperty(t.prototype, "o", {
        get: function() {
            var t = {
                "X-Goog-AuthUser": this.A
            }, n = this.S.auth.I([]);
            return n && (t.Authorization = n), t;
        },
        enumerable: !0,
        configurable: !0
    }), t;
}(), R = /** @class */ function() {
    function t(t, n) {
        this.S = t, this.A = n;
    }
    return t.prototype.getToken = function() {
        return Promise.resolve(new C(this.S, this.A));
    }, t.prototype.v = function(t) {
        // Fire with initial uid.
        t(_.u);
    }, t.prototype.m = function() {}, t.prototype.l = function() {}, t;
}();

/**
 * @license
 * Copyright 2017 Google Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Simple wrapper around a nullable UID. Mostly exists to make code more
 * readable.
 */
/**
 * Builds a CredentialsProvider depending on the type of
 * the credentials passed in.
 */
function N(t) {
    if (!t) return new A;
    switch (t.type) {
      case "gapi":
        var n = t.C;
        // Make sure this really is a Gapi client.
                return Wn(!("object" != typeof n || null === n || !n.auth || !n.auth.getAuthHeaderValueForFirstParty), "unexpected gapi interface"), 
        new R(n, t.A || "0");

      case "provider":
        return t.C;

      default:
        throw new E(T.INVALID_ARGUMENT, "makeCredentialsProvider failed due to invalid credential type");
    }
}

/**
 * @license
 * Copyright 2017 Google Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// The earlist date supported by Firestore timestamps (0001-01-01T00:00:00Z).
var O = /** @class */ function() {
    function t(t, n) {
        if (this.seconds = t, this.nanoseconds = n, n < 0) throw new E(T.INVALID_ARGUMENT, "Timestamp nanoseconds out of range: " + n);
        if (n >= 1e9) throw new E(T.INVALID_ARGUMENT, "Timestamp nanoseconds out of range: " + n);
        if (t < -62135596800) throw new E(T.INVALID_ARGUMENT, "Timestamp seconds out of range: " + t);
        // This will break in the year 10,000.
                if (t >= 253402300800) throw new E(T.INVALID_ARGUMENT, "Timestamp seconds out of range: " + t);
    }
    return t.now = function() {
        return t.fromMillis(Date.now());
    }, t.fromDate = function(n) {
        return t.fromMillis(n.getTime());
    }, t.fromMillis = function(n) {
        var i = Math.floor(n / 1e3);
        return new t(i, 1e6 * (n - 1e3 * i));
    }, t.prototype.toDate = function() {
        return new Date(this.toMillis());
    }, t.prototype.toMillis = function() {
        return 1e3 * this.seconds + this.nanoseconds / 1e6;
    }, t.prototype.R = function(t) {
        return this.seconds === t.seconds ? zn(this.nanoseconds, t.nanoseconds) : zn(this.seconds, t.seconds);
    }, t.prototype.isEqual = function(t) {
        return t.seconds === this.seconds && t.nanoseconds === this.nanoseconds;
    }, t.prototype.toString = function() {
        return "Timestamp(seconds=" + this.seconds + ", nanoseconds=" + this.nanoseconds + ")";
    }, t.prototype.valueOf = function() {
        // This method returns a string of the form <seconds>.<nanoseconds> where <seconds> is
        // translated to have a non-negative value and both <seconds> and <nanoseconds> are left-padded
        // with zeroes to be a consistent length. Strings with this format then have a lexiographical
        // ordering that matches the expected ordering. The <seconds> translation is done to avoid
        // having a leading negative sign (i.e. a leading '-' character) in its string representation,
        // which would affect its lexiographical ordering.
        var t = this.seconds - -62135596800;
        // Note: Up to 12 decimal digits are required to represent all valid 'seconds' values.
                return String(t).padStart(12, "0") + "." + String(this.nanoseconds).padStart(9, "0");
    }, t;
}(), D = /** @class */ function() {
    function t(t) {
        this.timestamp = t;
    }
    // TODO(b/34176344): Once we no longer need to use the old alpha protos,
    // delete this constructor and use a timestamp-backed version everywhere.
        return t.N = function(n) {
        var i = Math.floor(n / 1e6);
        return new t(new O(i, n % 1e6 * 1e3));
    }, t.O = function(n) {
        return new t(n);
    }, t.D = function() {
        return t.MIN;
    }, t.prototype.V = function(t) {
        return this.timestamp.R(t.timestamp);
    }, t.prototype.isEqual = function(t) {
        return this.timestamp.isEqual(t.timestamp);
    }, 
    /** Returns a number representation of the version for use in spec tests. */
    t.prototype.P = function() {
        // Convert to microseconds.
        return 1e6 * this.timestamp.seconds + this.timestamp.nanoseconds / 1e3;
    }, t.prototype.toString = function() {
        return "SnapshotVersion(" + this.timestamp.toString() + ")";
    }, t.prototype.k = function() {
        return this.timestamp;
    }, t.MIN = new t(new O(0, 0)), t;
}(), V = /** @class */ function() {
    function t(t, n, i) {
        void 0 === n ? n = 0 : n > t.length && Qn("offset " + n + " out of range " + t.length), 
        void 0 === i ? i = t.length - n : i > t.length - n && Qn("length " + i + " out of range " + (t.length - n)), 
        this.segments = t, this.offset = n, this.len = i;
    }
    return Object.defineProperty(t.prototype, "length", {
        get: function() {
            return this.len;
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.isEqual = function(n) {
        return 0 === t.F(this, n);
    }, t.prototype.child = function(n) {
        var i = this.segments.slice(this.offset, this.limit());
        return n instanceof t ? n.forEach((function(t) {
            i.push(t);
        })) : i.push(n), this.j(i);
    }, 
    /** The index of one past the last segment of the path. */
    t.prototype.limit = function() {
        return this.offset + this.length;
    }, t.prototype.U = function(t) {
        return t = void 0 === t ? 1 : t, Wn(this.length >= t, "Can't call popFirst() with less segments"), 
        this.j(this.segments, this.offset + t, this.length - t);
    }, t.prototype.L = function() {
        return Wn(!this.M(), "Can't call popLast() on empty path"), this.j(this.segments, this.offset, this.length - 1);
    }, t.prototype.q = function() {
        return Wn(!this.M(), "Can't call firstSegment() on empty path"), this.segments[this.offset];
    }, t.prototype.B = function() {
        return this.get(this.length - 1);
    }, t.prototype.get = function(t) {
        return Wn(t < this.length, "Index out of range"), this.segments[this.offset + t];
    }, t.prototype.M = function() {
        return 0 === this.length;
    }, t.prototype.W = function(t) {
        if (t.length < this.length) return !1;
        for (var n = 0; n < this.length; n++) if (this.get(n) !== t.get(n)) return !1;
        return !0;
    }, t.prototype.G = function(t) {
        if (this.length + 1 !== t.length) return !1;
        for (var n = 0; n < this.length; n++) if (this.get(n) !== t.get(n)) return !1;
        return !0;
    }, t.prototype.forEach = function(t) {
        for (var n = this.offset, i = this.limit(); n < i; n++) t(this.segments[n]);
    }, t.prototype.J = function() {
        return this.segments.slice(this.offset, this.limit());
    }, t.F = function(t, n) {
        for (var i = Math.min(t.length, n.length), e = 0; e < i; e++) {
            var r = t.get(e), s = n.get(e);
            if (r < s) return -1;
            if (r > s) return 1;
        }
        return t.length < n.length ? -1 : t.length > n.length ? 1 : 0;
    }, t;
}(), P = /** @class */ function(t) {
    function i() {
        return null !== t && t.apply(this, arguments) || this;
    }
    return n(i, t), i.prototype.j = function(t, n, e) {
        return new i(t, n, e);
    }, i.prototype.H = function() {
        // NOTE: The client is ignorant of any path segments containing escape
        // sequences (e.g. __id123__) and just passes them through raw (they exist
        // for legacy reasons and should not be used frequently).
        return this.J().join("/");
    }, i.prototype.toString = function() {
        return this.H();
    }, 
    /**
     * Creates a resource path from the given slash-delimited string.
     */
    i.Y = function(t) {
        // NOTE: The client is ignorant of any path segments containing escape
        // sequences (e.g. __id123__) and just passes them through raw (they exist
        // for legacy reasons and should not be used frequently).
        if (t.indexOf("//") >= 0) throw new E(T.INVALID_ARGUMENT, "Invalid path (" + t + "). Paths must not contain // in them.");
        // We may still have an empty segment at the beginning or end if they had a
        // leading or trailing slash (which we allow).
                return new i(t.split("/").filter((function(t) {
            return t.length > 0;
        })));
    }, i.K = new i([]), i;
}(V), k = /^[_a-zA-Z][_a-zA-Z0-9]*$/, F = /** @class */ function(t) {
    function i() {
        return null !== t && t.apply(this, arguments) || this;
    }
    return n(i, t), i.prototype.j = function(t, n, e) {
        return new i(t, n, e);
    }, 
    /**
     * Returns true if the string could be used as a segment in a field path
     * without escaping.
     */
    i.X = function(t) {
        return k.test(t);
    }, i.prototype.H = function() {
        return this.J().map((function(t) {
            return t = t.replace("\\", "\\\\").replace("`", "\\`"), i.X(t) || (t = "`" + t + "`"), 
            t;
        })).join(".");
    }, i.prototype.toString = function() {
        return this.H();
    }, 
    /**
     * Returns true if this field references the key of a document.
     */
    i.prototype.$ = function() {
        return 1 === this.length && "__name__" === this.get(0);
    }, 
    /**
     * The field designating the key of a document.
     */
    i.Z = function() {
        return new i([ "__name__" ]);
    }, 
    /**
     * Parses a field string from the given server-formatted string.
     *
     * - Splitting the empty string is not allowed (for now at least).
     * - Empty segments within the string (e.g. if there are two consecutive
     *   separators) are not allowed.
     *
     * TODO(b/37244157): we should make this more strict. Right now, it allows
     * non-identifier path components, even if they aren't escaped.
     */
    i.tt = function(t) {
        for (var n = [], e = "", r = 0, s = function() {
            if (0 === e.length) throw new E(T.INVALID_ARGUMENT, "Invalid field path (" + t + "). Paths must not be empty, begin with '.', end with '.', or contain '..'");
            n.push(e), e = "";
        }, u = !1; r < t.length; ) {
            var o = t[r];
            if ("\\" === o) {
                if (r + 1 === t.length) throw new E(T.INVALID_ARGUMENT, "Path has trailing escape character: " + t);
                var h = t[r + 1];
                if ("\\" !== h && "." !== h && "`" !== h) throw new E(T.INVALID_ARGUMENT, "Path has invalid escape sequence: " + t);
                e += h, r += 2;
            } else "`" === o ? (u = !u, r++) : "." !== o || u ? (e += o, r++) : (s(), r++);
        }
        if (s(), u) throw new E(T.INVALID_ARGUMENT, "Unterminated ` in path: " + t);
        return new i(n);
    }, i.K = new i([]), i;
}(V), j = /** @class */ function() {
    function t(n) {
        this.path = n, Wn(t.nt(n), "Invalid DocumentKey with an odd number of segments: " + n.J().join("/"));
    }
    return t.it = function(n) {
        return new t(P.Y(n).U(5));
    }, 
    /** Returns true if the document is in the specified collectionId. */
    t.prototype.et = function(t) {
        return this.path.length >= 2 && this.path.get(this.path.length - 2) === t;
    }, t.prototype.isEqual = function(t) {
        return null !== t && 0 === P.F(this.path, t.path);
    }, t.prototype.toString = function() {
        return this.path.toString();
    }, t.F = function(t, n) {
        return P.F(t.path, n.path);
    }, t.nt = function(t) {
        return t.length % 2 == 0;
    }, 
    /**
     * Creates and returns a new document key with the given segments.
     *
     * @param segments The segments of the path to the document
     * @return A new instance of DocumentKey
     */
    t.rt = function(n) {
        return new t(new P(n.slice()));
    }, t.EMPTY = new t(new P([])), t;
}(), U = /** @class */ function() {
    function t(t, n) {
        this.F = t, this.root = n || M.EMPTY;
    }
    // Returns a copy of the map, with the specified key/value added or replaced.
        return t.prototype.st = function(n, i) {
        return new t(this.F, this.root.st(n, i, this.F).copy(null, null, M.ut, null, null));
    }, 
    // Returns a copy of the map, with the specified key removed.
    t.prototype.remove = function(n) {
        return new t(this.F, this.root.remove(n, this.F).copy(null, null, M.ut, null, null));
    }, 
    // Returns the value of the node with the given key, or null.
    t.prototype.get = function(t) {
        for (var n = this.root; !n.M(); ) {
            var i = this.F(t, n.key);
            if (0 === i) return n.value;
            i < 0 ? n = n.left : i > 0 && (n = n.right);
        }
        return null;
    }, 
    // Returns the index of the element in this sorted map, or -1 if it doesn't
    // exist.
    t.prototype.indexOf = function(t) {
        for (
        // Number of nodes that were pruned when descending right
        var n = 0, i = this.root; !i.M(); ) {
            var e = this.F(t, i.key);
            if (0 === e) return n + i.left.size;
            e < 0 ? i = i.left : (
            // Count all nodes left of the node plus the node itself
            n += i.left.size + 1, i = i.right);
        }
        // Node not found
                return -1;
    }, t.prototype.M = function() {
        return this.root.M();
    }, Object.defineProperty(t.prototype, "size", {
        // Returns the total number of nodes in the map.
        get: function() {
            return this.root.size;
        },
        enumerable: !0,
        configurable: !0
    }), 
    // Returns the minimum key in the map.
    t.prototype.ot = function() {
        return this.root.ot();
    }, 
    // Returns the maximum key in the map.
    t.prototype.ht = function() {
        return this.root.ht();
    }, 
    // Traverses the map in key order and calls the specified action function
    // for each key/value pair. If action returns true, traversal is aborted.
    // Returns the first truthy value returned by action, or the last falsey
    // value returned by action.
    t.prototype.at = function(t) {
        return this.root.at(t);
    }, t.prototype.forEach = function(t) {
        this.at((function(n, i) {
            return t(n, i), !1;
        }));
    }, t.prototype.toString = function() {
        var t = [];
        return this.at((function(n, i) {
            return t.push(n + ":" + i), !1;
        })), "{" + t.join(", ") + "}";
    }, 
    // Traverses the map in reverse key order and calls the specified action
    // function for each key/value pair. If action returns true, traversal is
    // aborted.
    // Returns the first truthy value returned by action, or the last falsey
    // value returned by action.
    t.prototype.ct = function(t) {
        return this.root.ct(t);
    }, 
    // Returns an iterator over the SortedMap.
    t.prototype.ft = function() {
        return new L(this.root, null, this.F, !1);
    }, t.prototype.lt = function(t) {
        return new L(this.root, t, this.F, !1);
    }, t.prototype.dt = function() {
        return new L(this.root, null, this.F, !0);
    }, t.prototype.vt = function(t) {
        return new L(this.root, t, this.F, !0);
    }, t;
}(), L = /** @class */ function() {
    function t(t, n, i, e) {
        this.wt = e, this.pt = [];
        for (var r = 1; !t.M(); ) if (r = n ? i(t.key, n) : 1, 
        // flip the comparison if we're going in reverse
        e && (r *= -1), r < 0) 
        // This node is less than our start key. ignore it
        t = this.wt ? t.left : t.right; else {
            if (0 === r) {
                // This node is exactly equal to our start key. Push it on the stack,
                // but stop iterating;
                this.pt.push(t);
                break;
            }
            // This node is greater than our start key, add it to the stack and move
            // to the next one
            this.pt.push(t), t = this.wt ? t.right : t.left;
        }
    }
    return t.prototype.gt = function() {
        Wn(this.pt.length > 0, "getNext() called on iterator when hasNext() is false.");
        var t = this.pt.pop(), n = {
            key: t.key,
            value: t.value
        };
        if (this.wt) for (t = t.left; !t.M(); ) this.pt.push(t), t = t.right; else for (t = t.right; !t.M(); ) this.pt.push(t), 
        t = t.left;
        return n;
    }, t.prototype.bt = function() {
        return this.pt.length > 0;
    }, t.prototype.yt = function() {
        if (0 === this.pt.length) return null;
        var t = this.pt[this.pt.length - 1];
        return {
            key: t.key,
            value: t.value
        };
    }, t;
}(), M = /** @class */ function() {
    function t(n, i, e, r, s) {
        this.key = n, this.value = i, this.color = null != e ? e : t.RED, this.left = null != r ? r : t.EMPTY, 
        this.right = null != s ? s : t.EMPTY, this.size = this.left.size + 1 + this.right.size;
    }
    // Returns a copy of the current node, optionally replacing pieces of it.
        return t.prototype.copy = function(n, i, e, r, s) {
        return new t(null != n ? n : this.key, null != i ? i : this.value, null != e ? e : this.color, null != r ? r : this.left, null != s ? s : this.right);
    }, t.prototype.M = function() {
        return !1;
    }, 
    // Traverses the tree in key order and calls the specified action function
    // for each node. If action returns true, traversal is aborted.
    // Returns the first truthy value returned by action, or the last falsey
    // value returned by action.
    t.prototype.at = function(t) {
        return this.left.at(t) || t(this.key, this.value) || this.right.at(t);
    }, 
    // Traverses the tree in reverse key order and calls the specified action
    // function for each node. If action returns true, traversal is aborted.
    // Returns the first truthy value returned by action, or the last falsey
    // value returned by action.
    t.prototype.ct = function(t) {
        return this.right.ct(t) || t(this.key, this.value) || this.left.ct(t);
    }, 
    // Returns the minimum node in the tree.
    t.prototype.min = function() {
        return this.left.M() ? this : this.left.min();
    }, 
    // Returns the maximum key in the tree.
    t.prototype.ot = function() {
        return this.min().key;
    }, 
    // Returns the maximum key in the tree.
    t.prototype.ht = function() {
        return this.right.M() ? this.key : this.right.ht();
    }, 
    // Returns new tree, with the key/value added.
    t.prototype.st = function(t, n, i) {
        var e = this, r = i(t, e.key);
        return (e = r < 0 ? e.copy(null, null, null, e.left.st(t, n, i), null) : 0 === r ? e.copy(null, n, null, null, null) : e.copy(null, null, null, null, e.right.st(t, n, i)))._t();
    }, t.prototype.Tt = function() {
        if (this.left.M()) return t.EMPTY;
        var n = this;
        return n.left.Et() || n.left.left.Et() || (n = n.St()), (n = n.copy(null, null, null, n.left.Tt(), null))._t();
    }, 
    // Returns new tree, with the specified item removed.
    t.prototype.remove = function(n, i) {
        var e, r = this;
        if (i(n, r.key) < 0) r.left.M() || r.left.Et() || r.left.left.Et() || (r = r.St()), 
        r = r.copy(null, null, null, r.left.remove(n, i), null); else {
            if (r.left.Et() && (r = r.At()), r.right.M() || r.right.Et() || r.right.left.Et() || (r = r.It()), 
            0 === i(n, r.key)) {
                if (r.right.M()) return t.EMPTY;
                e = r.right.min(), r = r.copy(e.key, e.value, null, null, r.right.Tt());
            }
            r = r.copy(null, null, null, null, r.right.remove(n, i));
        }
        return r._t();
    }, t.prototype.Et = function() {
        return this.color;
    }, 
    // Returns new tree after performing any needed rotations.
    t.prototype._t = function() {
        var t = this;
        return t.right.Et() && !t.left.Et() && (t = t.Ct()), t.left.Et() && t.left.left.Et() && (t = t.At()), 
        t.left.Et() && t.right.Et() && (t = t.Rt()), t;
    }, t.prototype.St = function() {
        var t = this.Rt();
        return t.right.left.Et() && (t = (t = (t = t.copy(null, null, null, null, t.right.At())).Ct()).Rt()), 
        t;
    }, t.prototype.It = function() {
        var t = this.Rt();
        return t.left.left.Et() && (t = (t = t.At()).Rt()), t;
    }, t.prototype.Ct = function() {
        var n = this.copy(null, null, t.RED, null, this.right.left);
        return this.right.copy(null, null, this.color, n, null);
    }, t.prototype.At = function() {
        var n = this.copy(null, null, t.RED, this.left.right, null);
        return this.left.copy(null, null, this.color, null, n);
    }, t.prototype.Rt = function() {
        var t = this.left.copy(null, null, !this.left.color, null, null), n = this.right.copy(null, null, !this.right.color, null, null);
        return this.copy(null, null, !this.color, t, n);
    }, 
    // For testing.
    t.prototype.Nt = function() {
        var t = this.Ot();
        return Math.pow(2, t) <= this.size + 1;
    }, 
    // In a balanced RB tree, the black-depth (number of black nodes) from root to
    // leaves is equal on both sides.  This function verifies that or asserts.
    t.prototype.Ot = function() {
        if (this.Et() && this.left.Et()) throw Qn("Red node has red child(" + this.key + "," + this.value + ")");
        if (this.right.Et()) throw Qn("Right child of (" + this.key + "," + this.value + ") is red");
        var t = this.left.Ot();
        if (t !== this.right.Ot()) throw Qn("Black depths differ");
        return t + (this.Et() ? 0 : 1);
    }, 
    // Empty node is shared between all LLRB trees.
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    t.EMPTY = null, t.RED = !0, t.ut = !1, t;
}(), q = /** @class */ function() {
    function t() {
        this.size = 0;
    }
    return Object.defineProperty(t.prototype, "key", {
        get: function() {
            throw Qn("LLRBEmptyNode has no key.");
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(t.prototype, "value", {
        get: function() {
            throw Qn("LLRBEmptyNode has no value.");
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(t.prototype, "color", {
        get: function() {
            throw Qn("LLRBEmptyNode has no color.");
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(t.prototype, "left", {
        get: function() {
            throw Qn("LLRBEmptyNode has no left child.");
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(t.prototype, "right", {
        get: function() {
            throw Qn("LLRBEmptyNode has no right child.");
        },
        enumerable: !0,
        configurable: !0
    }), 
    // Returns a copy of the current node.
    t.prototype.copy = function(t, n, i, e, r) {
        return this;
    }, 
    // Returns a copy of the tree, with the specified key/value added.
    t.prototype.st = function(t, n, i) {
        return new M(t, n);
    }, 
    // Returns a copy of the tree, with the specified key removed.
    t.prototype.remove = function(t, n) {
        return this;
    }, t.prototype.M = function() {
        return !0;
    }, t.prototype.at = function(t) {
        return !1;
    }, t.prototype.ct = function(t) {
        return !1;
    }, t.prototype.ot = function() {
        return null;
    }, t.prototype.ht = function() {
        return null;
    }, t.prototype.Et = function() {
        return !1;
    }, 
    // For testing.
    t.prototype.Nt = function() {
        return !0;
    }, t.prototype.Ot = function() {
        return 0;
    }, t;
}();

// end LLRBEmptyNode
M.EMPTY = new q;

/**
 * @license
 * Copyright 2017 Google Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * SortedSet is an immutable (copy-on-write) collection that holds elements
 * in order specified by the provided comparator.
 *
 * NOTE: if provided comparator returns 0 for two elements, we consider them to
 * be equal!
 */
var x = /** @class */ function() {
    function t(t) {
        this.F = t, this.data = new U(this.F);
    }
    /**
     * Creates a SortedSet from the keys of the map.
     * This is currently implemented as an O(n) copy.
     */    return t.Dt = function(n) {
        var i = new t(n.F);
        return n.forEach((function(t) {
            i = i.add(t);
        })), i;
    }, t.prototype.has = function(t) {
        return null !== this.data.get(t);
    }, t.prototype.first = function() {
        return this.data.ot();
    }, t.prototype.last = function() {
        return this.data.ht();
    }, Object.defineProperty(t.prototype, "size", {
        get: function() {
            return this.data.size;
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.indexOf = function(t) {
        return this.data.indexOf(t);
    }, 
    /** Iterates elements in order defined by "comparator" */
    t.prototype.forEach = function(t) {
        this.data.at((function(n, i) {
            return t(n), !1;
        }));
    }, 
    /** Iterates over `elem`s such that: range[0] <= elem < range[1]. */
    t.prototype.Vt = function(t, n) {
        for (var i = this.data.lt(t[0]); i.bt(); ) {
            var e = i.gt();
            if (this.F(e.key, t[1]) >= 0) return;
            n(e.key);
        }
    }, 
    /**
     * Iterates over `elem`s such that: start <= elem until false is returned.
     */
    t.prototype.Pt = function(t, n) {
        var i;
        for (i = void 0 !== n ? this.data.lt(n) : this.data.ft(); i.bt(); ) {
            if (!t(i.gt().key)) return;
        }
    }, 
    /** Finds the least element greater than or equal to `elem`. */
    t.prototype.kt = function(t) {
        var n = this.data.lt(t);
        return n.bt() ? n.gt().key : null;
    }, t.prototype.ft = function() {
        return new B(this.data.ft());
    }, t.prototype.lt = function(t) {
        return new B(this.data.lt(t));
    }, 
    /** Inserts or updates an element */
    t.prototype.add = function(t) {
        return this.copy(this.data.remove(t).st(t, !0));
    }, 
    /** Deletes an element */
    t.prototype.delete = function(t) {
        return this.has(t) ? this.copy(this.data.remove(t)) : this;
    }, t.prototype.M = function() {
        return this.data.M();
    }, t.prototype.Ft = function(t) {
        var n = this;
        // Make sure `result` always refers to the larger one of the two sets.
                return n.size < t.size && (n = t, t = this), t.forEach((function(t) {
            n = n.add(t);
        })), n;
    }, t.prototype.isEqual = function(n) {
        if (!(n instanceof t)) return !1;
        if (this.size !== n.size) return !1;
        for (var i = this.data.ft(), e = n.data.ft(); i.bt(); ) {
            var r = i.gt().key, s = e.gt().key;
            if (0 !== this.F(r, s)) return !1;
        }
        return !0;
    }, t.prototype.J = function() {
        var t = [];
        return this.forEach((function(n) {
            t.push(n);
        })), t;
    }, t.prototype.toString = function() {
        var t = [];
        return this.forEach((function(n) {
            return t.push(n);
        })), "SortedSet(" + t.toString() + ")";
    }, t.prototype.copy = function(n) {
        var i = new t(this.F);
        return i.data = n, i;
    }, t;
}(), B = /** @class */ function() {
    function t(t) {
        this.jt = t;
    }
    return t.prototype.gt = function() {
        return this.jt.gt().key;
    }, t.prototype.bt = function() {
        return this.jt.bt();
    }, t;
}();

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function Q(t) {
    var n = 0;
    for (var i in t) Object.prototype.hasOwnProperty.call(t, i) && n++;
    return n;
}

function W(t, n) {
    for (var i in t) Object.prototype.hasOwnProperty.call(t, i) && n(i, t[i]);
}

function G(t) {
    for (var n in Wn(null != t && "object" == typeof t, "isEmpty() expects object parameter."), 
    t) if (Object.prototype.hasOwnProperty.call(t, n)) return !1;
    return !0;
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Immutable class that represents a "proto" byte string.
 *
 * Proto byte strings can either be Base64-encoded strings or Uint8Arrays when
 * sent on the wire. This class abstracts away this differentiation by holding
 * the proto byte string in a common class that must be converted into a string
 * before being sent as a proto.
 */ var z = /** @class */ function() {
    function t(t) {
        this.Ut = t;
    }
    return t.fromBase64String = function(n) {
        return new t(jn.Lt().atob(n));
    }, t.fromUint8Array = function(n) {
        return new t(
        /**
 * Helper function to convert an Uint8array to a binary string.
 */
        function(t) {
            for (var n = "", i = 0; i < t.length; ++i) n += String.fromCharCode(t[i]);
            return n;
        }
        /**
 * Helper function to convert a binary string to an Uint8Array.
 */ (n));
    }, t.prototype.toBase64 = function() {
        return jn.Lt().btoa(this.Ut);
    }, t.prototype.toUint8Array = function() {
        return function(t) {
            for (var n = new Uint8Array(t.length), i = 0; i < t.length; i++) n[i] = t.charCodeAt(i);
            return n;
        }
        /**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
        /**
 * Returns whether a variable is either undefined or null.
 */ (this.Ut);
    }, t.prototype.Mt = function() {
        return 2 * this.Ut.length;
    }, t.prototype.V = function(t) {
        return zn(this.Ut, t.Ut);
    }, t.prototype.isEqual = function(t) {
        return this.Ut === t.Ut;
    }, t.qt = new t(""), t;
}();

function J(t) {
    return null == t;
}

/** Returns whether the value represents -0. */ function H(t) {
    // Detect if the value is -0.0. Based on polyfill from
    // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
    return -0 === t && 1 / t == -1 / 0;
}

/**
 * Returns whether a value is an integer and in the safe integer range
 * @param value The value to test for being an integer and in the safe range
 */ function Y(t) {
    return "number" == typeof t && Number.isInteger(t) && !H(t) && t <= Number.MAX_SAFE_INTEGER && t >= Number.MIN_SAFE_INTEGER;
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Represents a locally-applied ServerTimestamp.
 *
 * Server Timestamps are backed by MapValues that contain an internal field
 * `__type__` with a value of `server_timestamp`. The previous value and local
 * write time are stored in its `__previous_value__` and `__local_write_time__`
 * fields respectively.
 *
 * Notes:
 * - ServerTimestampValue instances are created as the result of applying a
 *   TransformMutation (see TransformMutation.applyTo()). They can only exist in
 *   the local view of a document. Therefore they do not need to be parsed or
 *   serialized.
 * - When evaluated locally (e.g. for snapshot.data()), they by default
 *   evaluate to `null`. This behavior can be configured by passing custom
 *   FieldValueOptions to value().
 * - With respect to other ServerTimestampValues, they sort by their
 *   localWriteTime.
 */ function K(t) {
    var n, i;
    return "server_timestamp" === (null === (i = ((null === (n = null == t ? void 0 : t.mapValue) || void 0 === n ? void 0 : n.fields) || {}).__type__) || void 0 === i ? void 0 : i.stringValue);
}

/**
 * Creates a new ServerTimestamp proto value (using the internal format).
 */
/**
 * Returns the local time at which this timestamp was first set.
 */
function X(t) {
    var n = ut(t.mapValue.fields.__local_write_time__.timestampValue);
    return new O(n.seconds, n.nanos);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// A RegExp matching ISO 8601 UTC timestamps with optional fraction.
var $ = new RegExp(/^\d{4}-\d\d-\d\dT\d\d:\d\d:\d\d(?:\.(\d+))?Z$/);

/** Extracts the backend's type order for the provided value. */ function Z(t) {
    return "nullValue" in t ? 0 /* NullValue */ : "booleanValue" in t ? 1 /* BooleanValue */ : "integerValue" in t || "doubleValue" in t ? 2 /* NumberValue */ : "timestampValue" in t ? 3 /* TimestampValue */ : "stringValue" in t ? 5 /* StringValue */ : "bytesValue" in t ? 6 /* BlobValue */ : "referenceValue" in t ? 7 /* RefValue */ : "geoPointValue" in t ? 8 /* GeoPointValue */ : "arrayValue" in t ? 9 /* ArrayValue */ : "mapValue" in t ? K(t) ? 4 /* ServerTimestampValue */ : 10 /* ObjectValue */ : Qn("Invalid value type: " + JSON.stringify(t));
}

/** Tests `left` and `right` for equality based on the backend semantics. */ function tt(t, n) {
    var i = Z(t);
    if (i !== Z(n)) return !1;
    switch (i) {
      case 0 /* NullValue */ :
        return !0;

      case 1 /* BooleanValue */ :
        return t.booleanValue === n.booleanValue;

      case 4 /* ServerTimestampValue */ :
        return X(t).isEqual(X(n));

      case 3 /* TimestampValue */ :
        return function(t, n) {
            if ("string" == typeof t.timestampValue && "string" == typeof n.timestampValue && t.timestampValue.length === n.timestampValue.length) 
            // Use string equality for ISO 8601 timestamps
            return t.timestampValue === n.timestampValue;
            var i = ut(t.timestampValue), e = ut(n.timestampValue);
            return i.seconds === e.seconds && i.nanos === e.nanos;
        }(t, n);

      case 5 /* StringValue */ :
        return t.stringValue === n.stringValue;

      case 6 /* BlobValue */ :
        return function(t, n) {
            return ht(t.bytesValue).isEqual(ht(n.bytesValue));
        }(t, n);

      case 7 /* RefValue */ :
        return t.referenceValue === n.referenceValue;

      case 8 /* GeoPointValue */ :
        return function(t, n) {
            return ot(t.geoPointValue.latitude) === ot(n.geoPointValue.latitude) && ot(t.geoPointValue.longitude) === ot(n.geoPointValue.longitude);
        }(t, n);

      case 2 /* NumberValue */ :
        return function(t, n) {
            if ("integerValue" in t && "integerValue" in n) return ot(t.integerValue) === ot(n.integerValue);
            if ("doubleValue" in t && "doubleValue" in n) {
                var i = ot(t.doubleValue), e = ot(n.doubleValue);
                return i === e ? H(i) === H(e) : isNaN(i) && isNaN(e);
            }
            return !1;
        }(t, n);

      case 9 /* ArrayValue */ :
        return Jn(t.arrayValue.values || [], n.arrayValue.values || [], tt);

      case 10 /* ObjectValue */ :
        return function(t, n) {
            var i = t.mapValue.fields || {}, e = n.mapValue.fields || {};
            if (Q(i) !== Q(e)) return !1;
            for (var r in i) if (i.hasOwnProperty(r) && (void 0 === e[r] || !tt(i[r], e[r]))) return !1;
            return !0;
        }
        /** Returns true if the ArrayValue contains the specified element. */ (t, n);

      default:
        return Qn("Unexpected value type: " + JSON.stringify(t));
    }
}

function nt(t, n) {
    return void 0 !== (t.values || []).find((function(t) {
        return tt(t, n);
    }));
}

function it(t, n) {
    var i = Z(t), e = Z(n);
    if (i !== e) return zn(i, e);
    switch (i) {
      case 0 /* NullValue */ :
        return 0;

      case 1 /* BooleanValue */ :
        return zn(t.booleanValue, n.booleanValue);

      case 2 /* NumberValue */ :
        return function(t, n) {
            var i = ot(t.integerValue || t.doubleValue), e = ot(n.integerValue || n.doubleValue);
            return i < e ? -1 : i > e ? 1 : i === e ? 0 : 
            // one or both are NaN.
            isNaN(i) ? isNaN(e) ? 0 : -1 : 1;
        }(t, n);

      case 3 /* TimestampValue */ :
        return et(t.timestampValue, n.timestampValue);

      case 4 /* ServerTimestampValue */ :
        return et(X(t), X(n));

      case 5 /* StringValue */ :
        return zn(t.stringValue, n.stringValue);

      case 6 /* BlobValue */ :
        return function(t, n) {
            var i = ht(t), e = ht(n);
            return i.V(e);
        }(t.bytesValue, n.bytesValue);

      case 7 /* RefValue */ :
        return function(t, n) {
            for (var i = t.split("/"), e = n.split("/"), r = 0; r < i.length && r < e.length; r++) {
                var s = zn(i[r], e[r]);
                if (0 !== s) return s;
            }
            return zn(i.length, e.length);
        }(t.referenceValue, n.referenceValue);

      case 8 /* GeoPointValue */ :
        return function(t, n) {
            var i = zn(ot(t.latitude), ot(n.latitude));
            if (0 !== i) return i;
            return zn(ot(t.longitude), ot(n.longitude));
        }(t.geoPointValue, n.geoPointValue);

      case 9 /* ArrayValue */ :
        return function(t, n) {
            for (var i = t.values || [], e = n.values || [], r = 0; r < i.length && r < e.length; ++r) {
                var s = it(i[r], e[r]);
                if (s) return s;
            }
            return zn(i.length, e.length);
        }(t.arrayValue, n.arrayValue);

      case 10 /* ObjectValue */ :
        return function(t, n) {
            var i = t.fields || {}, e = Object.keys(i), r = n.fields || {}, s = Object.keys(r);
            // Even though MapValues are likely sorted correctly based on their insertion
            // order (e.g. when received from the backend), local modifications can bring
            // elements out of order. We need to re-sort the elements to ensure that
            // canonical IDs are independent of insertion order.
            e.sort(), s.sort();
            for (var u = 0; u < e.length && u < s.length; ++u) {
                var o = zn(e[u], s[u]);
                if (0 !== o) return o;
                var h = it(i[e[u]], r[s[u]]);
                if (0 !== h) return h;
            }
            return zn(e.length, s.length);
        }
        /**
 * Generates the canonical ID for the provided field value (as used in Target
 * serialization).
 */ (t.mapValue, n.mapValue);

      default:
        throw Qn("Invalid value type: " + i);
    }
}

function et(t, n) {
    if ("string" == typeof t && "string" == typeof n && t.length === n.length) return zn(t, n);
    var i = ut(t), e = ut(n), r = zn(i.seconds, e.seconds);
    return 0 !== r ? r : zn(i.nanos, e.nanos);
}

function rt(t) {
    return st(t);
}

function st(t) {
    return "nullValue" in t ? "null" : "booleanValue" in t ? "" + t.booleanValue : "integerValue" in t ? "" + t.integerValue : "doubleValue" in t ? "" + t.doubleValue : "timestampValue" in t ? (e = t.timestampValue, 
    "time(" + (r = ut(e)).seconds + "," + r.nanos + ")") : "stringValue" in t ? t.stringValue : "bytesValue" in t ? ht(t.bytesValue).toBase64() : "referenceValue" in t ? (i = t.referenceValue, 
    j.it(i).toString()) : "geoPointValue" in t ? "geo(" + (n = t.geoPointValue).latitude + "," + n.longitude + ")" : "arrayValue" in t ? function(t) {
        for (var n = "[", i = !0, e = 0, r = t.values || []; e < r.length; e++) {
            var s = r[e];
            i ? i = !1 : n += ",", n += st(s);
        }
        return n + "]";
    }
    /**
 * Converts the possible Proto values for a timestamp value into a "seconds and
 * nanos" representation.
 */ (t.arrayValue) : "mapValue" in t ? function(t) {
        for (
        // Iteration order in JavaScript is not guaranteed. To ensure that we generate
        // matching canonical IDs for identical maps, we need to sort the keys.
        var n = Object.keys(t.fields || {}).sort(), i = "{", e = !0, r = 0, s = n; r < s.length; r++) {
            var u = s[r];
            e ? e = !1 : i += ",", i += u + ":" + st(t.fields[u]);
        }
        return i + "}";
    }(t.mapValue) : Qn("Invalid value type: " + JSON.stringify(t));
    var n, i, e, r;
}

function ut(t) {
    // The json interface (for the browser) will return an iso timestamp string,
    // while the proto js library (for node) will return a
    // google.protobuf.Timestamp instance.
    if (Wn(!!t, "Cannot normalize null or undefined timestamp."), "string" == typeof t) {
        // The date string can have higher precision (nanos) than the Date class
        // (millis), so we do some custom parsing here.
        // Parse the nanos right out of the string.
        var n = 0, i = $.exec(t);
        if (Wn(!!i, "invalid timestamp: " + t), i[1]) {
            // Pad the fraction out to 9 digits (nanos).
            var e = i[1];
            e = (e + "000000000").substr(0, 9), n = Number(e);
        }
        // Parse the date to get the seconds.
                var r = new Date(t);
        return {
            seconds: Math.floor(r.getTime() / 1e3),
            nanos: n
        };
    }
    return {
        seconds: ot(t.seconds),
        nanos: n = ot(t.nanos)
    };
}

/**
 * Converts the possible Proto types for numbers into a JavaScript number.
 * Returns 0 if the value is not numeric.
 */ function ot(t) {
    // TODO(bjornick): Handle int64 greater than 53 bits.
    return "number" == typeof t ? t : "string" == typeof t ? Number(t) : 0;
}

/** Converts the possible Proto types for Blobs into a ByteString. */ function ht(t) {
    return "string" == typeof t ? z.fromBase64String(t) : z.fromUint8Array(t);
}

/** Returns a reference value for the provided database and key. */ function at(t, n) {
    return {
        referenceValue: "projects/" + t.projectId + "/databases/" + t.database + "/documents/" + n.path.H()
    };
}

/** Returns true if `value` is an IntegerValue . */ function ct(t) {
    return !!t && "integerValue" in t;
}

/** Returns true if `value` is a DoubleValue. */
/** Returns true if `value` is either an IntegerValue or a DoubleValue. */
function ft(t) {
    return ct(t) || function(t) {
        return !!t && "doubleValue" in t;
    }(t);
}

/** Returns true if `value` is an ArrayValue. */ function lt(t) {
    return !!t && "arrayValue" in t;
}

/** Returns true if `value` is a ReferenceValue. */ function dt(t) {
    return !!t && "referenceValue" in t;
}

/** Returns true if `value` is a NullValue. */ function vt(t) {
    return !!t && "nullValue" in t;
}

/** Returns true if `value` is NaN. */ function wt(t) {
    return !!t && "doubleValue" in t && isNaN(Number(t.doubleValue));
}

/** Returns true if `value` is a MapValue. */ function mt(t) {
    return !!t && "mapValue" in t;
}

/**
 * @license
 * Copyright 2018 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/** Transforms a value into a server-generated timestamp. */ var pt = /** @class */ function() {
    function t() {}
    return t.prototype.xt = function(t, n) {
        return function(t, n) {
            var i, e = {
                fields: (i = {}, i.__type__ = {
                    stringValue: "server_timestamp"
                }, i.__local_write_time__ = {
                    timestampValue: {
                        seconds: t.seconds,
                        nanos: t.nanoseconds
                    }
                }, i)
            };
            return n && (e.fields.__previous_value__ = n), {
                mapValue: e
            };
        }
        /**
 * Returns the value of the field before this ServerTimestamp was set.
 *
 * Preserving the previous values allows the user to display the last resoled
 * value until the backend responds with the timestamp.
 */ (n, t);
    }, t.prototype.Bt = function(t, n) {
        return n;
    }, t.prototype.Qt = function(t) {
        return null;
 // Server timestamps are idempotent and don't require a base value.
        }, t.prototype.isEqual = function(n) {
        return n instanceof t;
    }, t.instance = new t, t;
}(), gt = /** @class */ function() {
    function t(t) {
        this.elements = t;
    }
    return t.prototype.xt = function(t, n) {
        return this.apply(t);
    }, t.prototype.Bt = function(t, n) {
        // The server just sends null as the transform result for array operations,
        // so we have to calculate a result the same as we do for local
        // applications.
        return this.apply(t);
    }, t.prototype.apply = function(t) {
        for (var n = _t(t), i = function(t) {
            n.some((function(n) {
                return tt(n, t);
            })) || n.push(t);
        }, e = 0, r = this.elements; e < r.length; e++) {
            i(r[e]);
        }
        return {
            arrayValue: {
                values: n
            }
        };
    }, t.prototype.Qt = function(t) {
        return null;
 // Array transforms are idempotent and don't require a base value.
        }, t.prototype.isEqual = function(n) {
        return n instanceof t && Jn(this.elements, n.elements, tt);
    }, t;
}(), bt = /** @class */ function() {
    function t(t) {
        this.elements = t;
    }
    return t.prototype.xt = function(t, n) {
        return this.apply(t);
    }, t.prototype.Bt = function(t, n) {
        // The server just sends null as the transform result for array operations,
        // so we have to calculate a result the same as we do for local
        // applications.
        return this.apply(t);
    }, t.prototype.apply = function(t) {
        for (var n = _t(t), i = function(t) {
            n = n.filter((function(n) {
                return !tt(n, t);
            }));
        }, e = 0, r = this.elements; e < r.length; e++) {
            i(r[e]);
        }
        return {
            arrayValue: {
                values: n
            }
        };
    }, t.prototype.Qt = function(t) {
        return null;
 // Array transforms are idempotent and don't require a base value.
        }, t.prototype.isEqual = function(n) {
        return n instanceof t && Jn(this.elements, n.elements, tt);
    }, t;
}(), yt = /** @class */ function() {
    function t(t, n) {
        this.serializer = t, this.Wt = n, Wn(ft(n), "NumericIncrementTransform transform requires a NumberValue");
    }
    return t.prototype.xt = function(t, n) {
        // PORTING NOTE: Since JavaScript's integer arithmetic is limited to 53 bit
        // precision and resolves overflows by reducing precision, we do not
        // manually cap overflows at 2^63.
        var i = this.Qt(t), e = this.asNumber(i) + this.asNumber(this.Wt);
        return ct(i) && ct(this.Wt) ? this.serializer.Gt(e) : this.serializer.zt(e);
    }, t.prototype.Bt = function(t, n) {
        return Wn(null !== n, "Didn't receive transformResult for NUMERIC_ADD transform"), 
        n;
    }, 
    /**
     * Inspects the provided value, returning the provided value if it is already
     * a NumberValue, otherwise returning a coerced value of 0.
     */
    t.prototype.Qt = function(t) {
        return ft(t) ? t : {
            integerValue: 0
        };
    }, t.prototype.isEqual = function(n) {
        return n instanceof t && tt(this.Wt, n.Wt);
    }, t.prototype.asNumber = function(t) {
        return ot(t.integerValue || t.doubleValue);
    }, t;
}();

/** Transforms an array value via a union operation. */ function _t(t) {
    return lt(t) && t.arrayValue.values ? t.arrayValue.values.slice() : [];
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Provides a set of fields that can be used to partially patch a document.
 * FieldMask is used in conjunction with ObjectValue.
 * Examples:
 *   foo - Overwrites foo entirely with the provided value. If foo is not
 *         present in the companion ObjectValue, the field is deleted.
 *   foo.bar - Overwrites only the field bar of the object foo.
 *             If foo is not an object, foo is replaced with an object
 *             containing foo
 */ var Tt, Et, St = /** @class */ function() {
    function t(t) {
        this.fields = t;
        // TODO(dimond): validation of FieldMask
        }
    return t.Jt = function(n) {
        return new t(n);
    }, t.Ht = function(n) {
        var i = new x(F.F);
        return n.forEach((function(t) {
            return i = i.add(t);
        })), new t(i);
    }, 
    /**
     * Verifies that `fieldPath` is included by at least one field in this field
     * mask.
     *
     * This is an O(n) operation, where `n` is the size of the field mask.
     */
    t.prototype.Yt = function(t) {
        var n = !1;
        return this.fields.forEach((function(i) {
            i.W(t) && (n = !0);
        })), n;
    }, t.prototype.isEqual = function(t) {
        return this.fields.isEqual(t.fields);
    }, t;
}(), At = /** @class */ function() {
    function t(t, n) {
        this.field = t, this.transform = n;
    }
    return t.prototype.isEqual = function(t) {
        return this.field.isEqual(t.field) && this.transform.isEqual(t.transform);
    }, t;
}(), It = function(
/**
     * The version at which the mutation was committed:
     *
     * - For most operations, this is the updateTime in the WriteResult.
     * - For deletes, the commitTime of the WriteResponse (because deletes are
     *   not stored and have no updateTime).
     *
     * Note that these versions can be different: No-op writes will not change
     * the updateTime even though the commitTime advances.
     */
t, 
/**
     * The resulting fields returned from the backend after a
     * TransformMutation has been committed. Contains one FieldValue for each
     * FieldTransform that was in the mutation.
     *
     * Will be null if the mutation was not a TransformMutation.
     */
n) {
    this.version = t, this.transformResults = n;
}, Ct = /** @class */ function() {
    function t(t, n) {
        this.updateTime = t, this.exists = n, Wn(void 0 === t || void 0 === n, 'Precondition can specify "exists" or "updateTime" but not both');
    }
    /** Creates a new Precondition with an exists flag. */    return t.exists = function(n) {
        return new t(void 0, n);
    }, 
    /** Creates a new Precondition based on a version a document exists at. */
    t.updateTime = function(n) {
        return new t(n);
    }, Object.defineProperty(t.prototype, "Kt", {
        /** Returns whether this Precondition is empty. */
        get: function() {
            return void 0 === this.updateTime && void 0 === this.exists;
        },
        enumerable: !0,
        configurable: !0
    }), 
    /**
     * Returns true if the preconditions is valid for the given document
     * (or null if no document is available).
     */
    t.prototype.Xt = function(t) {
        return void 0 !== this.updateTime ? t instanceof Ut && t.version.isEqual(this.updateTime) : void 0 !== this.exists ? this.exists === t instanceof Ut : (Wn(this.Kt, "Precondition should be empty"), 
        !0);
    }, t.prototype.isEqual = function(t) {
        return !(n = this.updateTime, i = t.updateTime, (null != n ? !i || !n.isEqual(i) : n !== i) || this.exists !== t.exists);
        /** Helper to compare nullable (or undefined-able) objects using isEqual(). */
        var n, i;
        /** Helper to compare arrays using isEqual(). */    }, t.NONE = new t, t;
}(), Rt = /** @class */ function() {
    function t() {}
    return t.prototype.$t = function(t) {
        null != t && Wn(t.key.isEqual(this.key), "Can only apply a mutation to a document with the same key");
    }, 
    /**
     * Returns the version from the given document for use as the result of a
     * mutation. Mutations are defined to return the version of the base document
     * only if it is an existing document. Deleted and unknown documents have a
     * post-mutation version of SnapshotVersion.MIN.
     */
    t.Zt = function(t) {
        return t instanceof Ut ? t.version : D.MIN;
    }, t;
}(), Nt = /** @class */ function(t) {
    function i(n, i, e) {
        var r = t.call(this) || this;
        return r.key = n, r.value = i, r.tn = e, r.type = 0 /* Set */ , r;
    }
    return n(i, t), i.prototype.Bt = function(t, n) {
        this.$t(t), Wn(null == n.transformResults, "Transform results received by SetMutation.");
        // Unlike applyToLocalView, if we're applying a mutation to a remote
        // document the server has accepted the mutation so the precondition must
        // have held.
        var i = n.version;
        return new Ut(this.key, i, this.value, {
            hasCommittedMutations: !0
        });
    }, i.prototype.xt = function(t, n, i) {
        if (this.$t(t), !this.tn.Xt(t)) return t;
        var e = Rt.Zt(t);
        return new Ut(this.key, e, this.value, {
            nn: !0
        });
    }, i.prototype.in = function(t) {
        return null;
    }, i.prototype.isEqual = function(t) {
        return t instanceof i && this.key.isEqual(t.key) && this.value.isEqual(t.value) && this.tn.isEqual(t.tn);
    }, i;
}(Rt), Ot = /** @class */ function(t) {
    function i(n, i, e, r) {
        var s = t.call(this) || this;
        return s.key = n, s.data = i, s.en = e, s.tn = r, s.type = 1 /* Patch */ , s;
    }
    return n(i, t), i.prototype.Bt = function(t, n) {
        if (this.$t(t), Wn(null == n.transformResults, "Transform results received by PatchMutation."), 
        !this.tn.Xt(t)) 
        // Since the mutation was not rejected, we know that the  precondition
        // matched on the backend. We therefore must not have the expected version
        // of the document in our cache and return an UnknownDocument with the
        // known updateTime.
        return new Mt(this.key, n.version);
        var i = this.rn(t);
        return new Ut(this.key, n.version, i, {
            hasCommittedMutations: !0
        });
    }, i.prototype.xt = function(t, n, i) {
        if (this.$t(t), !this.tn.Xt(t)) return t;
        var e = Rt.Zt(t), r = this.rn(t);
        return new Ut(this.key, e, r, {
            nn: !0
        });
    }, i.prototype.in = function(t) {
        return null;
    }, i.prototype.isEqual = function(t) {
        return t instanceof i && this.key.isEqual(t.key) && this.en.isEqual(t.en) && this.tn.isEqual(t.tn);
    }, 
    /**
     * Patches the data of document if available or creates a new document. Note
     * that this does not check whether or not the precondition of this patch
     * holds.
     */
    i.prototype.rn = function(t) {
        var n;
        return n = t instanceof Ut ? t.data() : kt.EMPTY, this.sn(n);
    }, i.prototype.sn = function(t) {
        var n = this, i = t.un();
        return this.en.fields.forEach((function(t) {
            if (!t.M()) {
                var e = n.data.field(t);
                null !== e ? i.set(t, e) : i.delete(t);
            }
        })), i.on();
    }, i;
}(Rt), Dt = /** @class */ function(t) {
    function i(n, i) {
        var e = t.call(this) || this;
        return e.key = n, e.fieldTransforms = i, e.type = 2 /* Transform */ , 
        // NOTE: We set a precondition of exists: true as a safety-check, since we
        // always combine TransformMutations with a SetMutation or PatchMutation which
        // (if successful) should end up with an existing document.
        e.tn = Ct.exists(!0), e;
    }
    return n(i, t), i.prototype.Bt = function(t, n) {
        if (this.$t(t), Wn(null != n.transformResults, "Transform results missing for TransformMutation."), 
        !this.tn.Xt(t)) 
        // Since the mutation was not rejected, we know that the  precondition
        // matched on the backend. We therefore must not have the expected version
        // of the document in our cache and return an UnknownDocument with the
        // known updateTime.
        return new Mt(this.key, n.version);
        var i = this.hn(t), e = this.an(t, n.transformResults), r = n.version, s = this.cn(i.data(), e);
        return new Ut(this.key, r, s, {
            hasCommittedMutations: !0
        });
    }, i.prototype.xt = function(t, n, i) {
        if (this.$t(t), !this.tn.Xt(t)) return t;
        var e = this.hn(t), r = this.fn(i, t, n), s = this.cn(e.data(), r);
        return new Ut(this.key, e.version, s, {
            nn: !0
        });
    }, i.prototype.in = function(t) {
        for (var n = null, i = 0, e = this.fieldTransforms; i < e.length; i++) {
            var r = e[i], s = t instanceof Ut ? t.field(r.field) : void 0, u = r.transform.Qt(s || null);
            null != u && (n = null == n ? kt.ln().set(r.field, u) : n.set(r.field, u));
        }
        return n ? n.on() : null;
    }, i.prototype.isEqual = function(t) {
        return t instanceof i && this.key.isEqual(t.key) && Jn(this.fieldTransforms, t.fieldTransforms, (function(t, n) {
            return t.isEqual(n);
        })) && this.tn.isEqual(t.tn);
    }, 
    /**
     * Asserts that the given MaybeDocument is actually a Document and verifies
     * that it matches the key for this mutation. Since we only support
     * transformations with precondition exists this method is guaranteed to be
     * safe.
     */
    i.prototype.hn = function(t) {
        return Wn(t instanceof Ut, "Unknown MaybeDocument type " + t), Wn(t.key.isEqual(this.key), "Can only transform a document with the same key"), 
        t;
    }, 
    /**
     * Creates a list of "transform results" (a transform result is a field value
     * representing the result of applying a transform) for use after a
     * TransformMutation has been acknowledged by the server.
     *
     * @param baseDoc The document prior to applying this mutation batch.
     * @param serverTransformResults The transform results received by the server.
     * @return The transform results list.
     */
    i.prototype.an = function(t, n) {
        var i = [];
        Wn(this.fieldTransforms.length === n.length, "server transform result count (" + n.length + ") should match field transform count (" + this.fieldTransforms.length + ")");
        for (var e = 0; e < n.length; e++) {
            var r = this.fieldTransforms[e], s = r.transform, u = null;
            t instanceof Ut && (u = t.field(r.field)), i.push(s.Bt(u, n[e]));
        }
        return i;
    }, 
    /**
     * Creates a list of "transform results" (a transform result is a field value
     * representing the result of applying a transform) for use when applying a
     * TransformMutation locally.
     *
     * @param localWriteTime The local time of the transform mutation (used to
     *     generate ServerTimestampValues).
     * @param maybeDoc The current state of the document after applying all
     *     previous mutations.
     * @param baseDoc The document prior to applying this mutation batch.
     * @return The transform results list.
     */
    i.prototype.fn = function(t, n, i) {
        for (var e = [], r = 0, s = this.fieldTransforms; r < s.length; r++) {
            var u = s[r], o = u.transform, h = null;
            n instanceof Ut && (h = n.field(u.field)), null === h && i instanceof Ut && (
            // If the current document does not contain a value for the mutated
            // field, use the value that existed before applying this mutation
            // batch. This solves an edge case where a PatchMutation clears the
            // values in a nested map before the TransformMutation is applied.
            h = i.field(u.field)), e.push(o.xt(h, t));
        }
        return e;
    }, i.prototype.cn = function(t, n) {
        Wn(n.length === this.fieldTransforms.length, "TransformResults length mismatch.");
        for (var i = t.un(), e = 0; e < this.fieldTransforms.length; e++) {
            var r = this.fieldTransforms[e].field;
            i.set(r, n[e]);
        }
        return i.on();
    }, i;
}(Rt), Vt = /** @class */ function(t) {
    function i(n, i) {
        var e = t.call(this) || this;
        return e.key = n, e.tn = i, e.type = 3 /* Delete */ , e;
    }
    return n(i, t), i.prototype.Bt = function(t, n) {
        // Unlike applyToLocalView, if we're applying a mutation to a remote
        // document the server has accepted the mutation so the precondition must
        // have held.
        return this.$t(t), Wn(null == n.transformResults, "Transform results received by DeleteMutation."), 
        new Lt(this.key, n.version, {
            hasCommittedMutations: !0
        });
    }, i.prototype.xt = function(t, n, i) {
        return this.$t(t), this.tn.Xt(t) ? (t && Wn(t.key.isEqual(this.key), "Can only apply mutation to document with same key"), 
        new Lt(this.key, D.D())) : t;
    }, i.prototype.in = function(t) {
        return null;
    }, i.prototype.isEqual = function(t) {
        return t instanceof i && this.key.isEqual(t.key) && this.tn.isEqual(t.tn);
    }, i;
}(Rt), Pt = /** @class */ function(t) {
    function i(n, i) {
        var e = t.call(this) || this;
        return e.key = n, e.tn = i, e.type = 4 /* Verify */ , e;
    }
    return n(i, t), i.prototype.Bt = function(t, n) {
        Qn("VerifyMutation should only be used in Transactions.");
    }, i.prototype.xt = function(t, n, i) {
        Qn("VerifyMutation should only be used in Transactions.");
    }, i.prototype.in = function(t) {
        Qn("VerifyMutation should only be used in Transactions.");
    }, i.prototype.isEqual = function(t) {
        return t instanceof i && this.key.isEqual(t.key) && this.tn.isEqual(t.tn);
    }, i;
}(Rt), kt = /** @class */ function() {
    function t(t) {
        this.proto = t, Wn(!K(t), "ServerTimestamps should be converted to ServerTimestampValue");
    }
    /** Returns a new Builder instance that is based on an empty object. */    return t.ln = function() {
        return t.EMPTY.un();
    }, 
    /**
     * Returns the value at the given path or null.
     *
     * @param path the path to search
     * @return The value at the path or if there it doesn't exist.
     */
    t.prototype.field = function(t) {
        if (t.M()) return this.proto;
        for (var n = this.proto, i = 0; i < t.length - 1; ++i) {
            if (!n.mapValue.fields) return null;
            if (!mt(n = n.mapValue.fields[t.get(i)])) return null;
        }
        return (n = (n.mapValue.fields || {})[t.B()]) || null;
    }, 
    /**
     * Returns a FieldMask built from all FieldPaths starting from this
     * ObjectValue, including paths from nested objects.
     */
    t.prototype.en = function() {
        return this.dn(this.proto.mapValue);
    }, t.prototype.dn = function(t) {
        var n = this, i = new x(F.F);
        return W(t.fields || {}, (function(t, e) {
            var r = new F([ t ]);
            if (10 /* ObjectValue */ === Z(e)) {
                var s = n.dn(e.mapValue).fields;
                s.M() ? 
                // Preserve the empty map by adding it to the FieldMask.
                i = i.add(r) : 
                // For nested and non-empty ObjectValues, add the FieldPath of the
                // leaf nodes.
                s.forEach((function(t) {
                    i = i.add(r.child(t));
                }));
            } else 
            // For nested and non-empty ObjectValues, add the FieldPath of the leaf
            // nodes.
            i = i.add(r);
        })), St.Jt(i);
    }, t.prototype.isEqual = function(t) {
        return tt(this.proto, t.proto);
    }, 
    /** Creates a ObjectValueBuilder instance that is based on the current value. */
    t.prototype.un = function() {
        return new Ft(this);
    }, t.EMPTY = new t({
        mapValue: {}
    }), t;
}(), Ft = /** @class */ function() {
    /**
     * @param baseObject The object to mutate.
     */
    function t(t) {
        this.vn = t, 
        /** A map that contains the accumulated changes in this builder. */
        this.wn = new Map;
    }
    /**
     * Sets the field to the provided value.
     *
     * @param path The field path to set.
     * @param value The value to set.
     * @return The current Builder instance.
     */    return t.prototype.set = function(t, n) {
        return Wn(!t.M(), "Cannot set field for empty path on ObjectValue"), this.mn(t, n), 
        this;
    }, 
    /**
     * Removes the field at the specified path. If there is no field at the
     * specified path, nothing is changed.
     *
     * @param path The field path to remove.
     * @return The current Builder instance.
     */
    t.prototype.delete = function(t) {
        return Wn(!t.M(), "Cannot delete field for empty path on ObjectValue"), this.mn(t, null), 
        this;
    }, 
    /**
     * Adds `value` to the overlay map at `path`. Creates nested map entries if
     * needed.
     */
    t.prototype.mn = function(t, n) {
        for (var i = this.wn, e = 0; e < t.length - 1; ++e) {
            var r = t.get(e), s = i.get(r);
            s instanceof Map ? 
            // Re-use a previously created map
            i = s : s && 10 /* ObjectValue */ === Z(s) ? (
            // Convert the existing Protobuf MapValue into a map
            s = new Map(Object.entries(s.mapValue.fields || {})), i.set(r, s), i = s) : (
            // Create an empty map to represent the current nesting level
            s = new Map, i.set(r, s), i = s);
        }
        i.set(t.B(), n);
    }, 
    /** Returns an ObjectValue with all mutations applied. */
    t.prototype.on = function() {
        var t = this.pn(F.K, this.wn);
        return null != t ? new kt(t) : this.vn;
    }, 
    /**
     * Applies any overlays from `currentOverlays` that exist at `currentPath`
     * and returns the merged data at `currentPath` (or null if there were no
     * changes).
     *
     * @param currentPath The path at the current nesting level. Can be set to
     * FieldValue.EMPTY_PATH to represent the root.
     * @param currentOverlays The overlays at the current nesting level in the
     * same format as `overlayMap`.
     * @return The merged data at `currentPath` or null if no modifications
     * were applied.
     */
    t.prototype.pn = function(t, n) {
        var e = this, r = !1, s = this.vn.field(t), u = mt(s) ? // If there is already data at the current path, base our
        i({}, s.mapValue.fields) : {};
        return n.forEach((function(n, i) {
            if (n instanceof Map) {
                var s = e.pn(t.child(i), n);
                null != s && (u[i] = s, r = !0);
            } else null !== n ? (u[i] = n, r = !0) : u.hasOwnProperty(i) && (delete u[i], r = !0);
        })), r ? {
            mapValue: {
                fields: u
            }
        } : null;
    }, t;
}(), jt = /** @class */ function() {
    function t(t, n) {
        this.key = t, this.version = n;
    }
    return t.gn = function(t, n) {
        return j.F(t.key, n.key);
    }, t;
}(), Ut = /** @class */ function(t) {
    function i(n, i, e, r) {
        var s = t.call(this, n, i) || this;
        return s.bn = e, s.nn = !!r.nn, s.hasCommittedMutations = !!r.hasCommittedMutations, 
        s;
    }
    return n(i, t), i.prototype.field = function(t) {
        return this.bn.field(t);
    }, i.prototype.data = function() {
        return this.bn;
    }, i.prototype.yn = function() {
        return this.bn.proto;
    }, i.prototype.isEqual = function(t) {
        return t instanceof i && this.key.isEqual(t.key) && this.version.isEqual(t.version) && this.nn === t.nn && this.hasCommittedMutations === t.hasCommittedMutations && this.bn.isEqual(t.bn);
    }, i.prototype.toString = function() {
        return "Document(" + this.key + ", " + this.version + ", " + this.bn.toString() + ", {hasLocalMutations: " + this.nn + "}), {hasCommittedMutations: " + this.hasCommittedMutations + "})";
    }, Object.defineProperty(i.prototype, "hasPendingWrites", {
        get: function() {
            return this.nn || this.hasCommittedMutations;
        },
        enumerable: !0,
        configurable: !0
    }), i._n = function(t, n, i) {
        var e = n.field(t), r = i.field(t);
        return null !== e && null !== r ? it(e, r) : Qn("Trying to compare documents on fields that don't exist");
    }, i;
}(jt), Lt = /** @class */ function(t) {
    function i(n, i, e) {
        var r = t.call(this, n, i) || this;
        return r.hasCommittedMutations = !(!e || !e.hasCommittedMutations), r;
    }
    return n(i, t), i.prototype.toString = function() {
        return "NoDocument(" + this.key + ", " + this.version + ")";
    }, Object.defineProperty(i.prototype, "hasPendingWrites", {
        get: function() {
            return this.hasCommittedMutations;
        },
        enumerable: !0,
        configurable: !0
    }), i.prototype.isEqual = function(t) {
        return t instanceof i && t.hasCommittedMutations === this.hasCommittedMutations && t.version.isEqual(this.version) && t.key.isEqual(this.key);
    }, i;
}(jt), Mt = /** @class */ function(t) {
    function i() {
        return null !== t && t.apply(this, arguments) || this;
    }
    return n(i, t), i.prototype.toString = function() {
        return "UnknownDocument(" + this.key + ", " + this.version + ")";
    }, Object.defineProperty(i.prototype, "hasPendingWrites", {
        get: function() {
            return !0;
        },
        enumerable: !0,
        configurable: !0
    }), i.prototype.isEqual = function(t) {
        return t instanceof i && t.version.isEqual(this.version) && t.key.isEqual(this.key);
    }, i;
}(jt), qt = /** @class */ function() {
    /**
     * Initializes a Target with a path and optional additional query constraints.
     * Path must currently be empty if this is a collection group query.
     *
     * NOTE: you should always construct `Target` from `Query.toTarget` instead of
     * using this constructor, because `Query` provides an implicit `orderBy`
     * property.
     */
    function t(t, n, i, e, r, s, u) {
        void 0 === n && (n = null), void 0 === i && (i = []), void 0 === e && (e = []), 
        void 0 === r && (r = null), void 0 === s && (s = null), void 0 === u && (u = null), 
        this.path = t, this.collectionGroup = n, this.orderBy = i, this.filters = e, this.limit = r, 
        this.startAt = s, this.endAt = u, this.Tn = null;
    }
    return t.prototype.canonicalId = function() {
        if (null === this.Tn) {
            var t = this.path.H();
            null !== this.collectionGroup && (t += "|cg:" + this.collectionGroup), t += "|f:", 
            t += this.filters.map((function(t) {
                return t.canonicalId();
            })).join(","), t += "|ob:", t += this.orderBy.map((function(t) {
                return t.canonicalId();
            })).join(","), J(this.limit) || (t += "|l:", t += this.limit), this.startAt && (t += "|lb:", 
            t += this.startAt.canonicalId()), this.endAt && (t += "|ub:", t += this.endAt.canonicalId()), 
            this.Tn = t;
        }
        return this.Tn;
    }, t.prototype.toString = function() {
        var t = this.path.H();
        return null !== this.collectionGroup && (t += " collectionGroup=" + this.collectionGroup), 
        this.filters.length > 0 && (t += ", filters: [" + this.filters.join(", ") + "]"), 
        J(this.limit) || (t += ", limit: " + this.limit), this.orderBy.length > 0 && (t += ", orderBy: [" + this.orderBy.join(", ") + "]"), 
        this.startAt && (t += ", startAt: " + this.startAt.canonicalId()), this.endAt && (t += ", endAt: " + this.endAt.canonicalId()), 
        "Target(" + t + ")";
    }, t.prototype.isEqual = function(t) {
        if (this.limit !== t.limit) return !1;
        if (this.orderBy.length !== t.orderBy.length) return !1;
        for (var n = 0; n < this.orderBy.length; n++) if (!this.orderBy[n].isEqual(t.orderBy[n])) return !1;
        if (this.filters.length !== t.filters.length) return !1;
        for (n = 0; n < this.filters.length; n++) if (!this.filters[n].isEqual(t.filters[n])) return !1;
        return this.collectionGroup === t.collectionGroup && (!!this.path.isEqual(t.path) && (!!(null !== this.startAt ? this.startAt.isEqual(t.startAt) : null === t.startAt) && (null !== this.endAt ? this.endAt.isEqual(t.endAt) : null === t.endAt)));
    }, t.prototype.En = function() {
        return j.nt(this.path) && null === this.collectionGroup && 0 === this.filters.length;
    }, t;
}(), xt = /** @class */ function() {
    /**
     * Initializes a Query with a path and optional additional query constraints.
     * Path must currently be empty if this is a collection group query.
     */
    function t(t, n, i, e, r, s, u, o) {
        void 0 === n && (n = null), void 0 === i && (i = []), void 0 === e && (e = []), 
        void 0 === r && (r = null), void 0 === s && (s = "F" /* First */), void 0 === u && (u = null), 
        void 0 === o && (o = null), this.path = t, this.collectionGroup = n, this.Sn = i, 
        this.filters = e, this.limit = r, this.An = s, this.startAt = u, this.endAt = o, 
        this.In = null, 
        // The corresponding `Target` of this `Query` instance.
        this.Cn = null, this.startAt && this.Rn(this.startAt), this.endAt && this.Rn(this.endAt);
    }
    return t.Nn = function(n) {
        return new t(n);
    }, Object.defineProperty(t.prototype, "orderBy", {
        get: function() {
            if (null === this.In) {
                var t = this.On(), n = this.Dn();
                if (null !== t && null === n) 
                // In order to implicitly add key ordering, we must also add the
                // inequality filter field for it to be a valid query.
                // Note that the default inequality field and key ordering is ascending.
                t.$() ? this.In = [ Zt ] : this.In = [ new $t(t), Zt ]; else {
                    Wn(null === t || null !== n && t.isEqual(n), "First orderBy should match inequality field."), 
                    this.In = [];
                    for (var i = !1, e = 0, r = this.Sn; e < r.length; e++) {
                        var s = r[e];
                        this.In.push(s), s.field.$() && (i = !0);
                    }
                    if (!i) {
                        // The order of the implicit key ordering always matches the last
                        // explicit order by
                        var u = this.Sn.length > 0 ? this.Sn[this.Sn.length - 1].dir : Kt.ASCENDING;
                        this.In.push(u === Kt.ASCENDING ? Zt : tn);
                    }
                }
            }
            return this.In;
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.Vn = function(n) {
        Wn(null == this.On() || !(n instanceof Wt) || !n.Pn() || n.field.isEqual(this.On()), "Query must only have one inequality field."), 
        Wn(!this.En(), "No filtering allowed for document query");
        var i = this.filters.concat([ n ]);
        return new t(this.path, this.collectionGroup, this.Sn.slice(), i, this.limit, this.An, this.startAt, this.endAt);
    }, t.prototype.kn = function(n) {
        Wn(!this.startAt && !this.endAt, "Bounds must be set after orderBy");
        // TODO(dimond): validate that orderBy does not list the same key twice.
        var i = this.Sn.concat([ n ]);
        return new t(this.path, this.collectionGroup, i, this.filters.slice(), this.limit, this.An, this.startAt, this.endAt);
    }, t.prototype.Fn = function(n) {
        return new t(this.path, this.collectionGroup, this.Sn.slice(), this.filters.slice(), n, "F" /* First */ , this.startAt, this.endAt);
    }, t.prototype.jn = function(n) {
        return new t(this.path, this.collectionGroup, this.Sn.slice(), this.filters.slice(), n, "L" /* Last */ , this.startAt, this.endAt);
    }, t.prototype.Un = function(n) {
        return new t(this.path, this.collectionGroup, this.Sn.slice(), this.filters.slice(), this.limit, this.An, n, this.endAt);
    }, t.prototype.Ln = function(n) {
        return new t(this.path, this.collectionGroup, this.Sn.slice(), this.filters.slice(), this.limit, this.An, this.startAt, n);
    }, 
    /**
     * Helper to convert a collection group query into a collection query at a
     * specific path. This is used when executing collection group queries, since
     * we have to split the query into a set of collection queries at multiple
     * paths.
     */
    t.prototype.Mn = function(n) {
        return new t(n, 
        /*collectionGroup=*/ null, this.Sn.slice(), this.filters.slice(), this.limit, this.An, this.startAt, this.endAt);
    }, 
    /**
     * Returns true if this query does not specify any query constraints that
     * could remove results.
     */
    t.prototype.qn = function() {
        return 0 === this.filters.length && null === this.limit && null == this.startAt && null == this.endAt && (0 === this.Sn.length || 1 === this.Sn.length && this.Sn[0].field.$());
    }, 
    // TODO(b/29183165): This is used to get a unique string from a query to, for
    // example, use as a dictionary key, but the implementation is subject to
    // collisions. Make it collision-free.
    t.prototype.canonicalId = function() {
        return this.xn().canonicalId() + "|lt:" + this.An;
    }, t.prototype.toString = function() {
        return "Query(target=" + this.xn().toString() + "; limitType=" + this.An + ")";
    }, t.prototype.isEqual = function(t) {
        return this.xn().isEqual(t.xn()) && this.An === t.An;
    }, t.prototype.Bn = function(t, n) {
        for (var i = !1, e = 0, r = this.orderBy; e < r.length; e++) {
            var s = r[e], u = s.compare(t, n);
            if (0 !== u) return u;
            i = i || s.field.$();
        }
        // Assert that we actually compared by key
                return Wn(i, "orderBy used that doesn't compare on key field"), 0;
    }, t.prototype.matches = function(t) {
        return this.Qn(t) && this.Wn(t) && this.Gn(t) && this.zn(t);
    }, t.prototype.Jn = function() {
        return !J(this.limit) && "F" /* First */ === this.An;
    }, t.prototype.Hn = function() {
        return !J(this.limit) && "L" /* Last */ === this.An;
    }, t.prototype.Dn = function() {
        return this.Sn.length > 0 ? this.Sn[0].field : null;
    }, t.prototype.On = function() {
        for (var t = 0, n = this.filters; t < n.length; t++) {
            var i = n[t];
            if (i instanceof Wt && i.Pn()) return i.field;
        }
        return null;
    }, 
    // Checks if any of the provided Operators are included in the query and
    // returns the first one that is, or null if none are.
    t.prototype.Yn = function(t) {
        for (var n = 0, i = this.filters; n < i.length; n++) {
            var e = i[n];
            if (e instanceof Wt && t.indexOf(e.op) >= 0) return e.op;
        }
        return null;
    }, t.prototype.En = function() {
        return this.xn().En();
    }, t.prototype.Kn = function() {
        return null !== this.collectionGroup;
    }, 
    /**
     * Converts this `Query` instance to it's corresponding `Target`
     * representation.
     */
    t.prototype.xn = function() {
        if (!this.Cn) if ("F" /* First */ === this.An) this.Cn = new qt(this.path, this.collectionGroup, this.orderBy, this.filters, this.limit, this.startAt, this.endAt); else {
            for (
            // Flip the orderBy directions since we want the last results
            var t = [], n = 0, i = this.orderBy; n < i.length; n++) {
                var e = i[n], r = e.dir === Kt.DESCENDING ? Kt.ASCENDING : Kt.DESCENDING;
                t.push(new $t(e.field, r));
            }
            // We need to swap the cursors to match the now-flipped query ordering.
                        var s = this.endAt ? new Xt(this.endAt.position, !this.endAt.before) : null, u = this.startAt ? new Xt(this.startAt.position, !this.startAt.before) : null;
            // Now return as a LimitType.First query.
            this.Cn = new qt(this.path, this.collectionGroup, t, this.filters, this.limit, s, u);
        }
        return this.Cn;
    }, t.prototype.Qn = function(t) {
        var n = t.key.path;
        return null !== this.collectionGroup ? t.key.et(this.collectionGroup) && this.path.W(n) : j.nt(this.path) ? this.path.isEqual(n) : this.path.G(n);
    }, 
    /**
     * A document must have a value for every ordering clause in order to show up
     * in the results.
     */
    t.prototype.Wn = function(t) {
        for (var n = 0, i = this.Sn; n < i.length; n++) {
            var e = i[n];
            // order by key always matches
                        if (!e.field.$() && null === t.field(e.field)) return !1;
        }
        return !0;
    }, t.prototype.Gn = function(t) {
        for (var n = 0, i = this.filters; n < i.length; n++) {
            if (!i[n].matches(t)) return !1;
        }
        return !0;
    }, 
    /**
     * Makes sure a document is within the bounds, if provided.
     */
    t.prototype.zn = function(t) {
        return !(this.startAt && !this.startAt.Xn(this.orderBy, t)) && (!this.endAt || !this.endAt.Xn(this.orderBy, t));
    }, t.prototype.Rn = function(t) {
        Wn(t.position.length <= this.orderBy.length, "Bound is longer than orderBy");
    }, t;
}(), Bt = function() {}, Qt = /** @class */ function() {
    function t(t) {
        this.name = t;
    }
    return t.Y = function(n) {
        switch (n) {
          case "<":
            return t.LESS_THAN;

          case "<=":
            return t.LESS_THAN_OR_EQUAL;

          case "==":
            return t.EQUAL;

          case ">=":
            return t.GREATER_THAN_OR_EQUAL;

          case ">":
            return t.GREATER_THAN;

          case "array-contains":
            return t.ARRAY_CONTAINS;

          case "in":
            return t.IN;

          case "array-contains-any":
            return t.ARRAY_CONTAINS_ANY;

          default:
            return Qn("Unknown FieldFilter operator: " + n);
        }
    }, t.prototype.toString = function() {
        return this.name;
    }, t.prototype.isEqual = function(t) {
        return this.name === t.name;
    }, t.LESS_THAN = new t("<"), t.LESS_THAN_OR_EQUAL = new t("<="), t.EQUAL = new t("=="), 
    t.GREATER_THAN = new t(">"), t.GREATER_THAN_OR_EQUAL = new t(">="), t.ARRAY_CONTAINS = new t("array-contains"), 
    t.IN = new t("in"), t.ARRAY_CONTAINS_ANY = new t("array-contains-any"), t;
}(), Wt = /** @class */ function(t) {
    function i(n, i, e) {
        var r = t.call(this) || this;
        return r.field = n, r.op = i, r.value = e, r;
    }
    /**
     * Creates a filter based on the provided arguments.
     */    return n(i, t), i.create = function(t, n, e) {
        if (t.$()) return n === Qt.IN ? (Wn(lt(e), "Comparing on key with IN, but filter value not an ArrayValue"), 
        Wn((e.arrayValue.values || []).every((function(t) {
            return dt(t);
        })), "Comparing on key with IN, but an array value was not a RefValue"), new zt(t, e)) : (Wn(dt(e), "Comparing on key, but filter value not a RefValue"), 
        Wn(n !== Qt.ARRAY_CONTAINS && n !== Qt.ARRAY_CONTAINS_ANY, "'" + n.toString() + "' queries don't make sense on document keys."), 
        new Gt(t, n, e));
        if (vt(e)) {
            if (n !== Qt.EQUAL) throw new E(T.INVALID_ARGUMENT, "Invalid query. Null supports only equality comparisons.");
            return new i(t, n, e);
        }
        if (wt(e)) {
            if (n !== Qt.EQUAL) throw new E(T.INVALID_ARGUMENT, "Invalid query. NaN supports only equality comparisons.");
            return new i(t, n, e);
        }
        return n === Qt.ARRAY_CONTAINS ? new Jt(t, e) : n === Qt.IN ? (Wn(lt(e), "IN filter has invalid value: " + e.toString()), 
        new Ht(t, e)) : n === Qt.ARRAY_CONTAINS_ANY ? (Wn(lt(e), "ARRAY_CONTAINS_ANY filter has invalid value: " + e.toString()), 
        new Yt(t, e)) : new i(t, n, e);
    }, i.prototype.matches = function(t) {
        var n = t.field(this.field);
        // Only compare types with matching backend order (such as double and int).
                return null !== n && Z(this.value) === Z(n) && this.$n(it(n, this.value));
    }, i.prototype.$n = function(t) {
        switch (this.op) {
          case Qt.LESS_THAN:
            return t < 0;

          case Qt.LESS_THAN_OR_EQUAL:
            return t <= 0;

          case Qt.EQUAL:
            return 0 === t;

          case Qt.GREATER_THAN:
            return t > 0;

          case Qt.GREATER_THAN_OR_EQUAL:
            return t >= 0;

          default:
            return Qn("Unknown FieldFilter operator: " + this.op);
        }
    }, i.prototype.Pn = function() {
        return [ Qt.LESS_THAN, Qt.LESS_THAN_OR_EQUAL, Qt.GREATER_THAN, Qt.GREATER_THAN_OR_EQUAL ].indexOf(this.op) >= 0;
    }, i.prototype.canonicalId = function() {
        // TODO(b/29183165): Technically, this won't be unique if two values have
        // the same description, such as the int 3 and the string "3". So we should
        // add the types in here somehow, too.
        return this.field.H() + this.op.toString() + rt(this.value);
    }, i.prototype.isEqual = function(t) {
        return t instanceof i && (this.op.isEqual(t.op) && this.field.isEqual(t.field) && tt(this.value, t.value));
    }, i.prototype.toString = function() {
        return this.field.H() + " " + this.op + " " + rt(this.value);
    }, i;
}(Bt), Gt = /** @class */ function(t) {
    function i(n, i, e) {
        var r = t.call(this, n, i, e) || this;
        return Wn(dt(e), "KeyFieldFilter expects a ReferenceValue"), r.key = j.it(e.referenceValue), 
        r;
    }
    return n(i, t), i.prototype.matches = function(t) {
        var n = j.F(t.key, this.key);
        return this.$n(n);
    }, i;
}(Wt), zt = /** @class */ function(t) {
    function i(n, i) {
        var e = t.call(this, n, Qt.IN, i) || this;
        return Wn(lt(i), "KeyFieldInFilter expects an ArrayValue"), e.keys = (i.arrayValue.values || []).map((function(t) {
            return Wn(dt(t), "Comparing on key with IN, but an array value was not a ReferenceValue"), 
            j.it(t.referenceValue);
        })), e;
    }
    return n(i, t), i.prototype.matches = function(t) {
        return this.keys.some((function(n) {
            return n.isEqual(t.key);
        }));
    }, i;
}(Wt), Jt = /** @class */ function(t) {
    function i(n, i) {
        return t.call(this, n, Qt.ARRAY_CONTAINS, i) || this;
    }
    return n(i, t), i.prototype.matches = function(t) {
        var n = t.field(this.field);
        return lt(n) && nt(n.arrayValue, this.value);
    }, i;
}(Wt), Ht = /** @class */ function(t) {
    function i(n, i) {
        var e = t.call(this, n, Qt.IN, i) || this;
        return Wn(lt(i), "InFilter expects an ArrayValue"), e;
    }
    return n(i, t), i.prototype.matches = function(t) {
        var n = t.field(this.field);
        return null !== n && nt(this.value.arrayValue, n);
    }, i;
}(Wt), Yt = /** @class */ function(t) {
    function i(n, i) {
        var e = t.call(this, n, Qt.ARRAY_CONTAINS_ANY, i) || this;
        return Wn(lt(i), "ArrayContainsAnyFilter expects an ArrayValue"), e;
    }
    return n(i, t), i.prototype.matches = function(t) {
        var n = this, i = t.field(this.field);
        return !(!lt(i) || !i.arrayValue.values) && i.arrayValue.values.some((function(t) {
            return nt(n.value.arrayValue, t);
        }));
    }, i;
}(Wt), Kt = /** @class */ function() {
    function t(t) {
        this.name = t;
    }
    return t.prototype.toString = function() {
        return this.name;
    }, t.ASCENDING = new t("asc"), t.DESCENDING = new t("desc"), t;
}(), Xt = /** @class */ function() {
    function t(t, n) {
        this.position = t, this.before = n;
    }
    return t.prototype.canonicalId = function() {
        // TODO(b/29183165): Make this collision robust.
        return (this.before ? "b" : "a") + ":" + this.position.map((function(t) {
            return rt(t);
        })).join(",");
    }, 
    /**
     * Returns true if a document sorts before a bound using the provided sort
     * order.
     */
    t.prototype.Xn = function(t, n) {
        Wn(this.position.length <= t.length, "Bound has more components than query's orderBy");
        for (var i = 0, e = 0; e < this.position.length; e++) {
            var r = t[e], s = this.position[e];
            if (r.field.$()) Wn(dt(s), "Bound has a non-key value where the key path is being used."), 
            i = j.F(j.it(s.referenceValue), n.key); else {
                var u = n.field(r.field);
                Wn(null !== u, "Field should exist since document matched the orderBy already."), 
                i = it(s, u);
            }
            if (r.dir === Kt.DESCENDING && (i *= -1), 0 !== i) break;
        }
        return this.before ? i <= 0 : i < 0;
    }, t.prototype.isEqual = function(t) {
        if (null === t) return !1;
        if (this.before !== t.before || this.position.length !== t.position.length) return !1;
        for (var n = 0; n < this.position.length; n++) {
            if (!tt(this.position[n], t.position[n])) return !1;
        }
        return !0;
    }, t;
}(), $t = /** @class */ function() {
    function t(t, n) {
        this.field = t, void 0 === n && (n = Kt.ASCENDING), this.dir = n, this.Zn = t.$();
    }
    return t.prototype.compare = function(t, n) {
        var i = this.Zn ? Ut.gn(t, n) : Ut._n(this.field, t, n);
        switch (this.dir) {
          case Kt.ASCENDING:
            return i;

          case Kt.DESCENDING:
            return -1 * i;

          default:
            return Qn("Unknown direction: " + this.dir);
        }
    }, t.prototype.canonicalId = function() {
        // TODO(b/29183165): Make this collision robust.
        return this.field.H() + this.dir.toString();
    }, t.prototype.toString = function() {
        return this.field.H() + " (" + this.dir + ")";
    }, t.prototype.isEqual = function(t) {
        return this.dir === t.dir && this.field.isEqual(t.field);
    }, t;
}(), Zt = new $t(F.Z(), Kt.ASCENDING), tn = new $t(F.Z(), Kt.DESCENDING), nn = /** @class */ function() {
    function t(
    /** The target being listened to. */
    t, 
    /**
     * The target ID to which the target corresponds; Assigned by the
     * LocalStore for user listens and by the SyncEngine for limbo watches.
     */
    n, 
    /** The purpose of the target. */
    i, 
    /**
     * The sequence number of the last transaction during which this target data
     * was modified.
     */
    e, 
    /** The latest snapshot version seen for this target. */
    r, 
    /**
     * The maximum snapshot version at which the associated view
     * contained no limbo documents.
     */
    s, 
    /**
     * An opaque, server-assigned token that allows watching a target to be
     * resumed after disconnecting without retransmitting all the data that
     * matches the target. The resume token essentially identifies a point in
     * time from which the server should resume sending results.
     */
    u) {
        void 0 === r && (r = D.MIN), void 0 === s && (s = D.MIN), void 0 === u && (u = z.qt), 
        this.target = t, this.targetId = n, this.ti = i, this.sequenceNumber = e, this.ni = r, 
        this.lastLimboFreeSnapshotVersion = s, this.resumeToken = u;
    }
    /** Creates a new target data instance with an updated sequence number. */    return t.prototype.ii = function(n) {
        return new t(this.target, this.targetId, this.ti, n, this.ni, this.lastLimboFreeSnapshotVersion, this.resumeToken);
    }, 
    /**
     * Creates a new target data instance with an updated resume token and
     * snapshot version.
     */
    t.prototype.ei = function(n, i) {
        return new t(this.target, this.targetId, this.ti, this.sequenceNumber, i, this.lastLimboFreeSnapshotVersion, n);
    }, 
    /**
     * Creates a new target data instance with an updated last limbo free
     * snapshot version number.
     */
    t.prototype.ri = function(n) {
        return new t(this.target, this.targetId, this.ti, this.sequenceNumber, this.ni, n, this.resumeToken);
    }, t.prototype.isEqual = function(t) {
        return this.targetId === t.targetId && this.ti === t.ti && this.sequenceNumber === t.sequenceNumber && this.ni.isEqual(t.ni) && this.lastLimboFreeSnapshotVersion.isEqual(t.lastLimboFreeSnapshotVersion) && this.resumeToken.isEqual(t.resumeToken) && this.target.isEqual(t.target);
    }, t;
}(), en = /** @class */ function() {
    // TODO(b/33078163): just use simplest form of existence filter for now
    function t(t) {
        this.count = t;
    }
    return t.prototype.isEqual = function(t) {
        return t && t.count === this.count;
    }, t;
}();

/** A field path and the TransformOperation to perform upon it. */
/**
 * Determines whether an error code represents a permanent error when received
 * in response to a non-write operation.
 *
 * See isPermanentWriteError for classifying write errors.
 */
function rn(t) {
    switch (t) {
      case T.OK:
        return Qn("Treated status OK as error");

      case T.CANCELLED:
      case T.UNKNOWN:
      case T.DEADLINE_EXCEEDED:
      case T.RESOURCE_EXHAUSTED:
      case T.INTERNAL:
      case T.UNAVAILABLE:
 // Unauthenticated means something went wrong with our token and we need
        // to retry with new credentials which will happen automatically.
              case T.UNAUTHENTICATED:
        return !1;

      case T.INVALID_ARGUMENT:
      case T.NOT_FOUND:
      case T.ALREADY_EXISTS:
      case T.PERMISSION_DENIED:
      case T.FAILED_PRECONDITION:
 // Aborted might be retried in some scenarios, but that is dependant on
        // the context and should handled individually by the calling code.
        // See https://cloud.google.com/apis/design/errors.
              case T.ABORTED:
      case T.OUT_OF_RANGE:
      case T.UNIMPLEMENTED:
      case T.DATA_LOSS:
        return !0;

      default:
        return Qn("Unknown status code: " + t);
    }
}

/**
 * Determines whether an error code represents a permanent error when received
 * in response to a write operation.
 *
 * Write operations must be handled specially because as of b/119437764, ABORTED
 * errors on the write stream should be retried too (even though ABORTED errors
 * are not generally retryable).
 *
 * Note that during the initial handshake on the write stream an ABORTED error
 * signals that we should discard our stream token (i.e. it is permanent). This
 * means a handshake error should be classified with isPermanentError, above.
 */
/**
 * Maps an error Code from GRPC status code number, like 0, 1, or 14. These
 * are not the same as HTTP status codes.
 *
 * @returns The Code equivalent to the given GRPC status code. Fails if there
 *     is no match.
 */
function sn(t) {
    if (void 0 === t) 
    // This shouldn't normally happen, but in certain error cases (like trying
    // to send invalid proto messages) we may get an error with no GRPC code.
    return xn("GRPC error has no .code"), T.UNKNOWN;
    switch (t) {
      case Tt.OK:
        return T.OK;

      case Tt.CANCELLED:
        return T.CANCELLED;

      case Tt.UNKNOWN:
        return T.UNKNOWN;

      case Tt.DEADLINE_EXCEEDED:
        return T.DEADLINE_EXCEEDED;

      case Tt.RESOURCE_EXHAUSTED:
        return T.RESOURCE_EXHAUSTED;

      case Tt.INTERNAL:
        return T.INTERNAL;

      case Tt.UNAVAILABLE:
        return T.UNAVAILABLE;

      case Tt.UNAUTHENTICATED:
        return T.UNAUTHENTICATED;

      case Tt.INVALID_ARGUMENT:
        return T.INVALID_ARGUMENT;

      case Tt.NOT_FOUND:
        return T.NOT_FOUND;

      case Tt.ALREADY_EXISTS:
        return T.ALREADY_EXISTS;

      case Tt.PERMISSION_DENIED:
        return T.PERMISSION_DENIED;

      case Tt.FAILED_PRECONDITION:
        return T.FAILED_PRECONDITION;

      case Tt.ABORTED:
        return T.ABORTED;

      case Tt.OUT_OF_RANGE:
        return T.OUT_OF_RANGE;

      case Tt.UNIMPLEMENTED:
        return T.UNIMPLEMENTED;

      case Tt.DATA_LOSS:
        return T.DATA_LOSS;

      default:
        return Qn("Unknown status code: " + t);
    }
}

/**
 * Converts an HTTP response's error status to the equivalent error code.
 *
 * @param status An HTTP error response status ("FAILED_PRECONDITION",
 * "UNKNOWN", etc.)
 * @returns The equivalent Code. Non-matching responses are mapped to
 *     Code.UNKNOWN.
 */ (Et = Tt || (Tt = {}))[Et.OK = 0] = "OK", Et[Et.CANCELLED = 1] = "CANCELLED", 
Et[Et.UNKNOWN = 2] = "UNKNOWN", Et[Et.INVALID_ARGUMENT = 3] = "INVALID_ARGUMENT", 
Et[Et.DEADLINE_EXCEEDED = 4] = "DEADLINE_EXCEEDED", Et[Et.NOT_FOUND = 5] = "NOT_FOUND", 
Et[Et.ALREADY_EXISTS = 6] = "ALREADY_EXISTS", Et[Et.PERMISSION_DENIED = 7] = "PERMISSION_DENIED", 
Et[Et.UNAUTHENTICATED = 16] = "UNAUTHENTICATED", Et[Et.RESOURCE_EXHAUSTED = 8] = "RESOURCE_EXHAUSTED", 
Et[Et.FAILED_PRECONDITION = 9] = "FAILED_PRECONDITION", Et[Et.ABORTED = 10] = "ABORTED", 
Et[Et.OUT_OF_RANGE = 11] = "OUT_OF_RANGE", Et[Et.UNIMPLEMENTED = 12] = "UNIMPLEMENTED", 
Et[Et.INTERNAL = 13] = "INTERNAL", Et[Et.UNAVAILABLE = 14] = "UNAVAILABLE", Et[Et.DATA_LOSS = 15] = "DATA_LOSS";

/**
 * @license
 * Copyright 2017 Google Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
var un = new U(j.F);

function on() {
    return un;
}

function hn() {
    return on();
}

var an = new U(j.F);

function cn() {
    return an;
}

var fn = new U(j.F);

function ln() {
    return fn;
}

var dn = new x(j.F);

function vn() {
    for (var t = [], n = 0; n < arguments.length; n++) t[n] = arguments[n];
    for (var i = dn, e = 0, r = t; e < r.length; e++) {
        var s = r[e];
        i = i.add(s);
    }
    return i;
}

var wn = new x(zn);

function mn() {
    return wn;
}

/**
 * @license
 * Copyright 2017 Google Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * DocumentSet is an immutable (copy-on-write) collection that holds documents
 * in order specified by the provided comparator. We always add a document key
 * comparator on top of what is provided to guarantee document equality based on
 * the key.
 */ var pn = /** @class */ function() {
    /** The default ordering is by key if the comparator is omitted */
    function t(t) {
        // We are adding document key comparator to the end as it's the only
        // guaranteed unique property of a document.
        this.F = t ? function(n, i) {
            return t(n, i) || j.F(n.key, i.key);
        } : function(t, n) {
            return j.F(t.key, n.key);
        }, this.si = cn(), this.ui = new U(this.F);
    }
    /**
     * Returns an empty copy of the existing DocumentSet, using the same
     * comparator.
     */    return t.oi = function(n) {
        return new t(n.F);
    }, t.prototype.has = function(t) {
        return null != this.si.get(t);
    }, t.prototype.get = function(t) {
        return this.si.get(t);
    }, t.prototype.first = function() {
        return this.ui.ot();
    }, t.prototype.last = function() {
        return this.ui.ht();
    }, t.prototype.M = function() {
        return this.ui.M();
    }, 
    /**
     * Returns the index of the provided key in the document set, or -1 if the
     * document key is not present in the set;
     */
    t.prototype.indexOf = function(t) {
        var n = this.si.get(t);
        return n ? this.ui.indexOf(n) : -1;
    }, Object.defineProperty(t.prototype, "size", {
        get: function() {
            return this.ui.size;
        },
        enumerable: !0,
        configurable: !0
    }), 
    /** Iterates documents in order defined by "comparator" */
    t.prototype.forEach = function(t) {
        this.ui.at((function(n, i) {
            return t(n), !1;
        }));
    }, 
    /** Inserts or updates a document with the same key */
    t.prototype.add = function(t) {
        // First remove the element if we have it.
        var n = this.delete(t.key);
        return n.copy(n.si.st(t.key, t), n.ui.st(t, null));
    }, 
    /** Deletes a document with a given key */
    t.prototype.delete = function(t) {
        var n = this.get(t);
        return n ? this.copy(this.si.remove(t), this.ui.remove(n)) : this;
    }, t.prototype.isEqual = function(n) {
        if (!(n instanceof t)) return !1;
        if (this.size !== n.size) return !1;
        for (var i = this.ui.ft(), e = n.ui.ft(); i.bt(); ) {
            var r = i.gt().key, s = e.gt().key;
            if (!r.isEqual(s)) return !1;
        }
        return !0;
    }, t.prototype.toString = function() {
        var t = [];
        return this.forEach((function(n) {
            t.push(n.toString());
        })), 0 === t.length ? "DocumentSet ()" : "DocumentSet (\n  " + t.join("  \n") + "\n)";
    }, t.prototype.copy = function(n, i) {
        var e = new t;
        return e.F = this.F, e.si = n, e.ui = i, e;
    }, t;
}(), gn = /** @class */ function() {
    function t() {
        this.hi = new U(j.F);
    }
    return t.prototype.track = function(t) {
        var n = t.doc.key, i = this.hi.get(n);
        i ? 
        // Merge the new change with the existing change.
        0 /* Added */ !== t.type && 3 /* Metadata */ === i.type ? this.hi = this.hi.st(n, t) : 3 /* Metadata */ === t.type && 1 /* Removed */ !== i.type ? this.hi = this.hi.st(n, {
            type: i.type,
            doc: t.doc
        }) : 2 /* Modified */ === t.type && 2 /* Modified */ === i.type ? this.hi = this.hi.st(n, {
            type: 2 /* Modified */ ,
            doc: t.doc
        }) : 2 /* Modified */ === t.type && 0 /* Added */ === i.type ? this.hi = this.hi.st(n, {
            type: 0 /* Added */ ,
            doc: t.doc
        }) : 1 /* Removed */ === t.type && 0 /* Added */ === i.type ? this.hi = this.hi.remove(n) : 1 /* Removed */ === t.type && 2 /* Modified */ === i.type ? this.hi = this.hi.st(n, {
            type: 1 /* Removed */ ,
            doc: i.doc
        }) : 0 /* Added */ === t.type && 1 /* Removed */ === i.type ? this.hi = this.hi.st(n, {
            type: 2 /* Modified */ ,
            doc: t.doc
        }) : 
        // This includes these cases, which don't make sense:
        // Added->Added
        // Removed->Removed
        // Modified->Added
        // Removed->Modified
        // Metadata->Added
        // Removed->Metadata
        Qn("unsupported combination of changes: " + JSON.stringify(t) + " after " + JSON.stringify(i)) : this.hi = this.hi.st(n, t);
    }, t.prototype.ai = function() {
        var t = [];
        return this.hi.at((function(n, i) {
            t.push(i);
        })), t;
    }, t;
}(), bn = /** @class */ function() {
    function t(t, n, i, e, r, s, u, o) {
        this.query = t, this.docs = n, this.ci = i, this.docChanges = e, this.fi = r, this.fromCache = s, 
        this.li = u, this.di = o;
    }
    /** Returns a view snapshot as if all documents in the snapshot were added. */    return t.vi = function(n, i, e, r) {
        var s = [];
        return i.forEach((function(t) {
            s.push({
                type: 0 /* Added */ ,
                doc: t
            });
        })), new t(n, i, pn.oi(i), s, e, r, 
        /* syncStateChanged= */ !0, 
        /* excludesMetadataChanges= */ !1);
    }, Object.defineProperty(t.prototype, "hasPendingWrites", {
        get: function() {
            return !this.fi.M();
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.isEqual = function(t) {
        if (!(this.fromCache === t.fromCache && this.li === t.li && this.fi.isEqual(t.fi) && this.query.isEqual(t.query) && this.docs.isEqual(t.docs) && this.ci.isEqual(t.ci))) return !1;
        var n = this.docChanges, i = t.docChanges;
        if (n.length !== i.length) return !1;
        for (var e = 0; e < n.length; e++) if (n[e].type !== i[e].type || !n[e].doc.isEqual(i[e].doc)) return !1;
        return !0;
    }, t;
}(), yn = /** @class */ function() {
    function t(
    /**
     * The snapshot version this event brings us up to, or MIN if not set.
     */
    t, 
    /**
     * A map from target to changes to the target. See TargetChange.
     */
    n, 
    /**
     * A set of targets that is known to be inconsistent. Listens for these
     * targets should be re-established without resume tokens.
     */
    i, 
    /**
     * A set of which documents have changed or been deleted, along with the
     * doc's new values (if not deleted).
     */
    e, 
    /**
     * A set of which document updates are due only to limbo resolution targets.
     */
    r) {
        this.ni = t, this.wi = n, this.mi = i, this.pi = e, this.gi = r;
    }
    /**
     * HACK: Views require RemoteEvents in order to determine whether the view is
     * CURRENT, but secondary tabs don't receive remote events. So this method is
     * used to create a synthesized RemoteEvent that can be used to apply a
     * CURRENT status change to a View, for queries executed in a different tab.
     */
    // PORTING NOTE: Multi-tab only
        return t.bi = function(n, i) {
        var e = new Map;
        return e.set(n, _n.yi(n, i)), new t(D.MIN, e, mn(), on(), vn());
    }, t;
}(), _n = /** @class */ function() {
    function t(
    /**
     * An opaque, server-assigned token that allows watching a query to be resumed
     * after disconnecting without retransmitting all the data that matches the
     * query. The resume token essentially identifies a point in time from which
     * the server should resume sending results.
     */
    t, 
    /**
     * The "current" (synced) status of this target. Note that "current"
     * has special meaning in the RPC protocol that implies that a target is
     * both up-to-date and consistent with the rest of the watch stream.
     */
    n, 
    /**
     * The set of documents that were newly assigned to this target as part of
     * this remote event.
     */
    i, 
    /**
     * The set of documents that were already assigned to this target but received
     * an update during this remote event.
     */
    e, 
    /**
     * The set of documents that were removed from this target as part of this
     * remote event.
     */
    r) {
        this.resumeToken = t, this._i = n, this.Ti = i, this.Ei = e, this.Si = r;
    }
    /**
     * This method is used to create a synthesized TargetChanges that can be used to
     * apply a CURRENT status change to a View (for queries executed in a different
     * tab) or for new queries (to raise snapshots with correct CURRENT status).
     */    return t.yi = function(n, i) {
        return new t(z.qt, i, vn(), vn(), vn());
    }, t;
}(), Tn = function(
/** The new document applies to all of these targets. */
t, 
/** The new document is removed from all of these targets. */
n, 
/** The key of the document for this change. */
i, 
/**
     * The new document or NoDocument if it was deleted. Is null if the
     * document went out of view without the server sending a new document.
     */
e) {
    this.Ai = t, this.removedTargetIds = n, this.key = i, this.Ii = e;
}, En = function(t, n) {
    this.targetId = t, this.Ci = n;
}, Sn = function(
/** What kind of change occurred to the watch target. */
t, 
/** The target IDs that were added/removed/set. */
n, 
/**
     * An opaque, server-assigned token that allows watching a target to be
     * resumed after disconnecting without retransmitting all the data that
     * matches the target. The resume token essentially identifies a point in
     * time from which the server should resume sending results.
     */
i, 
/** An RPC error indicating why the watch failed. */
e) {
    void 0 === i && (i = z.qt), void 0 === e && (e = null), this.state = t, this.targetIds = n, 
    this.resumeToken = i, this.cause = e;
}, An = /** @class */ function() {
    function t() {
        /**
         * The number of pending responses (adds or removes) that we are waiting on.
         * We only consider targets active that have no pending responses.
         */
        this.Ri = 0, 
        /**
         * Keeps track of the document changes since the last raised snapshot.
         *
         * These changes are continuously updated as we receive document updates and
         * always reflect the current set of changes against the last issued snapshot.
         */
        this.Ni = Rn(), 
        /** See public getters for explanations of these fields. */
        this.Oi = z.qt, this.Di = !1, 
        /**
         * Whether this target state should be included in the next snapshot. We
         * initialize to true so that newly-added targets are included in the next
         * RemoteEvent.
         */
        this.Vi = !0;
    }
    return Object.defineProperty(t.prototype, "_i", {
        /**
         * Whether this target has been marked 'current'.
         *
         * 'Current' has special meaning in the RPC protocol: It implies that the
         * Watch backend has sent us all changes up to the point at which the target
         * was added and that the target is consistent with the rest of the watch
         * stream.
         */
        get: function() {
            return this.Di;
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(t.prototype, "resumeToken", {
        /** The last resume token sent to us for this target. */
        get: function() {
            return this.Oi;
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(t.prototype, "Pi", {
        /** Whether this target has pending target adds or target removes. */
        get: function() {
            return 0 !== this.Ri;
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(t.prototype, "ki", {
        /** Whether we have modified any state that should trigger a snapshot. */
        get: function() {
            return this.Vi;
        },
        enumerable: !0,
        configurable: !0
    }), 
    /**
     * Applies the resume token to the TargetChange, but only when it has a new
     * value. Empty resumeTokens are discarded.
     */
    t.prototype.Fi = function(t) {
        t.Mt() > 0 && (this.Vi = !0, this.Oi = t);
    }, 
    /**
     * Creates a target change from the current set of changes.
     *
     * To reset the document changes after raising this snapshot, call
     * `clearPendingChanges()`.
     */
    t.prototype.ji = function() {
        var t = vn(), n = vn(), i = vn();
        return this.Ni.forEach((function(e, r) {
            switch (r) {
              case 0 /* Added */ :
                t = t.add(e);
                break;

              case 2 /* Modified */ :
                n = n.add(e);
                break;

              case 1 /* Removed */ :
                i = i.add(e);
                break;

              default:
                Qn("Encountered invalid change type: " + r);
            }
        })), new _n(this.Oi, this.Di, t, n, i);
    }, 
    /**
     * Resets the document changes and sets `hasPendingChanges` to false.
     */
    t.prototype.Ui = function() {
        this.Vi = !1, this.Ni = Rn();
    }, t.prototype.Li = function(t, n) {
        this.Vi = !0, this.Ni = this.Ni.st(t, n);
    }, t.prototype.Mi = function(t) {
        this.Vi = !0, this.Ni = this.Ni.remove(t);
    }, t.prototype.qi = function() {
        this.Ri += 1;
    }, t.prototype.xi = function() {
        this.Ri -= 1;
    }, t.prototype.Bi = function() {
        this.Vi = !0, this.Di = !0;
    }, t;
}(), In = /** @class */ function() {
    function t(t) {
        this.Qi = t, 
        /** The internal state of all tracked targets. */
        this.Wi = new Map, 
        /** Keeps track of the documents to update since the last raised snapshot. */
        this.Gi = on(), 
        /** A mapping of document keys to their set of target IDs. */
        this.zi = Cn(), 
        /**
         * A list of targets with existence filter mismatches. These targets are
         * known to be inconsistent and their listens needs to be re-established by
         * RemoteStore.
         */
        this.Ji = new x(zn);
    }
    /**
     * Processes and adds the DocumentWatchChange to the current set of changes.
     */    return t.prototype.Hi = function(t) {
        for (var n = 0, i = t.Ai; n < i.length; n++) {
            var e = i[n];
            t.Ii instanceof Ut ? this.Yi(e, t.Ii) : t.Ii instanceof Lt && this.Ki(e, t.key, t.Ii);
        }
        for (var r = 0, s = t.removedTargetIds; r < s.length; r++) {
            e = s[r];
            this.Ki(e, t.key, t.Ii);
        }
    }, 
    /** Processes and adds the WatchTargetChange to the current set of changes. */
    t.prototype.Xi = function(t) {
        var n = this;
        this.$i(t, (function(i) {
            var e = n.Zi(i);
            switch (t.state) {
              case 0 /* NoChange */ :
                n.te(i) && e.Fi(t.resumeToken);
                break;

              case 1 /* Added */ :
                // We need to decrement the number of pending acks needed from watch
                // for this targetId.
                e.xi(), e.Pi || 
                // We have a freshly added target, so we need to reset any state
                // that we had previously. This can happen e.g. when remove and add
                // back a target for existence filter mismatches.
                e.Ui(), e.Fi(t.resumeToken);
                break;

              case 2 /* Removed */ :
                // We need to keep track of removed targets to we can post-filter and
                // remove any target changes.
                // We need to decrement the number of pending acks needed from watch
                // for this targetId.
                e.xi(), e.Pi || n.removeTarget(i), Wn(!t.cause, "WatchChangeAggregator does not handle errored targets");
                break;

              case 3 /* Current */ :
                n.te(i) && (e.Bi(), e.Fi(t.resumeToken));
                break;

              case 4 /* Reset */ :
                n.te(i) && (
                // Reset the target and synthesizes removes for all existing
                // documents. The backend will re-add any documents that still
                // match the target before it sends the next global snapshot.
                n.ne(i), e.Fi(t.resumeToken));
                break;

              default:
                Qn("Unknown target watch change state: " + t.state);
            }
        }));
    }, 
    /**
     * Iterates over all targetIds that the watch change applies to: either the
     * targetIds explicitly listed in the change or the targetIds of all currently
     * active targets.
     */
    t.prototype.$i = function(t, n) {
        t.targetIds.length > 0 ? t.targetIds.forEach(n) : this.Wi.forEach((function(t, i) {
            return n(i);
        }));
    }, 
    /**
     * Handles existence filters and synthesizes deletes for filter mismatches.
     * Targets that are invalidated by filter mismatches are added to
     * `pendingTargetResets`.
     */
    t.prototype.ie = function(t) {
        var n = t.targetId, i = t.Ci.count, e = this.ee(n);
        if (e) {
            var r = e.target;
            if (r.En()) if (0 === i) {
                // The existence filter told us the document does not exist. We deduce
                // that this document does not exist and apply a deleted document to
                // our updates. Without applying this deleted document there might be
                // another query that will raise this document as part of a snapshot
                // until it is resolved, essentially exposing inconsistency between
                // queries.
                var s = new j(r.path);
                this.Ki(n, s, new Lt(s, D.D()));
            } else Wn(1 === i, "Single document existence filter with count: " + i); else this.re(n) !== i && (
            // Existence filter mismatch: We reset the mapping and raise a new
            // snapshot with `isFromCache:true`.
            this.ne(n), this.Ji = this.Ji.add(n));
        }
    }, 
    /**
     * Converts the currently accumulated state into a remote event at the
     * provided snapshot version. Resets the accumulated changes before returning.
     */
    t.prototype.se = function(t) {
        var n = this, i = new Map;
        this.Wi.forEach((function(e, r) {
            var s = n.ee(r);
            if (s) {
                if (e._i && s.target.En()) {
                    // Document queries for document that don't exist can produce an empty
                    // result set. To update our local cache, we synthesize a document
                    // delete if we have not previously received the document. This
                    // resolves the limbo state of the document, removing it from
                    // limboDocumentRefs.
                    // TODO(dimond): Ideally we would have an explicit lookup target
                    // instead resulting in an explicit delete message and we could
                    // remove this special logic.
                    var u = new j(s.target.path);
                    null !== n.Gi.get(u) || n.ue(r, u) || n.Ki(r, u, new Lt(u, t));
                }
                e.ki && (i.set(r, e.ji()), e.Ui());
            }
        }));
        var e = vn();
        // We extract the set of limbo-only document updates as the GC logic
        // special-cases documents that do not appear in the target cache.
        
        // TODO(gsoltis): Expand on this comment once GC is available in the JS
        // client.
                this.zi.forEach((function(t, i) {
            var r = !0;
            i.Pt((function(t) {
                var i = n.ee(t);
                return !i || 2 /* LimboResolution */ === i.ti || (r = !1, !1);
            })), r && (e = e.add(t));
        }));
        var r = new yn(t, i, this.Ji, this.Gi, e);
        return this.Gi = on(), this.zi = Cn(), this.Ji = new x(zn), r;
    }, 
    /**
     * Adds the provided document to the internal list of document updates and
     * its document key to the given target's mapping.
     */
    // Visible for testing.
    t.prototype.Yi = function(t, n) {
        if (this.te(t)) {
            var i = this.ue(t, n.key) ? 2 /* Modified */ : 0 /* Added */;
            this.Zi(t).Li(n.key, i), this.Gi = this.Gi.st(n.key, n), this.zi = this.zi.st(n.key, this.oe(n.key).add(t));
        }
    }, 
    /**
     * Removes the provided document from the target mapping. If the
     * document no longer matches the target, but the document's state is still
     * known (e.g. we know that the document was deleted or we received the change
     * that caused the filter mismatch), the new document can be provided
     * to update the remote document cache.
     */
    // Visible for testing.
    t.prototype.Ki = function(t, n, i) {
        if (this.te(t)) {
            var e = this.Zi(t);
            this.ue(t, n) ? e.Li(n, 1 /* Removed */) : 
            // The document may have entered and left the target before we raised a
            // snapshot, so we can just ignore the change.
            e.Mi(n), this.zi = this.zi.st(n, this.oe(n).delete(t)), i && (this.Gi = this.Gi.st(n, i));
        }
    }, t.prototype.removeTarget = function(t) {
        this.Wi.delete(t);
    }, 
    /**
     * Returns the current count of documents in the target. This includes both
     * the number of documents that the LocalStore considers to be part of the
     * target as well as any accumulated changes.
     */
    t.prototype.re = function(t) {
        var n = this.Zi(t).ji();
        return this.Qi.he(t).size + n.Ti.size - n.Si.size;
    }, 
    /**
     * Increment the number of acks needed from watch before we can consider the
     * server to be 'in-sync' with the client's active targets.
     */
    t.prototype.qi = function(t) {
        this.Zi(t).qi();
    }, t.prototype.Zi = function(t) {
        var n = this.Wi.get(t);
        return n || (n = new An, this.Wi.set(t, n)), n;
    }, t.prototype.oe = function(t) {
        var n = this.zi.get(t);
        return n || (n = new x(zn), this.zi = this.zi.st(t, n)), n;
    }, 
    /**
     * Verifies that the user is still interested in this target (by calling
     * `getTargetDataForTarget()`) and that we are not waiting for pending ADDs
     * from watch.
     */
    t.prototype.te = function(t) {
        var n = null !== this.ee(t);
        return n || qn("WatchChangeAggregator", "Detected inactive target", t), n;
    }, 
    /**
     * Returns the TargetData for an active target (i.e. a target that the user
     * is still interested in that has no outstanding target change requests).
     */
    t.prototype.ee = function(t) {
        var n = this.Wi.get(t);
        return n && n.Pi ? null : this.Qi.ae(t);
    }, 
    /**
     * Resets the state of a Watch target to its initial state (e.g. sets
     * 'current' to false, clears the resume token and removes its target mapping
     * from all documents).
     */
    t.prototype.ne = function(t) {
        var n = this;
        Wn(!this.Wi.get(t).Pi, "Should only reset active targets"), this.Wi.set(t, new An), 
        this.Qi.he(t).forEach((function(i) {
            n.Ki(t, i, /*updatedDocument=*/ null);
        }));
    }, 
    /**
     * Returns whether the LocalStore considers the document to be part of the
     * specified target.
     */
    t.prototype.ue = function(t, n) {
        return this.Qi.he(t).has(n);
    }, t;
}();

/**
 * @license
 * Copyright 2017 Google Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * DocumentChangeSet keeps track of a set of changes to docs in a query, merging
 * duplicate events for the same doc.
 */ function Cn() {
    return new U(j.F);
}

function Rn() {
    return new U(j.F);
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ var Nn, On, Dn = ((Nn = {})[Kt.ASCENDING.name] = "ASCENDING", Nn[Kt.DESCENDING.name] = "DESCENDING", 
Nn), Vn = ((On = {})[Qt.LESS_THAN.name] = "LESS_THAN", On[Qt.LESS_THAN_OR_EQUAL.name] = "LESS_THAN_OR_EQUAL", 
On[Qt.GREATER_THAN.name] = "GREATER_THAN", On[Qt.GREATER_THAN_OR_EQUAL.name] = "GREATER_THAN_OR_EQUAL", 
On[Qt.EQUAL.name] = "EQUAL", On[Qt.ARRAY_CONTAINS.name] = "ARRAY_CONTAINS", On[Qt.IN.name] = "IN", 
On[Qt.ARRAY_CONTAINS_ANY.name] = "ARRAY_CONTAINS_ANY", On);

function Pn(t, n) {
    Wn(!J(t), n + " is missing");
}

/**
 * Generates JsonObject values for the Datastore API suitable for sending to
 * either GRPC stub methods or via the JSON/HTTP REST API.
 * TODO(klimt): We can remove the databaseId argument if we keep the full
 * resource name in documents.
 */ var kn = /** @class */ function() {
    function t(t, n) {
        this.ce = t, this.options = n;
    }
    return t.prototype.fe = function(t) {
        var n = void 0 === t.code ? T.UNKNOWN : sn(t.code);
        return new E(n, t.message || "");
    }, 
    /**
     * Returns a value for a number (or null) that's appropriate to put into
     * a google.protobuf.Int32Value proto.
     * DO NOT USE THIS FOR ANYTHING ELSE.
     * This method cheats. It's typed as returning "number" because that's what
     * our generated proto interfaces say Int32Value must be. But GRPC actually
     * expects a { value: <number> } struct.
     */
    t.prototype.le = function(t) {
        return this.options.de || J(t) ? t : {
            value: t
        };
    }, 
    /**
     * Returns a number (or null) from a google.protobuf.Int32Value proto.
     */
    t.prototype.ve = function(t) {
        var n;
        return J(n = "object" == typeof t ? t.value : t) ? null : n;
    }, 
    /**
     * Returns an IntegerValue for `value`.
     */
    t.prototype.Gt = function(t) {
        return {
            integerValue: "" + t
        };
    }, 
    /**
     * Returns an DoubleValue for `value` that is encoded based the serializer's
     * `useProto3Json` setting.
     */
    t.prototype.zt = function(t) {
        if (this.options.de) {
            if (isNaN(t)) return {
                doubleValue: "NaN"
            };
            if (t === 1 / 0) return {
                doubleValue: "Infinity"
            };
            if (t === -1 / 0) return {
                doubleValue: "-Infinity"
            };
        }
        return {
            doubleValue: H(t) ? "-0" : t
        };
    }, 
    /**
     * Returns a value for a number that's appropriate to put into a proto.
     * The return value is an IntegerValue if it can safely represent the value,
     * otherwise a DoubleValue is returned.
     */
    t.prototype.we = function(t) {
        return Y(t) ? this.Gt(t) : this.zt(t);
    }, 
    /**
     * Returns a value for a Date that's appropriate to put into a proto.
     */
    t.prototype.k = function(t) {
        return this.options.de ? new Date(1e3 * t.seconds).toISOString().replace(/\.\d*/, "").replace("Z", "") + "." + ("000000000" + t.nanoseconds).slice(-9) + "Z" : {
            seconds: "" + t.seconds,
            nanos: t.nanoseconds
        };
    }, t.prototype.O = function(t) {
        var n = ut(t);
        return new O(n.seconds, n.nanos);
    }, 
    /**
     * Returns a value for bytes that's appropriate to put in a proto.
     *
     * Visible for testing.
     */
    t.prototype.me = function(t) {
        return this.options.de ? t.toBase64() : t.toUint8Array();
    }, 
    /**
     * Returns a ByteString based on the proto string value.
     */
    t.prototype.pe = function(t) {
        return this.options.de ? (Wn(void 0 === t || "string" == typeof t, "value must be undefined or a string when using proto3 Json"), 
        z.fromBase64String(t || "")) : (Wn(void 0 === t || t instanceof Uint8Array, "value must be undefined or Uint8Array"), 
        z.fromUint8Array(t || new Uint8Array));
    }, t.prototype.toVersion = function(t) {
        return this.k(t.k());
    }, t.prototype.fromVersion = function(t) {
        return Wn(!!t, "Trying to deserialize version that isn't set"), D.O(this.O(t));
    }, t.prototype.ge = function(t, n) {
        return this.be(n || this.ce).child("documents").child(t).H();
    }, t.prototype.ye = function(t) {
        var n = P.Y(t);
        return Wn(Fn(n), "Tried to deserialize invalid key " + n.toString()), n;
    }, t.prototype._e = function(t) {
        return this.ge(t.path);
    }, t.prototype.it = function(t) {
        var n = this.ye(t);
        return Wn(n.get(1) === this.ce.projectId, "Tried to deserialize key from different project: " + n.get(1) + " vs " + this.ce.projectId), 
        Wn(!n.get(3) && !this.ce.database || n.get(3) === this.ce.database, "Tried to deserialize key from different database: " + n.get(3) + " vs " + this.ce.database), 
        new j(this.Te(n));
    }, t.prototype.Ee = function(t) {
        return this.ge(t);
    }, t.prototype.Se = function(t) {
        var n = this.ye(t);
        // In v1beta1 queries for collections at the root did not have a trailing
        // "/documents". In v1 all resource paths contain "/documents". Preserve the
        // ability to read the v1beta1 form for compatibility with queries persisted
        // in the local target cache.
                return 4 === n.length ? P.K : this.Te(n);
    }, Object.defineProperty(t.prototype, "Ae", {
        get: function() {
            return new P([ "projects", this.ce.projectId, "databases", this.ce.database ]).H();
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.be = function(t) {
        return new P([ "projects", t.projectId, "databases", t.database ]);
    }, t.prototype.Te = function(t) {
        return Wn(t.length > 4 && "documents" === t.get(4), "tried to deserialize invalid key " + t.toString()), 
        t.U(5);
    }, 
    /** Creates an api.Document from key and fields (but no create/update time) */
    t.prototype.Ie = function(t, n) {
        return {
            name: this._e(t),
            fields: n.proto.mapValue.fields
        };
    }, t.prototype.Ce = function(t) {
        return Wn(!t.nn, "Can't serialize documents with mutations."), {
            name: this._e(t.key),
            fields: t.yn().mapValue.fields,
            updateTime: this.k(t.version.k())
        };
    }, t.prototype.Re = function(t, n) {
        var i = this.it(t.name), e = this.fromVersion(t.updateTime), r = new kt({
            mapValue: {
                fields: t.fields
            }
        });
        return new Ut(i, e, r, {
            hasCommittedMutations: !!n
        });
    }, t.prototype.Ne = function(t) {
        Wn(!!t.found, "Tried to deserialize a found document from a missing document."), 
        Pn(t.found.name, "doc.found.name"), Pn(t.found.updateTime, "doc.found.updateTime");
        var n = this.it(t.found.name), i = this.fromVersion(t.found.updateTime), e = new kt({
            mapValue: {
                fields: t.found.fields
            }
        });
        return new Ut(n, i, e, {});
    }, t.prototype.Oe = function(t) {
        Wn(!!t.missing, "Tried to deserialize a missing document from a found document."), 
        Wn(!!t.readTime, "Tried to deserialize a missing document without a read time.");
        var n = this.it(t.missing), i = this.fromVersion(t.readTime);
        return new Lt(n, i);
    }, t.prototype.De = function(t) {
        return "found" in t ? this.Ne(t) : "missing" in t ? this.Oe(t) : Qn("invalid batch get response: " + JSON.stringify(t));
    }, t.prototype.Ve = function(t) {
        var n;
        if ("targetChange" in t) {
            Pn(t.targetChange, "targetChange");
            // proto3 default value is unset in JSON (undefined), so use 'NO_CHANGE'
            // if unset
            var i = this.Pe(t.targetChange.targetChangeType || "NO_CHANGE"), e = t.targetChange.targetIds || [], r = this.pe(t.targetChange.resumeToken), s = t.targetChange.cause, u = s && this.fe(s);
            n = new Sn(i, e, r, u || null);
        } else if ("documentChange" in t) {
            Pn(t.documentChange, "documentChange");
            var o = t.documentChange;
            Pn(o.document, "documentChange.name"), Pn(o.document.name, "documentChange.document.name"), 
            Pn(o.document.updateTime, "documentChange.document.updateTime");
            var h = this.it(o.document.name), a = this.fromVersion(o.document.updateTime), c = new kt({
                mapValue: {
                    fields: o.document.fields
                }
            }), f = new Ut(h, a, c, {}), l = o.targetIds || [], d = o.removedTargetIds || [];
            n = new Tn(l, d, f.key, f);
        } else if ("documentDelete" in t) {
            Pn(t.documentDelete, "documentDelete");
            var v = t.documentDelete;
            Pn(v.document, "documentDelete.document");
            h = this.it(v.document), a = v.readTime ? this.fromVersion(v.readTime) : D.D(), 
            f = new Lt(h, a), d = v.removedTargetIds || [];
            n = new Tn([], d, f.key, f);
        } else if ("documentRemove" in t) {
            Pn(t.documentRemove, "documentRemove");
            var w = t.documentRemove;
            Pn(w.document, "documentRemove");
            h = this.it(w.document), d = w.removedTargetIds || [];
            n = new Tn([], d, h, null);
        } else {
            if (!("filter" in t)) return Qn("Unknown change type " + JSON.stringify(t));
            // TODO(dimond): implement existence filter parsing with strategy.
            Pn(t.filter, "filter");
            var m = t.filter;
            Pn(m.targetId, "filter.targetId");
            var p = m.count || 0, g = new en(p), b = m.targetId;
            n = new En(b, g);
        }
        return n;
    }, t.prototype.Pe = function(t) {
        return "NO_CHANGE" === t ? 0 /* NoChange */ : "ADD" === t ? 1 /* Added */ : "REMOVE" === t ? 2 /* Removed */ : "CURRENT" === t ? 3 /* Current */ : "RESET" === t ? 4 /* Reset */ : Qn("Got unexpected TargetChange.state: " + t);
    }, t.prototype.ke = function(t) {
        // We have only reached a consistent snapshot for the entire stream if there
        // is a read_time set and it applies to all targets (i.e. the list of
        // targets is empty). The backend is guaranteed to send such responses.
        if (!("targetChange" in t)) return D.MIN;
        var n = t.targetChange;
        return n.targetIds && n.targetIds.length ? D.MIN : n.readTime ? this.fromVersion(n.readTime) : D.MIN;
    }, t.prototype.Fe = function(t) {
        var n, i = this;
        if (t instanceof Nt) n = {
            update: this.Ie(t.key, t.value)
        }; else if (t instanceof Vt) n = {
            delete: this._e(t.key)
        }; else if (t instanceof Ot) n = {
            update: this.Ie(t.key, t.data),
            updateMask: this.je(t.en)
        }; else if (t instanceof Dt) n = {
            transform: {
                document: this._e(t.key),
                fieldTransforms: t.fieldTransforms.map((function(t) {
                    return i.Ue(t);
                }))
            }
        }; else {
            if (!(t instanceof Pt)) return Qn("Unknown mutation type " + t.type);
            n = {
                verify: this._e(t.key)
            };
        }
        return t.tn.Kt || (n.currentDocument = this.Le(t.tn)), n;
    }, t.prototype.Me = function(t) {
        var n = this, i = t.currentDocument ? this.qe(t.currentDocument) : Ct.NONE;
        if (t.update) {
            Pn(t.update.name, "name");
            var e = this.it(t.update.name), r = new kt({
                mapValue: {
                    fields: t.update.fields
                }
            });
            if (t.updateMask) {
                var s = this.xe(t.updateMask);
                return new Ot(e, r, s, i);
            }
            return new Nt(e, r, i);
        }
        if (t.delete) {
            e = this.it(t.delete);
            return new Vt(e, i);
        }
        if (t.transform) {
            e = this.it(t.transform.document);
            var u = t.transform.fieldTransforms.map((function(t) {
                return n.Be(t);
            }));
            return Wn(!0 === i.exists, 'Transforms only support precondition "exists == true"'), 
            new Dt(e, u);
        }
        if (t.verify) {
            e = this.it(t.verify);
            return new Pt(e, i);
        }
        return Qn("unknown mutation proto: " + JSON.stringify(t));
    }, t.prototype.Le = function(t) {
        return Wn(!t.Kt, "Can't serialize an empty precondition"), void 0 !== t.updateTime ? {
            updateTime: this.toVersion(t.updateTime)
        } : void 0 !== t.exists ? {
            exists: t.exists
        } : Qn("Unknown precondition");
    }, t.prototype.qe = function(t) {
        return void 0 !== t.updateTime ? Ct.updateTime(this.fromVersion(t.updateTime)) : void 0 !== t.exists ? Ct.exists(t.exists) : Ct.NONE;
    }, t.prototype.Qe = function(t, n) {
        // NOTE: Deletes don't have an updateTime.
        var i = t.updateTime ? this.fromVersion(t.updateTime) : this.fromVersion(n);
        i.isEqual(D.MIN) && (
        // The Firestore Emulator currently returns an update time of 0 for
        // deletes of non-existing documents (rather than null). This breaks the
        // test "get deleted doc while offline with source=cache" as NoDocuments
        // with version 0 are filtered by IndexedDb's RemoteDocumentCache.
        // TODO(#2149): Remove this when Emulator is fixed
        i = this.fromVersion(n));
        var e = null;
        return t.transformResults && t.transformResults.length > 0 && (e = t.transformResults), 
        new It(i, e);
    }, t.prototype.We = function(t, n) {
        var i = this;
        return t && t.length > 0 ? (Wn(void 0 !== n, "Received a write result without a commit time"), 
        t.map((function(t) {
            return i.Qe(t, n);
        }))) : [];
    }, t.prototype.Ue = function(t) {
        var n = t.transform;
        if (n instanceof pt) return {
            fieldPath: t.field.H(),
            setToServerValue: "REQUEST_TIME"
        };
        if (n instanceof gt) return {
            fieldPath: t.field.H(),
            appendMissingElements: {
                values: n.elements
            }
        };
        if (n instanceof bt) return {
            fieldPath: t.field.H(),
            removeAllFromArray: {
                values: n.elements
            }
        };
        if (n instanceof yt) return {
            fieldPath: t.field.H(),
            increment: n.Wt
        };
        throw Qn("Unknown transform: " + t.transform);
    }, t.prototype.Be = function(t) {
        var n = null;
        if ("setToServerValue" in t) Wn("REQUEST_TIME" === t.setToServerValue, "Unknown server value transform proto: " + JSON.stringify(t)), 
        n = pt.instance; else if ("appendMissingElements" in t) {
            var i = t.appendMissingElements.values || [];
            n = new gt(i);
        } else if ("removeAllFromArray" in t) {
            i = t.removeAllFromArray.values || [];
            n = new bt(i);
        } else "increment" in t ? n = new yt(this, t.increment) : Qn("Unknown transform proto: " + JSON.stringify(t));
        var e = F.tt(t.fieldPath);
        return new At(e, n);
    }, t.prototype.Ge = function(t) {
        return {
            documents: [ this.Ee(t.path) ]
        };
    }, t.prototype.ze = function(t) {
        var n = t.documents.length;
        Wn(1 === n, "DocumentsTarget contained other than 1 document: " + n);
        var i = t.documents[0];
        return xt.Nn(this.Se(i)).xn();
    }, t.prototype.Je = function(t) {
        // Dissect the path into parent, collectionId, and optional key filter.
        var n = {
            structuredQuery: {}
        }, i = t.path;
        null !== t.collectionGroup ? (Wn(i.length % 2 == 0, "Collection Group queries should be within a document path or root."), 
        n.parent = this.Ee(i), n.structuredQuery.from = [ {
            collectionId: t.collectionGroup,
            allDescendants: !0
        } ]) : (Wn(i.length % 2 != 0, "Document queries with filters are not supported."), 
        n.parent = this.Ee(i.L()), n.structuredQuery.from = [ {
            collectionId: i.B()
        } ]);
        var e = this.He(t.filters);
        e && (n.structuredQuery.where = e);
        var r = this.Ye(t.orderBy);
        r && (n.structuredQuery.orderBy = r);
        var s = this.le(t.limit);
        return null !== s && (n.structuredQuery.limit = s), t.startAt && (n.structuredQuery.startAt = this.Ke(t.startAt)), 
        t.endAt && (n.structuredQuery.endAt = this.Ke(t.endAt)), n;
    }, t.prototype.Xe = function(t) {
        var n = this.Se(t.parent), i = t.structuredQuery, e = i.from ? i.from.length : 0, r = null;
        if (e > 0) {
            Wn(1 === e, "StructuredQuery.from with more than one collection is not supported.");
            var s = i.from[0];
            s.allDescendants ? r = s.collectionId : n = n.child(s.collectionId);
        }
        var u = [];
        i.where && (u = this.$e(i.where));
        var o = [];
        i.orderBy && (o = this.Ze(i.orderBy));
        var h = null;
        i.limit && (h = this.ve(i.limit));
        var a = null;
        i.startAt && (a = this.tr(i.startAt));
        var c = null;
        return i.endAt && (c = this.tr(i.endAt)), new xt(n, r, o, u, h, "F" /* First */ , a, c).xn();
    }, t.prototype.nr = function(t) {
        var n = this.ir(t.ti);
        return null == n ? null : {
            "goog-listen-tags": n
        };
    }, t.prototype.ir = function(t) {
        switch (t) {
          case 0 /* Listen */ :
            return null;

          case 1 /* ExistenceFilterMismatch */ :
            return "existence-filter-mismatch";

          case 2 /* LimboResolution */ :
            return "limbo-document";

          default:
            return Qn("Unrecognized query purpose: " + t);
        }
    }, t.prototype.xn = function(t) {
        var n, i = t.target;
        return (n = i.En() ? {
            documents: this.Ge(i)
        } : {
            query: this.Je(i)
        }).targetId = t.targetId, t.resumeToken.Mt() > 0 && (n.resumeToken = this.me(t.resumeToken)), 
        n;
    }, t.prototype.He = function(t) {
        var n = this;
        if (0 !== t.length) {
            var i = t.map((function(t) {
                return t instanceof Wt ? n.er(t) : Qn("Unrecognized filter: " + JSON.stringify(t));
            }));
            return 1 === i.length ? i[0] : {
                compositeFilter: {
                    op: "AND",
                    filters: i
                }
            };
        }
    }, t.prototype.$e = function(t) {
        var n = this;
        return t ? void 0 !== t.unaryFilter ? [ this.rr(t) ] : void 0 !== t.fieldFilter ? [ this.sr(t) ] : void 0 !== t.compositeFilter ? t.compositeFilter.filters.map((function(t) {
            return n.$e(t);
        })).reduce((function(t, n) {
            return t.concat(n);
        })) : Qn("Unknown filter: " + JSON.stringify(t)) : [];
    }, t.prototype.Ye = function(t) {
        var n = this;
        if (0 !== t.length) return t.map((function(t) {
            return n.ur(t);
        }));
    }, t.prototype.Ze = function(t) {
        var n = this;
        return t.map((function(t) {
            return n.or(t);
        }));
    }, t.prototype.Ke = function(t) {
        return {
            before: t.before,
            values: t.position
        };
    }, t.prototype.tr = function(t) {
        var n = !!t.before, i = t.values || [];
        return new Xt(i, n);
    }, 
    // visible for testing
    t.prototype.hr = function(t) {
        return Dn[t.name];
    }, 
    // visible for testing
    t.prototype.ar = function(t) {
        switch (t) {
          case "ASCENDING":
            return Kt.ASCENDING;

          case "DESCENDING":
            return Kt.DESCENDING;

          default:
            return;
        }
    }, 
    // visible for testing
    t.prototype.cr = function(t) {
        return Vn[t.name];
    }, t.prototype.lr = function(t) {
        switch (t) {
          case "EQUAL":
            return Qt.EQUAL;

          case "GREATER_THAN":
            return Qt.GREATER_THAN;

          case "GREATER_THAN_OR_EQUAL":
            return Qt.GREATER_THAN_OR_EQUAL;

          case "LESS_THAN":
            return Qt.LESS_THAN;

          case "LESS_THAN_OR_EQUAL":
            return Qt.LESS_THAN_OR_EQUAL;

          case "ARRAY_CONTAINS":
            return Qt.ARRAY_CONTAINS;

          case "IN":
            return Qt.IN;

          case "ARRAY_CONTAINS_ANY":
            return Qt.ARRAY_CONTAINS_ANY;

          case "OPERATOR_UNSPECIFIED":
            return Qn("Unspecified operator");

          default:
            return Qn("Unknown operator");
        }
    }, t.prototype.dr = function(t) {
        return {
            fieldPath: t.H()
        };
    }, t.prototype.vr = function(t) {
        return F.tt(t.fieldPath);
    }, 
    // visible for testing
    t.prototype.ur = function(t) {
        return {
            field: this.dr(t.field),
            direction: this.hr(t.dir)
        };
    }, t.prototype.or = function(t) {
        return new $t(this.vr(t.field), this.ar(t.direction));
    }, t.prototype.sr = function(t) {
        return Wt.create(this.vr(t.fieldFilter.field), this.lr(t.fieldFilter.op), t.fieldFilter.value);
    }, 
    // visible for testing
    t.prototype.er = function(t) {
        if (t.op === Qt.EQUAL) {
            if (wt(t.value)) return {
                unaryFilter: {
                    field: this.dr(t.field),
                    op: "IS_NAN"
                }
            };
            if (vt(t.value)) return {
                unaryFilter: {
                    field: this.dr(t.field),
                    op: "IS_NULL"
                }
            };
        }
        return {
            fieldFilter: {
                field: this.dr(t.field),
                op: this.cr(t.op),
                value: t.value
            }
        };
    }, t.prototype.rr = function(t) {
        switch (t.unaryFilter.op) {
          case "IS_NAN":
            var n = this.vr(t.unaryFilter.field);
            return Wt.create(n, Qt.EQUAL, {
                doubleValue: NaN
            });

          case "IS_NULL":
            var i = this.vr(t.unaryFilter.field);
            return Wt.create(i, Qt.EQUAL, {
                nullValue: "NULL_VALUE"
            });

          case "OPERATOR_UNSPECIFIED":
            return Qn("Unspecified filter");

          default:
            return Qn("Unknown filter");
        }
    }, t.prototype.je = function(t) {
        var n = [];
        return t.fields.forEach((function(t) {
            return n.push(t.H());
        })), {
            fieldPaths: n
        };
    }, t.prototype.xe = function(t) {
        var n = (t.fieldPaths || []).map((function(t) {
            return F.tt(t);
        }));
        return St.Ht(n);
    }, t;
}();

function Fn(t) {
    // Resource names have at least 4 components (project ID, database ID)
    return t.length >= 4 && "projects" === t.get(0) && "databases" === t.get(2);
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Provides singleton helpers where setup code can inject a platform at runtime.
 * setPlatform needs to be set before Firestore is used and must be set exactly
 * once.
 */ var jn = /** @class */ function() {
    function t() {}
    return t.wr = function(n) {
        t.platform && Qn("Platform already defined"), t.platform = n;
    }, t.Lt = function() {
        return t.platform || Qn("Platform not set"), t.platform;
    }, t;
}(), Un = new u("@firebase/firestore");

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// Helper methods are needed because variables can't be exported as read/write
function Ln() {
    return Un.logLevel;
}

function Mn(t) {
    Un.logLevel = t;
}

function qn(t) {
    for (var n = [], i = 1; i < arguments.length; i++) n[i - 1] = arguments[i];
    if (Un.logLevel <= o.DEBUG) {
        var r = n.map(Bn);
        Un.error.apply(Un, e([ "Firestore (" + y + "): " + t ], r));
    }
}

function xn(t) {
    for (var n = [], i = 1; i < arguments.length; i++) n[i - 1] = arguments[i];
    if (Un.logLevel <= o.ERROR) {
        var r = n.map(Bn);
        Un.error.apply(Un, e([ "Firestore (" + y + "): " + t ], r));
    }
}

/**
 * Converts an additional log parameter to a string representation.
 */ function Bn(t) {
    if ("string" == typeof t) return t;
    var n = jn.Lt();
    try {
        return n.mr(t);
    } catch (n) {
        // Converting to JSON failed, just log the object directly
        return t;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Unconditionally fails, throwing an Error with the given message.
 *
 * Returns any so it can be used in expressions:
 * @example
 * let futureVar = fail('not implemented yet');
 */ function Qn(t) {
    // Log the failure in addition to throw an exception, just in case the
    // exception is swallowed.
    var n = "FIRESTORE (" + y + ") INTERNAL ASSERTION FAILED: " + t;
    // NOTE: We don't use FirestoreError here because these are internal failures
    // that cannot be handled by the user. (Also it would create a circular
    // dependency between the error and assert modules which doesn't work.)
    throw xn(n), new Error(n);
}

/**
 * Fails if the given assertion condition is false, throwing an Error with the
 * given message if it did.
 */ function Wn(t, n) {
    t || Qn(n);
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ var Gn = /** @class */ function() {
    function t() {}
    return t.pr = function() {
        for (
        // Alphanumeric characters
        var t = ""; t.length < 20; ) for (var n = jn.Lt().randomBytes(40), i = 0, e = Array.from(n); i < e.length; i++) {
            var r = e[i];
            // Length of `chars` is 62. We only take bytes between 0 and 62*4-1
            // (both inclusive). The value is then evenly mapped to indices of `char`
            // via a modulo operation.
                        t.length < 20 && r <= 247 && (t += "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789".charAt(r % 62));
        }
        return Wn(20 === t.length, "Invalid auto ID: " + t), t;
    }, t;
}();

function zn(t, n) {
    return t < n ? -1 : t > n ? 1 : 0;
}

function Jn(t, n, i) {
    return t.length === n.length && t.every((function(t, e) {
        return i(t, n[e]);
    }));
}

/**
 * @license
 * Copyright 2017 Google Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ var Hn = 
/**
     * Constructs a DatabaseInfo using the provided host, databaseId and
     * persistenceKey.
     *
     * @param databaseId The database to use.
     * @param persistenceKey A unique identifier for this Firestore's local
     * storage (used in conjunction with the databaseId).
     * @param host The Firestore backend host to connect to.
     * @param ssl Whether to use SSL when connecting.
     * @param forceLongPolling Whether to use the forceLongPolling option
     * when using WebChannel as the network transport.
     */
function(t, n, i, e, r) {
    this.ce = t, this.persistenceKey = n, this.host = i, this.ssl = e, this.forceLongPolling = r;
}, Yn = /** @class */ function() {
    function t(t, n) {
        this.projectId = t, this.database = n || "(default)";
    }
    return Object.defineProperty(t.prototype, "gr", {
        get: function() {
            return "(default)" === this.database;
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.isEqual = function(n) {
        return n instanceof t && n.projectId === this.projectId && n.database === this.database;
    }, t.prototype.V = function(t) {
        return zn(this.projectId, t.projectId) || zn(this.database, t.database);
    }, t;
}(), Kn = /** @class */ function() {
    /**
     * @param batchId The unique ID of this mutation batch.
     * @param localWriteTime The original write time of this mutation.
     * @param baseMutations Mutations that are used to populate the base
     * values when this mutation is applied locally. This can be used to locally
     * overwrite values that are persisted in the remote document cache. Base
     * mutations are never sent to the backend.
     * @param mutations The user-provided mutations in this mutation batch.
     * User-provided mutations are applied both locally and remotely on the
     * backend.
     */
    function t(t, n, i, e) {
        this.batchId = t, this.br = n, this.baseMutations = i, this.mutations = e, Wn(e.length > 0, "Cannot create an empty mutation batch");
    }
    /**
     * Applies all the mutations in this MutationBatch to the specified document
     * to create a new remote document
     *
     * @param docKey The key of the document to apply mutations to.
     * @param maybeDoc The document to apply mutations to.
     * @param batchResult The result of applying the MutationBatch to the
     * backend.
     */    return t.prototype.Bt = function(t, n, i) {
        n && Wn(n.key.isEqual(t), "applyToRemoteDocument: key " + t + " should match maybeDoc key\n        " + n.key);
        var e = i.yr;
        Wn(e.length === this.mutations.length, "Mismatch between mutations length\n      (" + this.mutations.length + ") and mutation results length\n      (" + e.length + ").");
        for (var r = 0; r < this.mutations.length; r++) {
            var s = this.mutations[r];
            if (s.key.isEqual(t)) {
                var u = e[r];
                n = s.Bt(n, u);
            }
        }
        return n;
    }, 
    /**
     * Computes the local view of a document given all the mutations in this
     * batch.
     *
     * @param docKey The key of the document to apply mutations to.
     * @param maybeDoc The document to apply mutations to.
     */
    t.prototype.xt = function(t, n) {
        n && Wn(n.key.isEqual(t), "applyToLocalDocument: key " + t + " should match maybeDoc key\n        " + n.key);
        // First, apply the base state. This allows us to apply non-idempotent
        // transform against a consistent set of values.
                for (var i = 0, e = this.baseMutations; i < e.length; i++) {
            (o = e[i]).key.isEqual(t) && (n = o.xt(n, n, this.br));
        }
        // Second, apply all user-provided mutations.
        for (var r = n, s = 0, u = this.mutations; s < u.length; s++) {
            var o;
            (o = u[s]).key.isEqual(t) && (n = o.xt(n, r, this.br));
        }
        return n;
    }, 
    /**
     * Computes the local view for all provided documents given the mutations in
     * this batch.
     */
    t.prototype._r = function(t) {
        var n = this, i = t;
        // TODO(mrschmidt): This implementation is O(n^2). If we apply the mutations
        // directly (as done in `applyToLocalView()`), we can reduce the complexity
        // to O(n).
                return this.mutations.forEach((function(e) {
            var r = n.xt(e.key, t.get(e.key));
            r && (i = i.st(e.key, r));
        })), i;
    }, t.prototype.keys = function() {
        return this.mutations.reduce((function(t, n) {
            return t.add(n.key);
        }), vn());
    }, t.prototype.isEqual = function(t) {
        return this.batchId === t.batchId && Jn(this.mutations, t.mutations, (function(t, n) {
            return t.isEqual(n);
        })) && Jn(this.baseMutations, t.baseMutations, (function(t, n) {
            return t.isEqual(n);
        }));
    }, t;
}(), Xn = /** @class */ function() {
    function t(t, n, i, e, 
    /**
     * A pre-computed mapping from each mutated document to the resulting
     * version.
     */
    r) {
        this.batch = t, this.Tr = n, this.yr = i, this.streamToken = e, this.Er = r;
    }
    /**
     * Creates a new MutationBatchResult for the given batch and results. There
     * must be one result for each mutation in the batch. This static factory
     * caches a document=>version mapping (docVersions).
     */    return t.from = function(n, i, e, r) {
        Wn(n.mutations.length === e.length, "Mutations sent " + n.mutations.length + " must equal results received " + e.length);
        for (var s = ln(), u = n.mutations, o = 0; o < u.length; o++) s = s.st(u[o].key, e[o].version);
        return new t(n, i, e, r, s);
    }, t;
}(), $n = /** @class */ function() {
    function t(t) {
        this.Sr = t, 
        /**
         * The inner map for a key -> value pair. Due to the possibility of
         * collisions we keep a list of entries that we do a linear search through
         * to find an actual match. Note that collisions should be rare, so we still
         * expect near constant time lookups in practice.
         */
        this.Ar = {};
    }
    /** Get a value for this key, or undefined if it does not exist. */    return t.prototype.get = function(t) {
        var n = this.Sr(t), i = this.Ar[n];
        if (void 0 !== i) for (var e = 0, r = i; e < r.length; e++) {
            var s = r[e], u = s[0], o = s[1];
            if (u.isEqual(t)) return o;
        }
    }, t.prototype.has = function(t) {
        return void 0 !== this.get(t);
    }, 
    /** Put this key and value in the map. */
    t.prototype.set = function(t, n) {
        var i = this.Sr(t), e = this.Ar[i];
        if (void 0 !== e) {
            for (var r = 0; r < e.length; r++) if (e[r][0].isEqual(t)) return void (e[r] = [ t, n ]);
            e.push([ t, n ]);
        } else this.Ar[i] = [ [ t, n ] ];
    }, 
    /**
     * Remove this key from the map. Returns a boolean if anything was deleted.
     */
    t.prototype.delete = function(t) {
        var n = this.Sr(t), i = this.Ar[n];
        if (void 0 === i) return !1;
        for (var e = 0; e < i.length; e++) if (i[e][0].isEqual(t)) return 1 === i.length ? delete this.Ar[n] : i.splice(e, 1), 
        !0;
        return !1;
    }, t.prototype.forEach = function(t) {
        W(this.Ar, (function(n, i) {
            for (var e = 0, r = i; e < r.length; e++) {
                var s = r[e], u = s[0], o = s[1];
                t(u, o);
            }
        }));
    }, t.prototype.M = function() {
        return G(this.Ar);
    }, t;
}(), Zn = /** @class */ function() {
    function t(t, n, i, e) {
        this.targetId = t, this.fromCache = n, this.Ir = i, this.Cr = e;
    }
    return t.Rr = function(n, i) {
        for (var e = vn(), r = vn(), s = 0, u = i.docChanges; s < u.length; s++) {
            var o = u[s];
            switch (o.type) {
              case 0 /* Added */ :
                e = e.add(o.doc.key);
                break;

              case 1 /* Removed */ :
                r = r.add(o.doc.key);
 // do nothing
                        }
        }
        return new t(n, i.fromCache, e, r);
    }, t;
}(), ti = /** @class */ function() {
    function t(t, n) {
        var i = this;
        this.previousValue = t, n && (n.Nr = function(t) {
            return i.Or(t);
        }, this.Dr = function(t) {
            return n.Vr(t);
        });
    }
    return t.prototype.Or = function(t) {
        return this.previousValue = Math.max(t, this.previousValue), this.previousValue;
    }, t.prototype.next = function() {
        var t = ++this.previousValue;
        return this.Dr && this.Dr(t), t;
    }, t.Pr = -1, t;
}(), ni = function() {
    var t = this;
    this.promise = new Promise((function(n, i) {
        t.resolve = n, t.reject = i;
    }));
}, ii = /** @class */ function() {
    function t(t, n, i, e, r) {
        this.kr = t, this.Fr = n, this.jr = i, this.op = e, this.Ur = r, this.Lr = new ni, 
        this.then = this.Lr.promise.then.bind(this.Lr.promise), this.catch = this.Lr.promise.catch.bind(this.Lr.promise), 
        // It's normal for the deferred promise to be canceled (due to cancellation)
        // and so we attach a dummy catch callback to avoid
        // 'UnhandledPromiseRejectionWarning' log spam.
        this.Lr.promise.catch((function(t) {}));
    }
    /**
     * Creates and returns a DelayedOperation that has been scheduled to be
     * executed on the provided asyncQueue after the provided delayMs.
     *
     * @param asyncQueue The queue to schedule the operation on.
     * @param id A Timer ID identifying the type of operation this is.
     * @param delayMs The delay (ms) before the operation should be scheduled.
     * @param op The operation to run.
     * @param removalCallback A callback to be called synchronously once the
     *   operation is executed or canceled, notifying the AsyncQueue to remove it
     *   from its delayedOperations list.
     *   PORTING NOTE: This exists to prevent making removeDelayedOperation() and
     *   the DelayedOperation class public.
     */    return t.Mr = function(n, i, e, r, s) {
        var u = new t(n, i, Date.now() + e, r, s);
        return u.start(e), u;
    }, 
    /**
     * Starts the timer. This is called immediately after construction by
     * createAndSchedule().
     */
    t.prototype.start = function(t) {
        var n = this;
        this.qr = setTimeout((function() {
            return n.xr();
        }), t);
    }, 
    /**
     * Queues the operation to run immediately (if it hasn't already been run or
     * canceled).
     */
    t.prototype.Br = function() {
        return this.xr();
    }, 
    /**
     * Cancels the operation if it hasn't already been executed or canceled. The
     * promise will be rejected.
     *
     * As long as the operation has not yet been run, calling cancel() provides a
     * guarantee that the operation will not be run.
     */
    t.prototype.cancel = function(t) {
        null !== this.qr && (this.clearTimeout(), this.Lr.reject(new E(T.CANCELLED, "Operation cancelled" + (t ? ": " + t : ""))));
    }, t.prototype.xr = function() {
        var t = this;
        this.kr.Qr((function() {
            return null !== t.qr ? (t.clearTimeout(), t.op().then((function(n) {
                return t.Lr.resolve(n);
            }))) : Promise.resolve();
        }));
    }, t.prototype.clearTimeout = function() {
        null !== this.qr && (this.Ur(this), clearTimeout(this.qr), this.qr = null);
    }, t;
}(), ei = /** @class */ function() {
    function t() {
        // The last promise in the queue.
        this.Wr = Promise.resolve(), 
        // Is this AsyncQueue being shut down? Once it is set to true, it will not
        // be changed again.
        this.Gr = !1, 
        // Operations scheduled to be queued in the future. Operations are
        // automatically removed after they are run or canceled.
        this.zr = [], 
        // visible for testing
        this.Jr = null, 
        // Flag set while there's an outstanding AsyncQueue operation, used for
        // assertion sanity-checks.
        this.Hr = !1, 
        // List of TimerIds to fast-forward delays for.
        this.Yr = [];
    }
    return Object.defineProperty(t.prototype, "Kr", {
        // Is this AsyncQueue being shut down? If true, this instance will not enqueue
        // any new operations, Promises from enqueue requests will not resolve.
        get: function() {
            return this.Gr;
        },
        enumerable: !0,
        configurable: !0
    }), 
    /**
     * Adds a new operation to the queue without waiting for it to complete (i.e.
     * we ignore the Promise result).
     */
    t.prototype.Qr = function(t) {
        // eslint-disable-next-line @typescript-eslint/no-floating-promises
        this.enqueue(t);
    }, 
    /**
     * Regardless if the queue has initialized shutdown, adds a new operation to the
     * queue without waiting for it to complete (i.e. we ignore the Promise result).
     */
    t.prototype.Xr = function(t) {
        this.$r(), 
        // eslint-disable-next-line @typescript-eslint/no-floating-promises
        this.Zr(t);
    }, 
    /**
     * Regardless if the queue has initialized shutdown, adds a new operation to the
     * queue.
     */
    t.prototype.ts = function(t) {
        return this.$r(), this.Zr(t);
    }, 
    /**
     * Adds a new operation to the queue and initialize the shut down of this queue.
     * Returns a promise that will be resolved when the promise returned by the new
     * operation is (with its value).
     * Once this method is called, the only possible way to request running an operation
     * is through `enqueueAndForgetEvenAfterShutdown`.
     */
    t.prototype.ns = function(t) {
        return r(this, void 0, void 0, (function() {
            return s(this, (function(n) {
                switch (n.label) {
                  case 0:
                    return this.$r(), this.Gr ? [ 3 /*break*/ , 2 ] : (this.Gr = !0, [ 4 /*yield*/ , this.ts(t) ]);

                  case 1:
                    n.sent(), n.label = 2;

                  case 2:
                    return [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    /**
     * Adds a new operation to the queue. Returns a promise that will be resolved
     * when the promise returned by the new operation is (with its value).
     */
    t.prototype.enqueue = function(t) {
        return this.$r(), this.Gr ? new Promise((function(t) {})) : this.Zr(t);
    }, t.prototype.Zr = function(t) {
        var n = this, i = this.Wr.then((function() {
            return n.Hr = !0, t().catch((function(t) {
                n.Jr = t, n.Hr = !1;
                var i = t.stack || t.message || "";
                // Re-throw the error so that this.tail becomes a rejected Promise and
                // all further attempts to chain (via .then) will just short-circuit
                // and return the rejected Promise.
                throw xn("INTERNAL UNHANDLED ERROR: ", i), 
                // Escape the promise chain and throw the error globally so that
                // e.g. any global crash reporting library detects and reports it.
                // (but not for simulated errors in our tests since this breaks mocha)
                i.indexOf("Firestore Test Simulated Error") < 0 && setTimeout((function() {
                    throw t;
                }), 0), t;
            })).then((function(t) {
                return n.Hr = !1, t;
            }));
        }));
        return this.Wr = i, i;
    }, 
    /**
     * Schedules an operation to be queued on the AsyncQueue once the specified
     * `delayMs` has elapsed. The returned CancelablePromise can be used to cancel
     * the operation prior to its running.
     */
    t.prototype.es = function(t, n, i) {
        var e = this;
        this.$r(), Wn(n >= 0, "Attempted to schedule an operation with a negative delay of " + n), 
        // Fast-forward delays for timerIds that have been overriden.
        this.Yr.indexOf(t) > -1 && (n = 0);
        var r = ii.Mr(this, t, n, i, (function(t) {
            return e.rs(t);
        }));
        return this.zr.push(r), r;
    }, t.prototype.$r = function() {
        this.Jr && Qn("AsyncQueue is already failed: " + (this.Jr.stack || this.Jr.message));
    }, 
    /**
     * Verifies there's an operation currently in-progress on the AsyncQueue.
     * Unfortunately we can't verify that the running code is in the promise chain
     * of that operation, so this isn't a foolproof check, but it should be enough
     * to catch some bugs.
     */
    t.prototype.ss = function() {
        Wn(this.Hr, "verifyOpInProgress() called when no op in progress on this queue.");
    }, 
    /**
     * Waits until all currently queued tasks are finished executing. Delayed
     * operations are not run.
     */
    t.prototype.us = function() {
        return r(this, void 0, void 0, (function() {
            var t;
            return s(this, (function(n) {
                switch (n.label) {
                  case 0:
                    return [ 4 /*yield*/ , t = this.Wr ];

                  case 1:
                    n.sent(), n.label = 2;

                  case 2:
                    if (t !== this.Wr) return [ 3 /*break*/ , 0 ];
                    n.label = 3;

                  case 3:
                    return [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    /**
     * For Tests: Determine if a delayed operation with a particular TimerId
     * exists.
     */
    t.prototype.os = function(t) {
        for (var n = 0, i = this.zr; n < i.length; n++) {
            if (i[n].Fr === t) return !0;
        }
        return !1;
    }, 
    /**
     * For Tests: Runs some or all delayed operations early.
     *
     * @param lastTimerId Delayed operations up to and including this TimerId will
     *  be drained. Throws if no such operation exists. Pass TimerId.All to run
     *  all delayed operations.
     * @returns a Promise that resolves once all operations have been run.
     */
    t.prototype.hs = function(t) {
        var n = this;
        // Note that draining may generate more delayed ops, so we do that first.
                return this.us().then((function() {
            Wn("all" /* All */ === t || n.os(t), "Attempted to drain to missing operation " + t), 
            // Run ops in the same order they'd run if they ran naturally.
            n.zr.sort((function(t, n) {
                return t.jr - n.jr;
            }));
            for (var i = 0, e = n.zr; i < e.length; i++) {
                var r = e[i];
                if (r.Br(), "all" /* All */ !== t && r.Fr === t) break;
            }
            return n.us();
        }));
    }, 
    /**
     * For Tests: Skip all subsequent delays for a timer id.
     */
    t.prototype.as = function(t) {
        this.Yr.push(t);
    }, 
    /** Called once a DelayedOperation is run or canceled. */
    t.prototype.rs = function(t) {
        // NOTE: indexOf / slice are O(n), but delayedOperations is expected to be small.
        var n = this.zr.indexOf(t);
        Wn(n >= 0, "Delayed operation not found."), this.zr.splice(n, 1);
    }, t;
}(), ri = /** @class */ function() {
    function t(t) {
        var n = this;
        // NOTE: next/catchCallback will always point to our own wrapper functions,
        // not the user's raw next() or catch() callbacks.
                this.cs = null, this.fs = null, 
        // When the operation resolves, we'll set result or error and mark isDone.
        this.result = void 0, this.error = void 0, this.ls = !1, 
        // Set to true when .then() or .catch() are called and prevents additional
        // chaining.
        this.ds = !1, t((function(t) {
            n.ls = !0, n.result = t, n.cs && 
            // value should be defined unless T is Void, but we can't express
            // that in the type system.
            n.cs(t);
        }), (function(t) {
            n.ls = !0, n.error = t, n.fs && n.fs(t);
        }));
    }
    return t.prototype.catch = function(t) {
        return this.next(void 0, t);
    }, t.prototype.next = function(n, i) {
        var e = this;
        return this.ds && Qn("Called next() or catch() twice for PersistencePromise"), this.ds = !0, 
        this.ls ? this.error ? this.vs(i, this.error) : this.ws(n, this.result) : new t((function(t, r) {
            e.cs = function(i) {
                e.ws(n, i).next(t, r);
            }, e.fs = function(n) {
                e.vs(i, n).next(t, r);
            };
        }));
    }, t.prototype.ms = function() {
        var t = this;
        return new Promise((function(n, i) {
            t.next(n, i);
        }));
    }, t.prototype.ps = function(n) {
        try {
            var i = n();
            return i instanceof t ? i : t.resolve(i);
        } catch (n) {
            return t.reject(n);
        }
    }, t.prototype.ws = function(n, i) {
        return n ? this.ps((function() {
            return n(i);
        })) : t.resolve(i);
    }, t.prototype.vs = function(n, i) {
        return n ? this.ps((function() {
            return n(i);
        })) : t.reject(i);
    }, t.resolve = function(n) {
        return new t((function(t, i) {
            t(n);
        }));
    }, t.reject = function(n) {
        return new t((function(t, i) {
            i(n);
        }));
    }, t.gs = function(
    // Accept all Promise types in waitFor().
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    n) {
        return new t((function(t, i) {
            var e = 0, r = 0, s = !1;
            n.forEach((function(n) {
                ++e, n.next((function() {
                    ++r, s && r === e && t();
                }), (function(t) {
                    return i(t);
                }));
            })), s = !0, r === e && t();
        }));
    }, 
    /**
     * Given an array of predicate functions that asynchronously evaluate to a
     * boolean, implements a short-circuiting `or` between the results. Predicates
     * will be evaluated until one of them returns `true`, then stop. The final
     * result will be whether any of them returned `true`.
     */
    t.bs = function(n) {
        for (var i = t.resolve(!1), e = function(n) {
            i = i.next((function(i) {
                return i ? t.resolve(i) : n();
            }));
        }, r = 0, s = n; r < s.length; r++) {
            e(s[r]);
        }
        return i;
    }, t.forEach = function(t, n) {
        var i = this, e = [];
        return t.forEach((function(t, r) {
            e.push(n.call(i, t, r));
        })), this.gs(e);
    }, t;
}();

/** The default database name for a project. */
/**
 * @license
 * Copyright 2018 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function si(t, n) {
    var i = t[0], e = t[1], r = n[0], s = n[1], u = zn(i, r);
    return 0 === u ? zn(e, s) : u;
}

/**
 * Used to calculate the nth sequence number. Keeps a rolling buffer of the
 * lowest n values passed to `addElement`, and finally reports the largest of
 * them in `maxValue`.
 */ /** @class */ !function() {
    function t(t) {
        this.ys = t, this.buffer = new x(si), this._s = 0;
    }
    t.prototype.Ts = function() {
        return ++this._s;
    }, t.prototype.Es = function(t) {
        var n = [ t, this.Ts() ];
        if (this.buffer.size < this.ys) this.buffer = this.buffer.add(n); else {
            var i = this.buffer.last();
            si(n, i) < 0 && (this.buffer = this.buffer.delete(i).add(n));
        }
    }, Object.defineProperty(t.prototype, "maxValue", {
        get: function() {
            // Guaranteed to be non-empty. If we decide we are not collecting any
            // sequence numbers, nthSequenceNumber below short-circuits. If we have
            // decided that we are collecting n sequence numbers, it's because n is some
            // percentage of the existing sequence numbers. That means we should never
            // be in a situation where we are collecting sequence numbers but don't
            // actually have any.
            return this.buffer.last()[0];
        },
        enumerable: !0,
        configurable: !0
    });
}();

var ui = /** @class */ function() {
    function t(
    // When we attempt to collect, we will only do so if the cache size is greater than this
    // threshold. Passing `COLLECTION_DISABLED` here will cause collection to always be skipped.
    t, 
    // The percentage of sequence numbers that we will attempt to collect
    n, 
    // A cap on the total number of sequence numbers that will be collected. This prevents
    // us from collecting a huge number of sequence numbers if the cache has grown very large.
    i) {
        this.Ss = t, this.As = n, this.Is = i;
    }
    return t.Cs = function(n) {
        return new t(n, t.Rs, t.Ns);
    }, t.Os = -1, t.Ds = 1048576, t.Vs = 41943040, t.Rs = 10, t.Ns = 1e3, t.Ps = new t(t.Vs, t.Rs, t.Ns), 
    t.DISABLED = new t(t.Os, 0, 0), t;
}(), oi = (/** @class */ /** @class */ function() {
    function t(t, n) {
        this.ks = t, this.kr = n, this.Fs = !1, this.js = null;
    }
    t.prototype.start = function(t) {
        Wn(null === this.js, "Cannot start an already started LruScheduler"), this.ks.params.Ss !== ui.Os && this.Us(t);
    }, t.prototype.stop = function() {
        this.js && (this.js.cancel(), this.js = null);
    }, Object.defineProperty(t.prototype, "Ls", {
        get: function() {
            return null !== this.js;
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.Us = function(t) {
        var n = this;
        Wn(null === this.js, "Cannot schedule GC while a task is pending");
        var i = this.Fs ? 3e5 : 6e4;
        qn("LruGarbageCollector", "Garbage collection scheduled in " + i + "ms"), this.js = this.kr.es("lru_garbage_collection" /* LruGarbageCollection */ , i, (function() {
            return n.js = null, n.Fs = !0, t.Ms(n.ks).then((function() {
                return n.Us(t);
            })).catch(fi);
        }));
    };
}(), function() {
    function t() {
        // A set of outstanding references to a document sorted by key.
        this.qs = new x(hi.gn), 
        // A set of outstanding references to a document sorted by target id.
        this.xs = new x(hi.Bs);
    }
    /** Returns true if the reference set contains no references. */    return t.prototype.M = function() {
        return this.qs.M();
    }, 
    /** Adds a reference to the given document key for the given ID. */
    t.prototype.Qs = function(t, n) {
        var i = new hi(t, n);
        this.qs = this.qs.add(i), this.xs = this.xs.add(i);
    }, 
    /** Add references to the given document keys for the given ID. */
    t.prototype.Ws = function(t, n) {
        var i = this;
        t.forEach((function(t) {
            return i.Qs(t, n);
        }));
    }, 
    /**
     * Removes a reference to the given document key for the given
     * ID.
     */
    t.prototype.Gs = function(t, n) {
        this.zs(new hi(t, n));
    }, t.prototype.Js = function(t, n) {
        var i = this;
        t.forEach((function(t) {
            return i.Gs(t, n);
        }));
    }, 
    /**
     * Clears all references with a given ID. Calls removeRef() for each key
     * removed.
     */
    t.prototype.Hs = function(t) {
        var n = this, i = j.EMPTY, e = new hi(i, t), r = new hi(i, t + 1), s = [];
        return this.xs.Vt([ e, r ], (function(t) {
            n.zs(t), s.push(t.key);
        })), s;
    }, t.prototype.Ys = function() {
        var t = this;
        this.qs.forEach((function(n) {
            return t.zs(n);
        }));
    }, t.prototype.zs = function(t) {
        this.qs = this.qs.delete(t), this.xs = this.xs.delete(t);
    }, t.prototype.Ks = function(t) {
        var n = j.EMPTY, i = new hi(n, t), e = new hi(n, t + 1), r = vn();
        return this.xs.Vt([ i, e ], (function(t) {
            r = r.add(t.key);
        })), r;
    }, t.prototype.Xs = function(t) {
        var n = new hi(t, 0), i = this.qs.kt(n);
        return null !== i && t.isEqual(i.key);
    }, t;
}()), hi = /** @class */ function() {
    function t(t, n) {
        this.key = t, this.$s = n;
    }
    /** Compare by key then by ID */    return t.gn = function(t, n) {
        return j.F(t.key, n.key) || zn(t.$s, n.$s);
    }, 
    /** Compare by ID then by key */
    t.Bs = function(t, n) {
        return zn(t.$s, n.$s) || j.F(t.key, n.key);
    }, t;
}(), ai = /** @class */ function() {
    function t() {
        // A mapping of document key to the new cache entry that should be written (or null if any
        // existing cache entry should be removed).
        this.Zs = new $n((function(t) {
            return t.toString();
        })), this.tu = !1;
    }
    return Object.defineProperty(t.prototype, "readTime", {
        get: function() {
            return Wn(void 0 !== this.nu, "Read time is not set. All removeEntry() calls must include a readTime if `trackRemovals` is used."), 
            this.nu;
        },
        set: function(t) {
            // Right now (for simplicity) we just track a single readTime for all the
            // added entries since we expect them to all be the same, but we could
            // rework to store per-entry readTimes if necessary.
            Wn(void 0 === this.nu || this.nu.isEqual(t), "All changes in a RemoteDocumentChangeBuffer must have the same read time"), 
            this.nu = t;
        },
        enumerable: !0,
        configurable: !0
    }), 
    /**
     * Buffers a `RemoteDocumentCache.addEntry()` call.
     *
     * You can only modify documents that have already been retrieved via
     * `getEntry()/getEntries()` (enforced via IndexedDbs `apply()`).
     */
    t.prototype.iu = function(t, n) {
        this.eu(), this.readTime = n, this.Zs.set(t.key, t);
    }, 
    /**
     * Buffers a `RemoteDocumentCache.removeEntry()` call.
     *
     * You can only remove documents that have already been retrieved via
     * `getEntry()/getEntries()` (enforced via IndexedDbs `apply()`).
     */
    t.prototype.ru = function(t, n) {
        this.eu(), n && (this.readTime = n), this.Zs.set(t, null);
    }, 
    /**
     * Looks up an entry in the cache. The buffered changes will first be checked,
     * and if no buffered change applies, this will forward to
     * `RemoteDocumentCache.getEntry()`.
     *
     * @param transaction The transaction in which to perform any persistence
     *     operations.
     * @param documentKey The key of the entry to look up.
     * @return The cached Document or NoDocument entry, or null if we have nothing
     * cached.
     */
    t.prototype.su = function(t, n) {
        this.eu();
        var i = this.Zs.get(n);
        return void 0 !== i ? ri.resolve(i) : this.uu(t, n);
    }, 
    /**
     * Looks up several entries in the cache, forwarding to
     * `RemoteDocumentCache.getEntry()`.
     *
     * @param transaction The transaction in which to perform any persistence
     *     operations.
     * @param documentKeys The keys of the entries to look up.
     * @return A map of cached `Document`s or `NoDocument`s, indexed by key. If an
     *     entry cannot be found, the corresponding key will be mapped to a null
     *     value.
     */
    t.prototype.getEntries = function(t, n) {
        return this.ou(t, n);
    }, 
    /**
     * Applies buffered changes to the underlying RemoteDocumentCache, using
     * the provided transaction.
     */
    t.prototype.apply = function(t) {
        return this.eu(), this.tu = !0, this.hu(t);
    }, 
    /** Helper to assert this.changes is not null  */
    t.prototype.eu = function() {
        Wn(!this.tu, "Changes have already been applied.");
    }, t;
}(), ci = /** @class */ function() {
    function t(
    /** Manages our in-memory or durable persistence. */
    t, n, i) {
        this.persistence = t, this.au = n, 
        /**
         * The set of document references maintained by any local views.
         */
        this.cu = new oi, 
        /**
         * Maps a targetID to data about its target.
         *
         * PORTING NOTE: We are using an immutable data structure on Web to make re-runs
         * of `applyRemoteEvent()` idempotent.
         */
        this.fu = new U(zn), 
        /** Maps a target to its targetID. */
        // TODO(wuandy): Evaluate if TargetId can be part of Target.
        this.lu = new $n((function(t) {
            return t.canonicalId();
        })), 
        /**
         * The read time of the last entry processed by `getNewDocumentChanges()`.
         *
         * PORTING NOTE: This is only used for multi-tab synchronization.
         */
        this.du = D.MIN, Wn(t.Ls, "LocalStore was passed an unstarted persistence implementation"), 
        this.persistence.wu.vu(this.cu), this.mu = t.pu(i), this.gu = t.bu(), this.yu = t._u(), 
        this.Tu = new Ei(this.gu, this.mu, this.persistence.Eu()), this.au.Su(this.Tu);
    }
    /** Starts the LocalStore. */    return t.prototype.start = function() {
        return this.Au();
    }, 
    /**
     * Tells the LocalStore that the currently authenticated user has changed.
     *
     * In response the local store switches the mutation queue to the new user and
     * returns any resulting document changes.
     */
    // PORTING NOTE: Android and iOS only return the documents affected by the
    // change.
    t.prototype.Iu = function(t) {
        return r(this, void 0, void 0, (function() {
            var n, i, e, r = this;
            return s(this, (function(s) {
                switch (s.label) {
                  case 0:
                    return n = this.mu, i = this.Tu, [ 4 /*yield*/ , this.persistence.runTransaction("Handle user change", "readonly", (function(e) {
                        // Swap out the mutation queue, grabbing the pending mutation batches
                        // before and after.
                        var s;
                        return r.mu.Cu(e).next((function(u) {
                            return s = u, n = r.persistence.pu(t), 
                            // Recreate our LocalDocumentsView using the new
                            // MutationQueue.
                            i = new Ei(r.gu, n, r.persistence.Eu()), n.Cu(e);
                        })).next((function(t) {
                            for (var n = [], r = [], u = vn(), o = 0, h = s; o < h.length; o++) {
                                var a = h[o];
                                n.push(a.batchId);
                                for (var c = 0, f = a.mutations; c < f.length; c++) {
                                    var l = f[c];
                                    u = u.add(l.key);
                                }
                            }
                            for (var d = 0, v = t; d < v.length; d++) {
                                a = v[d];
                                r.push(a.batchId);
                                for (var w = 0, m = a.mutations; w < m.length; w++) {
                                    l = m[w];
                                    u = u.add(l.key);
                                }
                            }
                            // Return the set of all (potentially) changed documents and the list
                            // of mutation batch IDs that were affected by change.
                                                        return i.Ru(e, u).next((function(t) {
                                return {
                                    Nu: t,
                                    Ou: n,
                                    Du: r
                                };
                            }));
                        }));
                    })) ];

                  case 1:
                    return e = s.sent(), this.mu = n, this.Tu = i, this.au.Su(this.Tu), [ 2 /*return*/ , e ];
                }
            }));
        }));
    }, 
    /* Accept locally generated Mutations and commit them to storage. */
    t.prototype.Vu = function(t) {
        var n, i = this, e = O.now(), r = t.reduce((function(t, n) {
            return t.add(n.key);
        }), vn());
        return this.persistence.runTransaction("Locally write mutations", "readwrite", (function(s) {
            // Load and apply all existing mutations. This lets us compute the
            // current base state for all non-idempotent transforms before applying
            // any additional user-provided writes.
            return i.Tu.Ru(s, r).next((function(r) {
                n = r;
                for (
                // For non-idempotent mutations (such as `FieldValue.increment()`),
                // we record the base state in a separate patch mutation. This is
                // later used to guarantee consistent values and prevents flicker
                // even if the backend sends us an update that already includes our
                // transform.
                var u = [], o = 0, h = t; o < h.length; o++) {
                    var a = h[o], c = a.in(n.get(a.key));
                    null != c && 
                    // NOTE: The base state should only be applied if there's some
                    // existing document to override, so use a Precondition of
                    // exists=true
                    u.push(new Ot(a.key, c, c.en(), Ct.exists(!0)));
                }
                return i.mu.Pu(s, e, u, t);
            }));
        })).then((function(t) {
            var i = t._r(n);
            return {
                batchId: t.batchId,
                Zs: i
            };
        }));
    }, 
    /** Returns the local view of the documents affected by a mutation batch. */
    // PORTING NOTE: Multi-tab only.
    t.prototype.ku = function(t) {
        var n = this;
        return this.persistence.runTransaction("Lookup mutation documents", "readonly", (function(i) {
            return n.mu.Fu(i, t).next((function(t) {
                return t ? n.Tu.Ru(i, t) : ri.resolve(null);
            }));
        }));
    }, 
    /**
     * Acknowledge the given batch.
     *
     * On the happy path when a batch is acknowledged, the local store will
     *
     *  + remove the batch from the mutation queue;
     *  + apply the changes to the remote document cache;
     *  + recalculate the latency compensated view implied by those changes (there
     *    may be mutations in the queue that affect the documents but haven't been
     *    acknowledged yet); and
     *  + give the changed documents back the sync engine
     *
     * @returns The resulting (modified) documents.
     */
    t.prototype.ju = function(t) {
        var n = this;
        return this.persistence.runTransaction("Acknowledge batch", "readwrite-primary", (function(i) {
            var e = t.batch.keys(), r = n.gu.Uu({
                Lu: !0
            });
            return n.mu.ju(i, t.batch, t.streamToken).next((function() {
                return n.Mu(i, t, r);
            })).next((function() {
                return r.apply(i);
            })).next((function() {
                return n.mu.qu(i);
            })).next((function() {
                return n.Tu.Ru(i, e);
            }));
        }));
    }, 
    /**
     * Remove mutations from the MutationQueue for the specified batch;
     * LocalDocuments will be recalculated.
     *
     * @returns The resulting modified documents.
     */
    t.prototype.xu = function(t) {
        var n = this;
        return this.persistence.runTransaction("Reject batch", "readwrite-primary", (function(i) {
            var e;
            return n.mu.Bu(i, t).next((function(t) {
                return Wn(null !== t, "Attempt to reject nonexistent batch!"), e = t.keys(), n.mu.Qu(i, t);
            })).next((function() {
                return n.mu.qu(i);
            })).next((function() {
                return n.Tu.Ru(i, e);
            }));
        }));
    }, 
    /**
     * Returns the largest (latest) batch id in mutation queue that is pending server response.
     * Returns `BATCHID_UNKNOWN` if the queue is empty.
     */
    t.prototype.Wu = function() {
        var t = this;
        return this.persistence.runTransaction("Get highest unacknowledged batch id", "readonly", (function(n) {
            return t.mu.Wu(n);
        }));
    }, 
    /** Returns the last recorded stream token for the current user. */
    t.prototype.Gu = function() {
        var t = this;
        return this.persistence.runTransaction("Get last stream token", "readonly", (function(n) {
            return t.mu.Gu(n);
        }));
    }, 
    /**
     * Sets the stream token for the current user without acknowledging any
     * mutation batch. This is usually only useful after a stream handshake or in
     * response to an error that requires clearing the stream token.
     */
    t.prototype.zu = function(t) {
        var n = this;
        return this.persistence.runTransaction("Set last stream token", "readwrite-primary", (function(i) {
            return n.mu.zu(i, t);
        }));
    }, 
    /**
     * Returns the last consistent snapshot processed (used by the RemoteStore to
     * determine whether to buffer incoming snapshots from the backend).
     */
    t.prototype.Ju = function() {
        var t = this;
        return this.persistence.runTransaction("Get last remote snapshot version", "readonly", (function(n) {
            return t.yu.Ju(n);
        }));
    }, 
    /**
     * Update the "ground-state" (remote) documents. We assume that the remote
     * event reflects any write batches that have been acknowledged or rejected
     * (i.e. we do not re-apply local mutations to updates from this event).
     *
     * LocalDocuments are re-calculated if there are remaining mutations in the
     * queue.
     */
    t.prototype.Hu = function(n) {
        var i = this, e = n.ni, r = this.fu;
        return this.persistence.runTransaction("Apply remote event", "readwrite-primary", (function(s) {
            var u = i.gu.Uu({
                Lu: !0
            });
            // Reset newTargetDataByTargetMap in case this transaction gets re-run.
                        r = i.fu;
            var o = [];
            n.wi.forEach((function(n, u) {
                var h = r.get(u);
                if (h) {
                    // Only update the remote keys if the target is still active. This
                    // ensures that we can persist the updated target data along with
                    // the updated assignment.
                    o.push(i.yu.Yu(s, n.Si, u).next((function() {
                        return i.yu.Ku(s, n.Ti, u);
                    })));
                    var a = n.resumeToken;
                    // Update the resume token if the change includes one.
                                        if (a.Mt() > 0) {
                        var c = h.ei(a, e).ii(s.Xu);
                        r = r.st(u, c), 
                        // Update the target data if there are target changes (or if
                        // sufficient time has passed since the last update).
                        t.$u(h, c, n) && o.push(i.yu.Zu(s, c));
                    }
                }
            }));
            var h = on(), a = vn();
            // HACK: The only reason we allow a null snapshot version is so that we
            // can synthesize remote events when we get permission denied errors while
            // trying to resolve the state of a locally cached document that is in
            // limbo.
            if (n.pi.forEach((function(t, n) {
                a = a.add(t);
            })), 
            // Each loop iteration only affects its "own" doc, so it's safe to get all the remote
            // documents in advance in a single call.
            o.push(u.getEntries(s, a).next((function(t) {
                n.pi.forEach((function(r, a) {
                    var c = t.get(r);
                    // Note: The order of the steps below is important, since we want
                    // to ensure that rejected limbo resolutions (which fabricate
                    // NoDocuments with SnapshotVersion.MIN) never add documents to
                    // cache.
                                        a instanceof Lt && a.version.isEqual(D.MIN) ? (
                    // NoDocuments with SnapshotVersion.MIN are used in manufactured
                    // events. We remove these documents from cache since we lost
                    // access.
                    u.ru(r, e), h = h.st(r, a)) : null == c || a.version.V(c.version) > 0 || 0 === a.version.V(c.version) && c.hasPendingWrites ? (Wn(!D.MIN.isEqual(e), "Cannot add a document when the remote version is zero"), 
                    u.iu(a, e), h = h.st(r, a)) : qn("LocalStore", "Ignoring outdated watch update for ", r, ". Current version:", c.version, " Watch version:", a.version), 
                    n.gi.has(r) && o.push(i.persistence.wu.to(s, r));
                }));
            }))), !e.isEqual(D.MIN)) {
                var c = i.yu.Ju(s).next((function(t) {
                    return Wn(e.V(t) >= 0, "Watch stream reverted to previous snapshot?? " + e + " < " + t), 
                    i.yu.no(s, s.Xu, e);
                }));
                o.push(c);
            }
            return ri.gs(o).next((function() {
                return u.apply(s);
            })).next((function() {
                return i.Tu.io(s, h);
            }));
        })).then((function(t) {
            return i.fu = r, t;
        }));
    }, 
    /**
     * Returns true if the newTargetData should be persisted during an update of
     * an active target. TargetData should always be persisted when a target is
     * being released and should not call this function.
     *
     * While the target is active, TargetData updates can be omitted when nothing
     * about the target has changed except metadata like the resume token or
     * snapshot version. Occasionally it's worth the extra write to prevent these
     * values from getting too stale after a crash, but this doesn't have to be
     * too frequent.
     */
    t.$u = function(t, n, i) {
        // Always persist target data if we don't already have a resume token.
        return Wn(n.resumeToken.Mt() > 0, "Attempted to persist target data with no resume token"), 
        0 === t.resumeToken.Mt() || (n.ni.P() - t.ni.P() >= this.eo || i.Ti.size + i.Ei.size + i.Si.size > 0);
        // Don't allow resume token changes to be buffered indefinitely. This
        // allows us to be reasonably up-to-date after a crash and avoids needing
        // to loop over all active queries on shutdown. Especially in the browser
        // we may not get time to do anything interesting while the current tab is
        // closing.
        }, 
    /**
     * Notify local store of the changed views to locally pin documents.
     */
    t.prototype.ro = function(t) {
        for (var n = this, i = 0, e = t; i < e.length; i++) {
            var r = e[i], s = r.targetId;
            if (this.cu.Ws(r.Ir, s), this.cu.Js(r.Cr, s), !r.fromCache) {
                var u = this.fu.get(s);
                Wn(null !== u, "Can't set limbo-free snapshot version for unknown target: " + s);
                // Advance the last limbo free snapshot version
                var o = u.ni, h = u.ri(o);
                this.fu = this.fu.st(s, h);
            }
        }
        return this.persistence.runTransaction("notifyLocalViewChanges", "readwrite", (function(i) {
            return ri.forEach(t, (function(t) {
                return ri.forEach(t.Cr, (function(t) {
                    return n.persistence.wu.Gs(i, t);
                }));
            }));
        }));
    }, 
    /**
     * Gets the mutation batch after the passed in batchId in the mutation queue
     * or null if empty.
     * @param afterBatchId If provided, the batch to search after.
     * @returns The next mutation or null if there wasn't one.
     */
    t.prototype.so = function(t) {
        var n = this;
        return this.persistence.runTransaction("Get next mutation batch", "readonly", (function(i) {
            return void 0 === t && (t = -1), n.mu.uo(i, t);
        }));
    }, 
    /**
     * Read the current value of a Document with a given key or null if not
     * found - used for testing.
     */
    t.prototype.oo = function(t) {
        var n = this;
        return this.persistence.runTransaction("read document", "readonly", (function(i) {
            return n.Tu.ho(i, t);
        }));
    }, 
    /**
     * Assigns the given target an internal ID so that its results can be pinned so
     * they don't get GC'd. A target must be allocated in the local store before
     * the store can be used to manage its view.
     *
     * Allocating an already allocated `Target` will return the existing `TargetData`
     * for that `Target`.
     */
    t.prototype.ao = function(t) {
        var n = this;
        return this.persistence.runTransaction("Allocate target", "readwrite", (function(i) {
            var e;
            return n.yu.co(i, t).next((function(r) {
                return r ? (
                // This target has been listened to previously, so reuse the
                // previous targetID.
                // TODO(mcg): freshen last accessed date?
                e = r, ri.resolve(e)) : n.yu.fo(i).next((function(r) {
                    return e = new nn(t, r, 0 /* Listen */ , i.Xu), n.yu.lo(i, e).next((function() {
                        return e;
                    }));
                }));
            }));
        })).then((function(i) {
            return null === n.fu.get(i.targetId) && (n.fu = n.fu.st(i.targetId, i), n.lu.set(t, i.targetId)), 
            i;
        }));
    }, 
    /**
     * Returns the TargetData as seen by the LocalStore, including updates that may
     * have not yet been persisted to the TargetCache.
     */
    // Visible for testing.
    t.prototype.co = function(t, n) {
        var i = this.lu.get(n);
        return void 0 !== i ? ri.resolve(this.fu.get(i)) : this.yu.co(t, n);
    }, 
    /**
     * Unpin all the documents associated with the given target. If
     * `keepPersistedTargetData` is set to false and Eager GC enabled, the method
     * directly removes the associated target data from the target cache.
     *
     * Releasing a non-existing `Target` is a no-op.
     */
    // PORTING NOTE: `keepPersistedTargetData` is multi-tab only.
    t.prototype.do = function(t, n) {
        var i = this, e = this.fu.get(t);
        Wn(null !== e, "Tried to release nonexistent target: " + t);
        var r = n ? "readwrite" : "readwrite-primary";
        return this.persistence.runTransaction("Release target", r, (function(r) {
            // References for documents sent via Watch are automatically removed
            // when we delete a target's data from the reference delegate.
            // Since this does not remove references for locally mutated documents,
            // we have to remove the target associations for these documents
            // manually.
            // This operation needs to be run inside the transaction since EagerGC
            // uses the local view references during the transaction's commit.
            // Fortunately, the operation is safe to be re-run in case the
            // transaction fails since there are no side effects if the target has
            // already been removed.
            var s = i.cu.Hs(t);
            return n ? ri.resolve() : ri.forEach(s, (function(t) {
                return i.persistence.wu.Gs(r, t);
            })).next((function() {
                i.persistence.wu.removeTarget(r, e);
            }));
        })).then((function() {
            i.fu = i.fu.remove(t), i.lu.delete(e.target);
        }));
    }, 
    /**
     * Runs the specified query against the local store and returns the results,
     * potentially taking advantage of query data from previous executions (such
     * as the set of remote keys).
     *
     * @param usePreviousResults Whether results from previous executions can
     * be used to optimize this query execution.
     */
    t.prototype.vo = function(t, n) {
        var i = this, e = D.MIN, r = vn();
        return this.persistence.runTransaction("Execute query", "readonly", (function(s) {
            return i.co(s, t.xn()).next((function(t) {
                if (t) return e = t.lastLimboFreeSnapshotVersion, i.yu.wo(s, t.targetId).next((function(t) {
                    r = t;
                }));
            })).next((function() {
                return i.au.mo(s, t, n ? e : D.MIN, n ? r : vn());
            })).next((function(t) {
                return {
                    documents: t,
                    po: r
                };
            }));
        }));
    }, 
    /**
     * Returns the keys of the documents that are associated with the given
     * target id in the remote table.
     */
    t.prototype.bo = function(t) {
        var n = this;
        return this.persistence.runTransaction("Remote document keys", "readonly", (function(i) {
            return n.yu.wo(i, t);
        }));
    }, 
    // PORTING NOTE: Multi-tab only.
    t.prototype.yo = function() {
        return this.persistence.yo();
    }, 
    // PORTING NOTE: Multi-tab only.
    t.prototype._o = function(t) {
        this.mu.To(t);
    }, 
    // PORTING NOTE: Multi-tab only.
    t.prototype.Eo = function(t) {
        this.persistence.Eo(t);
    }, t.prototype.Mu = function(t, n, i) {
        var e = this, r = n.batch, s = r.keys(), u = ri.resolve();
        return s.forEach((function(e) {
            u = u.next((function() {
                return i.su(t, e);
            })).next((function(t) {
                var s = t, u = n.Er.get(e);
                Wn(null !== u, "ackVersions should contain every doc in the write."), (!s || s.version.V(u) < 0) && ((s = r.Bt(e, s, n)) ? 
                // We use the commitVersion as the readTime rather than the
                // document's updateTime since the updateTime is not advanced
                // for updates that do not modify the underlying document.
                i.iu(s, n.Tr) : Wn(!t, "Mutation batch " + r + " applied to document " + t + " resulted in null"));
            }));
        })), u.next((function() {
            return e.mu.Qu(t, r);
        }));
    }, t.prototype.Ms = function(t) {
        var n = this;
        return this.persistence.runTransaction("Collect garbage", "readwrite-primary", (function(i) {
            return t.So(i, n.fu);
        }));
    }, 
    // PORTING NOTE: Multi-tab only.
    t.prototype.Ao = function(t) {
        var n = this, i = this.fu.get(t);
        return i ? Promise.resolve(i.target) : this.persistence.runTransaction("Get target data", "readonly", (function(i) {
            return n.yu.ae(i, t).next((function(t) {
                return t ? t.target : null;
            }));
        }));
    }, 
    /**
     * Returns the set of documents that have been updated since the last call.
     * If this is the first call, returns the set of changes since client
     * initialization. Further invocations will return document changes since
     * the point of rejection.
     */
    // PORTING NOTE: Multi-tab only.
    t.prototype.Io = function() {
        var t = this;
        return this.persistence.runTransaction("Get new document changes", "readonly", (function(n) {
            return t.gu.Io(n, t.du);
        })).then((function(n) {
            var i = n.Co, e = n.readTime;
            return t.du = e, i;
        }));
    }, 
    /**
     * Reads the newest document change from persistence and forwards the internal
     * synchronization marker so that calls to `getNewDocumentChanges()`
     * only return changes that happened after client initialization.
     */
    // PORTING NOTE: Multi-tab only.
    t.prototype.Au = function() {
        return r(this, void 0, void 0, (function() {
            var t, n = this;
            return s(this, (function(i) {
                switch (i.label) {
                  case 0:
                    return t = this, [ 4 /*yield*/ , this.persistence.runTransaction("Synchronize last document change read time", "readonly", (function(t) {
                        return n.gu.Ro(t);
                    })) ];

                  case 1:
                    return t.du = i.sent(), [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    /**
     * The maximum time to leave a resume token buffered without writing it out.
     * This value is arbitrary: it's long enough to avoid several writes
     * (possibly indefinitely if updates come more frequently than this) but
     * short enough that restarting after crashing will still have a pretty
     * recent resume token.
     */
    t.eo = 3e8, t;
}();

/**
 * Verifies the error thrown by a LocalStore operation. If a LocalStore
 * operation fails because the primary lease has been taken by another client,
 * we ignore the error (the persistence layer will immediately call
 * `applyPrimaryLease` to propagate the primary state change). All other errors
 * are re-thrown.
 *
 * @param err An error returned by a LocalStore operation.
 * @return A Promise that resolves after we recovered, or the original error.
 */
function fi(t) {
    return r(this, void 0, void 0, (function() {
        return s(this, (function(n) {
            if (t.code !== T.FAILED_PRECONDITION || t.message !== _i) throw t;
            return qn("LocalStore", "Unexpectedly lost primary lease"), [ 2 /*return*/ ];
        }));
    }));
}

/**
 * @license
 * Copyright 2019 Google Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// The format of the LocalStorage key that stores the client state is:
//     firestore_clients_<persistence_prefix>_<instance_key>
/** Assembles the key for a client state in WebStorage */
function li(t, n) {
    return Wn(-1 === n.indexOf("_"), "Client key cannot contain '_', but was '" + n + "'"), 
    "firestore_clients_" + t + "_" + n;
}

// The format of the WebStorage key that stores the mutation state is:
//     firestore_mutations_<persistence_prefix>_<batch_id>
//     (for unauthenticated users)
// or: firestore_mutations_<persistence_prefix>_<batch_id>_<user_uid>

// 'user_uid' is last to avoid needing to escape '_' characters that it might
// contain.
/** Assembles the key for a mutation batch in WebStorage */
function di(t, n, i) {
    var e = "firestore_mutations_" + t + "_" + i;
    return n.t() && (e += "_" + n.uid), e;
}

// The format of the WebStorage key that stores a query target's metadata is:
//     firestore_targets_<persistence_prefix>_<target_id>
/** Assembles the key for a query state in WebStorage */
function vi(t, n) {
    return "firestore_targets_" + t + "_" + n;
}

// The WebStorage prefix that stores the primary tab's online state. The
// format of the key is:
//     firestore_online_state_<persistence_prefix>
/**
 * @license
 * Copyright 2018 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
var wi = /** @class */ function() {
    function t(t, n, i, e) {
        this.user = t, this.batchId = n, this.state = i, this.error = e, Wn(void 0 !== e == ("rejected" === i), "MutationMetadata must contain an error iff state is 'rejected'");
    }
    /**
     * Parses a MutationMetadata from its JSON representation in WebStorage.
     * Logs a warning and returns null if the format of the data is not valid.
     */    return t.No = function(n, i, e) {
        var r = JSON.parse(e), s = "object" == typeof r && -1 !== [ "pending", "acknowledged", "rejected" ].indexOf(r.state) && (void 0 === r.error || "object" == typeof r.error), u = void 0;
        return s && r.error && (s = "string" == typeof r.error.message && "string" == typeof r.error.code) && (u = new E(r.error.code, r.error.message)), 
        s ? new t(n, i, r.state, u) : (xn("SharedClientState", "Failed to parse mutation state for ID '" + i + "': " + e), 
        null);
    }, t.prototype.Oo = function() {
        var t = {
            state: this.state,
            updateTimeMs: Date.now()
        };
        return this.error && (t.error = {
            code: this.error.code,
            message: this.error.message
        }), JSON.stringify(t);
    }, t;
}(), mi = /** @class */ function() {
    function t(t, n, i) {
        this.targetId = t, this.state = n, this.error = i, Wn(void 0 !== i == ("rejected" === n), "QueryTargetMetadata must contain an error iff state is 'rejected'");
    }
    /**
     * Parses a QueryTargetMetadata from its JSON representation in WebStorage.
     * Logs a warning and returns null if the format of the data is not valid.
     */    return t.No = function(n, i) {
        var e = JSON.parse(i), r = "object" == typeof e && -1 !== [ "not-current", "current", "rejected" ].indexOf(e.state) && (void 0 === e.error || "object" == typeof e.error), s = void 0;
        return r && e.error && (r = "string" == typeof e.error.message && "string" == typeof e.error.code) && (s = new E(e.error.code, e.error.message)), 
        r ? new t(n, e.state, s) : (xn("SharedClientState", "Failed to parse target state for ID '" + n + "': " + i), 
        null);
    }, t.prototype.Oo = function() {
        var t = {
            state: this.state,
            updateTimeMs: Date.now()
        };
        return this.error && (t.error = {
            code: this.error.code,
            message: this.error.message
        }), JSON.stringify(t);
    }, t;
}(), pi = /** @class */ function() {
    function t(t, n) {
        this.clientId = t, this.activeTargetIds = n;
    }
    /**
     * Parses a RemoteClientState from the JSON representation in WebStorage.
     * Logs a warning and returns null if the format of the data is not valid.
     */    return t.No = function(n, i) {
        for (var e = JSON.parse(i), r = "object" == typeof e && e.activeTargetIds instanceof Array, s = mn(), u = 0; r && u < e.activeTargetIds.length; ++u) r = Y(e.activeTargetIds[u]), 
        s = s.add(e.activeTargetIds[u]);
        return r ? new t(n, s) : (xn("SharedClientState", "Failed to parse client data for instance '" + n + "': " + i), 
        null);
    }, t;
}(), gi = /** @class */ function() {
    function t(t, n) {
        this.clientId = t, this.onlineState = n;
    }
    /**
     * Parses a SharedOnlineState from its JSON representation in WebStorage.
     * Logs a warning and returns null if the format of the data is not valid.
     */    return t.No = function(n) {
        var i = JSON.parse(n);
        return "object" == typeof i && -1 !== [ "Unknown", "Online", "Offline" ].indexOf(i.onlineState) && "string" == typeof i.clientId ? new t(i.clientId, i.onlineState) : (xn("SharedClientState", "Failed to parse online state: " + n), 
        null);
    }, t;
}(), bi = /** @class */ function() {
    function t() {
        this.activeTargetIds = mn();
    }
    return t.prototype.Do = function(t) {
        this.activeTargetIds = this.activeTargetIds.add(t);
    }, t.prototype.Vo = function(t) {
        this.activeTargetIds = this.activeTargetIds.delete(t);
    }, 
    /**
     * Converts this entry into a JSON-encoded format we can use for WebStorage.
     * Does not encode `clientId` as it is part of the key in WebStorage.
     */
    t.prototype.Oo = function() {
        var t = {
            activeTargetIds: this.activeTargetIds.J(),
            updateTimeMs: Date.now()
        };
        return JSON.stringify(t);
    }, t;
}();

/**
 * Holds the state of a mutation batch, including its user ID, batch ID and
 * whether the batch is 'pending', 'acknowledged' or 'rejected'.
 */
// Visible for testing
/** @class */ !function() {
    function t(n, i, e, r, s) {
        if (this.Po = n, this.platform = i, this.persistenceKey = e, this.ko = r, this.Fo = null, 
        this.jo = null, this.Nr = null, this.Uo = {}, this.Lo = this.Mo.bind(this), this.Ls = !1, 
        /**
         * Captures WebStorage events that occur before `start()` is called. These
         * events are replayed once `WebStorageSharedClientState` is started.
         */
        this.qo = [], !t.xo(this.platform)) throw new E(T.UNIMPLEMENTED, "LocalStorage is not available on this platform.");
        // Escape the special characters mentioned here:
        // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Regular_Expressions
                var u = e.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
        this.storage = this.platform.window.localStorage, this.currentUser = s, this.Bo = li(this.persistenceKey, this.ko), 
        this.Qo = 
        /** Assembles the key for the current sequence number. */
        function(t) {
            return "firestore_sequence_number_" + t;
        }(this.persistenceKey), this.Uo[this.ko] = new bi, this.Wo = new RegExp("^firestore_clients_" + u + "_([^_]*)$"), 
        this.Go = new RegExp("^firestore_mutations_" + u + "_(\\d+)(?:_(.*))?$"), this.zo = new RegExp("^firestore_targets_" + u + "_(\\d+)$"), 
        this.Jo = 
        /** Assembles the key for the online state of the primary tab. */
        function(t) {
            return "firestore_online_state_" + t;
        }
        // The WebStorage key prefix for the key that stores the last sequence number allocated. The key
        // looks like 'firestore_sequence_number_<persistence_prefix>'.
        (this.persistenceKey), 
        // Rather than adding the storage observer during start(), we add the
        // storage observer during initialization. This ensures that we collect
        // events before other components populate their initial state (during their
        // respective start() calls). Otherwise, we might for example miss a
        // mutation that is added after LocalStore's start() processed the existing
        // mutations but before we observe WebStorage events.
        this.platform.window.addEventListener("storage", this.Lo);
    }
    /** Returns 'true' if WebStorage is available in the current environment. */    t.xo = function(t) {
        return !(!t.window || null == t.window.localStorage);
    }, t.prototype.start = function() {
        return r(this, void 0, void 0, (function() {
            var t, n, i, e, r, u, o, h, a, c, f, l = this;
            return s(this, (function(s) {
                switch (s.label) {
                  case 0:
                    return Wn(!this.Ls, "WebStorageSharedClientState already started"), Wn(null !== this.Fo, "syncEngine property must be set before calling start()"), 
                    Wn(null !== this.jo, "onlineStateHandler property must be set before calling start()"), 
                    [ 4 /*yield*/ , this.Fo.yo() ];

                  case 1:
                    for (t = s.sent(), n = 0, i = t; n < i.length; n++) (e = i[n]) !== this.ko && (r = this.getItem(li(this.persistenceKey, e))) && (u = pi.No(e, r)) && (this.Uo[u.clientId] = u);
                    for (this.Ho(), (o = this.storage.getItem(this.Jo)) && (h = this.Yo(o)) && this.Ko(h), 
                    a = 0, c = this.qo; a < c.length; a++) f = c[a], this.Mo(f);
                    return this.qo = [], 
                    // Register a window unload hook to remove the client metadata entry from
                    // WebStorage even if `shutdown()` was not called.
                    this.platform.window.addEventListener("unload", (function() {
                        return l.shutdown();
                    })), this.Ls = !0, [ 2 /*return*/ ];
                }
            }));
        }));
    }, t.prototype.Vr = function(t) {
        this.setItem(this.Qo, JSON.stringify(t));
    }, t.prototype.Xo = function() {
        var t = mn();
        return W(this.Uo, (function(n, i) {
            t = t.Ft(i.activeTargetIds);
        })), t;
    }, t.prototype.$o = function(t) {
        // This is not using `obj.forEach` since `forEach` doesn't support early
        // return.
        for (var n in this.Uo) if (this.Uo.hasOwnProperty(n) && this.Uo[n].activeTargetIds.has(t)) return !0;
        return !1;
    }, t.prototype.Zo = function(t) {
        this.th(t, "pending");
    }, t.prototype.nh = function(t, n, i) {
        this.th(t, n, i), 
        // Once a final mutation result is observed by other clients, they no longer
        // access the mutation's metadata entry. Since WebStorage replays events
        // in order, it is safe to delete the entry right after updating it.
        this.ih(t);
    }, t.prototype.eh = function(t) {
        var n = "not-current";
        // Lookup an existing query state if the target ID was already registered
        // by another tab
                if (this.$o(t)) {
            var i = this.storage.getItem(vi(this.persistenceKey, t));
            if (i) {
                var e = mi.No(t, i);
                e && (n = e.state);
            }
        }
        return this.rh.Do(t), this.Ho(), n;
    }, t.prototype.sh = function(t) {
        this.rh.Vo(t), this.Ho();
    }, t.prototype.uh = function(t) {
        return this.rh.activeTargetIds.has(t);
    }, t.prototype.oh = function(t) {
        this.removeItem(vi(this.persistenceKey, t));
    }, t.prototype.hh = function(t, n, i) {
        this.ah(t, n, i);
    }, t.prototype.Iu = function(t, n, i) {
        var e = this;
        n.forEach((function(t) {
            e.ih(t);
        })), this.currentUser = t, i.forEach((function(t) {
            e.Zo(t);
        }));
    }, t.prototype.fh = function(t) {
        this.lh(t);
    }, t.prototype.shutdown = function() {
        this.Ls && (this.platform.window.removeEventListener("storage", this.Lo), this.removeItem(this.Bo), 
        this.Ls = !1);
    }, t.prototype.getItem = function(t) {
        var n = this.storage.getItem(t);
        return qn("SharedClientState", "READ", t, n), n;
    }, t.prototype.setItem = function(t, n) {
        qn("SharedClientState", "SET", t, n), this.storage.setItem(t, n);
    }, t.prototype.removeItem = function(t) {
        qn("SharedClientState", "REMOVE", t), this.storage.removeItem(t);
    }, t.prototype.Mo = function(t) {
        var n = this;
        if (t.storageArea === this.storage) {
            if (qn("SharedClientState", "EVENT", t.key, t.newValue), t.key === this.Bo) return void xn("Received WebStorage notification for local change. Another client might have garbage-collected our state");
            this.Po.Qr((function() {
                return r(n, void 0, void 0, (function() {
                    var n, i, e, r, u, o;
                    return s(this, (function(s) {
                        if (!this.Ls) return this.qo.push(t), [ 2 /*return*/ ];
                        if (null === t.key) return [ 2 /*return*/ ];
                        if (this.Wo.test(t.key)) {
                            if (null == t.newValue) return i = this.dh(t.key), [ 2 /*return*/ , this.vh(i, null) ];
                            if (n = this.wh(t.key, t.newValue)) return [ 2 /*return*/ , this.vh(n.clientId, n) ];
                        } else if (this.Go.test(t.key)) {
                            if (null !== t.newValue && (e = this.mh(t.key, t.newValue))) return [ 2 /*return*/ , this.ph(e) ];
                        } else if (this.zo.test(t.key)) {
                            if (null !== t.newValue && (r = this.gh(t.key, t.newValue))) return [ 2 /*return*/ , this.bh(r) ];
                        } else if (t.key === this.Jo) {
                            if (null !== t.newValue && (u = this.Yo(t.newValue))) return [ 2 /*return*/ , this.Ko(u) ];
                        } else t.key === this.Qo && (Wn(!!this.Nr, "Missing sequenceNumberHandler"), (o = function(t) {
                            var n = ti.Pr;
                            if (null != t) try {
                                var i = JSON.parse(t);
                                Wn("number" == typeof i, "Found non-numeric sequence number"), n = i;
                            } catch (t) {
                                xn("SharedClientState", "Failed to read sequence number from WebStorage", t);
                            }
                            return n;
                        }
                        /**
 * `MemorySharedClientState` is a simple implementation of SharedClientState for
 * clients using memory persistence. The state in this class remains fully
 * isolated and no synchronization is performed.
 */ (t.newValue)) !== ti.Pr && this.Nr(o));
                        return [ 2 /*return*/ ];
                    }));
                }));
            }));
        }
    }, Object.defineProperty(t.prototype, "rh", {
        get: function() {
            return this.Uo[this.ko];
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.Ho = function() {
        this.setItem(this.Bo, this.rh.Oo());
    }, t.prototype.th = function(t, n, i) {
        var e = new wi(this.currentUser, t, n, i), r = di(this.persistenceKey, this.currentUser, t);
        this.setItem(r, e.Oo());
    }, t.prototype.ih = function(t) {
        var n = di(this.persistenceKey, this.currentUser, t);
        this.removeItem(n);
    }, t.prototype.lh = function(t) {
        var n = {
            clientId: this.ko,
            onlineState: t
        };
        this.storage.setItem(this.Jo, JSON.stringify(n));
    }, t.prototype.ah = function(t, n, i) {
        var e = vi(this.persistenceKey, t), r = new mi(t, n, i);
        this.setItem(e, r.Oo());
    }, 
    /**
     * Parses a client state key in WebStorage. Returns null if the key does not
     * match the expected key format.
     */
    t.prototype.dh = function(t) {
        var n = this.Wo.exec(t);
        return n ? n[1] : null;
    }, 
    /**
     * Parses a client state in WebStorage. Returns 'null' if the value could not
     * be parsed.
     */
    t.prototype.wh = function(t, n) {
        var i = this.dh(t);
        return Wn(null !== i, "Cannot parse client state key '" + t + "'"), pi.No(i, n);
    }, 
    /**
     * Parses a mutation batch state in WebStorage. Returns 'null' if the value
     * could not be parsed.
     */
    t.prototype.mh = function(t, n) {
        var i = this.Go.exec(t);
        Wn(null !== i, "Cannot parse mutation batch key '" + t + "'");
        var e = Number(i[1]), r = void 0 !== i[2] ? i[2] : null;
        return wi.No(new _(r), e, n);
    }, 
    /**
     * Parses a query target state from WebStorage. Returns 'null' if the value
     * could not be parsed.
     */
    t.prototype.gh = function(t, n) {
        var i = this.zo.exec(t);
        Wn(null !== i, "Cannot parse query target key '" + t + "'");
        var e = Number(i[1]);
        return mi.No(e, n);
    }, 
    /**
     * Parses an online state from WebStorage. Returns 'null' if the value
     * could not be parsed.
     */
    t.prototype.Yo = function(t) {
        return gi.No(t);
    }, t.prototype.ph = function(t) {
        return r(this, void 0, void 0, (function() {
            return s(this, (function(n) {
                return t.user.uid !== this.currentUser.uid ? (qn("SharedClientState", "Ignoring mutation for non-active user " + t.user.uid), 
                [ 2 /*return*/ ]) : [ 2 /*return*/ , this.Fo.yh(t.batchId, t.state, t.error) ];
            }));
        }));
    }, t.prototype.bh = function(t) {
        return this.Fo._h(t.targetId, t.state, t.error);
    }, t.prototype.vh = function(t, n) {
        var i = this, e = this.Xo();
        n ? this.Uo[t] = n : delete this.Uo[t];
        var u = this.Xo(), o = [], h = [];
        return u.forEach((function(t) {
            return r(i, void 0, void 0, (function() {
                return s(this, (function(n) {
                    return e.has(t) || o.push(t), [ 2 /*return*/ ];
                }));
            }));
        })), e.forEach((function(t) {
            return r(i, void 0, void 0, (function() {
                return s(this, (function(n) {
                    return u.has(t) || h.push(t), [ 2 /*return*/ ];
                }));
            }));
        })), this.Fo.Th(o, h);
    }, t.prototype.Ko = function(t) {
        // We check whether the client that wrote this online state is still active
        // by comparing its client ID to the list of clients kept active in
        // IndexedDb. If a client does not update their IndexedDb client state
        // within 5 seconds, it is considered inactive and we don't emit an online
        // state event.
        this.Uo[t.clientId] && this.jo(t.onlineState);
    };
}();

var yi = /** @class */ function() {
    function t() {
        this.Eh = new bi, this.Sh = {}, this.Fo = null, this.jo = null, this.Nr = null;
    }
    return t.prototype.Zo = function(t) {
        // No op.
    }, t.prototype.nh = function(t, n, i) {
        // No op.
    }, t.prototype.eh = function(t) {
        return this.Eh.Do(t), this.Sh[t] || "not-current";
    }, t.prototype.hh = function(t, n, i) {
        this.Sh[t] = n;
    }, t.prototype.sh = function(t) {
        this.Eh.Vo(t);
    }, t.prototype.uh = function(t) {
        return this.Eh.activeTargetIds.has(t);
    }, t.prototype.oh = function(t) {
        delete this.Sh[t];
    }, t.prototype.Xo = function() {
        return this.Eh.activeTargetIds;
    }, t.prototype.$o = function(t) {
        return this.Eh.activeTargetIds.has(t);
    }, t.prototype.start = function() {
        return this.Eh = new bi, Promise.resolve();
    }, t.prototype.Iu = function(t, n, i) {
        // No op.
    }, t.prototype.fh = function(t) {
        // No op.
    }, t.prototype.shutdown = function() {}, t.prototype.Vr = function(t) {}, t;
}(), _i = "The current tab is not in the required state to perform this operation. It might be necessary to refresh the browser tab.", Ti = /** @class */ function() {
    function t() {
        this.Ah = [];
    }
    return t.prototype.Ih = function(t) {
        this.Ah.push(t);
    }, t.prototype.Ch = function() {
        this.Ah.forEach((function(t) {
            return t();
        }));
    }, t;
}(), Ei = /** @class */ function() {
    function t(t, n, i) {
        this.Rh = t, this.mu = n, this.Nh = i;
    }
    /**
     * Get the local view of the document identified by `key`.
     *
     * @return Local view of the document or null if we don't have any cached
     * state for it.
     */    return t.prototype.ho = function(t, n) {
        var i = this;
        return this.mu.Oh(t, n).next((function(e) {
            return i.Dh(t, n, e);
        }));
    }, 
    /** Internal version of `getDocument` that allows reusing batches. */
    t.prototype.Dh = function(t, n, i) {
        return this.Rh.su(t, n).next((function(t) {
            for (var e = 0, r = i; e < r.length; e++) {
                t = r[e].xt(n, t);
            }
            return t;
        }));
    }, 
    // Returns the view of the given `docs` as they would appear after applying
    // all mutations in the given `batches`.
    t.prototype.Vh = function(t, n, i) {
        var e = hn();
        return n.forEach((function(t, n) {
            for (var r = 0, s = i; r < s.length; r++) {
                n = s[r].xt(t, n);
            }
            e = e.st(t, n);
        })), e;
    }, 
    /**
     * Gets the local view of the documents identified by `keys`.
     *
     * If we don't have cached state for a document in `keys`, a NoDocument will
     * be stored for that key in the resulting set.
     */
    t.prototype.Ru = function(t, n) {
        var i = this;
        return this.Rh.getEntries(t, n).next((function(n) {
            return i.io(t, n);
        }));
    }, 
    /**
     * Similar to `getDocuments`, but creates the local view from the given
     * `baseDocs` without retrieving documents from the local store.
     */
    t.prototype.io = function(t, n) {
        var i = this;
        return this.mu.Ph(t, n).next((function(e) {
            var r = i.Vh(t, n, e), s = on();
            return r.forEach((function(t, n) {
                // TODO(http://b/32275378): Don't conflate missing / deleted.
                n || (n = new Lt(t, D.D())), s = s.st(t, n);
            })), s;
        }));
    }, 
    /**
     * Performs a query against the local view of all documents.
     *
     * @param transaction The persistence transaction.
     * @param query The query to match documents against.
     * @param sinceReadTime If not set to SnapshotVersion.MIN, return only
     *     documents that have been read since this snapshot version (exclusive).
     */
    t.prototype.mo = function(t, n, i) {
        return n.En() ? this.kh(t, n.path) : n.Kn() ? this.Fh(t, n, i) : this.jh(t, n, i);
    }, t.prototype.kh = function(t, n) {
        // Just do a simple document lookup.
        return this.ho(t, new j(n)).next((function(t) {
            var n = cn();
            return t instanceof Ut && (n = n.st(t.key, t)), n;
        }));
    }, t.prototype.Fh = function(t, n, i) {
        var e = this;
        Wn(n.path.M(), "Currently we only support collection group queries at the root.");
        var r = n.collectionGroup, s = cn();
        return this.Nh.Uh(t, r).next((function(u) {
            // Perform a collection query against each parent that contains the
            // collectionId and aggregate the results.
            return ri.forEach(u, (function(u) {
                var o = n.Mn(u.child(r));
                return e.jh(t, o, i).next((function(t) {
                    t.forEach((function(t, n) {
                        s = s.st(t, n);
                    }));
                }));
            })).next((function() {
                return s;
            }));
        }));
    }, t.prototype.jh = function(t, n, i) {
        var e, r, s = this;
        // Query the remote documents and overlay mutations.
                return this.Rh.mo(t, n, i).next((function(i) {
            return e = i, s.mu.Lh(t, n);
        })).next((function(n) {
            // It is possible that a PatchMutation can make a document match a query, even if
            // the version in the RemoteDocumentCache is not a match yet (waiting for server
            // to ack). To handle this, we find all document keys affected by the PatchMutations
            // that are not in `result` yet, and back fill them via `remoteDocumentCache.getEntries`,
            // otherwise those `PatchMutations` will be ignored because no base document can be found,
            // and lead to missing result for the query.
            return r = n, s.Mh(t, r, e).next((function(t) {
                e = t;
                for (var n = 0, i = r; n < i.length; n++) for (var s = i[n], u = 0, o = s.mutations; u < o.length; u++) {
                    var h = o[u], a = h.key, c = e.get(a), f = h.xt(c, c, s.br);
                    e = f instanceof Ut ? e.st(a, f) : e.remove(a);
                }
            }));
        })).next((function() {
            // Finally, filter out any documents that don't actually match
            // the query.
            return e.forEach((function(t, i) {
                n.matches(i) || (e = e.remove(t));
            })), e;
        }));
    }, t.prototype.Mh = function(t, n, i) {
        for (var e = vn(), r = 0, s = n; r < s.length; r++) for (var u = 0, o = s[r].mutations; u < o.length; u++) {
            var h = o[u];
            h instanceof Ot && null === i.get(h.key) && (e = e.add(h.key));
        }
        var a = i;
        return this.Rh.getEntries(t, e).next((function(t) {
            return t.forEach((function(t, n) {
                null !== n && n instanceof Ut && (a = a.st(t, n));
            })), a;
        }));
    }, t;
}(), Si = /** @class */ function() {
    function t() {}
    return t.prototype.Su = function(t) {
        this.qh = t;
    }, t.prototype.mo = function(t, n, i, e) {
        var r = this;
        // Queries that match all documents don't benefit from using
        // IndexFreeQueries. It is more efficient to scan all documents in a
        // collection, rather than to perform individual lookups.
        return Wn(void 0 !== this.qh, "setLocalDocumentsView() not called"), n.qn() || i.isEqual(D.MIN) ? this.xh(t, n) : this.qh.Ru(t, e).next((function(s) {
            var u = r.Bh(n, s);
            return (n.Jn() || n.Hn()) && r.Qh(n.An, u, e, i) ? r.xh(t, n) : (Ln() <= o.DEBUG && qn("IndexFreeQueryEngine", "Re-using previous result from %s to execute query: %s", i.toString(), n.toString()), 
            r.qh.mo(t, n, i).next((function(t) {
                // We merge `previousResults` into `updateResults`, since
                // `updateResults` is already a DocumentMap. If a document is
                // contained in both lists, then its contents are the same.
                return u.forEach((function(n) {
                    t = t.st(n.key, n);
                })), t;
            })));
        }));
        // Queries that have never seen a snapshot without limbo free documents
        // should also be run as a full collection scan.
        }, 
    /** Applies the query filter and sorting to the provided documents.  */
    t.prototype.Bh = function(t, n) {
        // Sort the documents and re-apply the query filter since previously
        // matching documents do not necessarily still match the query.
        var i = new x((function(n, i) {
            return t.Bn(n, i);
        }));
        return n.forEach((function(n, e) {
            e instanceof Ut && t.matches(e) && (i = i.add(e));
        })), i;
    }, 
    /**
     * Determines if a limit query needs to be refilled from cache, making it
     * ineligible for index-free execution.
     *
     * @param sortedPreviousResults The documents that matched the query when it
     * was last synchronized, sorted by the query's comparator.
     * @param remoteKeys The document keys that matched the query at the last
     * snapshot.
     * @param limboFreeSnapshotVersion The version of the snapshot when the query
     * was last synchronized.
     */
    t.prototype.Qh = function(t, n, i, e) {
        // The query needs to be refilled if a previously matching document no
        // longer matches.
        if (i.size !== n.size) return !0;
        // Limit queries are not eligible for index-free query execution if there is
        // a potential that an older document from cache now sorts before a document
        // that was previously part of the limit. This, however, can only happen if
        // the document at the edge of the limit goes out of limit.
        // If a document that is not the limit boundary sorts differently,
        // the boundary of the limit itself did not change and documents from cache
        // will continue to be "rejected" by this boundary. Therefore, we can ignore
        // any modifications that don't affect the last document.
                var r = "F" /* First */ === t ? n.last() : n.first();
        return !!r && (r.hasPendingWrites || r.version.V(e) > 0);
    }, t.prototype.xh = function(t, n) {
        return Ln() <= o.DEBUG && qn("IndexFreeQueryEngine", "Using full collection scan to execute query: %s", n.toString()), 
        this.qh.mo(t, n, D.MIN);
    }, t;
}(), Ai = /** @class */ function() {
    function t() {
        this.Wh = new Ii;
    }
    return t.prototype.Gh = function(t, n) {
        return this.Wh.add(n), ri.resolve();
    }, t.prototype.Uh = function(t, n) {
        return ri.resolve(this.Wh.getEntries(n));
    }, t;
}(), Ii = /** @class */ function() {
    function t() {
        this.index = {};
    }
    // Returns false if the entry already existed.
        return t.prototype.add = function(t) {
        Wn(t.length % 2 == 1, "Expected a collection path.");
        var n = t.B(), i = t.L(), e = this.index[n] || new x(P.F), r = !e.has(i);
        return this.index[n] = e.add(i), r;
    }, t.prototype.has = function(t) {
        var n = t.B(), i = t.L(), e = this.index[n];
        return e && e.has(i);
    }, t.prototype.getEntries = function(t) {
        return (this.index[t] || new x(P.F)).J();
    }, t;
}(), Ci = /** @class */ function() {
    function t(t, n) {
        this.Nh = t, this.wu = n, 
        /**
         * The set of all mutations that have been sent but not yet been applied to
         * the backend.
         */
        this.mu = [], 
        /** Next value to use when assigning sequential IDs to each mutation batch. */
        this.zh = 1, 
        /** The last received stream token from the server, used to acknowledge which
         * responses the client has processed. Stream tokens are opaque checkpoint
         * markers whose only real value is their inclusion in the next request.
         */
        this.lastStreamToken = z.qt, 
        /** An ordered mapping between documents and the mutations batch IDs. */
        this.Jh = new x(hi.gn);
    }
    return t.prototype.Hh = function(t) {
        return ri.resolve(0 === this.mu.length);
    }, t.prototype.ju = function(t, n, i) {
        var e = n.batchId, r = this.Yh(e, "acknowledged");
        Wn(0 === r, "Can only acknowledge the first batch in the mutation queue");
        // Verify that the batch in the queue is the one to be acknowledged.
        var s = this.mu[r];
        return Wn(e === s.batchId, "Queue ordering failure: expected batch " + e + ", got batch " + s.batchId), 
        this.lastStreamToken = i, ri.resolve();
    }, t.prototype.Gu = function(t) {
        return ri.resolve(this.lastStreamToken);
    }, t.prototype.zu = function(t, n) {
        return this.lastStreamToken = n, ri.resolve();
    }, t.prototype.Pu = function(t, n, i, e) {
        Wn(0 !== e.length, "Mutation batches should not be empty");
        var r = this.zh;
        (this.zh++, this.mu.length > 0) && Wn(this.mu[this.mu.length - 1].batchId < r, "Mutation batchIDs must be monotonically increasing order");
        var s = new Kn(r, n, i, e);
        this.mu.push(s);
        // Track references by document key and index collection parents.
        for (var u = 0, o = e; u < o.length; u++) {
            var h = o[u];
            this.Jh = this.Jh.add(new hi(h.key, r)), this.Nh.Gh(t, h.key.path.L());
        }
        return ri.resolve(s);
    }, t.prototype.Bu = function(t, n) {
        return ri.resolve(this.Kh(n));
    }, t.prototype.Fu = function(t, n) {
        var i = this.Kh(n);
        return Wn(null != i, "Failed to find local mutation batch."), ri.resolve(i.keys());
    }, t.prototype.uo = function(t, n) {
        var i = n + 1, e = this.Xh(i), r = e < 0 ? 0 : e;
        // The requested batchId may still be out of range so normalize it to the
        // start of the queue.
                return ri.resolve(this.mu.length > r ? this.mu[r] : null);
    }, t.prototype.Wu = function() {
        return ri.resolve(0 === this.mu.length ? -1 : this.zh - 1);
    }, t.prototype.Cu = function(t) {
        return ri.resolve(this.mu.slice());
    }, t.prototype.Oh = function(t, n) {
        var i = this, e = new hi(n, 0), r = new hi(n, Number.POSITIVE_INFINITY), s = [];
        return this.Jh.Vt([ e, r ], (function(t) {
            Wn(n.isEqual(t.key), "Should only iterate over a single key's batches");
            var e = i.Kh(t.$s);
            Wn(null !== e, "Batches in the index must exist in the main table"), s.push(e);
        })), ri.resolve(s);
    }, t.prototype.Ph = function(t, n) {
        var i = this, e = new x(zn);
        return n.forEach((function(t) {
            var n = new hi(t, 0), r = new hi(t, Number.POSITIVE_INFINITY);
            i.Jh.Vt([ n, r ], (function(n) {
                Wn(t.isEqual(n.key), "For each key, should only iterate over a single key's batches"), 
                e = e.add(n.$s);
            }));
        })), ri.resolve(this.$h(e));
    }, t.prototype.Lh = function(t, n) {
        Wn(!n.Kn(), "CollectionGroup queries should be handled in LocalDocumentsView");
        // Use the query path as a prefix for testing if a document matches the
        // query.
        var i = n.path, e = i.length + 1, r = i;
        j.nt(r) || (r = r.child(""));
        var s = new hi(new j(r), 0), u = new x(zn);
        // Find unique batchIDs referenced by all documents potentially matching the
        // query.
                return this.Jh.Pt((function(t) {
            var n = t.key.path;
            return !!i.W(n) && (
            // Rows with document keys more than one segment longer than the query
            // path can't be matches. For example, a query on 'rooms' can't match
            // the document /rooms/abc/messages/xyx.
            // TODO(mcg): we'll need a different scanner when we implement
            // ancestor queries.
            n.length === e && (u = u.add(t.$s)), !0);
        }), s), ri.resolve(this.$h(u));
    }, t.prototype.$h = function(t) {
        var n = this, i = [];
        // Construct an array of matching batches, sorted by batchID to ensure that
        // multiple mutations affecting the same document key are applied in order.
                return t.forEach((function(t) {
            var e = n.Kh(t);
            null !== e && i.push(e);
        })), i;
    }, t.prototype.Qu = function(t, n) {
        var i = this;
        // Find the position of the first batch for removal. This need not be the
        // first entry in the queue.
                Wn(0 === this.Yh(n.batchId, "removed"), "Can only remove the first entry of the mutation queue"), 
        this.mu.shift();
        var e = this.Jh;
        return ri.forEach(n.mutations, (function(r) {
            var s = new hi(r.key, n.batchId);
            return e = e.delete(s), i.wu.Zh(t, r.key);
        })).next((function() {
            i.Jh = e;
        }));
    }, t.prototype.To = function(t) {
        // No-op since the memory mutation queue does not maintain a separate cache.
    }, t.prototype.Xs = function(t, n) {
        var i = new hi(n, 0), e = this.Jh.kt(i);
        return ri.resolve(n.isEqual(e && e.key));
    }, t.prototype.qu = function(t) {
        return 0 === this.mu.length && Wn(this.Jh.M(), "Document leak -- detected dangling mutation references when queue is empty."), 
        ri.resolve();
    }, 
    /**
     * Finds the index of the given batchId in the mutation queue and asserts that
     * the resulting index is within the bounds of the queue.
     *
     * @param batchId The batchId to search for
     * @param action A description of what the caller is doing, phrased in passive
     * form (e.g. "acknowledged" in a routine that acknowledges batches).
     */
    t.prototype.Yh = function(t, n) {
        var i = this.Xh(t);
        return Wn(i >= 0 && i < this.mu.length, "Batches must exist to be " + n), i;
    }, 
    /**
     * Finds the index of the given batchId in the mutation queue. This operation
     * is O(1).
     *
     * @return The computed index of the batch with the given batchId, based on
     * the state of the queue. Note this index can be negative if the requested
     * batchId has already been remvoed from the queue or past the end of the
     * queue if the batchId is larger than the last added batch.
     */
    t.prototype.Xh = function(t) {
        return 0 === this.mu.length ? 0 : t - this.mu[0].batchId;
        // Examine the front of the queue to figure out the difference between the
        // batchId and indexes in the array. Note that since the queue is ordered
        // by batchId, if the first batch has a larger batchId then the requested
        // batchId doesn't exist in the queue.
        }, 
    /**
     * A version of lookupMutationBatch that doesn't return a promise, this makes
     * other functions that uses this code easier to read and more efficent.
     */
    t.prototype.Kh = function(t) {
        var n = this.Xh(t);
        if (n < 0 || n >= this.mu.length) return null;
        var i = this.mu[n];
        return Wn(i.batchId === t, "If found batch must match"), i;
    }, t;
}();

/**
 * @license
 * Copyright 2017 Google Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ var Ri = /** @class */ function() {
    /**
     * @param sizer Used to assess the size of a document. For eager GC, this is expected to just
     * return 0 to avoid unnecessarily doing the work of calculating the size.
     */
    function t(t, n) {
        this.Nh = t, this.ta = n, 
        /** Underlying cache of documents and their read times. */
        this.docs = new U(j.F), 
        /** Size of all cached documents. */
        this.size = 0;
    }
    /**
     * Adds the supplied entry to the cache and updates the cache size as appropriate.
     *
     * All calls of `addEntry`  are required to go through the RemoteDocumentChangeBuffer
     * returned by `newChangeBuffer()`.
     */    return t.prototype.iu = function(t, n, i) {
        Wn(!i.isEqual(D.MIN), "Cannot add a document with a read time of zero");
        var e = n.key, r = this.docs.get(e), s = r ? r.size : 0, u = this.ta(n);
        return this.docs = this.docs.st(e, {
            na: n,
            size: u,
            readTime: i
        }), this.size += u - s, this.Nh.Gh(t, e.path.L());
    }, 
    /**
     * Removes the specified entry from the cache and updates the cache size as appropriate.
     *
     * All calls of `removeEntry` are required to go through the RemoteDocumentChangeBuffer
     * returned by `newChangeBuffer()`.
     */
    t.prototype.ru = function(t) {
        var n = this.docs.get(t);
        n && (this.docs = this.docs.remove(t), this.size -= n.size);
    }, t.prototype.su = function(t, n) {
        var i = this.docs.get(n);
        return ri.resolve(i ? i.na : null);
    }, t.prototype.getEntries = function(t, n) {
        var i = this, e = hn();
        return n.forEach((function(t) {
            var n = i.docs.get(t);
            e = e.st(t, n ? n.na : null);
        })), ri.resolve(e);
    }, t.prototype.mo = function(t, n, i) {
        Wn(!n.Kn(), "CollectionGroup queries should be handled in LocalDocumentsView");
        for (var e = cn(), r = new j(n.path.child("")), s = this.docs.lt(r)
        // Documents are ordered by key, so we can use a prefix scan to narrow down
        // the documents we need to match the query against.
        ; s.bt(); ) {
            var u = s.gt(), o = u.key, h = u.value, a = h.na, c = h.readTime;
            if (!n.path.W(o.path)) break;
            c.V(i) <= 0 || a instanceof Ut && n.matches(a) && (e = e.st(a.key, a));
        }
        return ri.resolve(e);
    }, t.prototype.ia = function(t, n) {
        return ri.forEach(this.docs, (function(t) {
            return n(t);
        }));
    }, t.prototype.Io = function(t, n) {
        throw new Error("getNewDocumentChanges() is not supported with MemoryPersistence");
    }, t.prototype.Ro = function(t) {
        return ri.resolve(D.MIN);
    }, t.prototype.Uu = function(n) {
        // `trackRemovals` is ignores since the MemoryRemoteDocumentCache keeps
        // a separate changelog and does not need special handling for removals.
        return new t.ea(this);
    }, t.prototype.ra = function(t) {
        return ri.resolve(this.size);
    }, 
    /**
     * Handles the details of adding and updating documents in the MemoryRemoteDocumentCache.
     */
    t.ea = /** @class */ function(t) {
        function i(n) {
            var i = t.call(this) || this;
            return i.sa = n, i;
        }
        return n(i, t), i.prototype.hu = function(t) {
            var n = this, i = [];
            return this.Zs.forEach((function(e, r) {
                r ? i.push(n.sa.iu(t, r, n.readTime)) : n.sa.ru(e);
            })), ri.gs(i);
        }, i.prototype.uu = function(t, n) {
            return this.sa.su(t, n);
        }, i.prototype.ou = function(t, n) {
            return this.sa.getEntries(t, n);
        }, i;
    }(ai), t;
}(), Ni = /** @class */ function() {
    /**
     * Instantiates a new TargetIdGenerator. If a seed is provided, the generator
     * will use the seed value as the next target ID.
     */
    function t(t, n) {
        this.ua = t, Wn((1 & t) === t, "Generator ID " + t + " contains more than 1 reserved bits"), 
        this.oa(void 0 !== n ? n : this.ua);
    }
    return t.prototype.next = function() {
        var t = this.ha;
        return this.ha += 2, t;
    }, 
    /**
     * Returns the ID that follows the given ID. Subsequent calls to `next()`
     * use the newly returned target ID as their base.
     */
    // PORTING NOTE: Multi-tab only.
    t.prototype.after = function(t) {
        return this.oa(t + 2), this.next();
    }, t.prototype.oa = function(t) {
        Wn((1 & t) === this.ua, "Cannot supply target ID from different generator ID"), 
        this.ha = t;
    }, t.aa = function() {
        return new t(0 /* QueryCache */ , 2);
    }, t.ca = function() {
        // Sync engine assigns target IDs for limbo document detection.
        return new t(1 /* SyncEngine */);
    }, t;
}(), Oi = /** @class */ function() {
    function t(t) {
        this.persistence = t, 
        /**
         * Maps a target to the data about that target
         */
        this.fa = new $n((function(t) {
            return t.canonicalId();
        })), 
        /** The last received snapshot version. */
        this.lastRemoteSnapshotVersion = D.MIN, 
        /** The highest numbered target ID encountered. */
        this.highestTargetId = 0, 
        /** The highest sequence number encountered. */
        this.la = 0, 
        /**
         * A ordered bidirectional mapping between documents and the remote target
         * IDs.
         */
        this.da = new oi, this.targetCount = 0, this.va = Ni.aa();
    }
    return t.prototype.$i = function(t, n) {
        return this.fa.forEach((function(t, i) {
            return n(i);
        })), ri.resolve();
    }, t.prototype.Ju = function(t) {
        return ri.resolve(this.lastRemoteSnapshotVersion);
    }, t.prototype.wa = function(t) {
        return ri.resolve(this.la);
    }, t.prototype.fo = function(t) {
        var n = this.va.after(this.highestTargetId);
        return this.highestTargetId = n, ri.resolve(n);
    }, t.prototype.no = function(t, n, i) {
        return i && (this.lastRemoteSnapshotVersion = i), n > this.la && (this.la = n), 
        ri.resolve();
    }, t.prototype.ma = function(t) {
        this.fa.set(t.target, t);
        var n = t.targetId;
        n > this.highestTargetId && (this.highestTargetId = n), t.sequenceNumber > this.la && (this.la = t.sequenceNumber);
    }, t.prototype.lo = function(t, n) {
        return Wn(!this.fa.has(n.target), "Adding a target that already exists"), this.ma(n), 
        this.targetCount += 1, ri.resolve();
    }, t.prototype.Zu = function(t, n) {
        return Wn(this.fa.has(n.target), "Updating a non-existent target"), this.ma(n), 
        ri.resolve();
    }, t.prototype.pa = function(t, n) {
        return Wn(this.targetCount > 0, "Removing a target from an empty cache"), Wn(this.fa.has(n.target), "Removing a non-existent target from the cache"), 
        this.fa.delete(n.target), this.da.Hs(n.targetId), this.targetCount -= 1, ri.resolve();
    }, t.prototype.ga = function(t, n, i) {
        var e = this, r = 0, s = [];
        return this.fa.forEach((function(u, o) {
            o.sequenceNumber <= n && null === i.get(o.targetId) && (e.fa.delete(u), s.push(e.ba(t, o.targetId)), 
            r++);
        })), ri.gs(s).next((function() {
            return r;
        }));
    }, t.prototype.ya = function(t) {
        return ri.resolve(this.targetCount);
    }, t.prototype.co = function(t, n) {
        var i = this.fa.get(n) || null;
        return ri.resolve(i);
    }, t.prototype.ae = function(t, n) {
        // This method is only needed for multi-tab and we can't implement it
        // efficiently without additional data structures.
        return Qn("Not yet implemented.");
    }, t.prototype.Ku = function(t, n, i) {
        this.da.Ws(n, i);
        var e = this.persistence.wu, r = [];
        return e && n.forEach((function(n) {
            r.push(e.Qs(t, n));
        })), ri.gs(r);
    }, t.prototype.Yu = function(t, n, i) {
        this.da.Js(n, i);
        var e = this.persistence.wu, r = [];
        return e && n.forEach((function(n) {
            r.push(e.Gs(t, n));
        })), ri.gs(r);
    }, t.prototype.ba = function(t, n) {
        return this.da.Hs(n), ri.resolve();
    }, t.prototype.wo = function(t, n) {
        var i = this.da.Ks(n);
        return ri.resolve(i);
    }, t.prototype.Xs = function(t, n) {
        return ri.resolve(this.da.Xs(n));
    }, t;
}(), Di = "You are using the memory-only build of Firestore. Persistence support is only available via the @firebase/firestore bundle or the firebase-firestore.js build.", Vi = /** @class */ function() {
    /**
     * The constructor accepts a factory for creating a reference delegate. This
     * allows both the delegate and this instance to have strong references to
     * each other without having nullable fields that would then need to be
     * checked or asserted on every access.
     */
    function t(t, n) {
        var i = this;
        this.clientId = t, this._a = {}, this.Ta = new ti(0), this.Ea = !1, this.Ea = !0, 
        this.wu = n(this), this.yu = new Oi(this);
        this.Nh = new Ai, this.Rh = new Ri(this.Nh, (function(t) {
            return i.wu.Sa(t);
        }));
    }
    return t.prototype.shutdown = function() {
        // No durable state to ensure is closed on shutdown.
        return this.Ea = !1, Promise.resolve();
    }, Object.defineProperty(t.prototype, "Ls", {
        get: function() {
            return this.Ea;
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.yo = function() {
        return r(this, void 0, void 0, (function() {
            return s(this, (function(t) {
                return [ 2 /*return*/ , [ this.clientId ] ];
            }));
        }));
    }, t.prototype.Aa = function(t) {
        // All clients using memory persistence act as primary.
        return t(!0);
    }, t.prototype.Ia = function() {
        // No op.
    }, t.prototype.Eo = function(t) {
        // No op.
    }, t.prototype.Eu = function() {
        return this.Nh;
    }, t.prototype.pu = function(t) {
        var n = this._a[t.i()];
        return n || (n = new Ci(this.Nh, this.wu), this._a[t.i()] = n), n;
    }, t.prototype._u = function() {
        return this.yu;
    }, t.prototype.bu = function() {
        return this.Rh;
    }, t.prototype.runTransaction = function(t, n, i) {
        var e = this;
        qn("MemoryPersistence", "Starting transaction:", t);
        var r = new Pi(this.Ta.next());
        return this.wu.Ca(), i(r).next((function(t) {
            return e.wu.Ra(r).next((function() {
                return t;
            }));
        })).ms().then((function(t) {
            return r.Ch(), t;
        }));
    }, t.prototype.Na = function(t, n) {
        return ri.bs(Object.values(this._a).map((function(i) {
            return function() {
                return i.Xs(t, n);
            };
        })));
    }, t;
}(), Pi = /** @class */ function(t) {
    function i(n) {
        var i = t.call(this) || this;
        return i.Xu = n, i;
    }
    return n(i, t), i;
}(Ti), ki = /** @class */ function() {
    function t(t) {
        this.persistence = t, this.Oa = null, this.Da = null;
    }
    return Object.defineProperty(t.prototype, "Va", {
        get: function() {
            if (this.Da) return this.Da;
            throw Qn("orphanedDocuments is only valid during a transaction.");
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.vu = function(t) {
        this.Oa = t;
    }, t.prototype.Qs = function(t, n) {
        return this.Va.delete(n), ri.resolve();
    }, t.prototype.Gs = function(t, n) {
        return this.Va.add(n), ri.resolve();
    }, t.prototype.Zh = function(t, n) {
        return this.Va.add(n), ri.resolve();
    }, t.prototype.removeTarget = function(t, n) {
        var i = this, e = this.persistence._u();
        return e.wo(t, n.targetId).next((function(t) {
            t.forEach((function(t) {
                return i.Va.add(t);
            }));
        })).next((function() {
            return e.pa(t, n);
        }));
    }, t.prototype.Ca = function() {
        this.Da = new Set;
    }, t.prototype.Ra = function(t) {
        var n = this, i = this.persistence.bu().Uu();
        // Remove newly orphaned documents.
                return ri.forEach(this.Va, (function(e) {
            return n.Pa(t, e).next((function(t) {
                t || i.ru(e);
            }));
        })).next((function() {
            return n.Da = null, i.apply(t);
        }));
    }, t.prototype.to = function(t, n) {
        var i = this;
        return this.Pa(t, n).next((function(t) {
            t ? i.Va.delete(n) : i.Va.add(n);
        }));
    }, t.prototype.Sa = function(t) {
        // For eager GC, we don't care about the document size, there are no size thresholds.
        return 0;
    }, t.prototype.Pa = function(t, n) {
        var i = this;
        return ri.bs([ function() {
            return i.persistence._u().Xs(t, n);
        }, function() {
            return i.persistence.Na(t, n);
        }, function() {
            return ri.resolve(i.Oa.Xs(n));
        } ]);
    }, t;
}(), Fi = /** @class */ function() {
    function t() {}
    return t.prototype.initialize = function(t, n, i, e, r, s) {
        if (s.ka) throw new E(T.FAILED_PRECONDITION, Di);
        return this.clientId = e, Promise.resolve();
    }, t.prototype.Fa = function() {
        var t = !1;
        return {
            Ls: t,
            start: function() {
                return t = !0;
            },
            stop: function() {
                return t = !1;
            }
        };
    }, t.prototype.ja = function() {
        return Wn(!!this.clientId, "initialize() not called"), new Vi(this.clientId, (function(t) {
            return new ki(t);
        }));
    }, t.prototype.Ua = function() {
        return new yi;
    }, t.prototype.clearPersistence = function() {
        throw new E(T.FAILED_PRECONDITION, Di);
    }, t;
}(), ji = /** @class */ function() {
    function t(
    /**
     * The AsyncQueue to run backoff operations on.
     */
    t, 
    /**
     * The ID to use when scheduling backoff operations on the AsyncQueue.
     */
    n, 
    /**
     * The initial delay (used as the base delay on the first retry attempt).
     * Note that jitter will still be applied, so the actual delay could be as
     * little as 0.5*initialDelayMs.
     */
    i, 
    /**
     * The multiplier to use to determine the extended base delay after each
     * attempt.
     */
    e, 
    /**
     * The maximum base delay after which no further backoff is performed.
     * Note that jitter will still be applied, so the actual delay could be as
     * much as 1.5*maxDelayMs.
     */
    r) {
        void 0 === i && (i = 1e3), void 0 === e && (e = 1.5), void 0 === r && (r = 6e4), 
        this.Po = t, this.Fr = n, this.La = i, this.Ma = e, this.qa = r, this.xa = 0, this.Ba = null, 
        /** The last backoff attempt, as epoch milliseconds. */
        this.Qa = Date.now(), this.reset();
    }
    /**
     * Resets the backoff delay.
     *
     * The very next backoffAndWait() will have no delay. If it is called again
     * (i.e. due to an error), initialDelayMs (plus jitter) will be used, and
     * subsequent ones will increase according to the backoffFactor.
     */    return t.prototype.reset = function() {
        this.xa = 0;
    }, 
    /**
     * Resets the backoff delay to the maximum delay (e.g. for use after a
     * RESOURCE_EXHAUSTED error).
     */
    t.prototype.Wa = function() {
        this.xa = this.qa;
    }, 
    /**
     * Returns a promise that resolves after currentDelayMs, and increases the
     * delay for any subsequent attempts. If there was a pending backoff operation
     * already, it will be canceled.
     */
    t.prototype.Ga = function(t) {
        var n = this;
        // Cancel any pending backoff operation.
                this.cancel();
        // First schedule using the current base (which may be 0 and should be
        // honored as such).
        var i = Math.floor(this.xa + this.za()), e = Math.max(0, Date.now() - this.Qa), r = Math.max(0, i - e);
        // Guard against lastAttemptTime being in the future due to a clock change.
                this.xa > 0 && qn("ExponentialBackoff", "Backing off for " + r + " ms (base delay: " + this.xa + " ms, delay with jitter: " + i + " ms, last attempt: " + e + " ms ago)"), 
        this.Ba = this.Po.es(this.Fr, r, (function() {
            return n.Qa = Date.now(), t();
        })), 
        // Apply backoff factor to determine next delay and ensure it is within
        // bounds.
        this.xa *= this.Ma, this.xa < this.La && (this.xa = this.La), this.xa > this.qa && (this.xa = this.qa);
    }, t.prototype.cancel = function() {
        null !== this.Ba && (this.Ba.cancel(), this.Ba = null);
    }, 
    /** Returns a random value in the range [-currentBaseMs/2, currentBaseMs/2] */
    t.prototype.za = function() {
        return (Math.random() - .5) * this.xa;
    }, t;
}(), Ui = /** @class */ function() {
    function t(t, n, i, e, r, s) {
        this.Po = t, this.Ja = i, this.connection = e, this.Ha = r, this.listener = s, this.state = 0 /* Initial */ , 
        /**
         * A close count that's incremented every time the stream is closed; used by
         * getCloseGuardedDispatcher() to invalidate callbacks that happen after
         * close.
         */
        this.Ya = 0, this.Ka = null, this.stream = null, this.Xa = new ji(t, n);
    }
    /**
     * Returns true if start() has been called and no error has occurred. True
     * indicates the stream is open or in the process of opening (which
     * encompasses respecting backoff, getting auth tokens, and starting the
     * actual RPC). Use isOpen() to determine if the stream is open and ready for
     * outbound requests.
     */    return t.prototype.$a = function() {
        return 1 /* Starting */ === this.state || 2 /* Open */ === this.state || 4 /* Backoff */ === this.state;
    }, 
    /**
     * Returns true if the underlying RPC is open (the onOpen() listener has been
     * called) and the stream is ready for outbound requests.
     */
    t.prototype.Za = function() {
        return 2 /* Open */ === this.state;
    }, 
    /**
     * Starts the RPC. Only allowed if isStarted() returns false. The stream is
     * not immediately ready for use: onOpen() will be invoked when the RPC is
     * ready for outbound requests, at which point isOpen() will return true.
     *
     * When start returns, isStarted() will return true.
     */
    t.prototype.start = function() {
        3 /* Error */ !== this.state ? (Wn(0 /* Initial */ === this.state, "Already started"), 
        this.auth()) : this.tc();
    }, 
    /**
     * Stops the RPC. This call is idempotent and allowed regardless of the
     * current isStarted() state.
     *
     * When stop returns, isStarted() and isOpen() will both return false.
     */
    t.prototype.stop = function() {
        return r(this, void 0, void 0, (function() {
            return s(this, (function(t) {
                switch (t.label) {
                  case 0:
                    return this.$a() ? [ 4 /*yield*/ , this.close(0 /* Initial */) ] : [ 3 /*break*/ , 2 ];

                  case 1:
                    t.sent(), t.label = 2;

                  case 2:
                    return [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    /**
     * After an error the stream will usually back off on the next attempt to
     * start it. If the error warrants an immediate restart of the stream, the
     * sender can use this to indicate that the receiver should not back off.
     *
     * Each error will call the onClose() listener. That function can decide to
     * inhibit backoff if required.
     */
    t.prototype.nc = function() {
        Wn(!this.$a(), "Can only inhibit backoff in a stopped state"), this.state = 0 /* Initial */ , 
        this.Xa.reset();
    }, 
    /**
     * Marks this stream as idle. If no further actions are performed on the
     * stream for one minute, the stream will automatically close itself and
     * notify the stream's onClose() handler with Status.OK. The stream will then
     * be in a !isStarted() state, requiring the caller to start the stream again
     * before further use.
     *
     * Only streams that are in state 'Open' can be marked idle, as all other
     * states imply pending network operations.
     */
    t.prototype.ic = function() {
        var t = this;
        // Starts the idle time if we are in state 'Open' and are not yet already
        // running a timer (in which case the previous idle timeout still applies).
                this.Za() && null === this.Ka && (this.Ka = this.Po.es(this.Ja, 6e4, (function() {
            return t.ec();
        })));
    }, 
    /** Sends a message to the underlying stream. */
    t.prototype.rc = function(t) {
        this.sc(), this.stream.send(t);
    }, 
    /** Called by the idle timer when the stream should close due to inactivity. */
    t.prototype.ec = function() {
        return r(this, void 0, void 0, (function() {
            return s(this, (function(t) {
                return this.Za() ? [ 2 /*return*/ , this.close(0 /* Initial */) ] : [ 2 /*return*/ ];
            }));
        }));
    }, 
    /** Marks the stream as active again. */
    t.prototype.sc = function() {
        this.Ka && (this.Ka.cancel(), this.Ka = null);
    }, 
    /**
     * Closes the stream and cleans up as necessary:
     *
     * * closes the underlying GRPC stream;
     * * calls the onClose handler with the given 'error';
     * * sets internal stream state to 'finalState';
     * * adjusts the backoff timer based on the error
     *
     * A new stream can be opened by calling start().
     *
     * @param finalState the intended state of the stream after closing.
     * @param error the error the connection was closed with.
     */
    t.prototype.close = function(t, n) {
        return r(this, void 0, void 0, (function() {
            return s(this, (function(i) {
                switch (i.label) {
                  case 0:
                    // Notify the listener that the stream closed.
                    return Wn(this.$a(), "Only started streams should be closed."), Wn(3 /* Error */ === t || J(n), "Can't provide an error when not in an error state."), 
                    // Cancel any outstanding timers (they're guaranteed not to execute).
                    this.sc(), this.Xa.cancel(), 
                    // Invalidates any stream-related callbacks (e.g. from auth or the
                    // underlying stream), guaranteeing they won't execute.
                    this.Ya++, 3 /* Error */ !== t ? 
                    // If this is an intentional close ensure we don't delay our next connection attempt.
                    this.Xa.reset() : n && n.code === T.RESOURCE_EXHAUSTED ? (
                    // Log the error. (Probably either 'quota exceeded' or 'max queue length reached'.)
                    xn(n.toString()), xn("Using maximum backoff delay to prevent overloading the backend."), 
                    this.Xa.Wa()) : n && n.code === T.UNAUTHENTICATED && 
                    // "unauthenticated" error means the token was rejected. Try force refreshing it in case it
                    // just expired.
                    this.Ha.l(), 
                    // Clean up the underlying stream because we are no longer interested in events.
                    null !== this.stream && (this.uc(), this.stream.close(), this.stream = null), 
                    // This state must be assigned before calling onClose() to allow the callback to
                    // inhibit backoff or otherwise manipulate the state in its non-started state.
                    this.state = t, [ 4 /*yield*/ , this.listener.oc(n) ];

                  case 1:
                    // Notify the listener that the stream closed.
                    return i.sent(), [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    /**
     * Can be overridden to perform additional cleanup before the stream is closed.
     * Calling super.tearDown() is not required.
     */
    t.prototype.uc = function() {}, t.prototype.auth = function() {
        var t = this;
        Wn(0 /* Initial */ === this.state, "Must be in initial state to auth"), this.state = 1 /* Starting */;
        var n = this.hc(this.Ya), i = this.Ya;
        // TODO(mikelehen): Just use dispatchIfNotClosed, but see TODO below.
                this.Ha.getToken().then((function(n) {
            // Stream can be stopped while waiting for authentication.
            // TODO(mikelehen): We really should just use dispatchIfNotClosed
            // and let this dispatch onto the queue, but that opened a spec test can
            // of worms that I don't want to deal with in this PR.
            t.Ya === i && 
            // Normally we'd have to schedule the callback on the AsyncQueue.
            // However, the following calls are safe to be called outside the
            // AsyncQueue since they don't chain asynchronous calls
            t.ac(n);
        }), (function(i) {
            n((function() {
                var n = new E(T.UNKNOWN, "Fetching auth token failed: " + i.message);
                return t.cc(n);
            }));
        }));
    }, t.prototype.ac = function(t) {
        var n = this;
        Wn(1 /* Starting */ === this.state, "Trying to start stream in a non-starting state");
        var i = this.hc(this.Ya);
        this.stream = this.fc(t), this.stream.lc((function() {
            i((function() {
                return Wn(1 /* Starting */ === n.state, "Expected stream to be in state Starting, but was " + n.state), 
                n.state = 2 /* Open */ , n.listener.lc();
            }));
        })), this.stream.oc((function(t) {
            i((function() {
                return n.cc(t);
            }));
        })), this.stream.onMessage((function(t) {
            i((function() {
                return n.onMessage(t);
            }));
        }));
    }, t.prototype.tc = function() {
        var t = this;
        Wn(3 /* Error */ === this.state, "Should only perform backoff when in Error state"), 
        this.state = 4 /* Backoff */ , this.Xa.Ga((function() {
            return r(t, void 0, void 0, (function() {
                return s(this, (function(t) {
                    return Wn(4 /* Backoff */ === this.state, "Backoff elapsed but state is now: " + this.state), 
                    this.state = 0 /* Initial */ , this.start(), Wn(this.$a(), "PersistentStream should have started"), 
                    [ 2 /*return*/ ];
                }));
            }));
        }));
    }, 
    // Visible for tests
    t.prototype.cc = function(t) {
        // In theory the stream could close cleanly, however, in our current model
        // we never expect this to happen because if we stop a stream ourselves,
        // this callback will never be called. To prevent cases where we retry
        // without a backoff accidentally, we set the stream to error in all cases.
        return Wn(this.$a(), "Can't handle server close on non-started stream"), qn("PersistentStream", "close with error: " + t), 
        this.stream = null, this.close(3 /* Error */ , t);
    }, 
    /**
     * Returns a "dispatcher" function that dispatches operations onto the
     * AsyncQueue but only runs them if closeCount remains unchanged. This allows
     * us to turn auth / stream callbacks into no-ops if the stream is closed /
     * re-opened, etc.
     */
    t.prototype.hc = function(t) {
        var n = this;
        return function(i) {
            n.Po.Qr((function() {
                return n.Ya === t ? i() : (qn("PersistentStream", "stream callback skipped by getCloseGuardedDispatcher."), 
                Promise.resolve());
            }));
        };
    }, t;
}(), Li = /** @class */ function(t) {
    function i(n, i, e, r, s) {
        var u = t.call(this, n, "listen_stream_connection_backoff" /* ListenStreamConnectionBackoff */ , "listen_stream_idle" /* ListenStreamIdle */ , i, e, s) || this;
        return u.serializer = r, u;
    }
    return n(i, t), i.prototype.fc = function(t) {
        return this.connection.dc("Listen", t);
    }, i.prototype.onMessage = function(t) {
        // A successful response means the stream is healthy
        this.Xa.reset();
        var n = this.serializer.Ve(t), i = this.serializer.ke(t);
        return this.listener.vc(n, i);
    }, 
    /**
     * Registers interest in the results of the given target. If the target
     * includes a resumeToken it will be included in the request. Results that
     * affect the target will be streamed back as WatchChange messages that
     * reference the targetId.
     */
    i.prototype.watch = function(t) {
        var n = {};
        n.database = this.serializer.Ae, n.addTarget = this.serializer.xn(t);
        var i = this.serializer.nr(t);
        i && (n.labels = i), this.rc(n);
    }, 
    /**
     * Unregisters interest in the results of the target associated with the
     * given targetId.
     */
    i.prototype.wc = function(t) {
        var n = {};
        n.database = this.serializer.Ae, n.removeTarget = t, this.rc(n);
    }, i;
}(Ui), Mi = /** @class */ function(t) {
    function i(n, i, e, r, s) {
        var u = t.call(this, n, "write_stream_connection_backoff" /* WriteStreamConnectionBackoff */ , "write_stream_idle" /* WriteStreamIdle */ , i, e, s) || this;
        return u.serializer = r, u.mc = !1, 
        /**
         * The last received stream token from the server, used to acknowledge which
         * responses the client has processed. Stream tokens are opaque checkpoint
         * markers whose only real value is their inclusion in the next request.
         *
         * PersistentWriteStream manages propagating this value from responses to the
         * next request.
         */
        u.lastStreamToken = z.qt, u;
    }
    return n(i, t), Object.defineProperty(i.prototype, "pc", {
        /**
         * Tracks whether or not a handshake has been successfully exchanged and
         * the stream is ready to accept mutations.
         */
        get: function() {
            return this.mc;
        },
        enumerable: !0,
        configurable: !0
    }), 
    // Override of PersistentStream.start
    i.prototype.start = function() {
        this.mc = !1, t.prototype.start.call(this);
    }, i.prototype.uc = function() {
        this.mc && this.gc([]);
    }, i.prototype.fc = function(t) {
        return this.connection.dc("Write", t);
    }, i.prototype.onMessage = function(t) {
        if (
        // Always capture the last stream token.
        Wn(!!t.streamToken, "Got a write response without a stream token"), this.lastStreamToken = this.serializer.pe(t.streamToken), 
        this.mc) {
            // A successful first write response means the stream is healthy,
            // Note, that we could consider a successful handshake healthy, however,
            // the write itself might be causing an error we want to back off from.
            this.Xa.reset();
            var n = this.serializer.We(t.writeResults, t.commitTime), i = this.serializer.fromVersion(t.commitTime);
            return this.listener.bc(i, n);
        }
        // The first response is always the handshake response
        return Wn(!t.writeResults || 0 === t.writeResults.length, "Got mutation results for handshake"), 
        this.mc = !0, this.listener.yc();
    }, 
    /**
     * Sends an initial streamToken to the server, performing the handshake
     * required to make the StreamingWrite RPC work. Subsequent
     * calls should wait until onHandshakeComplete was called.
     */
    i.prototype._c = function() {
        Wn(this.Za(), "Writing handshake requires an opened stream"), Wn(!this.mc, "Handshake already completed");
        // TODO(dimond): Support stream resumption. We intentionally do not set the
        // stream token on the handshake, ignoring any stream token we might have.
        var t = {};
        t.database = this.serializer.Ae, this.rc(t);
    }, 
    /** Sends a group of mutations to the Firestore backend to apply. */
    i.prototype.gc = function(t) {
        var n = this;
        Wn(this.Za(), "Writing mutations requires an opened stream"), Wn(this.mc, "Handshake must be complete before writing mutations"), 
        Wn(this.lastStreamToken.Mt() > 0, "Trying to write mutation without a token");
        var i = {
            streamToken: this.serializer.me(this.lastStreamToken),
            writes: t.map((function(t) {
                return n.serializer.Fe(t);
            }))
        };
        this.rc(i);
    }, i;
}(Ui), qi = /** @class */ function() {
    function t(t, n, i, e) {
        this.Po = t, this.connection = n, this.credentials = i, this.serializer = e;
    }
    return t.prototype.Tc = function(t) {
        return new Mi(this.Po, this.connection, this.credentials, this.serializer, t);
    }, t.prototype.Ec = function(t) {
        return new Li(this.Po, this.connection, this.credentials, this.serializer, t);
    }, t.prototype.commit = function(t) {
        var n = this, i = {
            database: this.serializer.Ae,
            writes: t.map((function(t) {
                return n.serializer.Fe(t);
            }))
        };
        return this.Sc("Commit", i).then((function(t) {
            return n.serializer.We(t.writeResults, t.commitTime);
        }));
    }, t.prototype.lookup = function(t) {
        var n = this, i = {
            database: this.serializer.Ae,
            documents: t.map((function(t) {
                return n.serializer._e(t);
            }))
        };
        return this.Ac("BatchGetDocuments", i).then((function(i) {
            var e = on();
            i.forEach((function(t) {
                var i = n.serializer.De(t);
                e = e.st(i.key, i);
            }));
            var r = [];
            return t.forEach((function(t) {
                var n = e.get(t);
                Wn(!!n, "Missing entity in write response for " + t), r.push(n);
            })), r;
        }));
    }, 
    /** Gets an auth token and invokes the provided RPC. */
    t.prototype.Sc = function(t, n) {
        var i = this;
        return this.credentials.getToken().then((function(e) {
            return i.connection.Sc(t, n, e);
        })).catch((function(t) {
            throw t.code === T.UNAUTHENTICATED && i.credentials.l(), t;
        }));
    }, 
    /** Gets an auth token and invokes the provided RPC with streamed results. */
    t.prototype.Ac = function(t, n) {
        var i = this;
        return this.credentials.getToken().then((function(e) {
            return i.connection.Ac(t, n, e);
        })).catch((function(t) {
            throw t.code === T.UNAUTHENTICATED && i.credentials.l(), t;
        }));
    }, t;
}();

/**
 * @license
 * Copyright 2017 Google Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Validates that no arguments were passed in the invocation of functionName.
 *
 * Forward the magic "arguments" variable as second parameter on which the
 * parameter validation is performed:
 * validateNoArgs('myFunction', arguments);
 */
function xi(t, n) {
    if (0 !== n.length) throw new E(T.INVALID_ARGUMENT, "Function " + t + "() does not support arguments, but was called with " + oe(n.length, "argument") + ".");
}

/**
 * Validates the invocation of functionName has the exact number of arguments.
 *
 * Forward the magic "arguments" variable as second parameter on which the
 * parameter validation is performed:
 * validateExactNumberOfArgs('myFunction', arguments, 2);
 */ function Bi(t, n, i) {
    if (n.length !== i) throw new E(T.INVALID_ARGUMENT, "Function " + t + "() requires " + oe(i, "argument") + ", but was called with " + oe(n.length, "argument") + ".");
}

/**
 * Validates the invocation of functionName has at least the provided number of
 * arguments (but can have many more).
 *
 * Forward the magic "arguments" variable as second parameter on which the
 * parameter validation is performed:
 * validateAtLeastNumberOfArgs('myFunction', arguments, 2);
 */ function Qi(t, n, i) {
    if (n.length < i) throw new E(T.INVALID_ARGUMENT, "Function " + t + "() requires at least " + oe(i, "argument") + ", but was called with " + oe(n.length, "argument") + ".");
}

/**
 * Validates the invocation of functionName has number of arguments between
 * the values provided.
 *
 * Forward the magic "arguments" variable as second parameter on which the
 * parameter validation is performed:
 * validateBetweenNumberOfArgs('myFunction', arguments, 2, 3);
 */ function Wi(t, n, i, e) {
    if (n.length < i || n.length > e) throw new E(T.INVALID_ARGUMENT, "Function " + t + "() requires between " + i + " and " + e + " arguments, but was called with " + oe(n.length, "argument") + ".");
}

/**
 * Validates the provided argument is an array and has as least the expected
 * number of elements.
 */ function Gi(t, n, i, e) {
    if (!(n instanceof Array) || n.length < e) throw new E(T.INVALID_ARGUMENT, "Function " + t + "() requires its " + i + " argument to be an array with at least " + oe(e, "element") + ".");
}

/**
 * Validates the provided positional argument has the native JavaScript type
 * using typeof checks.
 */ function zi(t, n, i, e) {
    Zi(t, n, ue(i) + " argument", e);
}

/**
 * Validates the provided argument has the native JavaScript type using
 * typeof checks or is undefined.
 */ function Ji(t, n, i, e) {
    void 0 !== e && zi(t, n, i, e);
}

/**
 * Validates the provided named option has the native JavaScript type using
 * typeof checks.
 */ function Hi(t, n, i, e) {
    Zi(t, n, i + " option", e);
}

/**
 * Validates the provided named option has the native JavaScript type using
 * typeof checks or is undefined.
 */ function Yi(t, n, i, e) {
    void 0 !== e && Hi(t, n, i, e);
}

function Ki(t, n, i, e, r) {
    void 0 !== e && function(t, n, i, e, r) {
        if (!(e instanceof Array)) throw new E(T.INVALID_ARGUMENT, "Function " + t + "() requires its " + n + " option to be an array, but it was: " + ne(e));
        for (var s = 0; s < e.length; ++s) if (!r(e[s])) throw new E(T.INVALID_ARGUMENT, "Function " + t + "() requires all " + n + " elements to be " + i + ", but the value at index " + s + " was: " + ne(e[s]));
    }(t, n, i, e, r);
}

/**
 * Validates that the provided named option equals one of the expected values.
 */
/**
 * Validates that the provided named option equals one of the expected values or
 * is undefined.
 */
function Xi(t, n, i, e, r) {
    void 0 !== e && function(t, n, i, e, r) {
        for (var s = [], u = 0, o = r; u < o.length; u++) {
            var h = o[u];
            if (h === e) return;
            s.push(ne(h));
        }
        var a = ne(e);
        throw new E(T.INVALID_ARGUMENT, "Invalid value " + a + " provided to function " + t + '() for option "' + i + '". Acceptable values: ' + s.join(", "));
    }(t, 0, i, e, r);
}

/**
 * Validates that the provided argument is a valid enum.
 *
 * @param functionName Function making the validation call.
 * @param enums Array containing all possible values for the enum.
 * @param position Position of the argument in `functionName`.
 * @param argument Arugment to validate.
 */ function $i(t, n, i, e) {
    if (!n.some((function(t) {
        return t === e;
    }))) throw new E(T.INVALID_ARGUMENT, "Invalid value " + ne(e) + " provided to function " + t + "() for its " + ue(i) + " argument. Acceptable values: " + n.join(", "));
}

/** Helper to validate the type of a provided input. */ function Zi(t, n, i, e) {
    if (!("object" === n ? te(e) : "non-empty string" === n ? "string" == typeof e && "" !== e : typeof e === n)) {
        var r = ne(e);
        throw new E(T.INVALID_ARGUMENT, "Function " + t + "() requires its " + i + " to be of type " + n + ", but it was: " + r);
    }
}

/**
 * Returns true if it's a non-null object without a custom prototype
 * (i.e. excludes Array, Date, etc.).
 */ function te(t) {
    return "object" == typeof t && null !== t && (Object.getPrototypeOf(t) === Object.prototype || null === Object.getPrototypeOf(t));
}

/** Returns a string describing the type / value of the provided input. */ function ne(t) {
    if (void 0 === t) return "undefined";
    if (null === t) return "null";
    if ("string" == typeof t) return t.length > 20 && (t = t.substring(0, 20) + "..."), 
    JSON.stringify(t);
    if ("number" == typeof t || "boolean" == typeof t) return "" + t;
    if ("object" == typeof t) {
        if (t instanceof Array) return "an array";
        var n = 
        /** Hacky method to try to get the constructor name for an object. */
        function(t) {
            if (t.constructor) {
                var n = /function\s+([^\s(]+)\s*\(/.exec(t.constructor.toString());
                if (n && n.length > 1) return n[1];
            }
            return null;
        }
        /** Validates the provided argument is defined. */ (t);
        return n ? "a custom " + n + " object" : "an object";
    }
    return "function" == typeof t ? "a function" : Qn("Unknown wrong type: " + typeof t);
}

function ie(t, n, i) {
    if (void 0 === i) throw new E(T.INVALID_ARGUMENT, "Function " + t + "() requires a valid " + ue(n) + " argument, but it was undefined.");
}

/**
 * Validates the provided positional argument is an object, and its keys and
 * values match the expected keys and types provided in optionTypes.
 */ function ee(t, n, i) {
    W(n, (function(n, e) {
        if (i.indexOf(n) < 0) throw new E(T.INVALID_ARGUMENT, "Unknown option '" + n + "' passed to function " + t + "(). Available options: " + i.join(", "));
    }));
}

/**
 * Helper method to throw an error that the provided argument did not pass
 * an instanceof check.
 */ function re(t, n, i, e) {
    var r = ne(e);
    return new E(T.INVALID_ARGUMENT, "Function " + t + "() requires its " + ue(i) + " argument to be a " + n + ", but it was: " + r);
}

function se(t, n, i) {
    if (i <= 0) throw new E(T.INVALID_ARGUMENT, 'Function "' + t + '()" requires its ' + ue(n) + " argument to be a positive number, but it was: " + i + ".");
}

/** Converts a number to its english word representation */ function ue(t) {
    switch (t) {
      case 1:
        return "first";

      case 2:
        return "second";

      case 3:
        return "third";

      default:
        return t + "th";
    }
}

/**
 * Formats the given word as plural conditionally given the preceding number.
 */ function oe(t, n) {
    return t + " " + n + (1 === t ? "" : "s");
}

/**
 * @license
 * Copyright 2017 Google Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Helper function to prevent instantiation through the constructor.
 *
 * This method creates a new constructor that throws when it's invoked.
 * The prototype of that constructor is then set to the prototype of the hidden
 * "class" to expose all the prototype methods and allow for instanceof
 * checks.
 *
 * To also make all the static methods available, all properties of the
 * original constructor are copied to the new constructor.
 */ function he(t, n) {
    function i() {
        var t = "This constructor is private.";
        throw n && (t += " ", t += n), new E(T.INVALID_ARGUMENT, t);
    }
    // Make sure instanceof checks work and all methods are exposed on the public
    // constructor
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
    return i.prototype = t.prototype, 
    // Copy any static methods/members
    Object.assign(i, t), i;
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/** Helper function to assert Uint8Array is available at runtime. */ function ae() {
    if ("undefined" == typeof Uint8Array) throw new E(T.UNIMPLEMENTED, "Uint8Arrays are not available in this environment.");
}

/** Helper function to assert Base64 functions are available at runtime. */ function ce() {
    if (!jn.Lt().Ic) throw new E(T.UNIMPLEMENTED, "Blobs are unavailable in Firestore in this environment.");
}

/**
 * Immutable class holding a blob (binary data).
 * This class is directly exposed in the public API.
 *
 * Note that while you can't hide the constructor in JavaScript code, we are
 * using the hack above to make sure no-one outside this module can call it.
 */ var fe = /** @class */ function() {
    function t(t) {
        ce(), this.Cc = t;
    }
    return t.fromBase64String = function(n) {
        Bi("Blob.fromBase64String", arguments, 1), zi("Blob.fromBase64String", "string", 1, n), 
        ce();
        try {
            return new t(z.fromBase64String(n));
        } catch (t) {
            throw new E(T.INVALID_ARGUMENT, "Failed to construct Blob from Base64 string: " + t);
        }
    }, t.fromUint8Array = function(n) {
        if (Bi("Blob.fromUint8Array", arguments, 1), ae(), !(n instanceof Uint8Array)) throw re("Blob.fromUint8Array", "Uint8Array", 1, n);
        return new t(z.fromUint8Array(n));
    }, t.prototype.toBase64 = function() {
        return Bi("Blob.toBase64", arguments, 0), ce(), this.Cc.toBase64();
    }, t.prototype.toUint8Array = function() {
        return Bi("Blob.toUint8Array", arguments, 0), ae(), this.Cc.toUint8Array();
    }, t.prototype.toString = function() {
        return "Blob(base64: " + this.toBase64() + ")";
    }, t.prototype.isEqual = function(t) {
        return this.Cc.isEqual(t.Cc);
    }, t;
}(), le = he(fe, "Use Blob.fromUint8Array() or Blob.fromBase64String() instead."), de = /** @class */ function() {
    /**
     * Creates a FieldPath from the provided field names. If more than one field
     * name is provided, the path will point to a nested field in a document.
     *
     * @param fieldNames A list of field names.
     */
    function t() {
        for (var t = [], n = 0; n < arguments.length; n++) t[n] = arguments[n];
        Gi("FieldPath", t, "fieldNames", 1);
        for (var i = 0; i < t.length; ++i) if (zi("FieldPath", "string", i, t[i]), 0 === t[i].length) throw new E(T.INVALID_ARGUMENT, "Invalid field name at argument $(i + 1). Field names must not be empty.");
        this.Rc = new F(t);
    }
    return t.documentId = function() {
        return t.Nc;
    }, t.prototype.isEqual = function(n) {
        if (!(n instanceof t)) throw re("isEqual", "FieldPath", 1, n);
        return this.Rc.isEqual(n.Rc);
    }, 
    /**
     * Internal Note: The backend doesn't technically support querying by
     * document ID. Instead it queries by the entire document name (full path
     * included), but in the cases we currently support documentId(), the net
     * effect is the same.
     */
    t.Nc = new t(F.Z().H()), t;
}(), ve = new RegExp("[~\\*/\\[\\]]");

// Public instance that disallows construction at runtime. This constructor is
// used when exporting Blob on firebase.firestore.Blob and will be called Blob
// publicly. Internally we still use Blob which has a type checked private
// constructor. Note that Blob and PublicBlob can be used interchangeably in
// instanceof checks.
// For our internal TypeScript code PublicBlob doesn't exist as a type, and so
// we need to use Blob as type and export it too.
/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * An opaque base class for FieldValue sentinel objects in our public API,
 * with public static methods for creating said sentinel objects.
 */
var we = /** @class */ function() {
    function t(t) {
        this.Oc = t;
    }
    return t.delete = function() {
        return xi("FieldValue.delete", arguments), me.instance;
    }, t.serverTimestamp = function() {
        return xi("FieldValue.serverTimestamp", arguments), pe.instance;
    }, t.arrayUnion = function() {
        for (var t = [], n = 0; n < arguments.length; n++) t[n] = arguments[n];
        // NOTE: We don't actually parse the data until it's used in set() or
        // update() since we need access to the Firestore instance.
        return Qi("FieldValue.arrayUnion", arguments, 1), new ge(t);
    }, t.arrayRemove = function() {
        for (var t = [], n = 0; n < arguments.length; n++) t[n] = arguments[n];
        // NOTE: We don't actually parse the data until it's used in set() or
        // update() since we need access to the Firestore instance.
        return Qi("FieldValue.arrayRemove", arguments, 1), new be(t);
    }, t.increment = function(t) {
        return zi("FieldValue.increment", "number", 1, t), Bi("FieldValue.increment", arguments, 1), 
        new ye(t);
    }, t.prototype.isEqual = function(t) {
        return this === t;
    }, t;
}(), me = /** @class */ function(t) {
    function i() {
        return t.call(this, "FieldValue.delete") || this;
    }
    /** Singleton instance. */    return n(i, t), i.instance = new i, i;
}(we), pe = /** @class */ function(t) {
    function i() {
        return t.call(this, "FieldValue.serverTimestamp") || this;
    }
    /** Singleton instance. */    return n(i, t), i.instance = new i, i;
}(we), ge = /** @class */ function(t) {
    function i(n) {
        var i = t.call(this, "FieldValue.arrayUnion") || this;
        return i.Dc = n, i;
    }
    return n(i, t), i;
}(we), be = /** @class */ function(t) {
    function i(n) {
        var i = t.call(this, "FieldValue.arrayRemove") || this;
        return i.Dc = n, i;
    }
    return n(i, t), i;
}(we), ye = /** @class */ function(t) {
    function i(n) {
        var i = t.call(this, "FieldValue.increment") || this;
        return i.Vc = n, i;
    }
    return n(i, t), i;
}(we), _e = he(we, "Use FieldValue.<field>() instead."), Te = /** @class */ function() {
    function t(t, n) {
        if (Bi("GeoPoint", arguments, 2), zi("GeoPoint", "number", 1, t), zi("GeoPoint", "number", 2, n), 
        !isFinite(t) || t < -90 || t > 90) throw new E(T.INVALID_ARGUMENT, "Latitude must be a number between -90 and 90, but was: " + t);
        if (!isFinite(n) || n < -180 || n > 180) throw new E(T.INVALID_ARGUMENT, "Longitude must be a number between -180 and 180, but was: " + n);
        this.Pc = t, this.kc = n;
    }
    return Object.defineProperty(t.prototype, "latitude", {
        /**
         * Returns the latitude of this geo point, a number between -90 and 90.
         */
        get: function() {
            return this.Pc;
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(t.prototype, "longitude", {
        /**
         * Returns the longitude of this geo point, a number between -180 and 180.
         */
        get: function() {
            return this.kc;
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.isEqual = function(t) {
        return this.Pc === t.Pc && this.kc === t.kc;
    }, 
    /**
     * Actually private to JS consumers of our API, so this function is prefixed
     * with an underscore.
     */
    t.prototype.R = function(t) {
        return zn(this.Pc, t.Pc) || zn(this.kc, t.kc);
    }, t;
}(), Ee = /^__.*__$/, Se = /** @class */ function() {
    function t(t, n, i) {
        this.data = t, this.en = n, this.fieldTransforms = i;
    }
    return t.prototype.Fc = function(t, n) {
        var i = [];
        return null !== this.en ? i.push(new Ot(t, this.data, this.en, n)) : i.push(new Nt(t, this.data, n)), 
        this.fieldTransforms.length > 0 && i.push(new Dt(t, this.fieldTransforms)), i;
    }, t;
}(), Ae = /** @class */ function() {
    function t(t, n, i) {
        this.data = t, this.en = n, this.fieldTransforms = i;
    }
    return t.prototype.Fc = function(t, n) {
        var i = [ new Ot(t, this.data, this.en, n) ];
        return this.fieldTransforms.length > 0 && i.push(new Dt(t, this.fieldTransforms)), 
        i;
    }, t;
}();

function Ie(t) {
    switch (t) {
      case 0 /* Set */ :
 // fall through
              case 2 /* MergeSet */ :
 // fall through
              case 1 /* Update */ :
        return !0;

      case 3 /* Argument */ :
      case 4 /* ArrayArgument */ :
        return !1;

      default:
        throw Qn("Unexpected case for UserDataSource: " + t);
    }
}

/** A "context" object passed around while parsing user data. */ var Ce = /** @class */ function() {
    /**
     * Initializes a ParseContext with the given source and path.
     *
     * @param dataSource Indicates what kind of API method this data came from.
     * @param methodName The name of the method the user called to create this
     *     ParseContext.
     * @param path A path within the object being parsed. This could be an empty
     *     path (in which case the context represents the root of the data being
     *     parsed), or a nonempty path (indicating the context represents a nested
     *     location within the data).
     * @param arrayElement Whether or not this context corresponds to an element
     *     of an array.
     * @param fieldTransforms A mutable list of field transforms encountered while
     *     parsing the data.
     * @param fieldMask A mutable list of field paths encountered while parsing
     *     the data.
     *
     * TODO(b/34871131): We don't support array paths right now, so path can be
     * null to indicate the context represents any location within an array (in
     * which case certain features will not work and errors will be somewhat
     * compromised).
     */
    function t(t, n, i, e, r, s) {
        this.jc = t, this.methodName = n, this.path = i, this.Uc = e, 
        // Minor hack: If fieldTransforms is undefined, we assume this is an
        // external call and we need to validate the entire path.
        void 0 === r && this.Lc(), this.Uc = void 0 !== e && e, this.fieldTransforms = r || [], 
        this.en = s || [];
    }
    return t.prototype.Mc = function(n) {
        var i = null == this.path ? null : this.path.child(n), e = new t(this.jc, this.methodName, i, 
        /*arrayElement=*/ !1, this.fieldTransforms, this.en);
        return e.qc(n), e;
    }, t.prototype.xc = function(n) {
        var i = null == this.path ? null : this.path.child(n), e = new t(this.jc, this.methodName, i, 
        /*arrayElement=*/ !1, this.fieldTransforms, this.en);
        return e.Lc(), e;
    }, t.prototype.Bc = function(n) {
        // TODO(b/34871131): We don't support array paths right now; so make path
        // null.
        return new t(this.jc, this.methodName, 
        /*path=*/ null, 
        /*arrayElement=*/ !0, this.fieldTransforms, this.en);
    }, t.prototype.Qc = function(t) {
        var n = null === this.path || this.path.M() ? "" : " (found in field " + this.path.toString() + ")";
        return new E(T.INVALID_ARGUMENT, "Function " + this.methodName + "() called with invalid data. " + t + n);
    }, 
    /** Returns 'true' if 'fieldPath' was traversed when creating this context. */
    t.prototype.contains = function(t) {
        return void 0 !== this.en.find((function(n) {
            return t.W(n);
        })) || void 0 !== this.fieldTransforms.find((function(n) {
            return t.W(n.field);
        }));
    }, t.prototype.Lc = function() {
        // TODO(b/34871131): Remove null check once we have proper paths for fields
        // within arrays.
        if (null !== this.path) for (var t = 0; t < this.path.length; t++) this.qc(this.path.get(t));
    }, t.prototype.qc = function(t) {
        if (0 === t.length) throw this.Qc("Document fields must not be empty");
        if (Ie(this.jc) && Ee.test(t)) throw this.Qc('Document fields cannot begin and end with "__"');
    }, t;
}(), Re = function(t, n) {
    this.ce = t, this.key = n;
}, Ne = /** @class */ function() {
    function t(t, n) {
        this.serializer = t, this.Wc = n;
    }
    /** Parse document data from a non-merge set() call. */    return t.prototype.Gc = function(t, n) {
        var i = new Ce(0 /* Set */ , t, F.K);
        De("Data must be an object, but it was:", i, n);
        var e = this.zc(n, i);
        return new Se(new kt(e), 
        /* fieldMask= */ null, i.fieldTransforms);
    }, 
    /** Parse document data from a set() call with '{merge:true}'. */
    t.prototype.Jc = function(t, n, i) {
        var e = new Ce(2 /* MergeSet */ , t, F.K);
        De("Data must be an object, but it was:", e, n);
        var r, s, u = this.zc(n, e);
        if (i) {
            for (var o = new x(F.F), h = 0, a = i; h < a.length; h++) {
                var c = a[h], f = void 0;
                if (c instanceof de) f = c.Rc; else {
                    if ("string" != typeof c) throw Qn("Expected stringOrFieldPath to be a string or a FieldPath");
                    f = Pe(t, c);
                }
                if (!e.contains(f)) throw new E(T.INVALID_ARGUMENT, "Field '" + f + "' is specified in your field mask but missing from your input data.");
                o = o.add(f);
            }
            r = St.Jt(o), s = e.fieldTransforms.filter((function(t) {
                return r.Yt(t.field);
            }));
        } else r = St.Ht(e.en), s = e.fieldTransforms;
        return new Se(new kt(u), r, s);
    }, 
    /** Parse update data from an update() call. */
    t.prototype.Hc = function(t, n) {
        var i = this, e = new Ce(1 /* Update */ , t, F.K);
        De("Data must be an object, but it was:", e, n);
        var r = new x(F.F), s = kt.ln();
        W(n, (function(n, u) {
            var o = Pe(t, n), h = e.xc(o);
            if ((u = i.Yc(u, h)) instanceof me) 
            // Add it to the field mask, but don't add anything to updateData.
            r = r.add(o); else {
                var a = i.Kc(u, h);
                null != a && (r = r.add(o), s.set(o, a));
            }
        }));
        var u = St.Jt(r);
        return new Ae(s.on(), u, e.fieldTransforms);
    }, 
    /** Parse update data from a list of field/value arguments. */
    t.prototype.Xc = function(t, n, i, e) {
        var r = new Ce(1 /* Update */ , t, F.K), s = [ Ve(t, n) ], u = [ i ];
        if (e.length % 2 != 0) throw new E(T.INVALID_ARGUMENT, "Function " + t + "() needs to be called with an even number of arguments that alternate between field names and values.");
        for (var o = 0; o < e.length; o += 2) s.push(Ve(t, e[o])), u.push(e[o + 1]);
        var h = new x(F.F), a = kt.ln();
        for (o = 0; o < s.length; ++o) {
            var c = s[o], f = r.xc(c), l = this.Yc(u[o], f);
            if (l instanceof me) 
            // Add it to the field mask, but don't add anything to updateData.
            h = h.add(c); else {
                var d = this.Kc(l, f);
                null != d && (h = h.add(c), a.set(c, d));
            }
        }
        var v = St.Jt(h);
        return new Ae(a.on(), v, r.fieldTransforms);
    }, 
    /**
     * Parse a "query value" (e.g. value in a where filter or a value in a cursor
     * bound).
     *
     * @param allowArrays Whether the query value is an array that may directly
     * contain additional arrays (e.g. the operand of an `in` query).
     */
    t.prototype.$c = function(t, n, i) {
        void 0 === i && (i = !1);
        var e = new Ce(i ? 4 /* ArrayArgument */ : 3 /* Argument */ , t, F.K), r = this.Kc(n, e);
        return Wn(null != r, "Parsed data should not be null."), Wn(0 === e.fieldTransforms.length, "Field transforms should have been disallowed."), 
        r;
    }, 
    /** Sends data through this.preConverter, handling any thrown errors. */
    t.prototype.Yc = function(t, n) {
        try {
            return this.Wc(t);
        } catch (t) {
            var i = ke(t);
            throw n.Qc(i);
        }
    }, 
    /**
     * Internal helper for parsing user data.
     *
     * @param input Data to be parsed.
     * @param context A context object representing the current path being parsed,
     * the source of the data being parsed, etc.
     * @return The parsed value, or null if the value was a FieldValue sentinel
     * that should not be included in the resulting parsed data.
     */
    t.prototype.Kc = function(t, n) {
        if (Oe(t = this.Yc(t, n))) return De("Unsupported field value:", n, t), this.zc(t, n);
        if (t instanceof we) 
        // FieldValues usually parse into transforms (except FieldValue.delete())
        // in which case we do not want to include this field in our parsed data
        // (as doing so will overwrite the field directly prior to the transform
        // trying to transform it). So we don't add this location to
        // context.fieldMask and we return null as our parsing result.
        return this.Zc(t, n), null;
        if (
        // If context.path is null we are inside an array and we don't support
        // field mask paths more granular than the top-level array.
        n.path && n.en.push(n.path), t instanceof Array) {
            // TODO(b/34871131): Include the path containing the array in the error
            // message.
            // In the case of IN queries, the parsed data is an array (representing
            // the set of values to be included for the IN query) that may directly
            // contain additional arrays (each representing an individual field
            // value), so we disable this validation.
            if (n.Uc && 4 /* ArrayArgument */ !== n.jc) throw n.Qc("Nested arrays are not supported");
            return this.tf(t, n);
        }
        return this.nf(t, n);
    }, t.prototype.zc = function(t, n) {
        var i = this, e = {};
        return G(t) ? 
        // If we encounter an empty object, we explicitly add it to the update
        // mask to ensure that the server creates a map entry.
        n.path && n.path.length > 0 && n.en.push(n.path) : W(t, (function(t, r) {
            var s = i.Kc(r, n.Mc(t));
            null != s && (e[t] = s);
        })), {
            mapValue: {
                fields: e
            }
        };
    }, t.prototype.tf = function(t, n) {
        for (var i = [], e = 0, r = 0, s = t; r < s.length; r++) {
            var u = s[r], o = this.Kc(u, n.Bc(e));
            null == o && (
            // Just include nulls in the array for fields being replaced with a
            // sentinel.
            o = {
                nullValue: "NULL_VALUE"
            }), i.push(o), e++;
        }
        return {
            arrayValue: {
                values: i
            }
        };
    }, 
    /**
     * "Parses" the provided FieldValueImpl, adding any necessary transforms to
     * context.fieldTransforms.
     */
    t.prototype.Zc = function(t, n) {
        // Sentinels are only supported with writes, and not within arrays.
        if (!Ie(n.jc)) throw n.Qc(t.Oc + "() can only be used with update() and set()");
        if (null === n.path) throw n.Qc(t.Oc + "() is not currently supported inside arrays");
        if (t instanceof me) {
            if (2 /* MergeSet */ !== n.jc) throw 1 /* Update */ === n.jc ? (Wn(n.path.length > 0, "FieldValue.delete() at the top level should have already been handled."), 
            n.Qc("FieldValue.delete() can only appear at the top level of your update data")) : n.Qc("FieldValue.delete() cannot be used with set() unless you pass {merge:true}");
            // No transform to add for a delete, but we need to add it to our
            // fieldMask so it gets deleted.
            n.en.push(n.path);
        } else if (t instanceof pe) n.fieldTransforms.push(new At(n.path, pt.instance)); else if (t instanceof ge) {
            var i = this.if(t.Oc, t.Dc), e = new gt(i);
            n.fieldTransforms.push(new At(n.path, e));
        } else if (t instanceof be) {
            i = this.if(t.Oc, t.Dc);
            var r = new bt(i);
            n.fieldTransforms.push(new At(n.path, r));
        } else if (t instanceof ye) {
            var s = this.$c("FieldValue.increment", t.Vc), u = new yt(this.serializer, s);
            n.fieldTransforms.push(new At(n.path, u));
        } else Qn("Unknown FieldValue type: " + t);
    }, 
    /**
     * Helper to parse a scalar value (i.e. not an Object, Array, or FieldValue)
     *
     * @return The parsed value
     */
    t.prototype.nf = function(t, n) {
        if (null === t) return {
            nullValue: "NULL_VALUE"
        };
        if ("number" == typeof t) return this.serializer.we(t);
        if ("boolean" == typeof t) return {
            booleanValue: t
        };
        if ("string" == typeof t) return {
            stringValue: t
        };
        if (t instanceof Date) {
            var i = O.fromDate(t);
            return {
                timestampValue: this.serializer.k(i)
            };
        }
        if (t instanceof O) {
            // Firestore backend truncates precision down to microseconds. To ensure
            // offline mode works the same with regards to truncation, perform the
            // truncation immediately without waiting for the backend to do that.
            i = new O(t.seconds, 1e3 * Math.floor(t.nanoseconds / 1e3));
            return {
                timestampValue: this.serializer.k(i)
            };
        }
        if (t instanceof Te) return {
            geoPointValue: {
                latitude: t.latitude,
                longitude: t.longitude
            }
        };
        if (t instanceof fe) return {
            bytesValue: this.serializer.me(t)
        };
        if (t instanceof Re) return {
            referenceValue: this.serializer.ge(t.key.path, t.ce)
        };
        throw n.Qc("Unsupported field value: " + ne(t));
    }, t.prototype.if = function(t, n) {
        var i = this;
        return n.map((function(n, e) {
            // Although array transforms are used with writes, the actual elements
            // being unioned or removed are not considered writes since they cannot
            // contain any FieldValue sentinels, etc.
            var r = new Ce(3 /* Argument */ , t, F.K);
            return i.Kc(n, r.Bc(e));
        }));
    }, t;
}();

/**
 * A placeholder object for DocumentReferences in this file, in order to
 * avoid a circular dependency. See the comments for `DataPreConverter` for
 * the full context.
 */
/**
 * Checks whether an object looks like a JSON object that should be converted
 * into a struct. Normal class/prototype instances are considered to look like
 * JSON objects since they should be converted to a struct value. Arrays, Dates,
 * GeoPoints, etc. are not considered to look like JSON objects since they map
 * to specific FieldValue types other than ObjectValue.
 */
function Oe(t) {
    return !("object" != typeof t || null === t || t instanceof Array || t instanceof Date || t instanceof O || t instanceof Te || t instanceof fe || t instanceof Re || t instanceof we);
}

function De(t, n, i) {
    if (!Oe(i) || !te(i)) {
        var e = ne(i);
        throw "an object" === e ? n.Qc(t + " a custom object") : n.Qc(t + " " + e);
    }
}

/**
 * Helper that calls fromDotSeparatedString() but wraps any error thrown.
 */ function Ve(t, n) {
    if (n instanceof de) return n.Rc;
    if ("string" == typeof n) return Pe(t, n);
    throw new E(T.INVALID_ARGUMENT, "Function " + t + "() called with invalid data. Field path arguments must be of type string or FieldPath.");
}

/**
 * Wraps fromDotSeparatedString with an error message about the method that
 * was thrown.
 * @param methodName The publicly visible method name
 * @param path The dot-separated string form of a field path which will be split
 * on dots.
 */ function Pe(t, n) {
    try {
        /**
 * Parses a field path string into a FieldPath, treating dots as separators.
 */
        return function(t) {
            if (t.search(ve) >= 0) throw new E(T.INVALID_ARGUMENT, "Invalid field path (" + t + "). Paths must not contain '~', '*', '/', '[', or ']'");
            try {
                return new (de.bind.apply(de, e([ void 0 ], t.split("."))));
            } catch (n) {
                throw new E(T.INVALID_ARGUMENT, "Invalid field path (" + t + "). Paths must not be empty, begin with '.', end with '.', or contain '..'");
            }
        }(n).Rc;
    } catch (n) {
        var i = ke(n);
        throw new E(T.INVALID_ARGUMENT, "Function " + t + "() called with invalid data. " + i);
    }
}

/**
 * Extracts the message from a caught exception, which should be an Error object
 * though JS doesn't guarantee that.
 */ function ke(t) {
    return t instanceof Error ? t.message : t.toString();
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Internal transaction object responsible for accumulating the mutations to
 * perform and the base versions for any documents read.
 */ var Fe = /** @class */ function() {
    function t(t) {
        this.ef = t, 
        // The version of each document that was read during this transaction.
        this.rf = ln(), this.mutations = [], this.sf = !1, 
        /**
         * A deferred usage error that occurred previously in this transaction that
         * will cause the transaction to fail once it actually commits.
         */
        this.uf = null, 
        /**
         * Set of documents that have been written in the transaction.
         *
         * When there's more than one write to the same key in a transaction, any
         * writes after the first are handled differently.
         */
        this.hf = new Set;
    }
    return t.prototype.lookup = function(t) {
        return r(this, void 0, void 0, (function() {
            var n, i = this;
            return s(this, (function(e) {
                switch (e.label) {
                  case 0:
                    if (this.af(), this.mutations.length > 0) throw new E(T.INVALID_ARGUMENT, "Firestore transactions require all reads to be executed before all writes.");
                    return [ 4 /*yield*/ , this.ef.lookup(t) ];

                  case 1:
                    return (n = e.sent()).forEach((function(t) {
                        t instanceof Lt || t instanceof Ut ? i.cf(t) : Qn("Document in a transaction was a " + t.constructor.name);
                    })), [ 2 /*return*/ , n ];
                }
            }));
        }));
    }, t.prototype.set = function(t, n) {
        this.write(n.Fc(t, this.tn(t))), this.hf.add(t);
    }, t.prototype.update = function(t, n) {
        try {
            this.write(n.Fc(t, this.ff(t)));
        } catch (t) {
            this.uf = t;
        }
        this.hf.add(t);
    }, t.prototype.delete = function(t) {
        this.write([ new Vt(t, this.tn(t)) ]), this.hf.add(t);
    }, t.prototype.commit = function() {
        return r(this, void 0, void 0, (function() {
            var t, n = this;
            return s(this, (function(i) {
                switch (i.label) {
                  case 0:
                    if (this.af(), this.uf) throw this.uf;
                    return t = this.rf, 
                    // For each mutation, note that the doc was written.
                    this.mutations.forEach((function(n) {
                        t = t.remove(n.key);
                    })), 
                    // For each document that was read but not written to, we want to perform
                    // a `verify` operation.
                    t.forEach((function(t, i) {
                        n.mutations.push(new Pt(t, n.tn(t)));
                    })), [ 4 /*yield*/ , this.ef.commit(this.mutations) ];

                  case 1:
                    return i.sent(), this.sf = !0, [ 2 /*return*/ ];
                }
            }));
        }));
    }, t.prototype.cf = function(t) {
        var n;
        if (t instanceof Ut) n = t.version; else {
            if (!(t instanceof Lt)) throw Qn("Document in a transaction was a " + t.constructor.name);
            // For deleted docs, we must use baseVersion 0 when we overwrite them.
            n = D.D();
        }
        var i = this.rf.get(t.key);
        if (null !== i) {
            if (!n.isEqual(i)) 
            // This transaction will fail no matter what.
            throw new E(T.ABORTED, "Document version changed between two reads.");
        } else this.rf = this.rf.st(t.key, n);
    }, 
    /**
     * Returns the version of this document when it was read in this transaction,
     * as a precondition, or no precondition if it was not read.
     */
    t.prototype.tn = function(t) {
        var n = this.rf.get(t);
        return !this.hf.has(t) && n ? Ct.updateTime(n) : Ct.NONE;
    }, 
    /**
     * Returns the precondition for a document if the operation is an update.
     */
    t.prototype.ff = function(t) {
        var n = this.rf.get(t);
        // The first time a document is written, we want to take into account the
        // read time and existence
                if (!this.hf.has(t) && n) {
            if (n.isEqual(D.D())) 
            // The document doesn't exist, so fail the transaction.
            // This has to be validated locally because you can't send a
            // precondition that a document does not exist without changing the
            // semantics of the backend write to be an insert. This is the reverse
            // of what we want, since we want to assert that the document doesn't
            // exist but then send the update and have it fail. Since we can't
            // express that to the backend, we have to validate locally.
            // Note: this can change once we can send separate verify writes in the
            // transaction.
            throw new E(T.INVALID_ARGUMENT, "Can't update a document that doesn't exist.");
            // Document exists, base precondition on document update time.
                        return Ct.updateTime(n);
        }
        // Document was not read, so we just use the preconditions for a blind
        // update.
        return Ct.exists(!0);
    }, t.prototype.write = function(t) {
        this.af(), this.mutations = this.mutations.concat(t);
    }, t.prototype.af = function() {
        Wn(!this.sf, "A transaction object cannot be used after its update callback has been invoked.");
    }, t;
}(), je = /** @class */ function() {
    function t(t, n) {
        this.kr = t, this.jo = n, 
        /** The current OnlineState. */
        this.state = "Unknown" /* Unknown */ , 
        /**
         * A count of consecutive failures to open the stream. If it reaches the
         * maximum defined by MAX_WATCH_STREAM_FAILURES, we'll set the OnlineState to
         * Offline.
         */
        this.lf = 0, 
        /**
         * A timer that elapses after ONLINE_STATE_TIMEOUT_MS, at which point we
         * transition from OnlineState.Unknown to OnlineState.Offline without waiting
         * for the stream to actually fail (MAX_WATCH_STREAM_FAILURES times).
         */
        this.df = null, 
        /**
         * Whether the client should log a warning message if it fails to connect to
         * the backend (initially true, cleared after a successful stream, or if we've
         * logged the message already).
         */
        this.vf = !0;
    }
    /**
     * Called by RemoteStore when a watch stream is started (including on each
     * backoff attempt).
     *
     * If this is the first attempt, it sets the OnlineState to Unknown and starts
     * the onlineStateTimer.
     */    return t.prototype.wf = function() {
        var t = this;
        0 === this.lf && (this.mf("Unknown" /* Unknown */), Wn(null === this.df, "onlineStateTimer shouldn't be started yet"), 
        this.df = this.kr.es("online_state_timeout" /* OnlineStateTimeout */ , 1e4, (function() {
            // NOTE: handleWatchStreamFailure() will continue to increment
            // watchStreamFailures even though we are already marked Offline,
            // but this is non-harmful.
            return t.df = null, Wn("Unknown" /* Unknown */ === t.state, "Timer should be canceled if we transitioned to a different state."), 
            t.pf("Backend didn't respond within 10 seconds."), t.mf("Offline" /* Offline */), 
            Promise.resolve();
        })));
    }, 
    /**
     * Updates our OnlineState as appropriate after the watch stream reports a
     * failure. The first failure moves us to the 'Unknown' state. We then may
     * allow multiple failures (based on MAX_WATCH_STREAM_FAILURES) before we
     * actually transition to the 'Offline' state.
     */
    t.prototype.gf = function(t) {
        "Online" /* Online */ === this.state ? (this.mf("Unknown" /* Unknown */), 
        // To get to OnlineState.Online, set() must have been called which would
        // have reset our heuristics.
        Wn(0 === this.lf, "watchStreamFailures must be 0"), Wn(null === this.df, "onlineStateTimer must be null")) : (this.lf++, 
        this.lf >= 1 && (this.bf(), this.pf("Connection failed 1 times. Most recent error: " + t.toString()), 
        this.mf("Offline" /* Offline */)));
    }, 
    /**
     * Explicitly sets the OnlineState to the specified state.
     *
     * Note that this resets our timers / failure counters, etc. used by our
     * Offline heuristics, so must not be used in place of
     * handleWatchStreamStart() and handleWatchStreamFailure().
     */
    t.prototype.set = function(t) {
        this.bf(), this.lf = 0, "Online" /* Online */ === t && (
        // We've connected to watch at least once. Don't warn the developer
        // about being offline going forward.
        this.vf = !1), this.mf(t);
    }, t.prototype.mf = function(t) {
        t !== this.state && (this.state = t, this.jo(t));
    }, t.prototype.pf = function(t) {
        var n = "Could not reach Cloud Firestore backend. " + t + "\nThis typically indicates that your device does not have a healthy Internet connection at the moment. The client will operate in offline mode until it is able to successfully connect to the backend.";
        this.vf ? (xn(n), this.vf = !1) : qn("OnlineStateTracker", n);
    }, t.prototype.bf = function() {
        null !== this.df && (this.df.cancel(), this.df = null);
    }, t;
}(), Ue = /** @class */ function() {
    function t(
    /**
     * The local store, used to fill the write pipeline with outbound mutations.
     */
    t, 
    /** The client-side proxy for interacting with the backend. */
    n, i, e, u) {
        var o = this;
        this.yf = t, this.ef = n, 
        /**
         * A list of up to MAX_PENDING_WRITES writes that we have fetched from the
         * LocalStore via fillWritePipeline() and have or will send to the write
         * stream.
         *
         * Whenever writePipeline.length > 0 the RemoteStore will attempt to start or
         * restart the write stream. When the stream is established the writes in the
         * pipeline will be sent in order.
         *
         * Writes remain in writePipeline until they are acknowledged by the backend
         * and thus will automatically be re-sent if the stream is interrupted /
         * restarted before they're acknowledged.
         *
         * Write responses from the backend are linked to their originating request
         * purely based on order, and so we can just shift() writes from the front of
         * the writePipeline as we receive responses.
         */
        this._f = [], 
        /**
         * A mapping of watched targets that the client cares about tracking and the
         * user has explicitly called a 'listen' for this target.
         *
         * These targets may or may not have been sent to or acknowledged by the
         * server. On re-establishing the listen stream, these targets should be sent
         * to the server. The targets removed with unlistens are removed eagerly
         * without waiting for confirmation from the listen stream.
         */
        this.Tf = new Map, this.Ef = null, 
        /**
         * Set to true by enableNetwork() and false by disableNetwork() and indicates
         * the user-preferred network state.
         */
        this.networkEnabled = !1, this.isPrimary = !1, this.Sf = u, this.Sf.Af((function(t) {
            i.Qr((function() {
                return r(o, void 0, void 0, (function() {
                    return s(this, (function(t) {
                        switch (t.label) {
                          case 0:
                            return this.If() ? (qn("RemoteStore", "Restarting streams for network reachability change."), 
                            [ 4 /*yield*/ , this.Cf() ]) : [ 3 /*break*/ , 2 ];

                          case 1:
                            t.sent(), t.label = 2;

                          case 2:
                            return [ 2 /*return*/ ];
                        }
                    }));
                }));
            }));
        })), this.Rf = new je(i, e), 
        // Create streams (but note they're not started yet).
        this.Nf = this.ef.Ec({
            lc: this.Of.bind(this),
            oc: this.Df.bind(this),
            vc: this.Vf.bind(this)
        }), this.Pf = this.ef.Tc({
            lc: this.kf.bind(this),
            oc: this.Ff.bind(this),
            yc: this.jf.bind(this),
            bc: this.bc.bind(this)
        });
    }
    /**
     * Starts up the remote store, creating streams, restoring state from
     * LocalStore, etc.
     */    return t.prototype.start = function() {
        return this.enableNetwork();
    }, 
    /** Re-enables the network. Idempotent. */
    t.prototype.enableNetwork = function() {
        return r(this, void 0, void 0, (function() {
            var t;
            return s(this, (function(n) {
                switch (n.label) {
                  case 0:
                    return this.networkEnabled = !0, this.If() ? (t = this.Pf, [ 4 /*yield*/ , this.yf.Gu() ]) : [ 3 /*break*/ , 3 ];

                  case 1:
                    // This will start the write stream if necessary.
                    return t.lastStreamToken = n.sent(), this.Uf() ? this.Lf() : this.Rf.set("Unknown" /* Unknown */), 
                    [ 4 /*yield*/ , this.Mf() ];

                  case 2:
                    // This will start the write stream if necessary.
                    n.sent(), n.label = 3;

                  case 3:
                    return [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    /**
     * Temporarily disables the network. The network can be re-enabled using
     * enableNetwork().
     */
    t.prototype.disableNetwork = function() {
        return r(this, void 0, void 0, (function() {
            return s(this, (function(t) {
                switch (t.label) {
                  case 0:
                    return this.networkEnabled = !1, [ 4 /*yield*/ , this.qf() ];

                  case 1:
                    return t.sent(), 
                    // Set the OnlineState to Offline so get()s return from cache, etc.
                    this.Rf.set("Offline" /* Offline */), [ 2 /*return*/ ];
                }
            }));
        }));
    }, t.prototype.qf = function() {
        return r(this, void 0, void 0, (function() {
            return s(this, (function(t) {
                switch (t.label) {
                  case 0:
                    return [ 4 /*yield*/ , this.Pf.stop() ];

                  case 1:
                    return t.sent(), [ 4 /*yield*/ , this.Nf.stop() ];

                  case 2:
                    return t.sent(), this._f.length > 0 && (qn("RemoteStore", "Stopping write stream with " + this._f.length + " pending writes"), 
                    this._f = []), this.xf(), [ 2 /*return*/ ];
                }
            }));
        }));
    }, t.prototype.shutdown = function() {
        return r(this, void 0, void 0, (function() {
            return s(this, (function(t) {
                switch (t.label) {
                  case 0:
                    return qn("RemoteStore", "RemoteStore shutting down."), this.networkEnabled = !1, 
                    [ 4 /*yield*/ , this.qf() ];

                  case 1:
                    return t.sent(), this.Sf.shutdown(), 
                    // Set the OnlineState to Unknown (rather than Offline) to avoid potentially
                    // triggering spurious listener events with cached data, etc.
                    this.Rf.set("Unknown" /* Unknown */), [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    /**
     * Starts new listen for the given target. Uses resume token if provided. It
     * is a no-op if the target of given `TargetData` is already being listened to.
     */
    t.prototype.listen = function(t) {
        this.Tf.has(t.targetId) || (
        // Mark this as something the client is currently listening for.
        this.Tf.set(t.targetId, t), this.Uf() ? 
        // The listen will be sent in onWatchStreamOpen
        this.Lf() : this.Nf.Za() && this.Bf(t));
    }, 
    /**
     * Removes the listen from server. It is a no-op if the given target id is
     * not being listened to.
     */
    t.prototype.Qf = function(t) {
        Wn(this.Tf.has(t), "unlisten called on target no currently watched: " + t), this.Tf.delete(t), 
        this.Nf.Za() && this.Wf(t), 0 === this.Tf.size && (this.Nf.Za() ? this.Nf.ic() : this.If() && 
        // Revert to OnlineState.Unknown if the watch stream is not open and we
        // have no listeners, since without any listens to send we cannot
        // confirm if the stream is healthy and upgrade to OnlineState.Online.
        this.Rf.set("Unknown" /* Unknown */));
    }, 
    /** {@link TargetMetadataProvider.getTargetDataForTarget} */
    t.prototype.ae = function(t) {
        return this.Tf.get(t) || null;
    }, 
    /** {@link TargetMetadataProvider.getRemoteKeysForTarget} */
    t.prototype.he = function(t) {
        return this.Fo.he(t);
    }, 
    /**
     * We need to increment the the expected number of pending responses we're due
     * from watch so we wait for the ack to process any messages from this target.
     */
    t.prototype.Bf = function(t) {
        this.Ef.qi(t.targetId), this.Nf.watch(t);
    }, 
    /**
     * We need to increment the expected number of pending responses we're due
     * from watch so we wait for the removal on the server before we process any
     * messages from this target.
     */
    t.prototype.Wf = function(t) {
        this.Ef.qi(t), this.Nf.wc(t);
    }, t.prototype.Lf = function() {
        Wn(this.Uf(), "startWatchStream() called when shouldStartWatchStream() is false."), 
        this.Ef = new In(this), this.Nf.start(), this.Rf.wf();
    }, 
    /**
     * Returns whether the watch stream should be started because it's necessary
     * and has not yet been started.
     */
    t.prototype.Uf = function() {
        return this.If() && !this.Nf.$a() && this.Tf.size > 0;
    }, t.prototype.If = function() {
        return this.isPrimary && this.networkEnabled;
    }, t.prototype.xf = function() {
        this.Ef = null;
    }, t.prototype.Of = function() {
        return r(this, void 0, void 0, (function() {
            var t = this;
            return s(this, (function(n) {
                return this.Tf.forEach((function(n, i) {
                    t.Bf(n);
                })), [ 2 /*return*/ ];
            }));
        }));
    }, t.prototype.Df = function(t) {
        return r(this, void 0, void 0, (function() {
            return s(this, (function(n) {
                return void 0 === t && 
                // Graceful stop (due to stop() or idle timeout). Make sure that's
                // desirable.
                Wn(!this.Uf(), "Watch stream was stopped gracefully while still needed."), this.xf(), 
                // If we still need the watch stream, retry the connection.
                this.Uf() ? (this.Rf.gf(t), this.Lf()) : 
                // No need to restart watch stream because there are no active targets.
                // The online state is set to unknown because there is no active attempt
                // at establishing a connection
                this.Rf.set("Unknown" /* Unknown */), [ 2 /*return*/ ];
            }));
        }));
    }, t.prototype.Vf = function(t, n) {
        return r(this, void 0, void 0, (function() {
            var i;
            return s(this, (function(e) {
                switch (e.label) {
                  case 0:
                    // Mark the client as online since we got a message from the server
                    return this.Rf.set("Online" /* Online */), t instanceof Sn && 2 /* Removed */ === t.state && t.cause ? [ 2 /*return*/ , this.Gf(t) ] : (t instanceof Tn ? this.Ef.Hi(t) : t instanceof En ? this.Ef.ie(t) : (Wn(t instanceof Sn, "Expected watchChange to be an instance of WatchTargetChange"), 
                    this.Ef.Xi(t)), n.isEqual(D.MIN) ? [ 3 /*break*/ , 3 ] : [ 4 /*yield*/ , this.yf.Ju() ]);

                  case 1:
                    return i = e.sent(), n.V(i) >= 0 ? [ 4 /*yield*/ , this.zf(n) ] : [ 3 /*break*/ , 3 ];

                    // We have received a target change with a global snapshot if the snapshot
                    // version is not equal to SnapshotVersion.MIN.
                                      case 2:
                    // We have received a target change with a global snapshot if the snapshot
                    // version is not equal to SnapshotVersion.MIN.
                    e.sent(), e.label = 3;

                  case 3:
                    return [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    /**
     * Takes a batch of changes from the Datastore, repackages them as a
     * RemoteEvent, and passes that on to the listener, which is typically the
     * SyncEngine.
     */
    t.prototype.zf = function(t) {
        var n = this;
        Wn(!t.isEqual(D.MIN), "Can't raise event for unknown SnapshotVersion");
        var i = this.Ef.se(t);
        // Update in-memory resume tokens. LocalStore will update the
        // persistent view of these when applying the completed RemoteEvent.
                // Finally raise remote event
        return i.wi.forEach((function(i, e) {
            if (i.resumeToken.Mt() > 0) {
                var r = n.Tf.get(e);
                // A watched target might have been removed already.
                                r && n.Tf.set(e, r.ei(i.resumeToken, t));
            }
        })), 
        // Re-establish listens for the targets that have been invalidated by
        // existence filter mismatches.
        i.mi.forEach((function(t) {
            var i = n.Tf.get(t);
            if (i) {
                // Clear the resume token for the target, since we're in a known mismatch
                // state.
                n.Tf.set(t, i.ei(z.qt, i.ni)), 
                // Cause a hard reset by unwatching and rewatching immediately, but
                // deliberately don't send a resume token so that we get a full update.
                n.Wf(t);
                // Mark the target we send as being on behalf of an existence filter
                // mismatch, but don't actually retain that in listenTargets. This ensures
                // that we flag the first re-listen this way without impacting future
                // listens of this target (that might happen e.g. on reconnect).
                var e = new nn(i.target, t, 1 /* ExistenceFilterMismatch */ , i.sequenceNumber);
                n.Bf(e);
            }
        })), this.Fo.Hu(i);
    }, 
    /** Handles an error on a target */
    t.prototype.Gf = function(t) {
        var n = this;
        Wn(!!t.cause, "Handling target error without a cause");
        var i = t.cause, e = Promise.resolve();
        return t.targetIds.forEach((function(t) {
            e = e.then((function() {
                return r(n, void 0, void 0, (function() {
                    return s(this, (function(n) {
                        // A watched target might have been removed already.
                        return this.Tf.has(t) ? (this.Tf.delete(t), this.Ef.removeTarget(t), [ 2 /*return*/ , this.Fo.Jf(t, i) ]) : [ 2 /*return*/ ];
                    }));
                }));
            }));
        })), e;
    }, 
    /**
     * Attempts to fill our write pipeline with writes from the LocalStore.
     *
     * Called internally to bootstrap or refill the write pipeline and by
     * SyncEngine whenever there are new mutations to process.
     *
     * Starts the write stream if necessary.
     */
    t.prototype.Mf = function() {
        return r(this, void 0, void 0, (function() {
            var t, n;
            return s(this, (function(i) {
                switch (i.label) {
                  case 0:
                    return this.Hf() ? (t = this._f.length > 0 ? this._f[this._f.length - 1].batchId : -1, 
                    [ 4 /*yield*/ , this.yf.so(t) ]) : [ 3 /*break*/ , 4 ];

                  case 1:
                    return null !== (n = i.sent()) ? [ 3 /*break*/ , 2 ] : (0 === this._f.length && this.Pf.ic(), 
                    [ 3 /*break*/ , 4 ]);

                  case 2:
                    return this.Yf(n), [ 4 /*yield*/ , this.Mf() ];

                  case 3:
                    i.sent(), i.label = 4;

                  case 4:
                    return this.Kf() && this.Xf(), [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    /**
     * Returns true if we can add to the write pipeline (i.e. the network is
     * enabled and the write pipeline is not full).
     */
    t.prototype.Hf = function() {
        return this.If() && this._f.length < 10;
    }, 
    // For testing
    t.prototype.$f = function() {
        return this._f.length;
    }, 
    /**
     * Queues additional writes to be sent to the write stream, sending them
     * immediately if the write stream is established.
     */
    t.prototype.Yf = function(t) {
        Wn(this.Hf(), "addToWritePipeline called when pipeline is full"), this._f.push(t), 
        this.Pf.Za() && this.Pf.pc && this.Pf.gc(t.mutations);
    }, t.prototype.Kf = function() {
        return this.If() && !this.Pf.$a() && this._f.length > 0;
    }, t.prototype.Xf = function() {
        Wn(this.Kf(), "startWriteStream() called when shouldStartWriteStream() is false."), 
        this.Pf.start();
    }, t.prototype.kf = function() {
        return r(this, void 0, void 0, (function() {
            return s(this, (function(t) {
                return this.Pf._c(), [ 2 /*return*/ ];
            }));
        }));
    }, t.prototype.jf = function() {
        var t = this;
        // Record the stream token.
                return this.yf.zu(this.Pf.lastStreamToken).then((function() {
            // Send the write pipeline now that the stream is established.
            for (var n = 0, i = t._f; n < i.length; n++) {
                var e = i[n];
                t.Pf.gc(e.mutations);
            }
        })).catch(fi);
    }, t.prototype.bc = function(t, n) {
        var i = this;
        // This is a response to a write containing mutations and should be
        // correlated to the first write in our write pipeline.
                Wn(this._f.length > 0, "Got result for empty write pipeline");
        var e = this._f.shift(), r = Xn.from(e, t, n, this.Pf.lastStreamToken);
        return this.Fo.Zf(r).then((function() {
            // It's possible that with the completion of this mutation another
            // slot has freed up.
            return i.Mf();
        }));
    }, t.prototype.Ff = function(t) {
        return r(this, void 0, void 0, (function() {
            var n = this;
            return s(this, (function(i) {
                // If the write stream closed due to an error, invoke the error callbacks if
                // there are pending writes.
                return void 0 === t && 
                // Graceful stop (due to stop() or idle timeout). Make sure that's
                // desirable.
                Wn(!this.Kf(), "Write stream was stopped gracefully while still needed."), t && this._f.length > 0 ? (void 0, 
                [ 2 /*return*/ , (this.Pf.pc ? this.tl(t) : this.nl(t)).then((function() {
                    // The write stream might have been started by refilling the write
                    // pipeline for failed writes
                    n.Kf() && n.Xf();
                })) ]) : [ 2 /*return*/ ];
            }));
        }));
    }, t.prototype.nl = function(t) {
        return r(this, void 0, void 0, (function() {
            return s(this, (function(n) {
                // Reset the token if it's a permanent error, signaling the write stream is
                // no longer valid. Note that the handshake does not count as a write: see
                // comments on isPermanentWriteError for details.
                return rn(t.code) ? (qn("RemoteStore", "RemoteStore error before completed handshake; resetting stream token: ", this.Pf.lastStreamToken), 
                this.Pf.lastStreamToken = z.qt, [ 2 /*return*/ , this.yf.zu(z.qt).catch(fi) ]) : [ 2 /*return*/ ];
            }));
        }));
    }, t.prototype.tl = function(t) {
        return r(this, void 0, void 0, (function() {
            var n, i = this;
            return s(this, (function(e) {
                // Only handle permanent errors here. If it's transient, just let the retry
                // logic kick in.
                return rn(r = t.code) && r !== T.ABORTED ? (n = this._f.shift(), 
                // In this case it's also unlikely that the server itself is melting
                // down -- this was just a bad request so inhibit backoff on the next
                // restart.
                this.Pf.nc(), [ 2 /*return*/ , this.Fo.il(n.batchId, t).then((function() {
                    // It's possible that with the completion of this mutation
                    // another slot has freed up.
                    return i.Mf();
                })) ]) : [ 2 /*return*/ ];
                var r;
                /**
 * Maps an error Code from a GRPC status identifier like 'NOT_FOUND'.
 *
 * @returns The Code equivalent to the given status string or undefined if
 *     there is no match.
 */            }));
        }));
    }, t.prototype.el = function() {
        return new Fe(this.ef);
    }, t.prototype.Cf = function() {
        return r(this, void 0, void 0, (function() {
            return s(this, (function(t) {
                switch (t.label) {
                  case 0:
                    return this.networkEnabled = !1, [ 4 /*yield*/ , this.qf() ];

                  case 1:
                    return t.sent(), this.Rf.set("Unknown" /* Unknown */), [ 4 /*yield*/ , this.enableNetwork() ];

                  case 2:
                    return t.sent(), [ 2 /*return*/ ];
                }
            }));
        }));
    }, t.prototype.rl = function() {
        return r(this, void 0, void 0, (function() {
            return s(this, (function(t) {
                switch (t.label) {
                  case 0:
                    return this.If() ? (
                    // Tear down and re-create our network streams. This will ensure we get a fresh auth token
                    // for the new user and re-fill the write pipeline with new mutations from the LocalStore
                    // (since mutations are per-user).
                    qn("RemoteStore", "RemoteStore restarting streams for new credential"), [ 4 /*yield*/ , this.Cf() ]) : [ 3 /*break*/ , 2 ];

                  case 1:
                    t.sent(), t.label = 2;

                  case 2:
                    return [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    /**
     * Toggles the network state when the client gains or loses its primary lease.
     */
    t.prototype.sl = function(t) {
        return r(this, void 0, void 0, (function() {
            return s(this, (function(n) {
                switch (n.label) {
                  case 0:
                    return this.isPrimary = t, t && this.networkEnabled ? [ 4 /*yield*/ , this.enableNetwork() ] : [ 3 /*break*/ , 2 ];

                  case 1:
                    return n.sent(), [ 3 /*break*/ , 4 ];

                  case 2:
                    return t ? [ 3 /*break*/ , 4 ] : [ 4 /*yield*/ , this.qf() ];

                  case 3:
                    n.sent(), this.Rf.set("Unknown" /* Unknown */), n.label = 4;

                  case 4:
                    return [ 2 /*return*/ ];
                }
            }));
        }));
    }, t;
}(), Le = function(t) {
    this.key = t;
}, Me = function(t) {
    this.key = t;
}, qe = /** @class */ function() {
    function t(t, 
    /** Documents included in the remote target */
    n) {
        this.query = t, this.ul = n, this.ol = null, 
        /**
         * A flag whether the view is current with the backend. A view is considered
         * current after it has seen the current flag from the backend and did not
         * lose consistency within the watch stream (e.g. because of an existence
         * filter mismatch).
         */
        this._i = !1, 
        /** Documents in the view but not in the remote target */
        this.hl = vn(), 
        /** Document Keys that have local changes */
        this.fi = vn(), this.al = new pn(t.Bn.bind(t));
    }
    return Object.defineProperty(t.prototype, "cl", {
        /**
         * The set of remote documents that the server has told us belongs to the target associated with
         * this view.
         */
        get: function() {
            return this.ul;
        },
        enumerable: !0,
        configurable: !0
    }), 
    /**
     * Iterates over a set of doc changes, applies the query limit, and computes
     * what the new results should be, what the changes were, and whether we may
     * need to go back to the local cache for more results. Does not make any
     * changes to the view.
     * @param docChanges The doc changes to apply to this view.
     * @param previousChanges If this is being called with a refill, then start
     *        with this set of docs and changes instead of the current view.
     * @return a new set of docs, changes, and refill flag.
     */
    t.prototype.fl = function(t, n) {
        var i = this, e = n ? n.ll : new gn, r = n ? n.al : this.al, s = n ? n.fi : this.fi, u = r, o = !1, h = this.query.Jn() && r.size === this.query.limit ? r.last() : null, a = this.query.Hn() && r.size === this.query.limit ? r.first() : null;
        // Drop documents out to meet limit/limitToLast requirement.
        if (t.at((function(t, n) {
            var c = r.get(t), f = n instanceof Ut ? n : null;
            f && (Wn(t.isEqual(f.key), "Mismatching keys found in document changes: " + t + " != " + f.key), 
            f = i.query.matches(f) ? f : null);
            var l = !!c && i.fi.has(c.key), d = !!f && (f.nn || 
            // We only consider committed mutations for documents that were
            // mutated during the lifetime of the view.
            i.fi.has(f.key) && f.hasCommittedMutations), v = !1;
            // Calculate change
            c && f ? c.data().isEqual(f.data()) ? l !== d && (e.track({
                type: 3 /* Metadata */ ,
                doc: f
            }), v = !0) : i.dl(c, f) || (e.track({
                type: 2 /* Modified */ ,
                doc: f
            }), v = !0, (h && i.query.Bn(f, h) > 0 || a && i.query.Bn(f, a) < 0) && (
            // This doc moved from inside the limit to outside the limit.
            // That means there may be some other doc in the local cache
            // that should be included instead.
            o = !0)) : !c && f ? (e.track({
                type: 0 /* Added */ ,
                doc: f
            }), v = !0) : c && !f && (e.track({
                type: 1 /* Removed */ ,
                doc: c
            }), v = !0, (h || a) && (
            // A doc was removed from a full limit query. We'll need to
            // requery from the local cache to see if we know about some other
            // doc that should be in the results.
            o = !0));
            v && (f ? (u = u.add(f), s = d ? s.add(t) : s.delete(t)) : (u = u.delete(t), s = s.delete(t)));
        })), this.query.Jn() || this.query.Hn()) for (;u.size > this.query.limit; ) {
            var c = this.query.Jn() ? u.last() : u.first();
            u = u.delete(c.key), s = s.delete(c.key), e.track({
                type: 1 /* Removed */ ,
                doc: c
            });
        }
        return Wn(!o || !n, "View was refilled using docs that themselves needed refilling."), 
        {
            al: u,
            ll: e,
            Qh: o,
            fi: s
        };
    }, t.prototype.dl = function(t, n) {
        // We suppress the initial change event for documents that were modified as
        // part of a write acknowledgment (e.g. when the value of a server transform
        // is applied) as Watch will send us the same document again.
        // By suppressing the event, we only raise two user visible events (one with
        // `hasPendingWrites` and the final state of the document) instead of three
        // (one with `hasPendingWrites`, the modified document with
        // `hasPendingWrites` and the final state of the document).
        return t.nn && n.hasCommittedMutations && !n.nn;
    }, 
    /**
     * Updates the view with the given ViewDocumentChanges and optionally updates
     * limbo docs and sync state from the provided target change.
     * @param docChanges The set of changes to make to the view's docs.
     * @param updateLimboDocuments Whether to update limbo documents based on this
     *        change.
     * @param targetChange A target change to apply for computing limbo docs and
     *        sync state.
     * @return A new ViewChange with the given docs, changes, and sync state.
     */
    // PORTING NOTE: The iOS/Android clients always compute limbo document changes.
    t.prototype.hu = function(t, n, i) {
        var e = this;
        Wn(!t.Qh, "Cannot apply changes that need a refill");
        var r = this.al;
        this.al = t.al, this.fi = t.fi;
        // Sort changes based on type and query comparator
        var s = t.ll.ai();
        s.sort((function(t, n) {
            return function(t, n) {
                var i = function(t) {
                    switch (t) {
                      case 0 /* Added */ :
                        return 1;

                      case 2 /* Modified */ :
                      case 3 /* Metadata */ :
                        // A metadata change is converted to a modified change at the public
                        // api layer.  Since we sort by document key and then change type,
                        // metadata and modified changes must be sorted equivalently.
                        return 2;

                      case 1 /* Removed */ :
                        return 0;

                      default:
                        return Qn("Unknown ChangeType: " + t);
                    }
                };
                return i(t) - i(n);
            }
            /**
 * @license
 * Copyright 2019 Google Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ (t.type, n.type) || e.query.Bn(t.doc, n.doc);
        })), this.vl(i);
        var u = n ? this.wl() : [], o = 0 === this.hl.size && this._i ? 1 /* Synced */ : 0 /* Local */ , h = o !== this.ol;
        return this.ol = o, 0 !== s.length || h ? {
            snapshot: new bn(this.query, t.al, r, s, t.fi, 0 /* Local */ === o, h, 
            /* excludesMetadataChanges= */ !1),
            ml: u
        } : {
            ml: u
        };
    }, 
    /**
     * Applies an OnlineState change to the view, potentially generating a
     * ViewChange if the view's syncState changes as a result.
     */
    t.prototype.pl = function(t) {
        return this._i && "Offline" /* Offline */ === t ? (
        // If we're offline, set `current` to false and then call applyChanges()
        // to refresh our syncState and generate a ViewChange as appropriate. We
        // are guaranteed to get a new TargetChange that sets `current` back to
        // true once the client is back online.
        this._i = !1, this.hu({
            al: this.al,
            ll: new gn,
            fi: this.fi,
            Qh: !1
        }, 
        /* updateLimboDocuments= */ !1)) : {
            ml: []
        };
    }, 
    /**
     * Returns whether the doc for the given key should be in limbo.
     */
    t.prototype.gl = function(t) {
        // If the remote end says it's part of this query, it's not in limbo.
        return !this.ul.has(t) && (
        // The local store doesn't think it's a result, so it shouldn't be in limbo.
        !!this.al.has(t) && !this.al.get(t).nn);
    }, 
    /**
     * Updates syncedDocuments, current, and limbo docs based on the given change.
     * Returns the list of changes to which docs are in limbo.
     */
    t.prototype.vl = function(t) {
        var n = this;
        t && (t.Ti.forEach((function(t) {
            return n.ul = n.ul.add(t);
        })), t.Ei.forEach((function(t) {
            return Wn(n.ul.has(t), "Modified document " + t + " not found in view.");
        })), t.Si.forEach((function(t) {
            return n.ul = n.ul.delete(t);
        })), this._i = t._i);
    }, t.prototype.wl = function() {
        var t = this;
        // We can only determine limbo documents when we're in-sync with the server.
                if (!this._i) return [];
        // TODO(klimt): Do this incrementally so that it's not quadratic when
        // updating many documents.
                var n = this.hl;
        this.hl = vn(), this.al.forEach((function(n) {
            t.gl(n.key) && (t.hl = t.hl.add(n.key));
        }));
        // Diff the new limbo docs with the old limbo docs.
        var i = [];
        return n.forEach((function(n) {
            t.hl.has(n) || i.push(new Me(n));
        })), this.hl.forEach((function(t) {
            n.has(t) || i.push(new Le(t));
        })), i;
    }, 
    /**
     * Update the in-memory state of the current view with the state read from
     * persistence.
     *
     * We update the query view whenever a client's primary status changes:
     * - When a client transitions from primary to secondary, it can miss
     *   LocalStorage updates and its query views may temporarily not be
     *   synchronized with the state on disk.
     * - For secondary to primary transitions, the client needs to update the list
     *   of `syncedDocuments` since secondary clients update their query views
     *   based purely on synthesized RemoteEvents.
     *
     * @param queryResult.documents - The documents that match the query according
     * to the LocalStore.
     * @param queryResult.remoteKeys - The keys of the documents that match the
     * query according to the backend.
     *
     * @return The ViewChange that resulted from this synchronization.
     */
    // PORTING NOTE: Multi-tab only.
    t.prototype.bl = function(t) {
        this.ul = t.po, this.hl = vn();
        var n = this.fl(t.documents);
        return this.hu(n, /*updateLimboDocuments=*/ !0);
    }, 
    /**
     * Returns a view snapshot as if this query was just listened to. Contains
     * a document add for every existing document and the `fromCache` and
     * `hasPendingWrites` status of the already established view.
     */
    // PORTING NOTE: Multi-tab only.
    t.prototype.yl = function() {
        return bn.vi(this.query, this.al, this.fi, 0 /* Local */ === this.ol);
    }, t;
}();

/**
 * @license
 * Copyright 2018 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ var xe = /** @class */ function() {
    function t(t, n, i, e) {
        this.kr = t, this._l = n, this.updateFunction = i, this.Lr = e, this.Tl = 5, this.Xa = new ji(this.kr, "retry_transaction" /* RetryTransaction */);
    }
    /** Runs the transaction and sets the result on deferred. */    return t.prototype.run = function() {
        this.El();
    }, t.prototype.El = function() {
        var t = this;
        this.Xa.Ga((function() {
            return r(t, void 0, void 0, (function() {
                var t, n, i = this;
                return s(this, (function(e) {
                    return t = this._l.el(), (n = this.Sl(t)) && n.then((function(n) {
                        i.kr.Qr((function() {
                            return t.commit().then((function() {
                                i.Lr.resolve(n);
                            })).catch((function(t) {
                                i.Al(t);
                            }));
                        }));
                    })).catch((function(t) {
                        i.Al(t);
                    })), [ 2 /*return*/ ];
                }));
            }));
        }));
    }, t.prototype.Sl = function(t) {
        try {
            var n = this.updateFunction(t);
            return !J(n) && n.catch && n.then ? n : (this.Lr.reject(Error("Transaction callback must return a Promise")), 
            null);
        } catch (t) {
            // Do not retry errors thrown by user provided updateFunction.
            return this.Lr.reject(t), null;
        }
    }, t.prototype.Al = function(t) {
        var n = this;
        this.Tl > 0 && this.Il(t) ? (this.Tl -= 1, this.kr.Qr((function() {
            return n.El(), Promise.resolve();
        }))) : this.Lr.reject(t);
    }, t.prototype.Il = function(t) {
        if ("FirebaseError" === t.name) {
            // In transactions, the backend will fail outdated reads with FAILED_PRECONDITION and
            // non-matching document versions with ABORTED. These errors should be retried.
            var n = t.code;
            return "aborted" === n || "failed-precondition" === n || !rn(n);
        }
        return !1;
    }, t;
}(), Be = function(
/**
     * The query itself.
     */
t, 
/**
     * The target number created by the client that is used in the watch
     * stream to identify this query.
     */
n, 
/**
     * The view is responsible for computing the final merged truth of what
     * docs are in the query. It gets notified of local and remote changes,
     * and applies the query filters and limits to determine the most correct
     * possible results.
     */
i) {
    this.query = t, this.targetId = n, this.view = i;
}, Qe = function(t) {
    this.key = t, 
    /**
         * Set to true once we've received a document. This is used in
         * getRemoteKeysForTarget() and ultimately used by WatchChangeAggregator to
         * decide whether it needs to manufacture a delete event for the target once
         * the target is CURRENT.
         */
    this.Cl = !1;
}, We = /** @class */ function() {
    function t(t, n, 
    // PORTING NOTE: Manages state synchronization in multi-tab environments.
    i, e, r) {
        this.yf = t, this._l = n, this.Rl = i, this.currentUser = e, this.Nl = r, this.Ol = null, 
        this.Dl = new $n((function(t) {
            return t.canonicalId();
        })), this.Vl = new Map, 
        /**
         * The keys of documents that are in limbo for which we haven't yet started a
         * limbo resolution query.
         */
        this.Pl = [], 
        /**
         * Keeps track of the target ID for each document that is in limbo with an
         * active target.
         */
        this.kl = new U(j.F), 
        /**
         * Keeps track of the information about an active limbo resolution for each
         * active target ID that was started for the purpose of limbo resolution.
         */
        this.Fl = new Map, this.jl = new oi, 
        /** Stores user completion handlers, indexed by User and BatchId. */
        this.Ul = {}, 
        /** Stores user callbacks waiting for all pending writes to be acknowledged. */
        this.Ll = new Map, this.Ml = Ni.ca(), 
        // The primary state is set to `true` or `false` immediately after Firestore
        // startup. In the interim, a client should only be considered primary if
        // `isPrimary` is true.
        this.isPrimary = void 0, this.onlineState = "Unknown" /* Unknown */;
    }
    return Object.defineProperty(t.prototype, "ql", {
        // Only used for testing.
        get: function() {
            return !0 === this.isPrimary;
        },
        enumerable: !0,
        configurable: !0
    }), 
    /** Subscribes to SyncEngine notifications. Has to be called exactly once. */
    t.prototype.subscribe = function(t) {
        Wn(null !== t, "SyncEngine listener cannot be null"), Wn(null === this.Ol, "SyncEngine already has a subscriber."), 
        this.Ol = t;
    }, 
    /**
     * Initiates the new listen, resolves promise when listen enqueued to the
     * server. All the subsequent view snapshots or errors are sent to the
     * subscribed handlers. Returns the targetId of the query.
     */
    t.prototype.listen = function(t) {
        return r(this, void 0, void 0, (function() {
            var n, i, e, r, u;
            return s(this, (function(s) {
                switch (s.label) {
                  case 0:
                    return this.xl("listen()"), (e = this.Dl.get(t)) ? (
                    // PORTING NOTE: With Multi-Tab Web, it is possible that a query view
                    // already exists when EventManager calls us for the first time. This
                    // happens when the primary tab is already listening to this query on
                    // behalf of another tab and the user of the primary also starts listening
                    // to the query. EventManager will not have an assigned target ID in this
                    // case and calls `listen` to obtain this ID.
                    n = e.targetId, this.Rl.eh(n), i = e.view.yl(), [ 3 /*break*/ , 4 ]) : [ 3 /*break*/ , 1 ];

                  case 1:
                    return [ 4 /*yield*/ , this.yf.ao(t.xn()) ];

                  case 2:
                    return r = s.sent(), u = this.Rl.eh(r.targetId), n = r.targetId, [ 4 /*yield*/ , this.Bl(t, n, "current" === u) ];

                  case 3:
                    i = s.sent(), this.isPrimary && this._l.listen(r), s.label = 4;

                  case 4:
                    return this.Ol.vc([ i ]), [ 2 /*return*/ , n ];
                }
            }));
        }));
    }, 
    /**
     * Registers a view for a previously unknown query and computes its initial
     * snapshot.
     */
    t.prototype.Bl = function(t, n, i) {
        return r(this, void 0, void 0, (function() {
            var e, r, u, o, h, a;
            return s(this, (function(s) {
                switch (s.label) {
                  case 0:
                    return [ 4 /*yield*/ , this.yf.vo(t, 
                    /* usePreviousResults= */ !0) ];

                  case 1:
                    return e = s.sent(), r = new qe(t, e.po), u = r.fl(e.documents), o = _n.yi(n, i && "Offline" /* Offline */ !== this.onlineState), 
                    Wn(0 === (h = r.hu(u, 
                    /* updateLimboDocuments= */ !0 === this.isPrimary, o)).ml.length, "View returned limbo docs before target ack from the server."), 
                    Wn(!!h.snapshot, "applyChanges for new view should always return a snapshot"), a = new Be(t, n, r), 
                    this.Dl.set(t, a), this.Vl.has(n) ? this.Vl.get(n).push(t) : this.Vl.set(n, [ t ]), 
                    [ 2 /*return*/ , h.snapshot ];
                }
            }));
        }));
    }, 
    /**
     * Reconcile the list of synced documents in an existing view with those
     * from persistence.
     */
    // PORTING NOTE: Multi-tab only.
    t.prototype.Ql = function(t) {
        return r(this, void 0, void 0, (function() {
            var n, i;
            return s(this, (function(e) {
                switch (e.label) {
                  case 0:
                    return [ 4 /*yield*/ , this.yf.vo(t.query, 
                    /* usePreviousResults= */ !0) ];

                  case 1:
                    return n = e.sent(), i = t.view.bl(n), this.isPrimary && this.Wl(t.targetId, i.ml), 
                    [ 2 /*return*/ , i ];
                }
            }));
        }));
    }, 
    /** Stops listening to the query. */
    t.prototype.Qf = function(t) {
        return r(this, void 0, void 0, (function() {
            var n, i, e = this;
            return s(this, (function(r) {
                switch (r.label) {
                  case 0:
                    return this.xl("unlisten()"), Wn(!!(n = this.Dl.get(t)), "Trying to unlisten on query not found:" + t), 
                    (i = this.Vl.get(n.targetId)).length > 1 ? (this.Vl.set(n.targetId, i.filter((function(n) {
                        return !n.isEqual(t);
                    }))), this.Dl.delete(t), [ 2 /*return*/ ]) : this.isPrimary ? (
                    // We need to remove the local query target first to allow us to verify
                    // whether any other client is still interested in this target.
                    this.Rl.sh(n.targetId), this.Rl.$o(n.targetId) ? [ 3 /*break*/ , 2 ] : [ 4 /*yield*/ , this.yf.do(n.targetId, /*keepPersistedTargetData=*/ !1).then((function() {
                        e.Rl.oh(n.targetId), e._l.Qf(n.targetId), e.Gl(n.targetId);
                    })).catch(fi) ]) : [ 3 /*break*/ , 3 ];

                  case 1:
                    r.sent(), r.label = 2;

                  case 2:
                    return [ 3 /*break*/ , 5 ];

                  case 3:
                    return this.Gl(n.targetId), [ 4 /*yield*/ , this.yf.do(n.targetId, 
                    /*keepPersistedTargetData=*/ !0) ];

                  case 4:
                    r.sent(), r.label = 5;

                  case 5:
                    return [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    /**
     * Initiates the write of local mutation batch which involves adding the
     * writes to the mutation queue, notifying the remote store about new
     * mutations and raising events for any changes this write caused.
     *
     * The promise returned by this call is resolved when the above steps
     * have completed, *not* when the write was acked by the backend. The
     * userCallback is resolved once the write was acked/rejected by the
     * backend (or failed locally for any other reason).
     */
    t.prototype.write = function(t, n) {
        return r(this, void 0, void 0, (function() {
            var i;
            return s(this, (function(e) {
                switch (e.label) {
                  case 0:
                    return this.xl("write()"), [ 4 /*yield*/ , this.yf.Vu(t) ];

                  case 1:
                    return i = e.sent(), this.Rl.Zo(i.batchId), this.zl(i.batchId, n), [ 4 /*yield*/ , this.Jl(i.Zs) ];

                  case 2:
                    return e.sent(), [ 4 /*yield*/ , this._l.Mf() ];

                  case 3:
                    return e.sent(), [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    /**
     * Takes an updateFunction in which a set of reads and writes can be performed
     * atomically. In the updateFunction, the client can read and write values
     * using the supplied transaction object. After the updateFunction, all
     * changes will be committed. If a retryable error occurs (ex: some other
     * client has changed any of the data referenced), then the updateFunction
     * will be called again after a backoff. If the updateFunction still fails
     * after all retries, then the transaction will be rejected.
     *
     * The transaction object passed to the updateFunction contains methods for
     * accessing documents and collections. Unlike other datastore access, data
     * accessed with the transaction will not reflect local changes that have not
     * been committed. For this reason, it is required that all reads are
     * performed before any writes. Transactions must be performed while online.
     *
     * The Deferred input is resolved when the transaction is fully committed.
     */
    t.prototype.runTransaction = function(t, n, i) {
        new xe(t, this._l, n, i).run();
    }, t.prototype.Hu = function(t) {
        return r(this, void 0, void 0, (function() {
            var n, i = this;
            return s(this, (function(e) {
                switch (e.label) {
                  case 0:
                    this.xl("applyRemoteEvent()"), e.label = 1;

                  case 1:
                    return e.trys.push([ 1, 4, , 6 ]), [ 4 /*yield*/ , this.yf.Hu(t) ];

                  case 2:
                    return n = e.sent(), 
                    // Update `receivedDocument` as appropriate for any limbo targets.
                    t.wi.forEach((function(t, n) {
                        var e = i.Fl.get(n);
                        e && (
                        // Since this is a limbo resolution lookup, it's for a single document
                        // and it could be added, modified, or removed, but not a combination.
                        Wn(t.Ti.size + t.Ei.size + t.Si.size <= 1, "Limbo resolution for single document contains multiple changes."), 
                        t.Ti.size > 0 ? e.Cl = !0 : t.Ei.size > 0 ? Wn(e.Cl, "Received change for limbo target document without add.") : t.Si.size > 0 && (Wn(e.Cl, "Received remove for limbo target document without add."), 
                        e.Cl = !1));
                    })), [ 4 /*yield*/ , this.Jl(n, t) ];

                  case 3:
                    return e.sent(), [ 3 /*break*/ , 6 ];

                  case 4:
                    return [ 4 /*yield*/ , fi(e.sent()) ];

                  case 5:
                    return e.sent(), [ 3 /*break*/ , 6 ];

                  case 6:
                    return [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    /**
     * Applies an OnlineState change to the sync engine and notifies any views of
     * the change.
     */
    t.prototype.pl = function(t, n) {
        // If we are the secondary client, we explicitly ignore the remote store's
        // online state (the local client may go offline, even though the primary
        // tab remains online) and only apply the primary tab's online state from
        // SharedClientState.
        if (this.isPrimary && 0 /* RemoteStore */ === n || !this.isPrimary && 1 /* SharedClientState */ === n) {
            this.xl("applyOnlineStateChange()");
            var i = [];
            this.Dl.forEach((function(n, e) {
                var r = e.view.pl(t);
                Wn(0 === r.ml.length, "OnlineState should not affect limbo documents."), r.snapshot && i.push(r.snapshot);
            })), this.Ol.Hl(t), this.Ol.vc(i), this.onlineState = t, this.isPrimary && this.Rl.fh(t);
        }
    }, t.prototype.Jf = function(t, n) {
        return r(this, void 0, void 0, (function() {
            var i, e, r, u, o, h = this;
            return s(this, (function(s) {
                switch (s.label) {
                  case 0:
                    return this.xl("rejectListens()"), 
                    // PORTING NOTE: Multi-tab only.
                    this.Rl.hh(t, "rejected", n), i = this.Fl.get(t), (e = i && i.key) ? (
                    // Since this query failed, we won't want to manually unlisten to it.
                    // So go ahead and remove it from bookkeeping.
                    this.kl = this.kl.remove(e), this.Fl.delete(t), this.Yl(), r = (r = new U(j.F)).st(e, new Lt(e, D.D())), 
                    u = vn().add(e), o = new yn(D.MIN, 
                    /* targetChanges= */ new Map, 
                    /* targetMismatches= */ new x(zn), r, u), [ 2 /*return*/ , this.Hu(o) ]) : [ 3 /*break*/ , 1 ];

                  case 1:
                    return [ 4 /*yield*/ , this.yf.do(t, /* keepPersistedTargetData */ !1).then((function() {
                        return h.Gl(t, n);
                    })).catch(fi) ];

                  case 2:
                    s.sent(), s.label = 3;

                  case 3:
                    return [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    // PORTING NOTE: Multi-tab only
    t.prototype.yh = function(t, n, i) {
        return r(this, void 0, void 0, (function() {
            var e;
            return s(this, (function(r) {
                switch (r.label) {
                  case 0:
                    return this.xl("applyBatchState()"), [ 4 /*yield*/ , this.yf.ku(t) ];

                  case 1:
                    return null === (e = r.sent()) ? (
                    // A throttled tab may not have seen the mutation before it was completed
                    // and removed from the mutation queue, in which case we won't have cached
                    // the affected documents. In this case we can safely ignore the update
                    // since that means we didn't apply the mutation locally at all (if we
                    // had, we would have cached the affected documents), and so we will just
                    // see any resulting document changes via normal remote document updates
                    // as applicable.
                    qn("SyncEngine", "Cannot apply mutation batch with id: " + t), [ 2 /*return*/ ]) : "pending" !== n ? [ 3 /*break*/ , 3 ] : [ 4 /*yield*/ , this._l.Mf() ];

                  case 2:
                    // If we are the primary client, we need to send this write to the
                    // backend. Secondary clients will ignore these writes since their remote
                    // connection is disabled.
                    return r.sent(), [ 3 /*break*/ , 4 ];

                  case 3:
                    "acknowledged" === n || "rejected" === n ? (
                    // NOTE: Both these methods are no-ops for batches that originated from
                    // other clients.
                    this.Kl(t, i || null), this.yf._o(t)) : Qn("Unknown batchState: " + n), r.label = 4;

                  case 4:
                    return [ 4 /*yield*/ , this.Jl(e) ];

                  case 5:
                    return r.sent(), [ 2 /*return*/ ];
                }
            }));
        }));
    }, t.prototype.Zf = function(t) {
        return r(this, void 0, void 0, (function() {
            var n, i;
            return s(this, (function(e) {
                switch (e.label) {
                  case 0:
                    this.xl("applySuccessfulWrite()"), n = t.batch.batchId, 
                    // The local store may or may not be able to apply the write result and
                    // raise events immediately (depending on whether the watcher is caught
                    // up), so we raise user callbacks first so that they consistently happen
                    // before listen events.
                    this.Kl(n, /*error=*/ null), this.Xl(n), e.label = 1;

                  case 1:
                    return e.trys.push([ 1, 4, , 6 ]), [ 4 /*yield*/ , this.yf.ju(t) ];

                  case 2:
                    return i = e.sent(), this.Rl.nh(n, "acknowledged"), [ 4 /*yield*/ , this.Jl(i) ];

                  case 3:
                    return e.sent(), [ 3 /*break*/ , 6 ];

                  case 4:
                    return [ 4 /*yield*/ , fi(e.sent()) ];

                  case 5:
                    return e.sent(), [ 3 /*break*/ , 6 ];

                  case 6:
                    return [ 2 /*return*/ ];
                }
            }));
        }));
    }, t.prototype.il = function(t, n) {
        return r(this, void 0, void 0, (function() {
            var i;
            return s(this, (function(e) {
                switch (e.label) {
                  case 0:
                    this.xl("rejectFailedWrite()"), 
                    // The local store may or may not be able to apply the write result and
                    // raise events immediately (depending on whether the watcher is caught up),
                    // so we raise user callbacks first so that they consistently happen before
                    // listen events.
                    this.Kl(t, n), this.Xl(t), e.label = 1;

                  case 1:
                    return e.trys.push([ 1, 4, , 6 ]), [ 4 /*yield*/ , this.yf.xu(t) ];

                  case 2:
                    return i = e.sent(), this.Rl.nh(t, "rejected", n), [ 4 /*yield*/ , this.Jl(i) ];

                  case 3:
                    return e.sent(), [ 3 /*break*/ , 6 ];

                  case 4:
                    return [ 4 /*yield*/ , fi(e.sent()) ];

                  case 5:
                    return e.sent(), [ 3 /*break*/ , 6 ];

                  case 6:
                    return [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    /**
     * Registers a user callback that resolves when all pending mutations at the moment of calling
     * are acknowledged .
     */
    t.prototype.$l = function(t) {
        return r(this, void 0, void 0, (function() {
            var n, i;
            return s(this, (function(e) {
                switch (e.label) {
                  case 0:
                    return this._l.If() || qn("SyncEngine", "The network is disabled. The task returned by 'awaitPendingWrites()' will not complete until the network is enabled."), 
                    [ 4 /*yield*/ , this.yf.Wu() ];

                  case 1:
                    return -1 === (n = e.sent()) ? (
                    // Trigger the callback right away if there is no pending writes at the moment.
                    t.resolve(), [ 2 /*return*/ ]) : ((i = this.Ll.get(n) || []).push(t), this.Ll.set(n, i), 
                    [ 2 /*return*/ ]);
                }
            }));
        }));
    }, 
    /**
     * Triggers the callbacks that are waiting for this batch id to get acknowledged by server,
     * if there are any.
     */
    t.prototype.Xl = function(t) {
        (this.Ll.get(t) || []).forEach((function(t) {
            t.resolve();
        })), this.Ll.delete(t);
    }, 
    /** Reject all outstanding callbacks waiting for pending writes to complete. */
    t.prototype.Zl = function(t) {
        this.Ll.forEach((function(n) {
            n.forEach((function(n) {
                n.reject(new E(T.CANCELLED, t));
            }));
        })), this.Ll.clear();
    }, t.prototype.zl = function(t, n) {
        var i = this.Ul[this.currentUser.i()];
        i || (i = new U(zn)), i = i.st(t, n), this.Ul[this.currentUser.i()] = i;
    }, 
    /**
     * Resolves or rejects the user callback for the given batch and then discards
     * it.
     */
    t.prototype.Kl = function(t, n) {
        var i = this.Ul[this.currentUser.i()];
        // NOTE: Mutations restored from persistence won't have callbacks, so it's
        // okay for there to be no callback for this ID.
                if (i) {
            var e = i.get(t);
            e && (Wn(t === i.ot(), "Mutation callbacks processed out-of-order?"), n ? e.reject(n) : e.resolve(), 
            i = i.remove(t)), this.Ul[this.currentUser.i()] = i;
        }
    }, t.prototype.Gl = function(t, n) {
        var i = this;
        void 0 === n && (n = null), this.Rl.sh(t), Wn(this.Vl.has(t) && 0 !== this.Vl.get(t).length, "There are no queries mapped to target id " + t);
        for (var e = 0, r = this.Vl.get(t); e < r.length; e++) {
            var s = r[e];
            this.Dl.delete(s), n && this.Ol.td(s, n);
        }
        if (this.Vl.delete(t), this.isPrimary) {
            var u = this.jl.Ks(t);
            this.jl.Hs(t), u.forEach((function(t) {
                i.jl.Xs(t) || 
                // We removed the last reference for this key
                i.nd(t);
            }));
        }
    }, t.prototype.nd = function(t) {
        // It's possible that the target already got removed because the query failed. In that case,
        // the key won't exist in `limboTargetsByKey`. Only do the cleanup if we still have the target.
        var n = this.kl.get(t);
        null !== n && (this._l.Qf(n), this.kl = this.kl.remove(t), this.Fl.delete(n), this.Yl());
    }, t.prototype.Wl = function(t, n) {
        for (var i = 0, e = n; i < e.length; i++) {
            var r = e[i];
            if (r instanceof Le) this.jl.Qs(r.key, t), this.ed(r); else if (r instanceof Me) {
                qn("SyncEngine", "Document no longer in limbo: " + r.key), this.jl.Gs(r.key, t), 
                this.jl.Xs(r.key) || 
                // We removed the last reference for this key
                this.nd(r.key);
            } else Qn("Unknown limbo change: " + JSON.stringify(r));
        }
    }, t.prototype.ed = function(t) {
        var n = t.key;
        this.kl.get(n) || (qn("SyncEngine", "New document in limbo: " + n), this.Pl.push(n), 
        this.Yl());
    }, 
    /**
     * Starts listens for documents in limbo that are enqueued for resolution,
     * subject to a maximum number of concurrent resolutions.
     *
     * Without bounding the number of concurrent resolutions, the server can fail
     * with "resource exhausted" errors which can lead to pathological client
     * behavior as seen in https://github.com/firebase/firebase-js-sdk/issues/2683.
     */
    t.prototype.Yl = function() {
        for (;this.Pl.length > 0 && this.kl.size < this.Nl; ) {
            var t = this.Pl.shift(), n = this.Ml.next();
            this.Fl.set(n, new Qe(t)), this.kl = this.kl.st(t, n), this._l.listen(new nn(xt.Nn(t.path).xn(), n, 2 /* LimboResolution */ , ti.Pr));
        }
    }, 
    // Visible for testing
    t.prototype.rd = function() {
        return this.kl;
    }, 
    // Visible for testing
    t.prototype.sd = function() {
        return this.Pl;
    }, t.prototype.Jl = function(t, n) {
        return r(this, void 0, void 0, (function() {
            var i, e, r, u = this;
            return s(this, (function(s) {
                switch (s.label) {
                  case 0:
                    return i = [], e = [], r = [], this.Dl.forEach((function(s, o) {
                        r.push(Promise.resolve().then((function() {
                            var n = o.view.fl(t);
                            return n.Qh ? u.yf.vo(o.query, /* usePreviousResults= */ !1).then((function(t) {
                                var i = t.documents;
                                return o.view.fl(i, n);
                            })) : n;
                            // The query has a limit and some docs were removed, so we need
                            // to re-run the query against the local store to make sure we
                            // didn't lose any good docs that had been past the limit.
                                                })).then((function(t) {
                            var r = n && n.wi.get(o.targetId), s = o.view.hu(t, 
                            /* updateLimboDocuments= */ !0 === u.isPrimary, r);
                            if (u.Wl(o.targetId, s.ml), s.snapshot) {
                                u.isPrimary && u.Rl.hh(o.targetId, s.snapshot.fromCache ? "not-current" : "current"), 
                                i.push(s.snapshot);
                                var h = Zn.Rr(o.targetId, s.snapshot);
                                e.push(h);
                            }
                        })));
                    })), [ 4 /*yield*/ , Promise.all(r) ];

                  case 1:
                    return s.sent(), this.Ol.vc(i), [ 4 /*yield*/ , this.yf.ro(e) ];

                  case 2:
                    return s.sent(), [ 2 /*return*/ ];
                }
            }));
        }));
    }, t.prototype.xl = function(t) {
        Wn(null !== this.Ol, "Trying to call " + t + " before calling subscribe().");
    }, t.prototype.rl = function(t) {
        return r(this, void 0, void 0, (function() {
            var n, i;
            return s(this, (function(e) {
                switch (e.label) {
                  case 0:
                    return n = !this.currentUser.isEqual(t), this.currentUser = t, n ? (
                    // Fails tasks waiting for pending writes requested by previous user.
                    this.Zl("'waitForPendingWrites' promise is rejected due to a user change."), [ 4 /*yield*/ , this.yf.Iu(t) ]) : [ 3 /*break*/ , 3 ];

                  case 1:
                    return i = e.sent(), 
                    // TODO(b/114226417): Consider calling this only in the primary tab.
                    this.Rl.Iu(t, i.Ou, i.Du), [ 4 /*yield*/ , this.Jl(i.Nu) ];

                  case 2:
                    e.sent(), e.label = 3;

                  case 3:
                    return [ 4 /*yield*/ , this._l.rl() ];

                  case 4:
                    return e.sent(), [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    // PORTING NOTE: Multi-tab only
    t.prototype.sl = function(t) {
        return r(this, void 0, void 0, (function() {
            var n, i, e, r, u, o, h = this;
            return s(this, (function(s) {
                switch (s.label) {
                  case 0:
                    return !0 !== t || !0 === this.isPrimary ? [ 3 /*break*/ , 3 ] : (this.isPrimary = !0, 
                    [ 4 /*yield*/ , this._l.sl(!0) ]);

                  case 1:
                    return s.sent(), u = this.Rl.Xo(), [ 4 /*yield*/ , this.ud(u.J()) ];

                  case 2:
                    for (n = s.sent(), i = 0, e = n; i < e.length; i++) r = e[i], this._l.listen(r);
                    return [ 3 /*break*/ , 7 ];

                  case 3:
                    return !1 !== t || !1 === this.isPrimary ? [ 3 /*break*/ , 7 ] : (this.isPrimary = !1, 
                    u = [], o = Promise.resolve(), this.Vl.forEach((function(t, n) {
                        h.Rl.uh(n) ? u.push(n) : o = o.then((function() {
                            return h.Gl(n), h.yf.do(n, 
                            /*keepPersistedTargetData=*/ !0);
                        })), h._l.Qf(n);
                    })), [ 4 /*yield*/ , o ]);

                  case 4:
                    return s.sent(), [ 4 /*yield*/ , this.ud(u) ];

                  case 5:
                    return s.sent(), this.od(), [ 4 /*yield*/ , this._l.sl(!1) ];

                  case 6:
                    s.sent(), s.label = 7;

                  case 7:
                    return [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    // PORTING NOTE: Multi-tab only.
    t.prototype.od = function() {
        var t = this;
        this.Fl.forEach((function(n, i) {
            t._l.Qf(i);
        })), this.jl.Ys(), this.Fl = new Map, this.kl = new U(j.F);
    }, 
    /**
     * Reconcile the query views of the provided query targets with the state from
     * persistence. Raises snapshots for any changes that affect the local
     * client and returns the updated state of all target's query data.
     */
    // PORTING NOTE: Multi-tab only.
    t.prototype.ud = function(t) {
        return r(this, void 0, void 0, (function() {
            var n, i, e, r, u, o, h, a, c, f, l, d, v;
            return s(this, (function(s) {
                switch (s.label) {
                  case 0:
                    n = [], i = [], e = 0, r = t, s.label = 1;

                  case 1:
                    return e < r.length ? (u = r[e], o = void 0, (h = this.Vl.get(u)) && 0 !== h.length ? [ 4 /*yield*/ , this.yf.do(u, 
                    /*keepPersistedTargetData=*/ !0) ] : [ 3 /*break*/ , 8 ]) : [ 3 /*break*/ , 14 ];

                  case 2:
                    // For queries that have a local View, we need to update their state
                    // in LocalStore (as the resume token and the snapshot version
                    // might have changed) and reconcile their views with the persisted
                    // state (the list of syncedDocuments may have gotten out of sync).
                    return s.sent(), [ 4 /*yield*/ , this.yf.ao(h[0].xn()) ];

                  case 3:
                    o = s.sent(), a = 0, c = h, s.label = 4;

                  case 4:
                    return a < c.length ? (f = c[a], Wn(!!(l = this.Dl.get(f)), "No query view found for " + f), 
                    [ 4 /*yield*/ , this.Ql(l) ]) : [ 3 /*break*/ , 7 ];

                  case 5:
                    (d = s.sent()).snapshot && i.push(d.snapshot), s.label = 6;

                  case 6:
                    return a++, [ 3 /*break*/ , 4 ];

                  case 7:
                    return [ 3 /*break*/ , 12 ];

                  case 8:
                    return Wn(!0 === this.isPrimary, "A secondary tab should never have an active target without an active query."), 
                    [ 4 /*yield*/ , this.yf.Ao(u) ];

                  case 9:
                    return Wn(!!(v = s.sent()), "Target for id " + u + " not found"), [ 4 /*yield*/ , this.yf.ao(v) ];

                  case 10:
                    return o = s.sent(), [ 4 /*yield*/ , this.Bl(this.hd(v), u, 
                    /*current=*/ !1) ];

                  case 11:
                    s.sent(), s.label = 12;

                  case 12:
                    n.push(o), s.label = 13;

                  case 13:
                    return e++, [ 3 /*break*/ , 1 ];

                  case 14:
                    return this.Ol.vc(i), [ 2 /*return*/ , n ];
                }
            }));
        }));
    }, 
    /**
     * Creates a `Query` object from the specified `Target`. There is no way to
     * obtain the original `Query`, so we synthesize a `Query` from the `Target`
     * object.
     *
     * The synthesized result might be different from the original `Query`, but
     * since the synthesized `Query` should return the same results as the
     * original one (only the presentation of results might differ), the potential
     * difference will not cause issues.
     */
    // PORTING NOTE: Multi-tab only
    t.prototype.hd = function(t) {
        return new xt(t.path, t.collectionGroup, t.orderBy, t.filters, t.limit, "F" /* First */ , t.startAt, t.endAt);
    }, 
    // PORTING NOTE: Multi-tab only
    t.prototype.yo = function() {
        return this.yf.yo();
    }, 
    // PORTING NOTE: Multi-tab only
    t.prototype._h = function(t, n, i) {
        return r(this, void 0, void 0, (function() {
            var e, r;
            return s(this, (function(s) {
                switch (s.label) {
                  case 0:
                    if (this.isPrimary) 
                    // If we receive a target state notification via WebStorage, we are
                    // either already secondary or another tab has taken the primary lease.
                    return qn("SyncEngine", "Ignoring unexpected query state notification."), [ 2 /*return*/ ];
                    if (!this.Vl.has(t)) return [ 3 /*break*/ , 7 ];
                    switch (n) {
                      case "current":
                      case "not-current":
                        return [ 3 /*break*/ , 1 ];

                      case "rejected":
                        return [ 3 /*break*/ , 4 ];
                    }
                    return [ 3 /*break*/ , 6 ];

                  case 1:
                    return [ 4 /*yield*/ , this.yf.Io() ];

                  case 2:
                    return e = s.sent(), r = yn.bi(t, "current" === n), [ 4 /*yield*/ , this.Jl(e, r) ];

                  case 3:
                    return s.sent(), [ 3 /*break*/ , 7 ];

                  case 4:
                    return [ 4 /*yield*/ , this.yf.do(t, 
                    /* keepPersistedTargetData */ !0) ];

                  case 5:
                    return s.sent(), this.Gl(t, i), [ 3 /*break*/ , 7 ];

                  case 6:
                    Qn("Unexpected target state: " + n), s.label = 7;

                  case 7:
                    return [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    // PORTING NOTE: Multi-tab only
    t.prototype.Th = function(t, n) {
        return r(this, void 0, void 0, (function() {
            var i, e, r, u, o, h, a, c, f, l = this;
            return s(this, (function(d) {
                switch (d.label) {
                  case 0:
                    if (!this.isPrimary) return [ 2 /*return*/ ];
                    i = 0, e = t, d.label = 1;

                  case 1:
                    return i < e.length ? (f = e[i], Wn(!this.Vl.has(f), "Trying to add an already active target"), 
                    [ 4 /*yield*/ , this.yf.Ao(f) ]) : [ 3 /*break*/ , 6 ];

                  case 2:
                    return Wn(!!(r = d.sent()), "Query data for active target " + f + " not found"), 
                    [ 4 /*yield*/ , this.yf.ao(r) ];

                  case 3:
                    return u = d.sent(), [ 4 /*yield*/ , this.Bl(this.hd(r), u.targetId, 
                    /*current=*/ !1) ];

                  case 4:
                    d.sent(), this._l.listen(u), d.label = 5;

                  case 5:
                    return i++, [ 3 /*break*/ , 1 ];

                  case 6:
                    o = function(t) {
                        return s(this, (function(n) {
                            switch (n.label) {
                              case 0:
                                // Check that the target is still active since the target might have been
                                // removed if it has been rejected by the backend.
                                return h.Vl.has(t) ? [ 4 /*yield*/ , h.yf.do(t, /* keepPersistedTargetData */ !1).then((function() {
                                    l._l.Qf(t), l.Gl(t);
                                })).catch(fi) ] : [ 2 /*return*/ , "continue" ];

                                // Release queries that are still active.
                                                              case 1:
                                // Release queries that are still active.
                                return n.sent(), [ 2 /*return*/ ];
                            }
                        }));
                    }, h = this, a = 0, c = n, d.label = 7;

                  case 7:
                    return a < c.length ? (f = c[a], [ 5 /*yield**/ , o(f) ]) : [ 3 /*break*/ , 10 ];

                  case 8:
                    d.sent(), d.label = 9;

                  case 9:
                    return a++, [ 3 /*break*/ , 7 ];

                  case 10:
                    return [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    // PORTING NOTE: Multi-tab only. In other clients, LocalStore is unaware of
    // the online state.
    t.prototype.enableNetwork = function() {
        return this.yf.Eo(!0), this._l.enableNetwork();
    }, 
    // PORTING NOTE: Multi-tab only. In other clients, LocalStore is unaware of
    // the online state.
    t.prototype.disableNetwork = function() {
        return this.yf.Eo(!1), this._l.disableNetwork();
    }, t.prototype.he = function(t) {
        var n = this.Fl.get(t);
        if (n && n.Cl) return vn().add(n.key);
        var i = vn(), e = this.Vl.get(t);
        if (!e) return i;
        for (var r = 0, s = e; r < s.length; r++) {
            var u = s[r], o = this.Dl.get(u);
            Wn(!!o, "No query view found for " + u), i = i.Ft(o.view.cl);
        }
        return i;
    }, t;
}(), Ge = function() {
    this.ad = null, this.targetId = 0, this.listeners = [];
}, ze = /** @class */ function() {
    function t(t) {
        this.Fo = t, this.fd = new $n((function(t) {
            return t.canonicalId();
        })), this.onlineState = "Unknown" /* Unknown */ , this.ld = new Set, this.Fo.subscribe(this);
    }
    return t.prototype.listen = function(t) {
        var n = t.query, i = !1, e = this.fd.get(n);
        (e || (i = !0, e = new Ge, this.fd.set(n, e)), e.listeners.push(t), Wn(!t.pl(this.onlineState), "applyOnlineStateChange() shouldn't raise an event for brand-new listeners."), 
        e.ad) && (t.dd(e.ad) && this.vd());
        return i ? this.Fo.listen(n).then((function(t) {
            return e.targetId = t, t;
        })) : Promise.resolve(e.targetId);
    }, t.prototype.Qf = function(t) {
        return r(this, void 0, void 0, (function() {
            var n, i, e, r;
            return s(this, (function(s) {
                return n = t.query, i = !1, (e = this.fd.get(n)) && (r = e.listeners.indexOf(t)) >= 0 && (e.listeners.splice(r, 1), 
                i = 0 === e.listeners.length), i ? (this.fd.delete(n), [ 2 /*return*/ , this.Fo.Qf(n) ]) : [ 2 /*return*/ ];
            }));
        }));
    }, t.prototype.vc = function(t) {
        for (var n = !1, i = 0, e = t; i < e.length; i++) {
            var r = e[i], s = r.query, u = this.fd.get(s);
            if (u) {
                for (var o = 0, h = u.listeners; o < h.length; o++) {
                    h[o].dd(r) && (n = !0);
                }
                u.ad = r;
            }
        }
        n && this.vd();
    }, t.prototype.td = function(t, n) {
        var i = this.fd.get(t);
        if (i) for (var e = 0, r = i.listeners; e < r.length; e++) {
            r[e].onError(n);
        }
        // Remove all listeners. NOTE: We don't need to call syncEngine.unlisten()
        // after an error.
                this.fd.delete(t);
    }, t.prototype.Hl = function(t) {
        this.onlineState = t;
        var n = !1;
        this.fd.forEach((function(i, e) {
            for (var r = 0, s = e.listeners; r < s.length; r++) {
                // Run global snapshot listeners if a consistent snapshot has been emitted.
                s[r].pl(t) && (n = !0);
            }
        })), n && this.vd();
    }, t.prototype.wd = function(t) {
        this.ld.add(t), 
        // Immediately fire an initial event, indicating all existing listeners
        // are in-sync.
        t.next();
    }, t.prototype.md = function(t) {
        this.ld.delete(t);
    }, 
    // Call all global snapshot listeners that have been set.
    t.prototype.vd = function() {
        this.ld.forEach((function(t) {
            t.next();
        }));
    }, t;
}(), Je = /** @class */ function() {
    function t(t, n, i) {
        this.query = t, this.pd = n, 
        /**
         * Initial snapshots (e.g. from cache) may not be propagated to the wrapped
         * observer. This flag is set to true once we've actually raised an event.
         */
        this.gd = !1, this.bd = null, this.onlineState = "Unknown" /* Unknown */ , this.options = i || {};
    }
    /**
     * Applies the new ViewSnapshot to this listener, raising a user-facing event
     * if applicable (depending on what changed, whether the user has opted into
     * metadata-only changes, etc.). Returns true if a user-facing event was
     * indeed raised.
     */    return t.prototype.dd = function(t) {
        if (Wn(t.docChanges.length > 0 || t.li, "We got a new snapshot with no changes?"), 
        !this.options.includeMetadataChanges) {
            for (
            // Remove the metadata only changes.
            var n = [], i = 0, e = t.docChanges; i < e.length; i++) {
                var r = e[i];
                3 /* Metadata */ !== r.type && n.push(r);
            }
            t = new bn(t.query, t.docs, t.ci, n, t.fi, t.fromCache, t.li, 
            /* excludesMetadataChanges= */ !0);
        }
        var s = !1;
        return this.gd ? this.yd(t) && (this.pd.next(t), s = !0) : this._d(t, this.onlineState) && (this.Td(t), 
        s = !0), this.bd = t, s;
    }, t.prototype.onError = function(t) {
        this.pd.error(t);
    }, 
    /** Returns whether a snapshot was raised. */
    t.prototype.pl = function(t) {
        this.onlineState = t;
        var n = !1;
        return this.bd && !this.gd && this._d(this.bd, t) && (this.Td(this.bd), n = !0), 
        n;
    }, t.prototype._d = function(t, n) {
        // Always raise the first event when we're synced
        if (Wn(!this.gd, "Determining whether to raise first event but already had first event"), 
        !t.fromCache) return !0;
        // NOTE: We consider OnlineState.Unknown as online (it should become Offline
        // or Online if we wait long enough).
                var i = "Offline" /* Offline */ !== n;
        // Don't raise the event if we're online, aren't synced yet (checked
        // above) and are waiting for a sync.
                return this.options.Ed && i ? (Wn(t.fromCache, "Waiting for sync, but snapshot is not from cache"), 
        !1) : !t.docs.M() || "Offline" /* Offline */ === n;
        // Raise data from cache if we have any documents or we are offline
        }, t.prototype.yd = function(t) {
        // We don't need to handle includeDocumentMetadataChanges here because
        // the Metadata only changes have already been stripped out if needed.
        // At this point the only changes we will see are the ones we should
        // propagate.
        if (t.docChanges.length > 0) return !0;
        var n = this.bd && this.bd.hasPendingWrites !== t.hasPendingWrites;
        return !(!t.li && !n) && !0 === this.options.includeMetadataChanges;
        // Generally we should have hit one of the cases above, but it's possible
        // to get here if there were only metadata docChanges and they got
        // stripped out.
        }, t.prototype.Td = function(t) {
        Wn(!this.gd, "Trying to raise initial events for second time"), t = bn.vi(t.query, t.docs, t.fi, t.fromCache), 
        this.gd = !0, this.pd.next(t);
    }, t;
}(), He = /** @class */ function() {
    function t(t, n, i, 
    /**
     * Asynchronous queue responsible for all of our internal processing. When
     * we get incoming work from the user (via public API) or the network
     * (incoming GRPC messages), we should always schedule onto this queue.
     * This ensures all of our work is properly serialized (e.g. we don't
     * start processing a new operation while the previous one is waiting for
     * an async I/O to complete).
     */
    e) {
        this.platform = t, this.Sd = n, this.credentials = i, this.kr = e, this.clientId = Gn.pr();
    }
    /**
     * Starts up the FirestoreClient, returning only whether or not enabling
     * persistence succeeded.
     *
     * The intent here is to "do the right thing" as far as users are concerned.
     * Namely, in cases where offline persistence is requested and possible,
     * enable it, but otherwise fall back to persistence disabled. For the most
     * part we expect this to succeed one way or the other so we don't expect our
     * users to actually wait on the firestore.enablePersistence Promise since
     * they generally won't care.
     *
     * Of course some users actually do care about whether or not persistence
     * was successfully enabled, so the Promise returned from this method
     * indicates this outcome.
     *
     * This presents a problem though: even before enablePersistence resolves or
     * rejects, users may have made calls to e.g. firestore.collection() which
     * means that the FirestoreClient in there will be available and will be
     * enqueuing actions on the async queue.
     *
     * Meanwhile any failure of an operation on the async queue causes it to
     * panic and reject any further work, on the premise that unhandled errors
     * are fatal.
     *
     * Consequently the fallback is handled internally here in start, and if the
     * fallback succeeds we signal success to the async queue even though the
     * start() itself signals failure.
     *
     * @param persistenceProvider Provider that returns the persistence
     *    implementation.
     * @param persistenceSettings Settings object to configure offline
     *     persistence.
     * @returns A deferred result indicating the user-visible result of enabling
     *     offline persistence. This method will reject this if IndexedDB fails to
     *     start for any reason. If usePersistence is false this is
     *     unconditionally resolved.
     */    return t.prototype.start = function(t, n) {
        var i = this;
        this.Ad();
        // We defer our initialization until we get the current user from
        // setChangeListener(). We block the async queue until we got the initial
        // user and the initialization is completed. This will prevent any scheduled
        // work from happening before initialization is completed.
        // If initializationDone resolved then the FirestoreClient is in a usable
        // state.
        var e = new ni, r = new ni, s = !1;
        // If usePersistence is true, certain classes of errors while starting are
        // recoverable but only by falling back to persistence disabled.
        
        // If there's an error in the first case but not in recovery we cannot
        // reject the promise blocking the async queue because this will cause the
        // async queue to panic.
                // Return only the result of enabling persistence. Note that this does not
        // need to await the completion of initializationDone because the result of
        // this method should not reflect any other kind of failure to start.
        return this.credentials.v((function(u) {
            s ? i.kr.Qr((function() {
                return i.rl(u);
            })) : (s = !0, i.Id(t, n, u, r).then((function() {
                return i.Cd(u);
            })).then(e.resolve, e.reject));
        })), 
        // Block the async queue until initialization is done
        this.kr.Qr((function() {
            return e.promise;
        })), r.promise;
    }, 
    /** Enables the network connection and requeues all pending operations. */
    t.prototype.enableNetwork = function() {
        var t = this;
        return this.Ad(), this.kr.enqueue((function() {
            return t.Fo.enableNetwork();
        }));
    }, 
    /**
     * Initializes persistent storage, attempting to use IndexedDB if
     * usePersistence is true or memory-only if false.
     *
     * If IndexedDB fails because it's already open in another tab or because the
     * platform can't possibly support our implementation then this method rejects
     * the persistenceResult and falls back on memory-only persistence.
     *
     * @param persistenceProvider The provider that provides either IndexedDb or
     *     memory-backed persistence
     * @param persistenceSettings Settings object to configure offline persistence
     * @param user The initial user
     * @param persistenceResult A deferred result indicating the user-visible
     *     result of enabling offline persistence. This method will reject this if
     *     IndexedDB fails to start for any reason. If usePersistence is false
     *     this is unconditionally resolved.
     * @returns a Promise indicating whether or not initialization should
     *     continue, i.e. that one of the persistence implementations actually
     *     succeeded.
     */
    t.prototype.Id = function(t, n, i, e) {
        return r(this, void 0, void 0, (function() {
            var r;
            return s(this, (function(s) {
                switch (s.label) {
                  case 0:
                    return s.trys.push([ 0, 2, , 3 ]), [ 4 /*yield*/ , t.initialize(this.kr, this.Sd, this.platform, this.clientId, i, n) ];

                  case 1:
                    return s.sent(), this.persistence = t.ja(), this.Rd = t.Fa(), this.Rl = t.Ua(), 
                    e.resolve(), [ 3 /*break*/ , 3 ];

                  case 2:
                    // An unknown failure on the first stage shuts everything down.
                    if (r = s.sent(), 
                    // Regardless of whether or not the retry succeeds, from an user
                    // perspective, offline persistence has failed.
                    e.reject(r), !this.Nd(r)) throw r;
                    return console.warn("Error enabling offline persistence. Falling back to persistence disabled: " + r), 
                    [ 2 /*return*/ , this.Id(new Fi, {
                        ka: !1
                    }, i, e) ];

                  case 3:
                    return [ 2 /*return*/ ];
                }
            }));
        }));
    }, 
    /**
     * Decides whether the provided error allows us to gracefully disable
     * persistence (as opposed to crashing the client).
     */
    t.prototype.Nd = function(t) {
        return "FirebaseError" === t.name ? t.code === T.FAILED_PRECONDITION || t.code === T.UNIMPLEMENTED : !("undefined" != typeof DOMException && t instanceof DOMException) || (
        // When the browser is out of quota we could get either quota exceeded
        // or an aborted error depending on whether the error happened during
        // schema migration.
        22 === t.code || 20 === t.code || 
        // Firefox Private Browsing mode disables IndexedDb and returns
        // INVALID_STATE for any usage.
        11 === t.code);
    }, 
    /**
     * Checks that the client has not been terminated. Ensures that other methods on
     * this class cannot be called after the client is terminated.
     */
    t.prototype.Ad = function() {
        if (this.kr.Kr) throw new E(T.FAILED_PRECONDITION, "The client has already been terminated.");
    }, 
    /**
     * Initializes the rest of the FirestoreClient, assuming the initial user
     * has been obtained from the credential provider and some persistence
     * implementation is available in this.persistence.
     */
    t.prototype.Cd = function(t) {
        var n = this;
        return qn("FirestoreClient", "Initializing. user=", t.uid), this.platform.Od(this.Sd).then((function(i) {
            return r(n, void 0, void 0, (function() {
                var n, e, u, o, h, a, c = this;
                return s(this, (function(f) {
                    switch (f.label) {
                      case 0:
                        return n = new Si, this.yf = new ci(this.persistence, n, t), [ 4 /*yield*/ , this.yf.start() ];

                      case 1:
                        // PORTING NOTE: LocalStore doesn't need an explicit start() on the Web.
                        return f.sent(), e = this.platform.Dd(), u = this.platform.Vd(this.Sd.ce), o = new qi(this.kr, i, this.credentials, u), 
                        h = function(t) {
                            return c.Fo.pl(t, 0 /* RemoteStore */);
                        }, a = function(t) {
                            return c.Fo.pl(t, 1 /* SharedClientState */);
                        }, this._l = new Ue(this.yf, o, this.kr, h, e), this.Fo = new We(this.yf, this._l, this.Rl, t, 100), 
                        this.Rl.jo = a, 
                        // Set up wiring between sync engine and other components
                        this._l.Fo = this.Fo, this.Rl.Fo = this.Fo, this.Pd = new ze(this.Fo), [ 4 /*yield*/ , this.Rl.start() ];

                      case 2:
                        // PORTING NOTE: LocalStore doesn't need an explicit start() on the Web.
                        return f.sent(), [ 4 /*yield*/ , this._l.start() ];

                      case 3:
                        // NOTE: This will immediately call the listener, so we make sure to
                        // set it after localStore / remoteStore are started.
                        return f.sent(), [ 4 /*yield*/ , this.persistence.Aa((function(t) {
                            return r(c, void 0, void 0, (function() {
                                return s(this, (function(n) {
                                    switch (n.label) {
                                      case 0:
                                        return [ 4 /*yield*/ , this.Fo.sl(t) ];

                                      case 1:
                                        return n.sent(), t && !this.Rd.Ls ? this.Rd.start(this.yf) : t || this.Rd.stop(), 
                                        [ 2 /*return*/ ];
                                    }
                                }));
                            }));
                        })) ];

                      case 4:
                        // When a user calls clearPersistence() in one client, all other clients
                        // need to be terminated to allow the delete to succeed.
                        // NOTE: This will immediately call the listener, so we make sure to
                        // set it after localStore / remoteStore are started.
                        return f.sent(), [ 4 /*yield*/ , this.persistence.Ia((function() {
                            return r(c, void 0, void 0, (function() {
                                return s(this, (function(t) {
                                    switch (t.label) {
                                      case 0:
                                        return [ 4 /*yield*/ , this.terminate() ];

                                      case 1:
                                        return t.sent(), [ 2 /*return*/ ];
                                    }
                                }));
                            }));
                        })) ];

                      case 5:
                        // When a user calls clearPersistence() in one client, all other clients
                        // need to be terminated to allow the delete to succeed.
                        return f.sent(), [ 2 /*return*/ ];
                    }
                }));
            }));
        }));
    }, t.prototype.rl = function(t) {
        return this.kr.ss(), qn("FirestoreClient", "Credential Changed. Current user: " + t.uid), 
        this.Fo.rl(t);
    }, 
    /** Disables the network connection. Pending operations will not complete. */
    t.prototype.disableNetwork = function() {
        var t = this;
        return this.Ad(), this.kr.enqueue((function() {
            return t.Fo.disableNetwork();
        }));
    }, t.prototype.terminate = function() {
        var t = this;
        return this.kr.ns((function() {
            return r(t, void 0, void 0, (function() {
                return s(this, (function(t) {
                    switch (t.label) {
                      case 0:
                        // PORTING NOTE: LocalStore does not need an explicit shutdown on web.
                        return this.Rd.stop(), [ 4 /*yield*/ , this._l.shutdown() ];

                      case 1:
                        return t.sent(), [ 4 /*yield*/ , this.Rl.shutdown() ];

                      case 2:
                        return t.sent(), [ 4 /*yield*/ , this.persistence.shutdown() ];

                      case 3:
                        return t.sent(), 
                        // `removeChangeListener` must be called after shutting down the
                        // RemoteStore as it will prevent the RemoteStore from retrieving
                        // auth tokens.
                        this.credentials.m(), [ 2 /*return*/ ];
                    }
                }));
            }));
        }));
    }, 
    /**
     * Returns a Promise that resolves when all writes that were pending at the time this
     * method was called received server acknowledgement. An acknowledgement can be either acceptance
     * or rejection.
     */
    t.prototype.waitForPendingWrites = function() {
        var t = this;
        this.Ad();
        var n = new ni;
        return this.kr.Qr((function() {
            return t.Fo.$l(n);
        })), n.promise;
    }, t.prototype.listen = function(t, n, i) {
        var e = this;
        this.Ad();
        var r = new Je(t, n, i);
        return this.kr.Qr((function() {
            return e.Pd.listen(r);
        })), r;
    }, t.prototype.Qf = function(t) {
        var n = this;
        // Checks for termination but does not raise error, allowing unlisten after
        // termination to be a no-op.
                this.kd || this.kr.Qr((function() {
            return n.Pd.Qf(t);
        }));
    }, t.prototype.Fd = function(t) {
        var n = this;
        return this.Ad(), this.kr.enqueue((function() {
            return n.yf.oo(t);
        })).then((function(t) {
            if (t instanceof Ut) return t;
            if (t instanceof Lt) return null;
            throw new E(T.UNAVAILABLE, "Failed to get document from cache. (However, this document may exist on the server. Run again without setting 'source' in the GetOptions to attempt to retrieve the document from the server.)");
        }));
    }, t.prototype.jd = function(t) {
        var n = this;
        return this.Ad(), this.kr.enqueue((function() {
            return r(n, void 0, void 0, (function() {
                var n, i, e;
                return s(this, (function(r) {
                    switch (r.label) {
                      case 0:
                        return [ 4 /*yield*/ , this.yf.vo(t, 
                        /* usePreviousResults= */ !0) ];

                      case 1:
                        return n = r.sent(), i = new qe(t, n.po), e = i.fl(n.documents), [ 2 /*return*/ , i.hu(e, 
                        /* updateLimboDocuments= */ !1).snapshot ];
                    }
                }));
            }));
        }));
    }, t.prototype.write = function(t) {
        var n = this;
        this.Ad();
        var i = new ni;
        return this.kr.Qr((function() {
            return n.Fo.write(t, i);
        })), i.promise;
    }, t.prototype.ce = function() {
        return this.Sd.ce;
    }, t.prototype.wd = function(t) {
        var n = this;
        this.Ad(), this.kr.Qr((function() {
            return n.Pd.wd(t), Promise.resolve();
        }));
    }, t.prototype.md = function(t) {
        // Checks for shutdown but does not raise error, allowing remove after
        // shutdown to be a no-op.
        this.kd || this.Pd.md(t);
    }, Object.defineProperty(t.prototype, "kd", {
        get: function() {
            // Technically, the asyncQueue is still running, but only accepting operations
            // related to termination or supposed to be run after termination. It is effectively
            // terminated to the eyes of users.
            return this.kr.Kr;
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.transaction = function(t) {
        var n = this;
        this.Ad();
        var i = new ni;
        return this.kr.Qr((function() {
            return n.Fo.runTransaction(n.kr, t, i), Promise.resolve();
        })), i.promise;
    }, t;
}(), Ye = /** @class */ function() {
    function t(t) {
        this.observer = t, 
        /**
         * When set to true, will not raise future events. Necessary to deal with
         * async detachment of listener.
         */
        this.muted = !1;
    }
    return t.prototype.next = function(t) {
        this.Ud(this.observer.next, t);
    }, t.prototype.error = function(t) {
        this.Ud(this.observer.error, t);
    }, t.prototype.Ld = function() {
        this.muted = !0;
    }, t.prototype.Ud = function(t, n) {
        var i = this;
        this.muted || setTimeout((function() {
            i.muted || t(n);
        }), 0);
    }, t;
}();

/**
 * TransactionRunner encapsulates the logic needed to run and retry transactions
 * with backoff.
 */
/**
 * @license
 * Copyright 2017 Google Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function Ke(t) {
    /**
 * Returns true if obj is an object and contains at least one of the specified
 * methods.
 */
    return function(t, n) {
        if ("object" != typeof t || null === t) return !1;
        for (var i = t, e = 0, r = n; e < r.length; e++) {
            var s = r[e];
            if (s in i && "function" == typeof i[s]) return !0;
        }
        return !1;
    }
    /**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
    /**
 * Converts Firestore's internal types to the JavaScript types that we expose
 * to the user.
 */ (t, [ "next", "error", "complete" ]);
}

var Xe = /** @class */ function() {
    function t(t, n, i, e) {
        this.firestore = t, this.timestampsInSnapshots = n, this.Md = i, this.converter = e;
    }
    return t.prototype.qd = function(t) {
        switch (Z(t)) {
          case 0 /* NullValue */ :
            return null;

          case 1 /* BooleanValue */ :
            return t.booleanValue;

          case 2 /* NumberValue */ :
            return ot(t.integerValue || t.doubleValue);

          case 3 /* TimestampValue */ :
            return this.xd(t.timestampValue);

          case 4 /* ServerTimestampValue */ :
            return this.Bd(t);

          case 5 /* StringValue */ :
            return t.stringValue;

          case 6 /* BlobValue */ :
            return new fe(ht(t.bytesValue));

          case 7 /* RefValue */ :
            return this.Qd(t.referenceValue);

          case 8 /* GeoPointValue */ :
            return new Te(t.geoPointValue.latitude, t.geoPointValue.longitude);

          case 9 /* ArrayValue */ :
            return this.Wd(t.arrayValue);

          case 10 /* ObjectValue */ :
            return this.Gd(t.mapValue);

          default:
            throw Qn("Invalid value type: " + JSON.stringify(t));
        }
    }, t.prototype.Gd = function(t) {
        var n = this, i = {};
        return W(t.fields || {}, (function(t, e) {
            i[t] = n.qd(e);
        })), i;
    }, t.prototype.Wd = function(t) {
        var n = this;
        return (t.values || []).map((function(t) {
            return n.qd(t);
        }));
    }, t.prototype.Bd = function(t) {
        switch (this.Md) {
          case "previous":
            var n = function t(n) {
                var i = n.mapValue.fields.__previous_value__;
                return K(i) ? t(i) : i;
            }(t);
            return null == n ? null : this.qd(n);

          case "estimate":
            return this.xd(X(t));

          default:
            return null;
        }
    }, t.prototype.xd = function(t) {
        var n = ut(t), i = new O(n.seconds, n.nanos);
        return this.timestampsInSnapshots ? i : i.toDate();
    }, t.prototype.Qd = function(t) {
        var n = P.Y(t);
        Wn(Fn(n), "ReferenceValue is not valid " + t);
        var i = new Yn(n.get(1), n.get(3)), e = new j(n.U(5));
        return i.isEqual(this.firestore.zd) || 
        // TODO(b/64130202): Somehow support foreign references.
        xn("Document " + e + " contains a document reference within a different database (" + i.projectId + "/" + i.database + ") which is not supported. It will be treated as a reference in the current database (" + this.firestore.zd.projectId + "/" + this.firestore.zd.database + ") instead."), 
        new er(e, this.firestore, this.converter);
    }, t;
}(), $e = ui.Os, Ze = /** @class */ function() {
    function t(t) {
        var n, i;
        if (void 0 === t.host) {
            if (void 0 !== t.ssl) throw new E(T.INVALID_ARGUMENT, "Can't provide ssl option if host option is not set");
            this.host = "firestore.googleapis.com", this.ssl = !0;
        } else Hi("settings", "non-empty string", "host", t.host), this.host = t.host, Yi("settings", "boolean", "ssl", t.ssl), 
        this.ssl = null === (n = t.ssl) || void 0 === n || n;
        if (ee("settings", t, [ "host", "ssl", "credentials", "timestampsInSnapshots", "cacheSizeBytes", "experimentalForceLongPolling" ]), 
        Yi("settings", "object", "credentials", t.credentials), this.credentials = t.credentials, 
        Yi("settings", "boolean", "timestampsInSnapshots", t.timestampsInSnapshots), 
        // Nobody should set timestampsInSnapshots anymore, but the error depends on
        // whether they set it to true or false...
        !0 === t.timestampsInSnapshots ? xn("\n  The timestampsInSnapshots setting now defaults to true and you no\n  longer need to explicitly set it. In a future release, the setting\n  will be removed entirely and so it is recommended that you remove it\n  from your firestore.settings() call now.") : !1 === t.timestampsInSnapshots && xn("\n  The timestampsInSnapshots setting will soon be removed. YOU MUST UPDATE\n  YOUR CODE.\n\n  To hide this warning, stop using the timestampsInSnapshots setting in your\n  firestore.settings({ ... }) call.\n\n  Once you remove the setting, Timestamps stored in Cloud Firestore will be\n  read back as Firebase Timestamp objects instead of as system Date objects.\n  So you will also need to update code expecting a Date to instead expect a\n  Timestamp. For example:\n\n  // Old:\n  const date = snapshot.get('created_at');\n  // New:\n  const timestamp = snapshot.get('created_at'); const date =\n  timestamp.toDate();\n\n  Please audit all existing usages of Date when you enable the new\n  behavior."), 
        this.timestampsInSnapshots = null === (i = t.timestampsInSnapshots) || void 0 === i || i, 
        Yi("settings", "number", "cacheSizeBytes", t.cacheSizeBytes), void 0 === t.cacheSizeBytes) this.cacheSizeBytes = ui.Vs; else {
            if (t.cacheSizeBytes !== $e && t.cacheSizeBytes < ui.Ds) throw new E(T.INVALID_ARGUMENT, "cacheSizeBytes must be at least " + ui.Ds);
            this.cacheSizeBytes = t.cacheSizeBytes;
        }
        Yi("settings", "boolean", "experimentalForceLongPolling", t.experimentalForceLongPolling), 
        this.forceLongPolling = void 0 !== t.experimentalForceLongPolling && t.experimentalForceLongPolling;
    }
    return t.prototype.isEqual = function(t) {
        return this.host === t.host && this.ssl === t.ssl && this.timestampsInSnapshots === t.timestampsInSnapshots && this.credentials === t.credentials && this.cacheSizeBytes === t.cacheSizeBytes && this.forceLongPolling === t.forceLongPolling;
    }, t;
}(), tr = /** @class */ function() {
    // Note: We are using `MemoryPersistenceProvider` as a default
    // PersistenceProvider to ensure backwards compatibility with the format
    // expected by the console build.
    function t(n, i, e) {
        var u = this;
        if (void 0 === e && (e = new Fi), this.Jd = null, 
        // Public for use in tests.
        // TODO(mikelehen): Use modularized initialization instead.
        this.Hd = new ei, this.INTERNAL = {
            delete: function() {
                return r(u, void 0, void 0, (function() {
                    return s(this, (function(t) {
                        switch (t.label) {
                          case 0:
                            // The client must be initalized to ensure that all subsequent API usage
                            // throws an exception.
                            return this.Yd(), [ 4 /*yield*/ , this.Kd.terminate() ];

                          case 1:
                            return t.sent(), [ 2 /*return*/ ];
                        }
                    }));
                }));
            }
        }, "object" == typeof n.options) {
            // This is very likely a Firebase app object
            // TODO(b/34177605): Can we somehow use instanceof?
            var o = n;
            this.Jd = o, this.zd = t.Xd(o), this.$d = o.name, this.Zd = new I(i);
        } else {
            var h = n;
            if (!h.projectId) throw new E(T.INVALID_ARGUMENT, "Must provide projectId");
            this.zd = new Yn(h.projectId, h.database), 
            // Use a default persistenceKey that lines up with FirebaseApp.
            this.$d = "[DEFAULT]", this.Zd = new A;
        }
        this.tv = e, this.nv = new Ze({}), this.iv = this.ev(this.zd);
    }
    return t.prototype.settings = function(t) {
        if (Bi("Firestore.settings", arguments, 1), zi("Firestore.settings", "object", 1, t), 
        mr(t, "persistence")) throw new E(T.INVALID_ARGUMENT, '"persistence" is now specified with a separate call to firestore.enablePersistence().');
        var n = new Ze(t);
        if (this.Kd && !this.nv.isEqual(n)) throw new E(T.FAILED_PRECONDITION, "Firestore has already been started and its settings can no longer be changed. You can only call settings() before calling any other methods on a Firestore object.");
        this.nv = n, void 0 !== n.credentials && (this.Zd = N(n.credentials));
    }, t.prototype.enableNetwork = function() {
        return this.Yd(), this.Kd.enableNetwork();
    }, t.prototype.disableNetwork = function() {
        return this.Yd(), this.Kd.disableNetwork();
    }, t.prototype.enablePersistence = function(t) {
        var n, i;
        if (this.Kd) throw new E(T.FAILED_PRECONDITION, "Firestore has already been started and persistence can no longer be enabled. You can only call enablePersistence() before calling any other methods on a Firestore object.");
        var e = !1;
        return t && (void 0 !== t.experimentalTabSynchronization && xn("The 'experimentalTabSynchronization' setting has been renamed to 'synchronizeTabs'. In a future release, the setting will be removed and it is recommended that you update your firestore.enablePersistence() call to use 'synchronizeTabs'."), 
        e = null !== (i = null !== (n = t.synchronizeTabs) && void 0 !== n ? n : t.experimentalTabSynchronization) && void 0 !== i && i), 
        this.rv(this.tv, {
            ka: !0,
            cacheSizeBytes: this.nv.cacheSizeBytes,
            synchronizeTabs: e
        });
    }, t.prototype.clearPersistence = function() {
        return r(this, void 0, void 0, (function() {
            var t, n = this;
            return s(this, (function(i) {
                if (void 0 !== this.Kd && !this.Kd.kd) throw new E(T.FAILED_PRECONDITION, "Persistence cannot be cleared after this Firestore instance is initialized.");
                return t = new ni, this.Hd.Xr((function() {
                    return r(n, void 0, void 0, (function() {
                        var n, i;
                        return s(this, (function(e) {
                            switch (e.label) {
                              case 0:
                                return e.trys.push([ 0, 2, , 3 ]), n = this.sv(), [ 4 /*yield*/ , this.tv.clearPersistence(n) ];

                              case 1:
                                return e.sent(), t.resolve(), [ 3 /*break*/ , 3 ];

                              case 2:
                                return i = e.sent(), t.reject(i), [ 3 /*break*/ , 3 ];

                              case 3:
                                return [ 2 /*return*/ ];
                            }
                        }));
                    }));
                })), [ 2 /*return*/ , t.promise ];
            }));
        }));
    }, t.prototype.terminate = function() {
        return this.app._removeServiceInstance("firestore"), this.INTERNAL.delete();
    }, Object.defineProperty(t.prototype, "uv", {
        get: function() {
            return this.Yd(), this.Kd.kd;
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.waitForPendingWrites = function() {
        return this.Yd(), this.Kd.waitForPendingWrites();
    }, t.prototype.onSnapshotsInSync = function(t) {
        if (this.Yd(), Ke(t)) return this.ov(t);
        zi("Firestore.onSnapshotsInSync", "function", 1, t);
        var n = {
            next: t
        };
        return this.ov(n);
    }, t.prototype.ov = function(t) {
        var n = this, i = new Ye({
            next: function() {
                t.next && t.next();
            },
            error: function(t) {
                throw Qn("Uncaught Error in onSnapshotsInSync");
            }
        });
        return this.Kd.wd(i), function() {
            i.Ld(), n.Kd.md(i);
        };
    }, t.prototype.Yd = function() {
        return this.Kd || 
        // Kick off starting the client but don't actually wait for it.
        // eslint-disable-next-line @typescript-eslint/no-floating-promises
        this.rv(new Fi, {
            ka: !1
        }), this.Kd;
    }, t.prototype.sv = function() {
        return new Hn(this.zd, this.$d, this.nv.host, this.nv.ssl, this.nv.forceLongPolling);
    }, t.prototype.rv = function(t, n) {
        Wn(!!this.nv.host, "FirestoreSettings.host is not set"), Wn(!this.Kd, "configureClient() called multiple times");
        var i = this.sv();
        return this.Kd = new He(jn.Lt(), i, this.Zd, this.Hd), this.Kd.start(t, n);
    }, t.prototype.ev = function(t) {
        var n = new kn(t, {
            de: jn.Lt().de
        });
        return new Ne(n, (function(n) {
            if (n instanceof er) {
                var i = t, e = n.firestore.zd;
                if (!e.isEqual(i)) throw new E(T.INVALID_ARGUMENT, "Document reference is for database " + e.projectId + "/" + e.database + " but should be for database " + i.projectId + "/" + i.database);
                return new Re(t, n.hv);
            }
            return n;
        }));
    }, t.Xd = function(t) {
        if (!mr(t.options, "projectId")) throw new E(T.INVALID_ARGUMENT, '"projectId" not provided in firebase.initializeApp.');
        var n = t.options.projectId;
        if (!n || "string" != typeof n) throw new E(T.INVALID_ARGUMENT, "projectId must be a string in FirebaseApp.options");
        return new Yn(n);
    }, Object.defineProperty(t.prototype, "app", {
        get: function() {
            if (!this.Jd) throw new E(T.FAILED_PRECONDITION, "Firestore was not initialized using the Firebase SDK. 'app' is not available");
            return this.Jd;
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.collection = function(t) {
        return Bi("Firestore.collection", arguments, 1), zi("Firestore.collection", "non-empty string", 1, t), 
        this.Yd(), new ar(P.Y(t), this);
    }, t.prototype.doc = function(t) {
        return Bi("Firestore.doc", arguments, 1), zi("Firestore.doc", "non-empty string", 1, t), 
        this.Yd(), er.av(P.Y(t), this);
    }, t.prototype.collectionGroup = function(t) {
        if (Bi("Firestore.collectionGroup", arguments, 1), zi("Firestore.collectionGroup", "non-empty string", 1, t), 
        t.indexOf("/") >= 0) throw new E(T.INVALID_ARGUMENT, "Invalid collection ID '" + t + "' passed to function Firestore.collectionGroup(). Collection IDs must not contain '/'.");
        return this.Yd(), new or(new xt(P.K, t), this);
    }, t.prototype.runTransaction = function(t) {
        var n = this;
        return Bi("Firestore.runTransaction", arguments, 1), zi("Firestore.runTransaction", "function", 1, t), 
        this.Yd().transaction((function(i) {
            return t(new nr(n, i));
        }));
    }, t.prototype.batch = function() {
        return this.Yd(), new ir(this);
    }, Object.defineProperty(t, "logLevel", {
        get: function() {
            switch (Ln()) {
              case o.DEBUG:
                return "debug";

              case o.SILENT:
                return "silent";

              default:
                // The default log level is error
                return "error";
            }
        },
        enumerable: !0,
        configurable: !0
    }), t.setLogLevel = function(t) {
        switch (Bi("Firestore.setLogLevel", arguments, 1), zi("Firestore.setLogLevel", "non-empty string", 1, t), 
        t) {
          case "debug":
            Mn(o.DEBUG);
            break;

          case "error":
            Mn(o.ERROR);
            break;

          case "silent":
            Mn(o.SILENT);
            break;

          default:
            throw new E(T.INVALID_ARGUMENT, "Invalid log level: " + t);
        }
    }, 
    // Note: this is not a property because the minifier can't work correctly with
    // the way TypeScript compiler outputs properties.
    t.prototype.cv = function() {
        return this.nv.timestampsInSnapshots;
    }, t;
}(), nr = /** @class */ function() {
    function t(t, n) {
        this.fv = t, this.lv = n;
    }
    return t.prototype.get = function(t) {
        var n = this;
        Bi("Transaction.get", arguments, 1);
        var i = dr("Transaction.get", t, this.fv);
        return this.lv.lookup([ i.hv ]).then((function(t) {
            if (!t || 1 !== t.length) return Qn("Mismatch in docs returned from document lookup.");
            var e = t[0];
            if (e instanceof Lt) return new sr(n.fv, i.hv, null, 
            /* fromCache= */ !1, 
            /* hasPendingWrites= */ !1, i.dv);
            if (e instanceof Ut) return new sr(n.fv, i.hv, e, 
            /* fromCache= */ !1, 
            /* hasPendingWrites= */ !1, i.dv);
            throw Qn("BatchGetDocumentsRequest returned unexpected document type: " + e.constructor.name);
        }));
    }, t.prototype.set = function(t, n, i) {
        Wi("Transaction.set", arguments, 2, 3);
        var e = dr("Transaction.set", t, this.fv);
        i = cr("Transaction.set", i);
        var r = wr(e.dv, n, "Transaction.set"), s = r[0], u = r[1], o = i.merge || i.mergeFields ? this.fv.iv.Jc(u, s, i.mergeFields) : this.fv.iv.Gc(u, s);
        return this.lv.set(e.hv, o), this;
    }, t.prototype.update = function(t, n, i) {
        for (var e, r, s = [], u = 3; u < arguments.length; u++) s[u - 3] = arguments[u];
        return "string" == typeof n || n instanceof de ? (Qi("Transaction.update", arguments, 3), 
        e = dr("Transaction.update", t, this.fv), r = this.fv.iv.Xc("Transaction.update", n, i, s)) : (Bi("Transaction.update", arguments, 2), 
        e = dr("Transaction.update", t, this.fv), r = this.fv.iv.Hc("Transaction.update", n)), 
        this.lv.update(e.hv, r), this;
    }, t.prototype.delete = function(t) {
        Bi("Transaction.delete", arguments, 1);
        var n = dr("Transaction.delete", t, this.fv);
        return this.lv.delete(n.hv), this;
    }, t;
}(), ir = /** @class */ function() {
    function t(t) {
        this.fv = t, this.vv = [], this.wv = !1;
    }
    return t.prototype.set = function(t, n, i) {
        Wi("WriteBatch.set", arguments, 2, 3), this.mv();
        var e = dr("WriteBatch.set", t, this.fv);
        i = cr("WriteBatch.set", i);
        var r = wr(e.dv, n, "WriteBatch.set"), s = r[0], u = r[1], o = i.merge || i.mergeFields ? this.fv.iv.Jc(u, s, i.mergeFields) : this.fv.iv.Gc(u, s);
        return this.vv = this.vv.concat(o.Fc(e.hv, Ct.NONE)), this;
    }, t.prototype.update = function(t, n, i) {
        for (var e, r, s = [], u = 3; u < arguments.length; u++) s[u - 3] = arguments[u];
        return this.mv(), "string" == typeof n || n instanceof de ? (Qi("WriteBatch.update", arguments, 3), 
        e = dr("WriteBatch.update", t, this.fv), r = this.fv.iv.Xc("WriteBatch.update", n, i, s)) : (Bi("WriteBatch.update", arguments, 2), 
        e = dr("WriteBatch.update", t, this.fv), r = this.fv.iv.Hc("WriteBatch.update", n)), 
        this.vv = this.vv.concat(r.Fc(e.hv, Ct.exists(!0))), this;
    }, t.prototype.delete = function(t) {
        Bi("WriteBatch.delete", arguments, 1), this.mv();
        var n = dr("WriteBatch.delete", t, this.fv);
        return this.vv = this.vv.concat(new Vt(n.hv, Ct.NONE)), this;
    }, t.prototype.commit = function() {
        return this.mv(), this.wv = !0, this.vv.length > 0 ? this.fv.Yd().write(this.vv) : Promise.resolve();
    }, t.prototype.mv = function() {
        if (this.wv) throw new E(T.FAILED_PRECONDITION, "A write batch can no longer be used after commit() has been called.");
    }, t;
}(), er = /** @class */ function() {
    function t(t, n, i) {
        this.hv = t, this.firestore = n, this.dv = i, this.Kd = this.firestore.Yd();
    }
    return t.av = function(n, i, e) {
        if (n.length % 2 != 0) throw new E(T.INVALID_ARGUMENT, "Invalid document reference. Document references must have an even number of segments, but " + n.H() + " has " + n.length);
        return new t(new j(n), i, e);
    }, Object.defineProperty(t.prototype, "id", {
        get: function() {
            return this.hv.path.B();
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(t.prototype, "parent", {
        get: function() {
            return new ar(this.hv.path.L(), this.firestore, this.dv);
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(t.prototype, "path", {
        get: function() {
            return this.hv.path.H();
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.collection = function(t) {
        if (Bi("DocumentReference.collection", arguments, 1), zi("DocumentReference.collection", "non-empty string", 1, t), 
        !t) throw new E(T.INVALID_ARGUMENT, "Must provide a non-empty collection name to collection()");
        var n = P.Y(t);
        return new ar(this.hv.path.child(n), this.firestore);
    }, t.prototype.isEqual = function(n) {
        if (!(n instanceof t)) throw re("isEqual", "DocumentReference", 1, n);
        return this.firestore === n.firestore && this.hv.isEqual(n.hv) && this.dv === n.dv;
    }, t.prototype.set = function(t, n) {
        Wi("DocumentReference.set", arguments, 1, 2), n = cr("DocumentReference.set", n);
        var i = wr(this.dv, t, "DocumentReference.set"), e = i[0], r = i[1], s = n.merge || n.mergeFields ? this.firestore.iv.Jc(r, e, n.mergeFields) : this.firestore.iv.Gc(r, e);
        return this.Kd.write(s.Fc(this.hv, Ct.NONE));
    }, t.prototype.update = function(t, n) {
        for (var i, e = [], r = 2; r < arguments.length; r++) e[r - 2] = arguments[r];
        return "string" == typeof t || t instanceof de ? (Qi("DocumentReference.update", arguments, 2), 
        i = this.firestore.iv.Xc("DocumentReference.update", t, n, e)) : (Bi("DocumentReference.update", arguments, 1), 
        i = this.firestore.iv.Hc("DocumentReference.update", t)), this.Kd.write(i.Fc(this.hv, Ct.exists(!0)));
    }, t.prototype.delete = function() {
        return Bi("DocumentReference.delete", arguments, 0), this.Kd.write([ new Vt(this.hv, Ct.NONE) ]);
    }, t.prototype.onSnapshot = function() {
        for (var t = [], n = 0; n < arguments.length; n++) t[n] = arguments[n];
        Wi("DocumentReference.onSnapshot", arguments, 1, 4);
        var i, e = {
            includeMetadataChanges: !1
        }, r = 0;
        "object" != typeof t[r] || Ke(t[r]) || (ee("DocumentReference.onSnapshot", e = t[r], [ "includeMetadataChanges" ]), 
        Yi("DocumentReference.onSnapshot", "boolean", "includeMetadataChanges", e.includeMetadataChanges), 
        r++);
        var s = {
            includeMetadataChanges: e.includeMetadataChanges
        };
        return Ke(t[r]) ? i = t[r] : (zi("DocumentReference.onSnapshot", "function", r, t[r]), 
        Ji("DocumentReference.onSnapshot", "function", r + 1, t[r + 1]), Ji("DocumentReference.onSnapshot", "function", r + 2, t[r + 2]), 
        i = {
            next: t[r],
            error: t[r + 1],
            complete: t[r + 2]
        }), this.pv(s, i);
    }, t.prototype.pv = function(t, n) {
        var i = this, e = function(t) {
            console.error("Uncaught Error in onSnapshot:", t);
        };
        n.error && (e = n.error.bind(n));
        var r = new Ye({
            next: function(t) {
                if (n.next) {
                    Wn(t.docs.size <= 1, "Too many documents returned on a document query");
                    var e = t.docs.get(i.hv);
                    n.next(new sr(i.firestore, i.hv, e, t.fromCache, t.hasPendingWrites, i.dv));
                }
            },
            error: e
        }), s = this.Kd.listen(xt.Nn(this.hv.path), r, t);
        return function() {
            r.Ld(), i.Kd.Qf(s);
        };
    }, t.prototype.get = function(t) {
        var n = this;
        return Wi("DocumentReference.get", arguments, 0, 1), lr("DocumentReference.get", t), 
        new Promise((function(i, e) {
            t && "cache" === t.source ? n.firestore.Yd().Fd(n.hv).then((function(t) {
                i(new sr(n.firestore, n.hv, t, 
                /*fromCache=*/ !0, t instanceof Ut && t.nn, n.dv));
            }), e) : n.gv(i, e, t);
        }));
    }, t.prototype.gv = function(t, n, i) {
        var e = this.pv({
            includeMetadataChanges: !0,
            Ed: !0
        }, {
            next: function(r) {
                // Remove query first before passing event to user to avoid
                // user actions affecting the now stale query.
                e(), !r.exists && r.metadata.fromCache ? 
                // TODO(dimond): If we're online and the document doesn't
                // exist then we resolve with a doc.exists set to false. If
                // we're offline however, we reject the Promise in this
                // case. Two options: 1) Cache the negative response from
                // the server so we can deliver that even when you're
                // offline 2) Actually reject the Promise in the online case
                // if the document doesn't exist.
                n(new E(T.UNAVAILABLE, "Failed to get document because the client is offline.")) : r.exists && r.metadata.fromCache && i && "server" === i.source ? n(new E(T.UNAVAILABLE, 'Failed to get document from server. (However, this document does exist in the local cache. Run again without setting source to "server" to retrieve the cached document.)')) : t(r);
            },
            error: n
        });
    }, t.prototype.withConverter = function(n) {
        return new t(this.hv, this.firestore, n);
    }, t;
}(), rr = /** @class */ function() {
    function t(t, n) {
        this.hasPendingWrites = t, this.fromCache = n;
    }
    return t.prototype.isEqual = function(t) {
        return this.hasPendingWrites === t.hasPendingWrites && this.fromCache === t.fromCache;
    }, t;
}(), sr = /** @class */ function() {
    function t(t, n, i, e, r, s) {
        this.fv = t, this.hv = n, this.bv = i, this.yv = e, this._v = r, this.dv = s;
    }
    return t.prototype.data = function(t) {
        if (Wi("DocumentSnapshot.data", arguments, 0, 1), t = fr("DocumentSnapshot.data", t), 
        this.bv) {
            // We only want to use the converter and create a new DocumentSnapshot
            // if a converter has been provided.
            if (this.dv) {
                var n = new ur(this.fv, this.hv, this.bv, this.yv, this._v);
                return this.dv.fromFirestore(n, t);
            }
            var i = new Xe(this.fv, this.fv.cv(), t.serverTimestamps, 
            /* converter= */ void 0);
            return i.qd(this.bv.yn());
        }
    }, t.prototype.get = function(t, n) {
        if (Wi("DocumentSnapshot.get", arguments, 1, 2), n = fr("DocumentSnapshot.get", n), 
        this.bv) {
            var i = this.bv.data().field(Ve("DocumentSnapshot.get", t));
            if (null !== i) {
                var e = new Xe(this.fv, this.fv.cv(), n.serverTimestamps, this.dv);
                return e.qd(i);
            }
        }
    }, Object.defineProperty(t.prototype, "id", {
        get: function() {
            return this.hv.path.B();
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(t.prototype, "ref", {
        get: function() {
            return new er(this.hv, this.fv, this.dv);
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(t.prototype, "exists", {
        get: function() {
            return null !== this.bv;
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(t.prototype, "metadata", {
        get: function() {
            return new rr(this._v, this.yv);
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.isEqual = function(n) {
        if (!(n instanceof t)) throw re("isEqual", "DocumentSnapshot", 1, n);
        return this.fv === n.fv && this.yv === n.yv && this.hv.isEqual(n.hv) && (null === this.bv ? null === n.bv : this.bv.isEqual(n.bv)) && this.dv === n.dv;
    }, t;
}(), ur = /** @class */ function(t) {
    function i() {
        return null !== t && t.apply(this, arguments) || this;
    }
    return n(i, t), i.prototype.data = function(n) {
        var i = t.prototype.data.call(this, n);
        return Wn(void 0 !== i, "Document in a QueryDocumentSnapshot should exist"), i;
    }, i;
}(sr), or = /** @class */ function() {
    function t(t, n, i) {
        this.Tv = t, this.firestore = n, this.dv = i;
    }
    return t.prototype.where = function(n, i, e) {
        Bi("Query.where", arguments, 3), ie("Query.where", 3, e);
        // Enumerated from the WhereFilterOp type in index.d.ts.
        var r, s = [ "<", "<=", "==", ">=", ">", "array-contains", "in", "array-contains-any" ];
        $i("Query.where", s, 2, i);
        var u = Ve("Query.where", n), o = Qt.Y(i);
        if (u.$()) {
            if (o === Qt.ARRAY_CONTAINS || o === Qt.ARRAY_CONTAINS_ANY) throw new E(T.INVALID_ARGUMENT, "Invalid Query. You can't perform '" + o.toString() + "' queries on FieldPath.documentId().");
            if (o === Qt.IN) {
                this.Ev(e, o);
                for (var h = [], a = 0, c = e; a < c.length; a++) {
                    var f = c[a];
                    h.push(this.Sv(f));
                }
                r = {
                    arrayValue: {
                        values: h
                    }
                };
            } else r = this.Sv(e);
        } else o !== Qt.IN && o !== Qt.ARRAY_CONTAINS_ANY || this.Ev(e, o), r = this.firestore.iv.$c("Query.where", e, 
        // We only allow nested arrays for IN queries.
        /** allowArrays = */ o === Qt.IN);
        var l = Wt.create(u, o, r);
        return this.Av(l), new t(this.Tv.Vn(l), this.firestore, this.dv);
    }, t.prototype.orderBy = function(n, i) {
        var e;
        if (Wi("Query.orderBy", arguments, 1, 2), Ji("Query.orderBy", "non-empty string", 2, i), 
        void 0 === i || "asc" === i) e = Kt.ASCENDING; else {
            if ("desc" !== i) throw new E(T.INVALID_ARGUMENT, "Function Query.orderBy() has unknown direction '" + i + "', expected 'asc' or 'desc'.");
            e = Kt.DESCENDING;
        }
        if (null !== this.Tv.startAt) throw new E(T.INVALID_ARGUMENT, "Invalid query. You must not call Query.startAt() or Query.startAfter() before calling Query.orderBy().");
        if (null !== this.Tv.endAt) throw new E(T.INVALID_ARGUMENT, "Invalid query. You must not call Query.endAt() or Query.endBefore() before calling Query.orderBy().");
        var r = Ve("Query.orderBy", n), s = new $t(r, e);
        return this.Iv(s), new t(this.Tv.kn(s), this.firestore, this.dv);
    }, t.prototype.limit = function(n) {
        return Bi("Query.limit", arguments, 1), zi("Query.limit", "number", 1, n), se("Query.limit", 1, n), 
        new t(this.Tv.Fn(n), this.firestore, this.dv);
    }, t.prototype.limitToLast = function(n) {
        return Bi("Query.limitToLast", arguments, 1), zi("Query.limitToLast", "number", 1, n), 
        se("Query.limitToLast", 1, n), new t(this.Tv.jn(n), this.firestore, this.dv);
    }, t.prototype.startAt = function(n) {
        for (var i = [], e = 1; e < arguments.length; e++) i[e - 1] = arguments[e];
        Qi("Query.startAt", arguments, 1);
        var r = this.Cv("Query.startAt", n, i, 
        /*before=*/ !0);
        return new t(this.Tv.Un(r), this.firestore, this.dv);
    }, t.prototype.startAfter = function(n) {
        for (var i = [], e = 1; e < arguments.length; e++) i[e - 1] = arguments[e];
        Qi("Query.startAfter", arguments, 1);
        var r = this.Cv("Query.startAfter", n, i, 
        /*before=*/ !1);
        return new t(this.Tv.Un(r), this.firestore, this.dv);
    }, t.prototype.endBefore = function(n) {
        for (var i = [], e = 1; e < arguments.length; e++) i[e - 1] = arguments[e];
        Qi("Query.endBefore", arguments, 1);
        var r = this.Cv("Query.endBefore", n, i, 
        /*before=*/ !0);
        return new t(this.Tv.Ln(r), this.firestore, this.dv);
    }, t.prototype.endAt = function(n) {
        for (var i = [], e = 1; e < arguments.length; e++) i[e - 1] = arguments[e];
        Qi("Query.endAt", arguments, 1);
        var r = this.Cv("Query.endAt", n, i, 
        /*before=*/ !1);
        return new t(this.Tv.Ln(r), this.firestore, this.dv);
    }, t.prototype.isEqual = function(n) {
        if (!(n instanceof t)) throw re("isEqual", "Query", 1, n);
        return this.firestore === n.firestore && this.Tv.isEqual(n.Tv);
    }, t.prototype.withConverter = function(n) {
        return new t(this.Tv, this.firestore, n);
    }, 
    /** Helper function to create a bound from a document or fields */
    t.prototype.Cv = function(t, n, i, e) {
        if (ie(t, 1, n), n instanceof sr) {
            if (i.length > 0) throw new E(T.INVALID_ARGUMENT, "Too many arguments provided to " + t + "().");
            var r = n;
            if (!r.exists) throw new E(T.NOT_FOUND, "Can't use a DocumentSnapshot that doesn't exist for " + t + "().");
            return this.Rv(r.bv, e);
        }
        var s = [ n ].concat(i);
        return this.Nv(t, s, e);
    }, 
    /**
     * Create a Bound from a query and a document.
     *
     * Note that the Bound will always include the key of the document
     * and so only the provided document will compare equal to the returned
     * position.
     *
     * Will throw if the document does not contain all fields of the order by
     * of the query or if any of the fields in the order by are an uncommitted
     * server timestamp.
     */
    t.prototype.Rv = function(t, n) {
        // Because people expect to continue/end a query at the exact document
        // provided, we need to use the implicit sort order rather than the explicit
        // sort order, because it's guaranteed to contain the document key. That way
        // the position becomes unambiguous and the query continues/ends exactly at
        // the provided document. Without the key (by using the explicit sort
        // orders), multiple documents could match the position, yielding duplicate
        // results.
        for (var i = [], e = 0, r = this.Tv.orderBy; e < r.length; e++) {
            var s = r[e];
            if (s.field.$()) i.push(at(this.firestore.zd, t.key)); else {
                var u = t.field(s.field);
                if (K(u)) throw new E(T.INVALID_ARGUMENT, 'Invalid query. You are trying to start or end a query using a document for which the field "' + s.field + '" is an uncommitted server timestamp. (Since the value of this field is unknown, you cannot start/end a query with it.)');
                if (null === u) {
                    var o = s.field.H();
                    throw new E(T.INVALID_ARGUMENT, "Invalid query. You are trying to start or end a query using a document for which the field '" + o + "' (used as the orderBy) does not exist.");
                }
                i.push(u);
            }
        }
        return new Xt(i, n);
    }, 
    /**
     * Converts a list of field values to a Bound for the given query.
     */
    t.prototype.Nv = function(t, n, i) {
        // Use explicit order by's because it has to match the query the user made
        var e = this.Tv.Sn;
        if (n.length > e.length) throw new E(T.INVALID_ARGUMENT, "Too many arguments provided to " + t + "(). The number of arguments must be less than or equal to the number of Query.orderBy() clauses");
        for (var r = [], s = 0; s < n.length; s++) {
            var u = n[s];
            if (e[s].field.$()) {
                if ("string" != typeof u) throw new E(T.INVALID_ARGUMENT, "Invalid query. Expected a string for document ID in " + t + "(), but got a " + typeof u);
                if (!this.Tv.Kn() && -1 !== u.indexOf("/")) throw new E(T.INVALID_ARGUMENT, "Invalid query. When querying a collection and ordering by FieldPath.documentId(), the value passed to " + t + "() must be a plain document ID, but '" + u + "' contains a slash.");
                var o = this.Tv.path.child(P.Y(u));
                if (!j.nt(o)) throw new E(T.INVALID_ARGUMENT, "Invalid query. When querying a collection group and ordering by FieldPath.documentId(), the value passed to " + t + "() must result in a valid document path, but '" + o + "' is not because it contains an odd number of segments.");
                var h = new j(o);
                r.push(at(this.firestore.zd, h));
            } else {
                var a = this.firestore.iv.$c(t, u);
                r.push(a);
            }
        }
        return new Xt(r, i);
    }, t.prototype.onSnapshot = function() {
        for (var t = [], n = 0; n < arguments.length; n++) t[n] = arguments[n];
        Wi("Query.onSnapshot", arguments, 1, 4);
        var i, e = {}, r = 0;
        return "object" != typeof t[r] || Ke(t[r]) || (ee("Query.onSnapshot", e = t[r], [ "includeMetadataChanges" ]), 
        Yi("Query.onSnapshot", "boolean", "includeMetadataChanges", e.includeMetadataChanges), 
        r++), Ke(t[r]) ? i = t[r] : (zi("Query.onSnapshot", "function", r, t[r]), Ji("Query.onSnapshot", "function", r + 1, t[r + 1]), 
        Ji("Query.onSnapshot", "function", r + 2, t[r + 2]), i = {
            next: t[r],
            error: t[r + 1],
            complete: t[r + 2]
        }), this.Ov(this.Tv), this.pv(e, i);
    }, t.prototype.pv = function(t, n) {
        var i = this, e = function(t) {
            console.error("Uncaught Error in onSnapshot:", t);
        };
        n.error && (e = n.error.bind(n));
        var r = new Ye({
            next: function(t) {
                n.next && n.next(new hr(i.firestore, i.Tv, t, i.dv));
            },
            error: e
        }), s = this.firestore.Yd(), u = s.listen(this.Tv, r, t);
        return function() {
            r.Ld(), s.Qf(u);
        };
    }, t.prototype.Ov = function(t) {
        if (t.Hn() && 0 === t.Sn.length) throw new E(T.UNIMPLEMENTED, "limitToLast() queries require specifying at least one orderBy() clause");
    }, t.prototype.get = function(t) {
        var n = this;
        return Wi("Query.get", arguments, 0, 1), lr("Query.get", t), this.Ov(this.Tv), new Promise((function(i, e) {
            t && "cache" === t.source ? n.firestore.Yd().jd(n.Tv).then((function(t) {
                i(new hr(n.firestore, n.Tv, t, n.dv));
            }), e) : n.gv(i, e, t);
        }));
    }, t.prototype.gv = function(t, n, i) {
        var e = this.pv({
            includeMetadataChanges: !0,
            Ed: !0
        }, {
            next: function(r) {
                // Remove query first before passing event to user to avoid
                // user actions affecting the now stale query.
                e(), r.metadata.fromCache && i && "server" === i.source ? n(new E(T.UNAVAILABLE, 'Failed to get documents from server. (However, these documents may exist in the local cache. Run again without setting source to "server" to retrieve the cached documents.)')) : t(r);
            },
            error: n
        });
    }, 
    /**
     * Parses the given documentIdValue into a ReferenceValue, throwing
     * appropriate errors if the value is anything other than a DocumentReference
     * or String, or if the string is malformed.
     */
    t.prototype.Sv = function(t) {
        if ("string" == typeof t) {
            if ("" === t) throw new E(T.INVALID_ARGUMENT, "Invalid query. When querying with FieldPath.documentId(), you must provide a valid document ID, but it was an empty string.");
            if (!this.Tv.Kn() && -1 !== t.indexOf("/")) throw new E(T.INVALID_ARGUMENT, "Invalid query. When querying a collection by FieldPath.documentId(), you must provide a plain document ID, but '" + t + "' contains a '/' character.");
            var n = this.Tv.path.child(P.Y(t));
            if (!j.nt(n)) throw new E(T.INVALID_ARGUMENT, "Invalid query. When querying a collection group by FieldPath.documentId(), the value provided must result in a valid document path, but '" + n + "' is not because it has an odd number of segments (" + n.length + ").");
            return at(this.firestore.zd, new j(n));
        }
        if (t instanceof er) {
            var i = t;
            return at(this.firestore.zd, i.hv);
        }
        throw new E(T.INVALID_ARGUMENT, "Invalid query. When querying with FieldPath.documentId(), you must provide a valid string or a DocumentReference, but it was: " + ne(t) + ".");
    }, 
    /**
     * Validates that the value passed into a disjunctrive filter satisfies all
     * array requirements.
     */
    t.prototype.Ev = function(t, n) {
        if (!Array.isArray(t) || 0 === t.length) throw new E(T.INVALID_ARGUMENT, "Invalid Query. A non-empty array is required for '" + n.toString() + "' filters.");
        if (t.length > 10) throw new E(T.INVALID_ARGUMENT, "Invalid Query. '" + n.toString() + "' filters support a maximum of 10 elements in the value array.");
        if (t.indexOf(null) >= 0) throw new E(T.INVALID_ARGUMENT, "Invalid Query. '" + n.toString() + "' filters cannot contain 'null' in the value array.");
        if (t.filter((function(t) {
            return Number.isNaN(t);
        })).length > 0) throw new E(T.INVALID_ARGUMENT, "Invalid Query. '" + n.toString() + "' filters cannot contain 'NaN' in the value array.");
    }, t.prototype.Av = function(t) {
        if (t instanceof Wt) {
            var n = [ Qt.ARRAY_CONTAINS, Qt.ARRAY_CONTAINS_ANY ], i = [ Qt.IN, Qt.ARRAY_CONTAINS_ANY ], e = n.indexOf(t.op) >= 0, r = i.indexOf(t.op) >= 0;
            if (t.Pn()) {
                var s = this.Tv.On();
                if (null !== s && !s.isEqual(t.field)) throw new E(T.INVALID_ARGUMENT, "Invalid query. All where filters with an inequality (<, <=, >, or >=) must be on the same field. But you have inequality filters on '" + s.toString() + "' and '" + t.field.toString() + "'");
                var u = this.Tv.Dn();
                null !== u && this.Dv(t.field, u);
            } else if (r || e) {
                // You can have at most 1 disjunctive filter and 1 array filter. Check if
                // the new filter conflicts with an existing one.
                var o = null;
                if (r && (o = this.Tv.Yn(i)), null === o && e && (o = this.Tv.Yn(n)), null != o) 
                // We special case when it's a duplicate op to give a slightly clearer error message.
                throw o === t.op ? new E(T.INVALID_ARGUMENT, "Invalid query. You cannot use more than one '" + t.op.toString() + "' filter.") : new E(T.INVALID_ARGUMENT, "Invalid query. You cannot use '" + t.op.toString() + "' filters with '" + o.toString() + "' filters.");
            }
        }
    }, t.prototype.Iv = function(t) {
        if (null === this.Tv.Dn()) {
            // This is the first order by. It must match any inequality.
            var n = this.Tv.On();
            null !== n && this.Dv(n, t.field);
        }
    }, t.prototype.Dv = function(t, n) {
        if (!n.isEqual(t)) throw new E(T.INVALID_ARGUMENT, "Invalid query. You have a where filter with an inequality (<, <=, >, or >=) on field '" + t.toString() + "' and so you must also use '" + t.toString() + "' as your first Query.orderBy(), but your first Query.orderBy() is on field '" + n.toString() + "' instead.");
    }, t;
}(), hr = /** @class */ function() {
    function t(t, n, i, e) {
        this.fv = t, this.Vv = n, this.Pv = i, this.dv = e, this.kv = null, this.Fv = null, 
        this.metadata = new rr(i.hasPendingWrites, i.fromCache);
    }
    return Object.defineProperty(t.prototype, "docs", {
        get: function() {
            var t = [];
            return this.forEach((function(n) {
                return t.push(n);
            })), t;
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(t.prototype, "empty", {
        get: function() {
            return this.Pv.docs.M();
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(t.prototype, "size", {
        get: function() {
            return this.Pv.docs.size;
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.forEach = function(t, n) {
        var i = this;
        Wi("QuerySnapshot.forEach", arguments, 1, 2), zi("QuerySnapshot.forEach", "function", 1, t), 
        this.Pv.docs.forEach((function(e) {
            t.call(n, i.jv(e));
        }));
    }, Object.defineProperty(t.prototype, "query", {
        get: function() {
            return new or(this.Vv, this.fv, this.dv);
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.docChanges = function(t) {
        t && (ee("QuerySnapshot.docChanges", t, [ "includeMetadataChanges" ]), Yi("QuerySnapshot.docChanges", "boolean", "includeMetadataChanges", t.includeMetadataChanges));
        var n = !(!t || !t.includeMetadataChanges);
        if (n && this.Pv.di) throw new E(T.INVALID_ARGUMENT, "To include metadata changes with your document changes, you must also pass { includeMetadataChanges:true } to onSnapshot().");
        return this.kv && this.Fv === n || (this.kv = 
        /**
 * Calculates the array of firestore.DocumentChange's for a given ViewSnapshot.
 *
 * Exported for testing.
 */
        function(t, n, i, e) {
            if (i.ci.M()) {
                // Special case the first snapshot because index calculation is easy and
                // fast
                var r, s = 0;
                return i.docChanges.map((function(n) {
                    var u = new ur(t, n.doc.key, n.doc, i.fromCache, i.fi.has(n.doc.key), e);
                    return Wn(0 /* Added */ === n.type, "Invalid event type for first snapshot"), Wn(!r || i.query.Bn(r, n.doc) < 0, "Got added events in wrong order"), 
                    r = n.doc, {
                        type: "added",
                        doc: u,
                        oldIndex: -1,
                        newIndex: s++
                    };
                }));
            }
            // A DocumentSet that is updated incrementally as changes are applied to use
            // to lookup the index of a document.
            var u = i.ci;
            return i.docChanges.filter((function(t) {
                return n || 3 /* Metadata */ !== t.type;
            })).map((function(n) {
                var r = new ur(t, n.doc.key, n.doc, i.fromCache, i.fi.has(n.doc.key), e), s = -1, o = -1;
                return 0 /* Added */ !== n.type && (Wn((s = u.indexOf(n.doc.key)) >= 0, "Index for document not found"), 
                u = u.delete(n.doc.key)), 1 /* Removed */ !== n.type && (o = (u = u.add(n.doc)).indexOf(n.doc.key)), 
                {
                    type: vr(n.type),
                    doc: r,
                    oldIndex: s,
                    newIndex: o
                };
            }));
        }(this.fv, n, this.Pv, this.dv), this.Fv = n), this.kv;
    }, 
    /** Check the equality. The call can be very expensive. */
    t.prototype.isEqual = function(n) {
        if (!(n instanceof t)) throw re("isEqual", "QuerySnapshot", 1, n);
        return this.fv === n.fv && this.Vv.isEqual(n.Vv) && this.Pv.isEqual(n.Pv) && this.dv === n.dv;
    }, t.prototype.jv = function(t) {
        return new ur(this.fv, t.key, t, this.metadata.fromCache, this.Pv.fi.has(t.key), this.dv);
    }, t;
}();

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// settings() defaults:
e([ "length", "forEach", "map" ], "undefined" != typeof Symbol ? [ Symbol.iterator ] : []).forEach((function(t) {
    /**
     * We are (re-)defining properties on QuerySnapshot.prototype.docChanges which
     * is a Function. This could fail, in particular in the case of 'length' which
     * already exists on Function.prototype and on IE11 is improperly defined with
     * `{ configurable: false }`. So we wrap this in a try/catch to ensure that we
     * still have a functional SDK.
     */
    try {
        Object.defineProperty(hr.prototype.docChanges, t, {
            get: function() {
                // TODO(2018/11/01): As of 2018/04/17 we're changing docChanges from an array
                // into a method. Because this is a runtime breaking change and somewhat subtle
                // (both Array and Function have a .length, etc.), we'll replace commonly-used
                // properties (including Symbol.iterator) to throw a custom error message. In
                // ~6 months we can delete the custom error as most folks will have hopefully
                // migrated.
                return function() {
                    throw new E(T.INVALID_ARGUMENT, 'QuerySnapshot.docChanges has been changed from a property into a method, so usages like "querySnapshot.docChanges" should become "querySnapshot.docChanges()"');
                }();
            }
        });
    } catch (t) {}
 // Ignore this failure intentionally
}));

var ar = /** @class */ function(t) {
    function i(n, i, e) {
        var r = t.call(this, xt.Nn(n), i, e) || this;
        if (r.Uv = n, n.length % 2 != 1) throw new E(T.INVALID_ARGUMENT, "Invalid collection reference. Collection references must have an odd number of segments, but " + n.H() + " has " + n.length);
        return r;
    }
    return n(i, t), Object.defineProperty(i.prototype, "id", {
        get: function() {
            return this.Tv.path.B();
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(i.prototype, "parent", {
        get: function() {
            var t = this.Tv.path.L();
            return t.M() ? null : new er(new j(t), this.firestore);
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(i.prototype, "path", {
        get: function() {
            return this.Tv.path.H();
        },
        enumerable: !0,
        configurable: !0
    }), i.prototype.doc = function(t) {
        if (Wi("CollectionReference.doc", arguments, 0, 1), 
        // We allow omission of 'pathString' but explicitly prohibit passing in both
        // 'undefined' and 'null'.
        0 === arguments.length && (t = Gn.pr()), zi("CollectionReference.doc", "non-empty string", 1, t), 
        "" === t) throw new E(T.INVALID_ARGUMENT, "Document path must be a non-empty string");
        var n = P.Y(t);
        return er.av(this.Tv.path.child(n), this.firestore, this.dv);
    }, i.prototype.add = function(t) {
        Bi("CollectionReference.add", arguments, 1);
        var n = this.dv ? this.dv.toFirestore(t) : t;
        zi("CollectionReference.add", "object", 1, n);
        var i = this.doc();
        return i.set(t).then((function() {
            return i;
        }));
    }, i.prototype.withConverter = function(t) {
        return new i(this.Uv, this.firestore, t);
    }, i;
}(or);

function cr(t, n) {
    if (void 0 === n) return {
        merge: !1
    };
    if (ee(t, n, [ "merge", "mergeFields" ]), Yi(t, "boolean", "merge", n.merge), Ki(t, "mergeFields", "a string or a FieldPath", n.mergeFields, (function(t) {
        return "string" == typeof t || t instanceof de;
    })), void 0 !== n.mergeFields && void 0 !== n.merge) throw new E(T.INVALID_ARGUMENT, "Invalid options passed to function " + t + '(): You cannot specify both "merge" and "mergeFields".');
    return n;
}

function fr(t, n) {
    return void 0 === n ? {} : (ee(t, n, [ "serverTimestamps" ]), Xi(t, 0, "serverTimestamps", n.serverTimestamps, [ "estimate", "previous", "none" ]), 
    n);
}

function lr(t, n) {
    Ji(t, "object", 1, n), n && (ee(t, n, [ "source" ]), Xi(t, 0, "source", n.source, [ "default", "server", "cache" ]));
}

function dr(t, n, i) {
    if (n instanceof er) {
        if (n.firestore !== i) throw new E(T.INVALID_ARGUMENT, "Provided document reference is from a different Firestore instance.");
        return n;
    }
    throw re(t, "DocumentReference", 1, n);
}

function vr(t) {
    switch (t) {
      case 0 /* Added */ :
        return "added";

      case 2 /* Modified */ :
      case 3 /* Metadata */ :
        return "modified";

      case 1 /* Removed */ :
        return "removed";

      default:
        return Qn("Unknown change type: " + t);
    }
}

/**
 * Converts custom model object of type T into DocumentData by applying the
 * converter if it exists.
 *
 * This function is used when converting user objects to DocumentData
 * because we want to provide the user with a more specific error message if
 * their set() or fails due to invalid data originating from a toFirestore()
 * call.
 */ function wr(t, n, i) {
    var e;
    return t ? (e = t.toFirestore(n), i = "toFirestore() in " + i) : e = n, [ e, i ];
}

function mr(t, n) {
    return Object.prototype.hasOwnProperty.call(t, n);
}

// Export the classes with a private constructor (it will fail if invoked
// at runtime). Note that this still allows instanceof checks.
// We're treating the variables as class names, so disable checking for lower
// case variable names.
var pr = he(tr, "Use firebase.firestore() instead."), gr = he(nr, "Use firebase.firestore().runTransaction() instead."), br = he(ir, "Use firebase.firestore().batch() instead."), yr = he(er, "Use firebase.firestore().doc() instead."), _r = he(sr), Tr = he(ur), Er = he(or), Sr = he(hr), Ar = he(ar, "Use firebase.firestore().collection() instead."), Ir = {
    Firestore: pr,
    GeoPoint: Te,
    Timestamp: O,
    Blob: le,
    Transaction: gr,
    WriteBatch: br,
    DocumentReference: yr,
    DocumentSnapshot: _r,
    Query: Er,
    QueryDocumentSnapshot: Tr,
    QuerySnapshot: Sr,
    CollectionReference: Ar,
    FieldPath: de,
    FieldValue: _e,
    setLogLevel: tr.setLogLevel,
    CACHE_SIZE_UNLIMITED: $e
};

/**
 * @license
 * Copyright 2019 Google Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
var Cr = /** @class */ function() {
    function t() {}
    return t.prototype.Af = function(t) {
        // No-op.
    }, t.prototype.shutdown = function() {
        // No-op.
    }, t;
}(), Rr = /** @class */ function() {
    function t() {
        var t = this;
        this.Lv = function() {
            return t.Mv();
        }, this.qv = function() {
            return t.xv();
        }, this.Bv = [], this.Qv();
    }
    return t.prototype.Af = function(t) {
        this.Bv.push(t);
    }, t.prototype.shutdown = function() {
        window.removeEventListener("online", this.Lv), window.removeEventListener("offline", this.qv);
    }, t.prototype.Qv = function() {
        window.addEventListener("online", this.Lv), window.addEventListener("offline", this.qv);
    }, t.prototype.Mv = function() {
        qn("ConnectivityMonitor", "Network connectivity changed: AVAILABLE");
        for (var t = 0, n = this.Bv; t < n.length; t++) {
            (0, n[t])(0 /* AVAILABLE */);
        }
    }, t.prototype.xv = function() {
        qn("ConnectivityMonitor", "Network connectivity changed: UNAVAILABLE");
        for (var t = 0, n = this.Bv; t < n.length; t++) {
            (0, n[t])(1 /* UNAVAILABLE */);
        }
    }, 
    // TODO(chenbrian): Consider passing in window either into this component or
    // here for testing via FakeWindow.
    /** Checks that all used attributes of window are available. */
    t.xo = function() {
        return "undefined" != typeof window && void 0 !== window.addEventListener && void 0 !== window.removeEventListener;
    }, t;
}(), Nr = /** @class */ function() {
    function t(t) {
        this.Wv = t.Wv, this.Gv = t.Gv;
    }
    return t.prototype.lc = function(t) {
        Wn(!this.zv, "Called onOpen on stream twice!"), this.zv = t;
    }, t.prototype.oc = function(t) {
        Wn(!this.Jv, "Called onClose on stream twice!"), this.Jv = t;
    }, t.prototype.onMessage = function(t) {
        Wn(!this.Hv, "Called onMessage on stream twice!"), this.Hv = t;
    }, t.prototype.close = function() {
        this.Gv();
    }, t.prototype.send = function(t) {
        this.Wv(t);
    }, t.prototype.Yv = function() {
        Wn(void 0 !== this.zv, "Cannot call onOpen because no callback was set"), this.zv();
    }, t.prototype.Kv = function(t) {
        Wn(void 0 !== this.Jv, "Cannot call onClose because no callback was set"), this.Jv(t);
    }, t.prototype.Xv = function(t) {
        Wn(void 0 !== this.Hv, "Cannot call onMessage because no callback was set"), this.Hv(t);
    }, t;
}(), Or = {
    BatchGetDocuments: "batchGet",
    Commit: "commit"
}, Dr = "gl-js/ fire/" + y, Vr = /** @class */ function() {
    function t(t) {
        this.ce = t.ce;
        var n = t.ssl ? "https" : "http";
        this.$v = n + "://" + t.host, this.forceLongPolling = t.forceLongPolling;
    }
    /**
     * Modifies the headers for a request, adding any authorization token if
     * present and any additional headers for the request.
     */    return t.prototype.Zv = function(t, n) {
        if (n) for (var i in n.o) n.o.hasOwnProperty(i) && (t[i] = n.o[i]);
        t["X-Goog-Api-Client"] = Dr;
    }, t.prototype.Sc = function(t, n, e) {
        var r = this, s = this.tw(t);
        return new Promise((function(u, o) {
            var h = new a;
            h.listenOnce(c.COMPLETE, (function() {
                try {
                    switch (h.getLastErrorCode()) {
                      case f.NO_ERROR:
                        var n = h.getResponseJson();
                        qn("Connection", "XHR received:", JSON.stringify(n)), u(n);
                        break;

                      case f.TIMEOUT:
                        qn("Connection", 'RPC "' + t + '" timed out'), o(new E(T.DEADLINE_EXCEEDED, "Request time out"));
                        break;

                      case f.HTTP_ERROR:
                        var i = h.getStatus();
                        if (qn("Connection", 'RPC "' + t + '" failed with status:', i, "response text:", h.getResponseText()), 
                        i > 0) {
                            var e = h.getResponseJson().error;
                            if (e && e.status && e.message) {
                                var r = (s = e.status, a = s.toLowerCase().replace("_", "-"), Object.values(T).indexOf(a) >= 0 ? a : T.UNKNOWN);
                                o(new E(r, e.message));
                            } else o(new E(T.UNKNOWN, "Server responded with status " + h.getStatus()));
                        } else 
                        // If we received an HTTP_ERROR but there's no status code,
                        // it's most probably a connection issue
                        qn("Connection", 'RPC "' + t + '" failed'), o(new E(T.UNAVAILABLE, "Connection failed."));
                        break;

                      default:
                        Qn('RPC "' + t + '" failed with unanticipated webchannel error ' + h.getLastErrorCode() + ": " + h.getLastError() + ", giving up.");
                    }
                } finally {
                    qn("Connection", 'RPC "' + t + '" completed.');
                }
                var s, a;
            }));
            // The database field is already encoded in URL. Specifying it again in
            // the body is not necessary in production, and will cause duplicate field
            // errors in the Firestore Emulator. Let's remove it.
            var l = i({}, n);
            delete l.database;
            var d = JSON.stringify(l);
            qn("Connection", "XHR sending: ", s + " " + d);
            // Content-Type: text/plain will avoid preflight requests which might
            // mess with CORS and redirects by proxies. If we add custom headers
            // we will need to change this code to potentially use the
            // $httpOverwrite parameter supported by ESF to avoid
            // triggering preflight requests.
            var v = {
                "Content-Type": "text/plain"
            };
            r.Zv(v, e), h.send(s, "POST", d, v, 15);
        }));
    }, t.prototype.Ac = function(t, n, i) {
        // The REST API automatically aggregates all of the streamed results, so we
        // can just use the normal invoke() method.
        return this.Sc(t, n, i);
    }, t.prototype.dc = function(t, n) {
        var i = [ this.$v, "/", "google.firestore.v1.Firestore", "/", t, "/channel" ], e = l(), r = {
            // Required for backend stickiness, routing behavior is based on this
            // parameter.
            httpSessionIdParam: "gsessionid",
            initMessageHeaders: {},
            messageUrlParams: {
                // This param is used to improve routing and project isolation by the
                // backend and must be included in every request.
                database: "projects/" + this.ce.projectId + "/databases/" + this.ce.database
            },
            sendRawJson: !0,
            supportsCrossDomainXhr: !0,
            internalChannelParams: {
                // Override the default timeout (randomized between 10-20 seconds) since
                // a large write batch on a slow internet connection may take a long
                // time to send to the backend. Rather than have WebChannel impose a
                // tight timeout which could lead to infinite timeouts and retries, we
                // set it very large (5-10 minutes) and rely on the browser's builtin
                // timeouts to kick in if the request isn't working.
                forwardChannelRequestTimeoutMs: 6e5
            },
            forceLongPolling: this.forceLongPolling
        };
        this.Zv(r.initMessageHeaders, n), 
        // Sending the custom headers we just added to request.initMessageHeaders
        // (Authorization, etc.) will trigger the browser to make a CORS preflight
        // request because the XHR will no longer meet the criteria for a "simple"
        // CORS request:
        // https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS#Simple_requests
        // Therefore to avoid the CORS preflight request (an extra network
        // roundtrip), we use the httpHeadersOverwriteParam option to specify that
        // the headers should instead be encoded into a special "$httpHeaders" query
        // parameter, which is recognized by the webchannel backend. This is
        // formally defined here:
        // https://github.com/google/closure-library/blob/b0e1815b13fb92a46d7c9b3c30de5d6a396a3245/closure/goog/net/rpc/httpcors.js#L32
        // TODO(b/145624756): There is a backend bug where $httpHeaders isn't respected if the request
        // doesn't have an Origin header. So we have to exclude a few browser environments that are
        // known to (sometimes) not include an Origin. See
        // https://github.com/firebase/firebase-js-sdk/issues/1491.
        v() || w() || m() || p() || g() || b() || (r.httpHeadersOverwriteParam = "$httpHeaders");
        var s = i.join("");
        qn("Connection", "Creating WebChannel: " + s + " " + r);
        var u = e.createWebChannel(s, r), o = !1, h = !1, a = new Nr({
            Wv: function(t) {
                h ? qn("Connection", "Not sending because WebChannel is closed:", t) : (o || (qn("Connection", "Opening WebChannel transport."), 
                u.open(), o = !0), qn("Connection", "WebChannel sending:", t), u.send(t));
            },
            Gv: function() {
                return u.close();
            }
        }), c = function(t, n) {
            // TODO(dimond): closure typing seems broken because WebChannel does
            // not implement goog.events.Listenable
            u.listen(t, (function(t) {
                try {
                    n(t);
                } catch (t) {
                    setTimeout((function() {
                        throw t;
                    }), 0);
                }
            }));
        };
        // WebChannel supports sending the first message with the handshake - saving
        // a network round trip. However, it will have to call send in the same
        // JS event loop as open. In order to enforce this, we delay actually
        // opening the WebChannel until send is called. Whether we have called
        // open is tracked with this variable.
                return c(d.EventType.OPEN, (function() {
            h || qn("Connection", "WebChannel transport opened.");
        })), c(d.EventType.CLOSE, (function() {
            h || (h = !0, qn("Connection", "WebChannel transport closed"), a.Kv());
        })), c(d.EventType.ERROR, (function(t) {
            h || (h = !0, qn("Connection", "WebChannel transport errored:", t), a.Kv(new E(T.UNAVAILABLE, "The operation could not be completed")));
        })), c(d.EventType.MESSAGE, (function(t) {
            var n;
            if (!h) {
                var i = t.data[0];
                Wn(!!i, "Got a webchannel message without data.");
                // TODO(b/35143891): There is a bug in One Platform that caused errors
                // (and only errors) to be wrapped in an extra array. To be forward
                // compatible with the bug we need to check either condition. The latter
                // can be removed once the fix has been rolled out.
                // Use any because msgData.error is not typed.
                var e = i, r = e.error || (null === (n = e[0]) || void 0 === n ? void 0 : n.error);
                if (r) {
                    qn("Connection", "WebChannel received error:", r);
                    // error.status will be a string like 'OK' or 'NOT_FOUND'.
                    var s = r.status, o = function(t) {
                        // lookup by string
                        // eslint-disable-next-line @typescript-eslint/no-explicit-any
                        var n = Tt[t];
                        if (void 0 !== n) return sn(n);
                    }(s), c = r.message;
                    void 0 === o && (o = T.INTERNAL, c = "Unknown error status: " + s + " with message " + r.message), 
                    // Mark closed so no further events are propagated
                    h = !0, a.Kv(new E(o, c)), u.close();
                } else qn("Connection", "WebChannel received:", i), a.Xv(i);
            }
        })), setTimeout((function() {
            // Technically we could/should wait for the WebChannel opened event,
            // but because we want to send the first message with the WebChannel
            // handshake we pretend the channel opened here (asynchronously), and
            // then delay the actual open until the first message is sent.
            a.Yv();
        }), 0), a;
    }, 
    // visible for testing
    t.prototype.tw = function(t) {
        var n = Or[t];
        return Wn(void 0 !== n, "Unknown REST mapping for: " + t), this.$v + "/v1/projects/" + this.ce.projectId + "/databases/" + this.ce.database + "/documents:" + n;
    }, t;
}(), Pr = window.crypto || window.__PRIVATE_msCrypto, kr = /** @class */ function() {
    function t() {
        this.de = !0, this.Ic = "undefined" != typeof atob;
    }
    return Object.defineProperty(t.prototype, "document", {
        get: function() {
            return "undefined" != typeof document ? document : null;
        },
        enumerable: !0,
        configurable: !0
    }), Object.defineProperty(t.prototype, "window", {
        get: function() {
            return "undefined" != typeof window ? window : null;
        },
        enumerable: !0,
        configurable: !0
    }), t.prototype.Od = function(t) {
        return Promise.resolve(new Vr(t));
    }, t.prototype.Dd = function() {
        return Rr.xo() ? new Rr : new Cr;
    }, t.prototype.Vd = function(t) {
        return new kn(t, {
            de: !0
        });
    }, t.prototype.mr = function(t) {
        return JSON.stringify(t);
    }, t.prototype.atob = function(t) {
        return atob(t);
    }, t.prototype.btoa = function(t) {
        return btoa(t);
    }, t.prototype.randomBytes = function(t) {
        if (t <= 0) return new Uint8Array;
        var n = new Uint8Array(t);
        return Pr.getRandomValues(n), n;
    }, t;
}();

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * @license
 * Copyright 2017 Google Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * This code needs to run before Firestore is used. This can be achieved in
 * several ways:
 *   1) Through the JSCompiler compiling this code and then (automatically)
 *      executing it before exporting the Firestore symbols.
 *   2) Through importing this module first in a Firestore main module
 */
jn.wr(new kr);

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Registers the memory-only Firestore build with the components framework.
 */
function Fr(t) {
    !
    /**
 * Configures Firestore as part of the Firebase SDK by calling registerService.
 *
 * @param firebase The FirebaseNamespace to register Firestore with
 * @param firestoreFactory A factory function that returns a new Firestore
 *    instance.
 */
    function(t, n) {
        t.INTERNAL.registerComponent(new h("firestore", (function(t) {
            var i = t.getProvider("app").getImmediate();
            return n(i, t.getProvider("auth-internal"));
        }), "PUBLIC" /* PUBLIC */).setServiceProps(i({}, Ir)));
    }(t, (function(t, n) {
        return new tr(t, n, new Fi);
    })), t.registerVersion("@firebase/firestore", "1.13.1");
}

Fr(t);

export { Fr as __PRIVATE_registerFirestore };
//# sourceMappingURL=index.memory.esm.js.map
